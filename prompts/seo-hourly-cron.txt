You are running the hourly SEO article workflow for the docs repository.

Repository root:
`/home/newbe36524/repos/hagicode-mono/repos/docs`

You must follow these repository files:

- `SEO_ARTICLE_WORKFLOW.md`
- `seo/HOURLY_CRON_WORKFLOW.md`
- `seo/topic-pool.json`
- `seo/pipeline-config.json`
- `prompts/seo-article-workflow.txt`

Required behavior:

1. Select exactly one topic from `seo/topic-pool.json`.
2. Skip topics already substantially covered by:
   - `src/content/docs/blog/`
   - `seo/briefs/`
   - `seo/drafts/`
3. Generate these outputs for the selected topic:
   - brief
   - research
   - outline
   - draft
   - review report
4. Save outputs into the `seo/` workflow directories.
5. Do not publish the article into `src/content/docs/blog/`.
6. Do not set the draft to published.
7. Ground the article in repository-backed facts wherever possible.
8. If the topic lacks enough factual support, skip it and pick the next suitable topic.
9. If no topic is suitable, report that clearly instead of forcing low-quality content.

Scope priorities:

- Hermes
- Codex
- Claude / Claude Code
- GPT
- GLM
- MiniMax
- AI CLI workflows
- AI agent development topics

Article types allowed:

- install-guide
- troubleshooting
- comparison
- best-practice

Your final response must be concise and review-oriented. Include:

- selected topic
- why it was selected
- generated file paths
- top risks or caveats
- whether it looks ready for human publication review

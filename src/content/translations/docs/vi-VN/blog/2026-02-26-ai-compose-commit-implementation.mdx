---
title: "Cam kết soạn thảo AI: Sử dụng AI để tái cấu trúc quy trình làm việc cam kết Git một cách thông minh"
date: 2026-02-26
tags: [AI, Git, Commit, Conventional Commits, HagiCode, Orleans, SignalR, "C#", .NET]
description: "Trong quá trình phát triển phần mềm, việc biên dịch mã là một công việc thường ngày mà mọi lập trình viên đều phải đối mặt hàng ngày. Nhưng bạn đã bao giờ gặp phải tình huống này chưa: cuối cùng"
---

## Cam kết soạn thảo AI: Sử dụng AI để tái cấu trúc quy trình làm việc cam kết Git một cách thông minh

## Giới thiệu

Trong quá trình phát triển phần mềm, việc biên dịch mã là một công việc thường ngày mà mọi lập trình viên đều phải đối mặt hàng ngày. Nhưng bạn đã bao giờ gặp phải tình huống này chưa: vào cuối ngày làm việc, bạn mở Git, thấy hàng tá tệp đã sửa đổi chưa được phân loại và không biết cách sắp xếp chúng thành các cam kết hợp lý?

Cách tiếp cận truyền thống là sắp xếp các tệp theo nhóm theo cách thủ công, cam kết từng tệp một và viết thông báo cam kết. Quá trình này vừa tốn thời gian vừa dễ xảy ra lỗi. Chúng ta thường lãng phí khá nhiều thời gian cho việc này, và suy cho cùng, không ai muốn lo lắng về những công việc tẻ nhạt này vào đêm khuya khi họ đã mệt mỏi.

Trong dự án HagiCode, chúng tôi đã giới thiệu một tính năng mới - AI Compose Commit - được thiết kế để chuyển đổi hoàn toàn quy trình công việc này. Bằng cách sử dụng AI để phân tích một cách thông minh tất cả các thay đổi chưa được cam kết trong cây làm việc, nó sẽ tự động nhóm chúng thành nhiều cam kết hợp lý và thực hiện các thao tác cam kết tuân thủ tiêu chuẩn. Trong bài viết này, chúng ta sẽ đi sâu vào các nguyên tắc triển khai, kiến ​​trúc kỹ thuật cũng như những thách thức và giải pháp mà chúng ta gặp phải trong thực tế.

## Giới thiệu về HagiCode

Cách tiếp cận được chia sẻ trong bài viết này xuất phát từ kinh nghiệm thực tế của chúng tôi trong [Mã Hagi](https://hagicode.com) dự án.

## Nền

### Điểm yếu của cam kết Git truyền thống

Là một hệ thống kiểm soát phiên bản, Git mang đến cho các nhà phát triển khả năng quản lý mã mạnh mẽ. Nhưng trong thực tế, việc cam kết thường trở thành nút thắt trong quy trình phát triển:

1. **Việc nhóm thủ công tốn nhiều thời gian**: Khi có nhiều thay đổi đối với tệp, nhà phát triển cần kiểm tra từng tệp một và quyết định những thay đổi nào thuộc về cùng một tính năng. Điều đó đòi hỏi rất nhiều nỗ lực tinh thần.
2. **Chất lượng thông báo cam kết không nhất quán**: Viết thông báo cam kết tuân theo thông số kỹ thuật của Cam kết thông thường đòi hỏi kinh nghiệm và kỹ năng và những người mới bắt đầu thường tạo ra các cam kết không chuẩn.
3. **Quản lý nhiều kho lưu trữ phức tạp**: Trong môi trường monorepo, việc chuyển đổi giữa các kho lưu trữ khác nhau sẽ làm tăng thêm độ phức tạp trong hoạt động.
4. **Quy trình làm việc bị gián đoạn**: Việc chuyển mã sẽ làm gián đoạn quá trình suy nghĩ của bạn và làm ảnh hưởng đến hiệu quả mã hóa.

Những vấn đề này đặc biệt rõ ràng trong các dự án lớn và môi trường nhóm cộng tác. Một công cụ phát triển tốt sẽ cho phép các nhà phát triển tập trung vào công việc mã hóa cốt lõi thay vì sa lầy vào quy trình làm việc cam kết rườm rà.

### Xu hướng phát triển được hỗ trợ bởi AI

Trong những năm gần đây, AI ngày càng được sử dụng rộng rãi trong phát triển phần mềm. Từ hoàn thiện mã và phát hiện lỗi đến tạo tài liệu tự động, AI đang dần đạt đến mọi giai đoạn của quá trình phát triển. Trong quy trình công việc Git, mặc dù một số công cụ đã hỗ trợ tạo thông báo cam kết, nhưng hầu hết đều bị giới hạn ở các kịch bản cam kết đơn lẻ và thiếu khả năng phân tích và nhóm các thay đổi một cách thông minh trên toàn bộ cây làm việc.

HagiCode cũng gặp phải những điểm yếu này trong quá trình phát triển. Chúng tôi đã thử nhiều công cụ, nhưng mỗi công cụ đều có hạn chế này hay hạn chế khác. Chức năng chưa hoàn chỉnh hoặc trải nghiệm người dùng không đủ tốt. Đó là lý do tại sao cuối cùng chúng tôi quyết định tự mình triển khai Cam kết soạn thảo AI.

Tính năng AI Compose Commit của HagiCode được tạo ra để lấp đầy khoảng trống đó. Nó không chỉ tạo ra các thông báo cam kết - nó còn đảm nhận toàn bộ quá trình từ phân tích tệp đến thực thi cam kết.

## vấn đề

### Những thách thức kỹ thuật

Trong khi triển khai Cam kết soạn thư AI, chúng tôi đã gặp phải một số thách thức kỹ thuật:

1. **Hiểu ngữ nghĩa tệp**: AI cần hiểu mối quan hệ ngữ nghĩa giữa các thay đổi của tệp và quyết định tệp nào thuộc cùng một mô-đun chức năng. Điều này đòi hỏi phải phân tích sâu về nội dung tệp, cấu trúc thư mục và bối cảnh thay đổi.

2. **Chiến lược nhóm cam kết**: Nên xác định tiêu chuẩn nhóm hợp lý như thế nào? Theo tính năng, theo mô-đun hoặc theo loại tệp? Các dự án khác nhau có thể cần các chiến lược khác nhau.

3. **Phản hồi theo thời gian thực và xử lý không đồng bộ**: Thao tác với Git có thể mất nhiều thời gian, đặc biệt là khi xử lý số lượng lớn tệp. Làm cách nào chúng tôi có thể hoàn thành các hoạt động phức tạp trong khi vẫn duy trì được trải nghiệm tốt cho người dùng?

4. **Hỗ trợ nhiều kho lưu trữ**: Trong kiến trúc monorepo, các hoạt động cần được định tuyến chính xác giữa kho lưu trữ chính và kho lưu trữ phụ.

5. **Xử lý lỗi và khôi phục**: Nếu một lần xác nhận không thành công thì các lần xác nhận đã thực hiện sẽ được xử lý như thế nào? Các tập tin đã được dàn dựng có cần được khôi phục không?

6. **Tính nhất quán của thông báo cam kết**: Thông báo cam kết được tạo cần phải phù hợp với phong cách hiện có của dự án và vẫn nhất quán với các cam kết trước đây.

### Cân nhắc về hiệu suất

Việc xử lý AI qua một số lượng lớn các thay đổi tệp tiêu tốn đáng kể thời gian và tài nguyên tính toán. Chúng tôi cần tối ưu hóa trong các lĩnh vực sau:

- Giảm các cuộc gọi AI không cần thiết
- Tối ưu hóa cách xây dựng bối cảnh tệp
- Thực hiện phân nhóm hoạt động Git hiệu quả

Tất cả những vấn đề này đều xuất hiện trong quá trình sử dụng HagiCode thực tế và chúng tôi chỉ đạt được giải pháp tương đối hoàn chỉnh thông qua việc lặp lại và tối ưu hóa nhiều lần. Nếu bạn đang xây dựng một công cụ tương tự, chúng tôi hy vọng trải nghiệm của chúng tôi sẽ mang lại cho bạn chút cảm hứng.

## Giải pháp

### Thiết kế kiến trúc tổng thể

Chúng tôi đã áp dụng kiến trúc phân lớp để triển khai Cam kết soạn thảo AI, đảm bảo khả năng mở rộng và bảo trì tốt:

#### 1. Lớp API (Lớp Web)

**GitController** cung cấp `POST /api/git/auto-compose-commit` điểm cuối làm điểm vào. Để tối ưu hóa trải nghiệm người dùng, chúng tôi đã áp dụng mẫu không đồng bộ kích hoạt và quên:

- Sau khi client gửi yêu cầu, server ngay lập tức trả về HTTP 202 Accepted
- Quá trình xử lý AI thực tế chạy không đồng bộ ở chế độ nền
- Khi quá trình xử lý kết thúc, khách hàng sẽ được thông báo qua SignalR

Thiết kế này đảm bảo rằng ngay cả khi quá trình xử lý AI mất vài phút, người dùng vẫn nhận được phản hồi ngay lập tức và không cảm thấy hệ thống bị đóng băng.

#### 2. Lớp dịch vụ ứng dụng (Lớp ứng dụng)

**GitAppService** chịu trách nhiệm về logic kinh doanh cốt lõi:

- Phát hiện kho lưu trữ: hỗ trợ quản lý nhiều kho lưu trữ trong một monorepo
- Quản lý khóa: ngăn ngừa xung đột do hoạt động đồng thời gây ra
- Phối hợp dàn tệp: tương tác với luồng xử lý AI
- Khôi phục lỗi: khôi phục trạng thái khi xảy ra lỗi

#### 3. Lớp tính toán phân tán (Orleans Grains)

**AIGrain** đóng vai trò là đơn vị thực thi cốt lõi cho các hoạt động AI. Nó thực hiện các `AutoComposeCommitAsync` phương pháp từ `IAIGrain` giao diện:

```csharp
// Define the interface method for AI-powered automatic commit composition
// Parameter notes:
// - projectId: unique project identifier
// - unstagedFiles: list of unstaged files, including file paths and status information
// - projectPath: project root directory path (optional), used to access project context
// Return value: a response object containing execution results, including success/failure status and detailed information
[Alias("AutoComposeCommitAsync")]
[ResponseTimeout("00:20:00")] // 20-minute timeout, suitable for handling large change sets
Task<AutoComposeCommitResponseDto> AutoComposeCommitAsync(
    string projectId,
    GitFileStatusDto[] unstagedFiles,
    string? projectPath = null);
```

Phương pháp này đặt thời gian chờ là 20 phút để xử lý các tập hợp thay đổi lớn. Khi sử dụng HagiCode trong thế giới thực, chúng tôi nhận thấy rằng một số dự án có thể liên quan đến hàng trăm tệp đã thay đổi trong một lần chuyển, đòi hỏi nhiều thời gian xử lý hơn.

#### 4. Lớp dịch vụ AI

Thông qua phần tóm tắt `IAIService` giao diện, chúng tôi đã triển khai kiến trúc dịch vụ AI có thể cắm được. Chúng tôi hiện đang sử dụng dịch vụ Claude Helper nhưng có thể dễ dàng chuyển sang các nhà cung cấp AI khác.

### Logic triển khai cốt lõi

#### Xây dựng bối cảnh tệp

AI cần hiểu trạng thái của từng tệp trước khi có thể đưa ra quyết định thông minh. Chúng tôi xây dựng ngữ cảnh tập tin thông qua `BuildFileChangesXml` phương pháp:

```csharp
/// <summary>
/// Build an XML representation of file changes to provide the AI with complete file context information
/// </summary>
/// <param name="stagedFiles">List of staged files, including file path, status, and old path (for rename operations)</param>
/// <returns>A formatted XML string containing metadata for all files</returns>
private static string BuildFileChangesXml(GitFileStatusDto[] stagedFiles)
{
    var sb = new StringBuilder();
    sb.AppendLine("<files>");

    foreach (var file in stagedFiles)
    {
        sb.AppendLine("  <file>");
        // Use XML escaping to ensure special characters do not break the XML structure
        sb.AppendLine($"    <path>{System.Security.SecurityElement.Escape(file.Path)}</path>");
        sb.AppendLine($"    <status>{System.Security.SecurityElement.Escape(file.Status)}</status>");

        // Handle file rename scenarios and record the old path so the AI can understand change relationships
        if (!string.IsNullOrEmpty(file.OldPath))
        {
            sb.AppendLine($"    <oldPath>{System.Security.SecurityElement.Escape(file.OldPath)}</oldPath>");
        }

        sb.AppendLine("  </file>");
    }

    sb.AppendLine("</files>");
    return sb.ToString();
}
```

Bối cảnh dựa trên XML này bao gồm các đường dẫn tệp, trạng thái và đường dẫn cũ cho các hoạt động đổi tên, cung cấp cho AI siêu dữ liệu hoàn chỉnh. Với định dạng XML có cấu trúc, chúng tôi đảm bảo rằng AI có thể hiểu chính xác trạng thái và loại thay đổi của từng tệp.

#### Quản lý quyền AI

Để cho phép AI trực tiếp thực hiện các thao tác Git, chúng tôi đã định cấu hình các quyền công cụ toàn diện:

```csharp
// Define the set of tools the AI can use, including file operations and Git command execution permissions
// Read/Write/Edit: file reading, writing, and editing capabilities
// Bash(git:*): permission to execute all Git commands
// Other Bash commands: used to inspect file contents and directory structure so the AI can understand context
var allowedTools = new[]
{
    "Read", "Write", "Edit",
    "Bash(git:*)", "Bash(cat:*)", "Bash(ls:*)", "Bash(find:*)",
    "Bash(grep:*)", "Bash(head:*)", "Bash(tail:*)", "Bash(wc:*)"
};

// Build the complete AI request object
var request = new AIRequest
{
    Prompt = prompt,                          // Complete prompt template, including task instructions and constraints
    WorkingDirectory = projectPath ?? GetTempDirectory(), // Working directory, ensuring the AI runs in the correct project context
    AllowedTools = allowedTools,               // Allowed tool set
    PermissionMode = PermissionMode.bypassPermissions, // Bypass permission checks so Git operations can run directly
    LanguagePreference = languagePreference         // Language preference setting, ensuring commit messages match user expectations
};
```

Ở đây chúng tôi sử dụng `PermissionMode.bypassPermissions`, cho phép AI thực thi các lệnh Git trực tiếp mà không cần xác nhận của người dùng. Đây là trọng tâm của thiết kế tính năng nhưng cũng yêu cầu xác thực đầu vào nghiêm ngặt để ngăn chặn việc lạm dụng. Trong quá trình triển khai sản xuất HagiCode, chúng tôi đã đảm bảo tính an toàn của cơ chế này thông qua xác thực tham số phụ trợ và giám sát nhật ký.

#### Cam kết phân tích kết quả

Sau khi AI thực hiện xong, nó sẽ trả về các kết quả có cấu trúc. Chúng tôi đã triển khai chiến lược phân tích cú pháp kép để đảm bảo tính tương thích:

```csharp
/// <summary>
/// Parse commit execution results returned by the AI, supporting both delimiter format and regex format
/// </summary>
/// <param name="aiResponse">Raw response content returned by the AI</param>
/// <returns>A parsed list of commit results, where each result includes the commit hash and execution status</returns>
private List<CommitResultDto> ParseCommitExecutionResults(string aiResponse)
{
    var results = new List<CommitResultDto>();

    // Prefer delimiter-based parsing (new format), which is more explicit and reliable
    if (aiResponse.Contains("---"))
    {
        logger.LogDebug("Using delimiter-based parsing for AI response");
        results = ParseDelimitedFormat(aiResponse);

        if (results.Count > 0)
        {
            return results; // Successfully parsed, return the results directly
        }

        logger.LogWarning("Delimiter-based parsing produced no results, falling back to regex");
    }
    else
    {
        logger.LogDebug("No delimiter found, using legacy regex-based parsing");
    }

    // Fall back to regex parsing (old format) to ensure backward compatibility
    return ParseLegacyFormat(aiResponse);
}
```

Định dạng dấu phân cách sử dụng `---` để phân tách các cam kết, làm cho cấu trúc rõ ràng và dễ phân tích:

```
---
Commit 1: abc123def456
feat(auth): add user login functionality

Implement JWT-based authentication with login form and API endpoints.

Co-Authored-By: Hagicode <noreply@hagicode.com>
---
Commit 2: 789ghi012jkl
docs(readme): update installation instructions

Add new setup steps for Docker environment.

Co-Authored-By: Hagicode <noreply@hagicode.com>
---
```

Định dạng này giúp cho việc phân tích cú pháp trở nên đơn giản và đáng tin cậy, đồng thời vẫn dễ đọc đối với con người.

### Cơ chế quản lý khóa

Để ngăn xung đột trạng thái do hoạt động đồng thời gây ra, chúng tôi đã triển khai cơ chế khóa kho lưu trữ:

```csharp
// Acquire the repository lock to prevent concurrent operations
// Parameter notes:
// - fullPath: full repository path, used to identify different repository instances
// - requestedBy: requester identifier, used for tracking and logging
await _autoComposeLockService.AcquireLockAsync(fullPath, requestedBy);

try
{
    // Execute the AI Compose Commit operation
    // This section calls an Orleans Grain method to perform the actual AI processing and Git operations
    await aiGrain.AutoComposeCommitAsync(projectId, unstagedFiles, projectPath);
}
finally
{
    // Ensure the lock is released whether the operation succeeds or fails
    // Using a finally block guarantees lock release even when exceptions occur, preventing deadlocks
    await _autoComposeLockService.ReleaseLockAsync(fullPath);
}
```

Khóa có thời gian chờ là 20 phút, phù hợp với thời gian chờ được sử dụng cho các hoạt động của AI. Nếu thao tác không thành công hoặc hết thời gian, hệ thống sẽ tự động mở khóa để tránh bị chặn vĩnh viễn. Trong quá trình sử dụng HagiCode thực tế, chúng tôi nhận thấy cơ chế khóa này cực kỳ quan trọng, đặc biệt là trong môi trường cộng tác nơi nhiều nhà phát triển có thể kích hoạt Cam kết soạn thảo AI cùng một lúc.

### Thông báo theo thời gian thực của SignalR

Sau khi quá trình xử lý hoàn tất, hệ thống sẽ gửi thông báo đến giao diện người dùng thông qua SignalR:

```csharp
/// <summary>
/// Send a notification when automatic commit composition is complete
/// </summary>
/// <param name="projectId">Project identifier, used to route the notification to the correct client</param>
/// <param name="totalCount">Total number of commits, including successes and failures</param>
/// <param name="successCount">Number of successful commits</param>
/// <param name="failureCount">Number of failed commits</param>
/// <param name="success">Whether the overall operation succeeded</param>
/// <param name="error">Error message (if the operation failed)</param>
private async Task SendAutoComposeCommitNotificationAsync(
    string projectId,
    int totalCount,
    int successCount,
    int failureCount,
    bool success,
    string? error)
{
    try
    {
        // Build the notification DTO containing detailed execution results
        var notification = new AutoComposeCommitCompletedDto
        {
            ProjectId = projectId,
            TotalCount = totalCount,
            SuccessCount = successCount,
            FailureCount = failureCount,
            Success = success,
            Error = error
        };

        // Broadcast the notification to all connected clients through the SignalR Hub
        await messageService.SendAutoComposeCommitCompletedAsync(notification);

        logger.LogInformation(
            "Auto compose commit notification sent for project {ProjectId}: {SuccessCount}/{TotalCount} succeeded",
            projectId, successCount, totalCount);
    }
    catch (Exception ex)
    {
        // Log notification errors without affecting the main operation flow
        // A notification failure should not cause the entire operation to fail
        logger.LogError(ex, "Failed to send auto compose commit notification for project {ProjectId}", projectId);
    }
}
```

Sau khi giao diện người dùng nhận được thông báo, nó có thể cập nhật giao diện người dùng để hiển thị xem cam kết thành công hay thất bại, cải thiện trải nghiệm người dùng. Cơ chế phản hồi theo thời gian thực này đã nhận được phản hồi mạnh mẽ từ người dùng HagiCode, những người có thể thấy rõ khi nào hoạt động kết thúc và kết quả là gì.

## Thực hiện

### Thiết kế kỹ thuật nhanh chóng

Hành vi của AI hoàn toàn được xác định bởi lời nhắc, vì vậy chúng tôi đã thiết kế cẩn thận mẫu lời nhắc cho Cam kết soạn thư tự động. Lấy phiên bản tiếng Trung làm ví dụ (`auto-compose-commit.zh-CN.hbs`):

#### Hỗ trợ chế độ không tương tác

Khi bắt đầu lời nhắc, chúng tôi tuyên bố rõ ràng về việc hỗ trợ chế độ thực thi không tương tác, đây là một yêu cầu quan trọng đối với các tập lệnh CI/CD và tự động hóa:

```handlebars
**Important Note**: This prompt may run in a non-interactive environment (such as CI/CD or automation scripts).

**Non-Interactive Mode**:
- Do not use AskUserQuestion or any interactive tools
- When user input is required:
  - Use sensible defaults (for example, use feat as the commit type)
  - Skip optional confirmation steps
  - Record any assumptions made
```

Thiết kế này đảm bảo rằng AI Compose Commit có thể được sử dụng không chỉ trong môi trường IDE tương tác mà còn được tích hợp vào quy trình CI/CD để cung cấp quy trình làm việc cam kết hoàn toàn tự động.

#### Cơ chế bảo vệ chi nhánh

Để ngăn AI thực hiện các hoạt động nguy hiểm, chúng tôi đã thêm các quy tắc bảo vệ nhánh nghiêm ngặt vào lời nhắc:

```handlebars
**Branch Protection**:
- Do not perform any branch switching operations (git checkout, git switch)
- All `git commit` commands must run on the current branch
- Do not create, delete, or rename branches
- Do not modify untracked files or unstaged changes
- If branch switching is required to complete the operation, return an error instead of executing it
```

Bằng cách hạn chế phạm vi sử dụng công cụ của AI, các quy tắc này đảm bảo an toàn vận hành. Trong thử nghiệm thực tế của HagiCode, chúng tôi đã xác minh tính hiệu quả của những hạn chế này: khi AI gặp phải tình huống cần chuyển nhánh, nó sẽ trả về lỗi một cách an toàn thay vì thực hiện hành động nguy hiểm.

#### Cây quyết định nhóm thông minh

Lời nhắc xác định logic quyết định để nhóm tệp một cách chi tiết:

```handlebars
**File Grouping Decision Tree**:
├── Is it a configuration file (package.json, tsconfig.json, .env, etc.)?
│   ├── Yes -> separate commit (type: chore or build)
│   └── No -> continue
├── Is it a documentation file (README.md, *.md, docs/**)?
│   ├── Yes -> separate commit (type: docs)
│   └── No -> continue
├── Is it related to the same feature?
│   ├── Yes -> merge into the same commit
│   └── No -> commit separately
└── Is it a cross-module change?
    ├── Yes -> group by module
    └── No -> group by feature
```

Cây quyết định này cung cấp cho AI logic phân nhóm rõ ràng, đảm bảo các cam kết được tạo vẫn hợp lý về mặt ngữ nghĩa. Trong quá trình sử dụng HagiCode thực tế, chúng tôi nhận thấy rằng cây quyết định này có thể xử lý phần lớn các tình huống phổ biến và kết quả phân nhóm phù hợp với kỳ vọng của nhà phát triển.

#### Phân tích tính nhất quán về định dạng lịch sử

Để giữ cho thông báo cam kết nhất quán với lịch sử dự án, lời nhắc yêu cầu AI phân tích lịch sử cam kết gần đây trước khi tạo:

```handlebars
**Historical Format Consistency**: Before generating commit messages, you **must** analyze the current repository's commit history to match the existing style.

1. Use `git log -n 15 --pretty=format:"%H|%s|%b%n---%n"` to get the recent commit history
2. Analyze the commits to identify:
   - Structural patterns: does the project use multi-paragraph messages? Are there `Changes:` or `Capabilities:` sections?
   - Language patterns: are commit messages in English, Chinese, or mixed?
   - Common types: which commit types are most often used (`feat`, `fix`, `docs`, etc.)?
   - Special formatting: are there `Co-Authored-By` lines? Any other project-specific conventions?
3. Generate commit messages that follow the detected patterns
```

Phân tích này đảm bảo rằng các thông điệp cam kết do AI tạo ra không có cảm giác lạc lõng mà thay vào đó vẫn phù hợp về mặt phong cách với lịch sử của dự án. Trong các dự án đa ngôn ngữ của HagiCode, tính năng này đặc biệt quan trọng vì nó có thể tự động chọn ngôn ngữ và định dạng phù hợp dựa trên lịch sử cam kết.

#### Yêu cầu đồng tác giả

Mọi cam kết phải bao gồm thông tin Đồng tác giả:

```handlebars
**Important**: Every commit must include Co-Authored-By information
- Use the following format: `git commit -m "type(scope): subject" -m "" -m "Co-Authored-By: Hagicode <noreply@hagicode.com>"`
- Or include the `Co-Authored-By` line directly in the commit message
```

Điều này không chỉ để tuân thủ đóng góp mà còn để theo dõi lịch sử cam kết được hỗ trợ bởi AI. HagiCode coi đây là quy tắc bắt buộc để đảm bảo rằng tất cả các cam kết do AI tạo ra đều có dấu nguồn rõ ràng.

### Hướng dẫn quy trình làm việc

Quy trình làm việc đầy đủ của AI Compose Commit như sau:

1. **Trình kích hoạt người dùng**: Người dùng nhấp vào nút "Cam kết tự động soạn thảo AI" trong bảng Trạng thái Git hoặc Vùng hành động nhanh.
2. **Yêu cầu API**: Giao diện người dùng gửi yêu cầu POST tới `/api/git/auto-compose-commit` điểm cuối.
3. **Phản hồi ngay lập tức**: Máy chủ trả về HTTP 202 được chấp nhận mà không cần đợi quá trình xử lý kết thúc.
4. **Xử lý nền**:
   - GitAppService có được khóa kho lưu trữ
   - Cuộc gọi `AIGrain.AutoComposeCommitAsync`
   - Xây dựng ngữ cảnh tệp XML
   - Thực thi lời nhắc AI để AI có thể phân tích và thực hiện các cam kết
5. **Thực thi AI**:
   - Sử dụng các lệnh Git để có được tất cả các thay đổi chưa được thực hiện
   - Đọc nội dung tập tin để hiểu bản chất của những thay đổi
   - Nhóm các tệp theo mối quan hệ ngữ nghĩa
   - Thực hiện `git add` và `git commit` cho mỗi nhóm
6. **Phân tích kết quả**: Phân tích kết quả thực thi do AI trả về.
7. **Gửi thông báo**: Thông báo cho giao diện người dùng thông qua SignalR.
8. **Mở khóa**: Mở khóa kho lưu trữ cho dù thao tác thành công hay thất bại.

Quy trình công việc này được thiết kế để người dùng có thể tiếp tục công việc khác ngay sau khi bắt đầu thao tác mà không cần đợi AI kết thúc. Phản hồi từ người dùng HagiCode cho thấy mô hình xử lý không đồng bộ này cải thiện đáng kể trải nghiệm quy trình làm việc.

### Cơ chế xử lý lỗi

Chúng tôi đã triển khai xử lý lỗi nhiều lớp:

#### 1. Xác thực đầu vào

```csharp
// Validate request parameters to prevent invalid requests from reaching backend processing logic
if (request.UnstagedFiles == null || request.UnstagedFiles.Count == 0)
{
    return BadRequest(new
    {
        message = "No unstaged files provided. Please make changes in the working directory first.",
        status = "validation_failed"
    });
}
```

#### 2. Khôi phục lỗi

Nếu xảy ra lỗi trong quá trình xử lý AI, hệ thống sẽ thực hiện thao tác khôi phục và hủy các tệp đã được sắp xếp, ngăn không cho trạng thái không nhất quán bị bỏ lại. Trong quá trình sử dụng HagiCode thực tế, cơ chế này đã cứu chúng tôi khỏi nhiều lần gián đoạn không mong muốn và đảm bảo tính toàn vẹn trạng thái của kho lưu trữ.

#### 3. Xử lý thời gian chờ

Thời gian chờ 20 phút đảm bảo rằng các hoạt động kéo dài không chặn tài nguyên vô thời hạn. Sau một thời gian chờ, hệ thống sẽ mở khóa và thông báo cho người dùng rằng thao tác không thành công. Trong quá trình sử dụng HagiCode thực tế, chúng tôi nhận thấy rằng hầu hết các thao tác hoàn thành trong vòng 2 đến 5 phút và chỉ những tập hợp thay đổi cực lớn mới đạt đến giới hạn thời gian chờ.

## Thực tiễn tốt nhất

### Các phương pháp hay nhất để sử dụng Cam kết soạn thảo AI

#### 1. Chọn đúng thời điểm

Cam kết AI Compose phù hợp nhất cho các tình huống sau:

- Vào cuối ngày làm việc, khi bạn cần xử lý các thay đổi trên nhiều tệp trong một đợt
- Sau hoạt động tái cấu trúc, khi một số tệp liên quan cần được chuyển giao riêng
- Sau khi một tính năng được hoàn thành, khi những thay đổi liên quan cần được nhóm lại thành các commit

Nó không phù hợp cho các tình huống sau:

- Cam kết nhanh cho một tệp duy nhất (cam kết bình thường sẽ nhanh hơn)
- Các kịch bản yêu cầu kiểm soát chính xác nội dung cam kết
- Cam kết chứa thông tin nhạy cảm cần có sự xem xét của con người

#### 2. Xem xét các cam kết do AI tạo ra

Mặc dù tính năng phân nhóm thông minh được hỗ trợ bởi AI rất mạnh mẽ nhưng các nhà phát triển vẫn nên xem lại các cam kết đã tạo:

- Kiểm tra xem nhóm có phù hợp với mong đợi không
- Xác minh tính chính xác của thông điệp cam kết
- Xác nhận rằng không có tệp nào bị bỏ sót hoặc đưa vào không chính xác

Nếu bạn tìm thấy một nhóm không hợp lý, bạn có thể sử dụng `git reset --soft HEAD~N` để hoàn tác nó và tập hợp lại. Kinh nghiệm của HagiCode cho thấy ngay cả khi việc phân nhóm AI là thông minh, việc xem xét thủ công vẫn có giá trị, đặc biệt đối với các cam kết tính năng quan trọng.

#### 3. Phù hợp với tiêu chuẩn dự án

Đảm bảo cấu hình Git của dự án của bạn hỗ trợ Cam kết thông thường:

```bash
# Install commitlint
npm install -g @commitlint/cli @commitlint/config-conventional

# Configure commitlint
echo "module.exports = {extends: ['@commitlint/config-conventional']}" > commitlint.config.js
```

Điều này cho phép bạn xác thực định dạng thông báo cam kết trong quy trình làm việc CI/CD và giữ cho nó phù hợp với định dạng do AI Compose Commit tạo ra.

### Đề xuất xây dựng các tính năng tương tự

Nếu bạn muốn triển khai tính năng cam kết tương tự được hỗ trợ bởi AI trong dự án của riêng mình, đây là những gợi ý của chúng tôi:

#### 1. Bắt đầu nhỏ

Bắt đầu với việc tạo thông báo cam kết duy nhất, sau đó dần dần mở rộng sang nhóm nhiều cam kết. Điều này làm cho việc xác nhận và lặp lại dễ dàng hơn. HagiCode cũng đi theo con đường tương tự: các phiên bản đầu tiên chỉ hỗ trợ các lần xác nhận duy nhất và sau đó được mở rộng sang nhóm thông minh trên nhiều lần xác nhận.

#### 2. Sử dụng SDK AI trưởng thành

Không triển khai logic gọi AI từ đầu. Việc sử dụng SDK hiện có giúp giảm thời gian phát triển và các lỗi tiềm ẩn. Chúng tôi đã sử dụng dịch vụ Claude Helper, dịch vụ này cung cấp giao diện ổn định và khả năng xử lý lỗi hiệu quả.

#### 3. Đầu tư vào thiết kế nhanh chóng

Chất lượng nhanh chóng quyết định trực tiếp đến chất lượng đầu ra. Dành thời gian thiết kế lời nhắc chi tiết, bao gồm:

- Xóa mô tả nhiệm vụ
- Yêu cầu định dạng đầu ra cụ thể
- Quy tắc xử lý các trường hợp cạnh
- Ví dụ minh họa

HagiCode đã đầu tư rất nhiều vào thiết kế nhanh chóng và đây là một trong những lý do chính khiến tính năng này thành công.

#### 4. Triển khai xử lý lỗi toàn diện

Hoạt động của AI có thể thất bại vì nhiều lý do, chẳng hạn như sự cố mạng, giới hạn tốc độ API hoặc kiểm duyệt nội dung. Đảm bảo hệ thống của bạn có thể xử lý các lỗi này một cách khéo léo và cung cấp thông tin lỗi có ý nghĩa.

#### 5. Cung cấp cơ chế can thiệp thủ công

Đừng tự động hóa mọi thứ hoàn toàn. Để người dùng kiểm soát. Cung cấp các tùy chọn để xem xét kết quả nhóm, điều chỉnh nhóm và chỉnh sửa thông báo cam kết theo cách thủ công để cân bằng giữa tính tự động hóa và tính linh hoạt. Mặc dù HagiCode hỗ trợ thực thi tự động nhưng nó vẫn duy trì khả năng xem trước và điều chỉnh.

### Mẹo tối ưu hóa hiệu suất

#### 1. Lọc tệp

Khi xây dựng ngữ cảnh tệp, hãy lọc ra các tệp không cần phân tích AI:

```csharp
// Filter out generated files and excessively large files to reduce the AI processing burden
var relevantFiles = stagedFiles
    .Where(f => !IsGeneratedFile(f.Path))
    .Where(f => !IsLargeFile(f.Path))
    .ToArray();
```

#### 2. Xử lý song song

Nếu nhiều kho lưu trữ độc lập được hỗ trợ, các cam kết trong các kho lưu trữ khác nhau có thể được xử lý song song để cải thiện hiệu quả tổng thể.

#### 3. Tối ưu hóa bộ đệm

Dự án bộ đệm cam kết kết quả phân tích lịch sử để tránh phân tích lại chúng mỗi lần. Tùy chọn định dạng lịch sử có thể được lưu trữ trong các tệp cấu hình để giảm các lệnh gọi AI.

## Kết luận

AI Compose Commit thể hiện sự ứng dụng sâu sắc của công nghệ AI vào các công cụ phát triển phần mềm. Bằng cách phân tích thông minh các thay đổi của tệp, tự động nhóm các cam kết và tạo thông báo cam kết tuân thủ tiêu chuẩn, nó cải thiện đáng kể hiệu quả của quy trình công việc Git và cho phép các nhà phát triển tập trung hơn vào công việc mã hóa cốt lõi.

Trong quá trình thực hiện, chúng tôi đã học được một số bài học quan trọng:

1. **Phản hồi của người dùng rất quan trọng**: Các phiên bản đầu tiên sử dụng tính năng chờ đồng bộ và người dùng đã báo cáo trải nghiệm kém. Sau khi chuyển sang mô hình đốt và quên, sự hài lòng được cải thiện đáng kể.
2. **Thiết kế lời nhắc quyết định chất lượng**: Lời nhắc được thiết kế cẩn thận sẽ đảm bảo chất lượng đầu ra AI nhiều hơn so với một thuật toán phức tạp.
3. **An toàn luôn được đặt lên hàng đầu**: Việc cấp cho AI quyền thực thi các lệnh Git trực tiếp sẽ cải thiện hiệu quả nhưng phải đi kèm với các ràng buộc và xác thực nghiêm ngặt.
4. **Cải tiến lũy tiến mang lại hiệu quả tốt nhất**: Bắt đầu với các tình huống đơn giản và tăng dần độ phức tạp có nhiều khả năng thành công hơn là cố gắng thực hiện mọi thứ cùng một lúc.

Trong tương lai, chúng tôi có kế hoạch tối ưu hóa hơn nữa Cam kết soạn thảo AI, bao gồm:

- Hỗ trợ nhiều chiến lược nhóm cam kết hơn (theo thời gian, theo nhà phát triển, v.v.)
- Tích hợp quy trình đánh giá mã để kích hoạt đánh giá tự động trước khi cam kết
- Hỗ trợ các mẫu tin nhắn cam kết tùy chỉnh để đáp ứng nhu cầu cá nhân hóa của các dự án khác nhau

Nếu bạn thấy cách tiếp cận được chia sẻ trong bài viết này có giá trị, hãy dùng thử HagiCode và trải nghiệm cách tính năng này hoạt động trong quá trình phát triển thực tế. Suy cho cùng, thực hành là tiêu chí duy nhất để kiểm chứng sự thật.

---

Cảm ơn bạn đã đọc. Nếu bạn thấy bài viết này hữu ích, vui lòng nhấp vào nút thích bên dưới để nhiều người có thể khám phá nó.

Nội dung này được tạo bằng sự cộng tác có sự hỗ trợ của AI, được tôi xem xét và phản ánh quan điểm cũng như quan điểm của riêng tôi.

- tác giả: [newbe36524](https://www.newbe.pro)
- Liên kết bài viết: [https://docs.hagicode.com/blog/2026-02-26-ai-compose-commit-implementation/](https://docs.hagicode.com/blog/2026-02-26-ai-compose-commit-implementation/)
- Thông báo bản quyền: Trừ khi có quy định khác, tất cả các bài viết trên blog này đều được cấp phép theo BY-NC-SA. Vui lòng trích dẫn nguồn khi đăng lại.

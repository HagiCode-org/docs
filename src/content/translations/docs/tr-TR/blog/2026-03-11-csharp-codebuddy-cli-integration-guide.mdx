---
title: "CodeBuddy CLI'yi C# Arka Ucuna Entegre Etmeye Yönelik Pratik Kılavuz"
date: 2026-03-12
tags: ["C#", CodeBuddy, AI Provider, ACP, "Integration Guide"]
description: "Bu makalede, AI kodlama asistanı yeteneklerini uçtan uca sunabilmeniz için CodeBuddy CLI'yi bir C# arka uç projesine entegre etmeye yönelik eksiksiz bir yaklaşım anlatılmaktadır."
---

## CodeBuddy CLI'yi C# Arka Ucuna Entegre Etmeye Yönelik Pratik Kılavuz

> Bu makalede, AI kodlama asistanı yeteneklerini uçtan uca sunabilmeniz için CodeBuddy CLI'yi bir C# arka uç projesine entegre etmeye yönelik eksiksiz bir yaklaşım anlatılmaktadır.

:::not[Zaman bağlamı]
Bu makalede CodeBuddy entegrasyon tasarımı ve sağlayıcı yapısı yayınlandığı tarihteki haliyle ele alınmaktadır. Kalıcı olarak desteklenen mevcut kapsam için bkz. [AI Aracısı CLI Kurulumu](/tr-TR/related-software-installation/ai-agent-cli/) ve [Kurulum Kılavuzu](/tr-TR/installation); `IFlowCli` artık yalnızca geçmiş uyumluluk değeri olarak tutuluyor ve mevcut aktif destek kapsamının bir parçası değil.
:::

## Arka plan

Modern yapay zeka kodlama asistanı geliştirmede, tek bir yapay zeka sağlayıcısı genellikle karmaşık ve değişen geliştirme senaryolarını karşılayamaz. Çok işlevli bir yapay zeka kodlama asistanı olarak HagiCode'un, daha iyi bir kullanıcı deneyimi sunmak için birden fazla Yapay Zeka Sağlayıcısını desteklemesi gerekiyor. Kullanıcıların yeterli seçim özgürlüğüne sahip olması gerekir. 2026'nın başlarında proje önemli bir kararla karşı karşıya kaldı: C# arka ucunda CodeBuddy ACP (Ajan İletişim Protokolü) entegrasyon yeteneklerinin nasıl geri yükleneceği.

Proje daha önce CodeBuddy entegrasyonunu uygulamıştı ancak ilgili kod, yeniden düzenleme sırasında kaldırıldı. Orada şikayet edecek pek bir şey yok; yinelemeli geliştirme sırasında daima bir şeyler geride kalır. Bu teknik çözümün amacı, bu yeteneği tamamen geri kazandırmak ve mimariyi daha sağlam ve bakımı kolay olacak şekilde geliştirmekti.

Ayrıca birden fazla AI kodlama asistanını kendi projenize bağlamayı düşünüyorsanız aşağıdaki yaklaşım size bazı fikirler verebilir. Pek çok tuzağa düştükten sonra özetlediğimiz dersleri yansıtıyor ve belki de birkaç dolambaçlı yoldan kaçınmanıza yardımcı olabilir.

## HagiCode Hakkında

Bu makalede paylaşılan yaklaşım, uygulamadaki deneyimlerimizden gelmektedir. [Hagi Kodu](https://hagicode.com) proje. HagiCode, birden fazla Yapay Zeka Sağlayıcısını ve platformlar arası çalışmayı destekleyen açık kaynaklı bir Yapay Zeka kodlama asistanı projesidir. Farklı kullanıcı tercihlerini karşılamak için farklı AI kodlama asistanları arasında esnek bir şekilde geçiş yapmamız gerekiyor; burada açıklanan CodeBuddy entegrasyonunu tam olarak bu nedenle geliştirdik.

HagiCode, AI Sağlayıcılarının takılabilir bileşenler olarak uygulandığı modüler bir tasarım kullanır. Bu mimari, mevcut özellikleri etkilemeden kolayca yeni AI desteği eklememize olanak tanır. Bir tasarım başlangıçta iyi yapıldığında, daha sonra pek çok sorundan kurtarır. Teknik mimarimizle ilgileniyorsanız kaynak kodunun tamamını GitHub'da görüntüleyebilirsiniz.

## Mimari Tasarım

### Katmanlı Mimariye Genel Bakış

C# ve CodeBuddy arasındaki entegrasyon net, katmanlı bir mimari kullanır. Bu tasarım sorumlulukları açıkça ortaya koyar ve uzun vadeli bakımı çok daha kolaylaştırır:

```
┌─────────────────────────────────────────────┐
│           Provider Contract Layer           │
│   AIProviderType enum + extension methods   │
├─────────────────────────────────────────────┤
│           Provider Factory Layer            │
│   AIProviderFactory dependency injection factory │
├─────────────────────────────────────────────┤
│           Provider Implementation Layer     │
│   CodebuddyCliProvider concrete implementation │
├─────────────────────────────────────────────┤
│           ACP Infrastructure Layer          │
│  ACPSessionManager / StdioAcpTransport      │
│  AcpRpcClient / AcpAgentClient              │
└─────────────────────────────────────────────┘
```

Bu katmanlamanın faydaları nelerdir? Basitçe söylemek gerekirse, her katman diğerinin yolundan uzak durur. Daha sonra iletişim mekanizmasını değiştirmek istersek, örneğin `stdio` WebSocket'e geçişte yalnızca alt katmanı değiştirmemiz gerekir ve bunun üzerindeki iş mantığına dokunulmaz. Hiç kimse bir iletişim değişikliğinin kod tabanının tamamına yayılmasını istemez.

### Çekirdek Bileşen Dağılımı

**Sağlayıcı sözleşme katmanı** tüm mimarinin temelidir. biz tanımlarız `AIProviderType` numaralandırma, nerede `CodebuddyCli = 3` enum değeri olarak kullanılır ve uzatma yöntemleri aracılığıyla dizeler ve numaralandırmalar arasında çift yönlü eşleme uygular. Bu, yapılandırma dosyalarındaki dizelerin uygun şekilde numaralandırmalara dönüştürülmesine ve numaralandırmaların çıktıda hata ayıklama için tekrar dizelere dönüştürülmesine olanak tanır.

**Sağlayıcı fabrika katmanı**, yapılandırmaya göre ilgili Sağlayıcı örneğinin oluşturulmasından sorumludur. .NET bağımlılık enjeksiyonunu birlikte kullanır `ActivatorUtilities.CreateInstance` Dinamik yaratım için. Fabrika modelinin avantajı, yeni bir Sağlayıcı eklerken mevcut kodu değiştirmek yerine yalnızca oluşturma mantığını eklemenizin gerekmesidir.

**Sağlayıcı uygulama katmanı** asıl işin gerçekleştiği yerdir. `CodebuddyCliProvider` uygular `IAIProvider` arayüz ve iki çağırma modu sağlar: `ExecuteAsync` akışsız aramalar için ve `StreamAsync` çağrı akışı için.

**ACP altyapı katmanı**, alttaki iletişim temelini sağlar. Bu katman, süreç yönetimi, mesaj serileştirme ve yanıt ayrıştırma dahil tüm protokol ayrıntılarını yönetir. Üzerindeki her şeyi sabit tutan temeldir.

## İletişim Mekanizması

### Stdio Aktarım Modu

CodeBuddy, harici işlemlerle iletişim kurmak için **Stdio (standart giriş/çıkış)** kullanır. Başlatma komutu basittir:

```bash
codebuddy --acp
```

Bundan sonra JSON-RPC mesajları standart giriş ve çıkış yoluyla değiştirilir. Bu yaklaşımın çeşitli avantajları vardır:

1. **Hızlı başlatma**: yerel süreç iletişimi ağ gecikmesini önler
2. **Basit yapılandırma**: yalnızca yürütülebilir yolu belirtmeniz gerekir
3. **Ortam yalıtımı**: Her oturum bağımsız bir süreçte çalışır, dolayısıyla birbirlerini etkilemez

İletişim sırasında ortam değişkeni enjeksiyonu desteklenir. Yaygın örnekler şunları içerir:

- `CODEBUDDY_API_KEY`: API anahtarı kimlik doğrulaması
- `CODEBUDDY_INTERNET_ENVIRONMENT`: ağ ortamı yapılandırması

İnsanlar arasındaki iletişimde olduğu gibi öncelikle uygun bir kanalı seçmek yardımcı olur.

### Mesaj Protokolü

ACP, JSON-RPC 2.0'ı temel alır. Mesaj formatı kabaca şuna benzer:

```json
// Request message
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "agent/prompt",
  "params": {
    "prompt": "Help me write a sorting algorithm",
    "sessionId": "session-123"
  }
}

// Response message
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": "Here is the AI response..."
  }
}
```

Gerçek uygulamada, tüm bu protokol ayrıntılarını bir araya getiriyoruz, böylece üst iş katmanının yalnızca istem ve yanıtla ilgilenmesi yeterli oluyor.

## Temel Uygulama

### 1. Sağlayıcı Sözleşmesini Yenileyin

Öncelikle enum dosyasındaki CodeBuddy türünü geri yükleyin:

```csharp
// PCode.Models/AIProviderType.cs
public enum AIProviderType
{
    ClaudeCodeCli = 0,
    CodexCli = 1,
    GitHubCopilot = 2,
    CodebuddyCli = 3,  // Restore this enum value
    OpenCodeCli = 4,
    IFlowCli = 5,
}
```

Ardından, yapılandırma dosyasının Sağlayıcıyı dizeye göre belirtebilmesi için uzantı yöntemlerine dize eşlemesi ekleyin:

```csharp
// AIProviderTypeExtensions.cs
private static readonly Dictionary<string, AIProviderType> _typeMap = new(
    StringComparer.OrdinalIgnoreCase)
{
    ["CodebuddyCli"] = AIProviderType.CodebuddyCli,
    ["Codebuddy"] = AIProviderType.CodebuddyCli,
    ["codebuddy"] = AIProviderType.CodebuddyCli,
    // ... Mappings for other providers
};
```

### 2. Sağlayıcı Fabrikasını Entegre Edin

Fabrika sınıfına bir CodeBuddy oluşturma dalı ekleyin:

```csharp
// AIProviderFactory.cs
private IAIProvider? CreateProvider(AIProviderType providerType, ProviderConfiguration config)
{
    return providerType switch
    {
        AIProviderType.CodebuddyCli =>
            ActivatorUtilities.CreateInstance<CodebuddyCliProvider>(
                _serviceProvider,
                Options.Create(config)),
        // ... Other providers
        _ => throw new NotSupportedException($"Provider {providerType} not supported")
    };
}
```

Bu, bağımlılık enjeksiyonunu kullanır `ActivatorUtilities`Yapıcı parametre enjeksiyonunu otomatik olarak yöneten ve çok kullanışlı olan.

### 3. Sağlayıcı Uygulamasını Tamamlayın

Aşağıda temel uygulama yer almaktadır `CodebuddyCliProvider`, hem akışlı hem de akışsız çağrı modlarını kapsar:

```csharp
public class CodebuddyCliProvider : IAIProvider
{
    private readonly ILogger<CodebuddyCliProvider> _logger;
    private readonly IACPSessionManager _sessionManager;
    private readonly ProviderConfiguration _config;

    public string Name => "CodebuddyCli";
    public bool SupportsStreaming => true;
    public ProviderCapabilities Capabilities { get; }

    public CodebuddyCliProvider(
        ILogger<CodebuddyCliProvider> logger,
        IACPSessionManager sessionManager,
        IOptions<ProviderConfiguration> config)
    {
        _logger = logger;
        _sessionManager = sessionManager;
        _config = config.Value;

        // Define the capabilities of the current Provider
        Capabilities = new ProviderCapabilities
        {
            SupportsStreaming = true,
            SupportsTools = true,
            SupportsSystemMessages = true,
            SupportsArtifacts = false,
            MaxTokens = 8192
        };
    }

    // Non-streaming call: return all results together after completion
    public async Task<AIResponse> ExecuteAsync(
        AIRequest request,
        CancellationToken cancellationToken = default)
    {
        // Create an independent session for the request
        var session = await _sessionManager.CreateSessionAsync(
            "CodebuddyCli",
            request.WorkingDirectory,
            cancellationToken,
            request.SessionId);

        try
        {
            var fullPrompt = BuildPrompt(request);
            await session.SendPromptAsync(fullPrompt, cancellationToken);

            var responseBuilder = new StringBuilder();
            var toolCalls = new List<AIToolCall>();

            // Collect all response chunks
            await foreach (var chunk in StreamFromSession(session, cancellationToken))
            {
                if (!string.IsNullOrEmpty(chunk.Content))
                {
                    responseBuilder.Append(chunk.Content);
                }
                // Handle tool calls...
            }

            return new AIResponse
            {
                Content = AIResultContentSanitizer.SanitizeResultContent(
                    responseBuilder.ToString()),
                ToolCalls = toolCalls,
                Provider = Name,
                Model = string.Empty
            };
        }
        finally
        {
            // Release session resources
            await session.DisposeAsync();
        }
    }

    // Streaming call: return response chunks in real time
    public async IAsyncEnumerable<AIStreamingChunk> StreamAsync(
        AIRequest request,
        [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var session = await _sessionManager.CreateSessionAsync(
            "CodebuddyCli",
            request.WorkingDirectory,
            cancellationToken);

        try
        {
            var fullPrompt = BuildPrompt(request);
            await session.SendPromptAsync(fullPrompt, cancellationToken);

            await foreach (var chunk in StreamFromSession(session, cancellationToken))
            {
                yield return chunk;
            }
        }
        finally
        {
            await session.DisposeAsync();
        }
    }

    private async IAsyncEnumerable<AIStreamingChunk> StreamFromSession(
        IACPSession session,
        [EnumeratorCancellation] CancellationToken cancellationToken)
    {
        // Iterate through all updates in the session
        await foreach (var notification in session.ReceiveUpdatesAsync(cancellationToken))
        {
            switch (notification.Update)
            {
                case AgentMessageChunkSessionUpdate agentMessage:
                    // Handle text content chunks
                    if (agentMessage.Content is AcpImp.TextContentBlock textContent)
                    {
                        yield return new AIStreamingChunk
                        {
                            Content = textContent.Text,
                            Type = StreamingChunkType.ContentDelta,
                            IsComplete = false
                        };
                    }
                    break;

                case ToolCallSessionUpdate toolCall:
                    // Handle tool calls
                    yield return new AIStreamingChunk
                    {
                        Content = string.Empty,
                        Type = StreamingChunkType.ToolCallDelta,
                        ToolCallDelta = new AIToolCallDelta
                        {
                            Id = toolCall.ToolCallId,
                            Name = toolCall.Kind.ToString(),
                            Arguments = toolCall.RawInput?.ToString()
                        }
                    };
                    break;

                case AcpImp.PromptCompletedSessionUpdate:
                    // Response complete
                    yield break;
            }
        }
    }

    // Build the full prompt
    private string BuildPrompt(AIRequest request, string? embeddedCommandPrompt = null)
    {
        var sb = new StringBuilder();

        // Embedded command prompt, if present
        if (!string.IsNullOrEmpty(embeddedCommandPrompt))
        {
            sb.AppendLine(embeddedCommandPrompt);
            sb.AppendLine();
        }

        // System message
        if (!string.IsNullOrEmpty(request.SystemMessage))
        {
            sb.AppendLine(request.SystemMessage);
            sb.AppendLine();
        }

        // User prompt
        sb.Append(request.Prompt);
        return sb.ToString();
    }
}
```

Bu kodda birkaç önemli nokta vardır:

1. **Oturum yönetimi**: Her istek bağımsız bir oturum oluşturur ve istek tamamlandıktan sonra kaynakları serbest bırakır. Bu deneme yanılma yoluyla öğrenilen bir derstir. Oturumun yeniden kullanımı iyi yönetilmezse durum kirliliği kolayca ortaya çıkar.

2. **Akış işleme**: `IAsyncEnumerable` tüm içeriğin bitmesini beklemek yerine yanıtın henüz oluşturuluyorken döndürülmesine olanak tanır. Bu özellikle uzun metin senaryoları için önemlidir ve kullanıcı deneyimini önemli ölçüde artırır.

3. **Araç çağrıları**: CodeBuddy, araç çağrısını (İşlev Çağrısı) destekler. `ToolCallSessionUpdate`. Bu yetenek, karmaşık kod düzenleme görevleri için kritik öneme sahiptir.

4. **İçerik filtreleme**: `AIResultContentSanitizer` Think blok içeriğini filtrelemek ve çıktıyı temiz tutmak için kullanılır.

### 4. Bağımlılık Enjeksiyon Yapılandırması

Modül kaydı sırasında ilgili hizmetleri ekleyin:

```csharp
// PCodeClaudeHelperModule.cs
public void ConfigureModule(IServiceCollection context)
{
    // Register Provider
    context.Services.AddTransient<CodebuddyCliProvider>();

    // Register ACP infrastructure
    context.Services.AddSingleton<IACPSessionManager, ACPSessionManager>();
    context.Services.AddSingleton<IAcpPlatformConfigurationResolver, AcpPlatformConfigurationResolver>();
    context.Services.AddSingleton<IAIRequestToAcpMapper, AIRequestToAcpMapper>();
    context.Services.AddSingleton<IAcpToAIResponseMapper, AcpToAIResponseMapper>();
}
```

## Yapılandırma Örneği

### Yapılandırma Dosyası

CodeBuddy ile ilgili yapılandırmayı şuraya ekleyin: `appsettings.json`:

```yaml
AI:
  # Default Provider to use
  DefaultProvider: "CodebuddyCli"

  # Provider configuration
  Providers:
    CodebuddyCli:
      Type: "CodebuddyCli"
      WorkingDirectory: "C:/projects/my-app"
      ExecutablePath: "C:/tools/codebuddy.cmd"

  # Platform-specific configuration
  PlatformConfigurations:
    CodebuddyCli:
      ExecutablePath: "C:/tools/codebuddy.cmd"
      Arguments: "--acp"
      StartupTimeoutMs: 5000
      EnvironmentVariables:
        CODEBUDDY_API_KEY: "${CODEBUDDY_API_KEY}"
        CODEBUDDY_INTERNET_ENVIRONMENT: "production"
```

### Yapılandırma Modeli

İlgili konfigürasyon modeli tanımı:

```csharp
public class CodebuddyPlatformConfiguration : IAcpPlatformConfiguration
{
    public string ProviderName => "CodebuddyCli";
    public AcpTransportType TransportType => AcpTransportType.Stdio;

    public string ExecutablePath { get; set; } = "codebuddy";
    public string Arguments { get; set; } = "--acp";
    public int StartupTimeoutMs { get; set; } = 5000;

    public Dictionary<string, string?>? EnvironmentVariables { get; set; }
}
```

## Öğrenilen Pratik Dersler

### Tuzak Günlüğü

Uygulama sırasında bazı tipik tuzaklarla karşılaştık ve bunları burada paylaşmak başkalarının da aynı sapmalardan kaçınmasına yardımcı olabilir:

1. **Oturum sızıntısı sorunu**: İlk başta oturumlar doğru şekilde yayınlanmadı ve bu durum süreç kaynaklarını tüketti. Çözüm kullanmaktı `try-finally` Her istek için kaynakların serbest bırakılmasını sağlamak.

2. **Ortam değişkeni aktarımı**: Windows ve Linux farklı ortam değişkeni sözdizimi kullanır, bu nedenle daha sonra standart hale getirdik `Dictionary<string, string?>` bunu halletmek için.

3. **Zaman aşımı yapılandırması**: CLI'nin başlatılması zaman alır, bu nedenle hızlı istek hatalarını önlemek için 5 saniyelik bir başlatma zaman aşımı ayarladık.

4. **Kodlama sorunları**: Windows'ta varsayılan kodlama, bozuk Çince metinlere neden olabilir; bu nedenle, işlem başlatılırken UTF-8 kodlaması açıkça belirtilir.

### Performans Optimizasyonu

1. **Oturum havuzu**: Sık sık yapılan kısa istekler için süreçleri yeniden kullanmak amacıyla bir oturum havuzu oluşturmayı düşünün
2. **Bağlantı önbelleği**: fabrika sınıfı, Sağlayıcı örneklerinin önbelleğe alınmasını zaten desteklemektedir
3. **Önce eşzamansız**: iş parçacıklarının engellenmesini önlemek için baştan sona eşzamansız programlamayı kullanın

Performans her zaman optimize edilmeye değerdir. Kullanıcılar ne kadar uzun süre beklerse deneyim o kadar kötü olur.

## Sonuç

Bu makalede, CodeBuddy CLI'yi bir C# arka ucuna entegre etmek için mimari tasarımdan somut uygulamaya kadar tüm süreci kapsayan eksiksiz bir çözüm tanıtıldı. Katmanlı bir mimari aracılığıyla protokol ayrıntılarını iş mantığından ayırarak kodu daha net ve bakımı kolay hale getiriyoruz.

Temel çıkarımlar:

- Sağlayıcı sözleşme katmanı, fabrika katmanı, uygulama katmanı ve altyapı katmanıyla katmanlı bir mimari kullanın
- İşlemler arası iletişim için Stdio üzerinden JSON-RPC kullanın
- Bağımlılık ekleme yoluyla esnek yapılandırma ve genişletilebilirlik uygulayın
- Hem akışlı hem de akışsız çağrı modlarını sağlayın

Bu yaklaşım yalnızca CodeBuddy için uygun değildir; yeni Yapay Zeka Sağlayıcıları eklemek de aynı yolu izleyebilir. Siz de benzer bir çoklu AI Sağlayıcı entegrasyonu oluşturuyorsanız bu makalenin size yararlı bir referans vereceğini umuyorum.

---

## Referanslar

- [CodeBuddy Resmi Belgeleri](https://codebuddy.example.com)
- [ACP Protokolü Spesifikasyonu](https://acp-protocol.example.com)
- [HagiCode Proje Ana Sayfası](https://hagicode.com)
- [HagiCode GitHub Deposu](https://github.com/HagiCode-org/site)
- [.NET Bağımlılık Ekleme En İyi Uygulamaları](https://docs.microsoft.com/zh-cn/dotnet/core/extensions/dependency-injection)

---

**Bu makale size yardımcı olduysa:**

- GitHub'da bize bir Yıldız verin: [github.com/HagiCode-org/site](https://github.com/HagiCode-org/site)
- Daha fazla bilgi edinmek için resmi web sitesini ziyaret edin: [hagicode.com](https://hagicode.com)
- 30 dakikalık pratik demoyu izleyin: [www.bilibili.com/video/BV1pirZBuEzq/](https://www.bilibili.com/video/BV1pirZBuEzq/)
- Tek tıklamayla kurulumu deneyin: [docs.hagicode.com/installation/docker-compose](https://docs.hagicode.com/installation/docker-compose)
- Masaüstü uygulaması için hızlı kurulum: [hagicode.com/desktop/](https://hagicode.com/desktop/)
- Herkese açık beta başladı; yüklemekten ve denemekten çekinmeyin

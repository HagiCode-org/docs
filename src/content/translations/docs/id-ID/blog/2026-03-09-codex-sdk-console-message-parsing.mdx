---
title: Panduan Lengkap untuk Penguraian Pesan Konsol Codex SDK
date: 2026-03-10
tags: [Codex SDK, TypeScript, AI Executor, Event Stream]
description: "Artikel ini menjelaskan mekanisme aliran peristiwa Codex SDK, penguraian jenis pesan, dan praktik terbaik dalam proyek nyata, membantu pengembang dengan cepat menguasai"
---

## Panduan Lengkap untuk Penguraian Pesan Konsol Codex SDK

> Artikel ini menjelaskan mekanisme aliran peristiwa Codex SDK, penguraian jenis pesan, dan praktik terbaik dalam proyek nyata, membantu pengembang dengan cepat menguasai keterampilan inti di balik layanan eksekusi AI.

## Latar Belakang

Saat membangun layanan eksekusi AI berdasarkan Codex SDK, kita pasti menghadapi pertanyaan praktis: bagaimana cara menangani pesan peristiwa streaming yang dikembalikan oleh Codex? Pesan-pesan ini berisi informasi penting seperti status eksekusi, konten keluaran, dan detail kesalahan, sehingga memerlukan penanganan yang hati-hati.

Sebagai bagian dari proyek HagiCode, kami membutuhkan pelaksana yang andal untuk skenario asisten pengkodean AI. Itulah sebabnya kami memutuskan untuk mempelajari mekanisme aliran peristiwa Codex SDK secara mendalam. Lagi pula, hanya dengan memahami cara kerja pesan-pesan yang mendasarinya, kita dapat membangun platform eksekusi AI tingkat perusahaan.

Codex SDK adalah SDK bantuan pemrograman yang dirilis oleh OpenAI. Ia mengembalikan hasil eksekusi melalui Aliran Acara. Berbeda dengan model permintaan-respons tradisional, Codex menggunakan peristiwa yang dialirkan sehingga kita dapat:

- Dapatkan kemajuan eksekusi secara real time
- Tangani kesalahan dengan segera
- Dapatkan statistik penggunaan token terperinci
- Mendukung tugas-tugas kompleks yang sudah berjalan lama

Memahami jenis peristiwa ini dan menguraikannya dengan benar sangat penting untuk menerapkan pelaksana AI yang berkemampuan penuh. Pada akhirnya, tidak ada seorang pun yang mau bekerja dengan kotak hitam.

## Tentang HagiCode

Solusi yang dibagikan dalam artikel ini berasal dari pengalaman praktis kami di [Kode Hagi](https://hagicode.com) proyek. HagiCode adalah proyek asisten pengkodean AI sumber terbuka yang didedikasikan untuk memberikan dukungan pengkodean cerdas kepada pengembang. Selama pengembangan, kami perlu membangun layanan eksekusi AI yang andal untuk menangani permintaan eksekusi kode pengguna, yang merupakan alasan langsung kami memperkenalkan Codex SDK.

Sebagai asisten pengkodean AI, HagiCode perlu menangani berbagai skenario eksekusi kode yang kompleks: mendapatkan kemajuan eksekusi secara real-time, menangani kesalahan dengan segera, dan mengumpulkan statistik penggunaan token yang terperinci. Dengan memahami secara mendalam mekanisme aliran peristiwa Codex SDK, kita dapat membangun eksekutor yang memenuhi persyaratan lingkungan produksi. Pada akhirnya, baik itu perangkat lunak atau kehidupan nyata, semuanya mendapat manfaat dari akumulasi dan penyempurnaan yang stabil.

## Mekanisme Aliran Acara

### Konsep Dasar

Codex SDK menggunakan `thread.runStreamed()` metode untuk mengembalikan iterator acara asinkron:

```typescript
import { Codex } from '@openai/codex-sdk';

const client = new Codex({
  apiKey: process.env.CODEX_API_KEY,
  baseUrl: process.env.CODEX_BASE_URL,
});

const thread = client.startThread({
  workingDirectory: '/path/to/project',
  skipGitRepoCheck: false,
});

const { events } = await thread.runStreamed('your prompt here', {
  outputSchema: {
    type: 'object',
    properties: {
      output: { type: 'string' },
      status: { type: 'string', enum: ['ok', 'action_required'] },
    },
    required: ['output', 'status'],
  },
});

for await (const event of events) {
  // Handle each event
}
```

### Penjelasan Detail Jenis Acara

| Jenis Acara | Deskripsi | Data Kunci |
|---------|------|----------|
| `thread.started` | Rangkaian pesan berhasil dimulai | `thread_id` |
| `item.updated` | Konten pesan diperbarui | `item.text` |
| `item.completed` | Pesan selesai | `item.text` |
| `turn.completed` | Eksekusi selesai | `usage` (penggunaan token) |
| `turn.failed` | Eksekusi gagal | `error.message` |
| `error` | Peristiwa kesalahan | `message` |

Dalam proyek nyata, komponen pelaksana HagiCode dibangun di atas jenis peristiwa ini. Kami perlu menangani setiap jenis peristiwa dengan hati-hati untuk memastikan pengalaman pengguna yang lancar. Sistem yang baik dibangun dengan memperhatikan detail secara serius.

## Implementasi Penguraian Pesan

### Mengekstrak Isi Pesan

Konten pesan diekstraksi melalui event handler:

```typescript
private handleThreadEvent(event: ThreadEvent, onMessage: (content: string) => void): void {
  // Only handle message update and completion events
  if (event.type !== 'item.updated' && event.type !== 'item.completed') {
    return;
  }

  // Only handle agent message content
  if (event.item.type !== 'agent_message') {
    return;
  }

  // Extract text content
  onMessage(event.item.text);
}
```

Poin-poin penting:
- Hanya menangani `item.updated` dan `item.completed` acara
- Hanya menangani konten bertipe `agent_message`
- Isi pesan ada di `event.item.text` bidang

### Penguraian Keluaran Terstruktur

Codex mendukung keluaran terstruktur JSON. Anda dapat menentukan format pengembalian melalui `outputSchema` parameter:

```typescript
const DEFAULT_OUTPUT_SCHEMA = {
  type: 'object',
  properties: {
    output: { type: 'string' },
    status: { type: 'string', enum: ['ok', 'action_required'] },
  },
  required: ['output', 'status'],
  additionalProperties: false,
} as const;
```

Fungsi penguraian mencoba mengurai JSON, dan jika gagal, fungsi tersebut akan dikembalikan ke teks mentah.

```typescript
function toStructuredOutput(raw: string): StructuredOutput {
  try {
    const parsed = JSON.parse(raw) as Partial<StructuredOutput>;
    if (typeof parsed.output === 'string') {
      return {
        output: parsed.output,
        status: parsed.status === 'action_required' ? 'action_required' : 'ok',
      };
    }
  } catch {
    // JSON parsing failed, fall back to the raw text
  }

  return {
    output: raw,
    status: 'ok',
  };
}
```

### Alur Penanganan Acara Lengkap

```typescript
private async runWithStreaming(
  thread: Thread,
  input: CodexStageExecutionInput
): Promise<{ output: string; usage: Usage | null }> {
  const abortController = new AbortController();
  const timeoutHandle = setTimeout(() => {
    abortController.abort();
  }, Math.max(1000, input.timeoutMs));

  let latestMessage = '';
  let usage: Usage | null = null;
  let emittedLength = 0;

  try {
    const { events } = await thread.runStreamed(input.prompt, {
      outputSchema: DEFAULT_OUTPUT_SCHEMA,
      signal: abortController.signal,
    });

    for await (const event of events) {
      // Handle message content
      this.handleThreadEvent(event, (nextContent) => {
        const delta = nextContent.slice(emittedLength);
        if (delta.length > 0) {
          emittedLength = nextContent.length;
          input.callbacks?.onChunk?.(delta);  // Streaming callback
        }
        latestMessage = nextContent;
      });

      // Process different data based on the event type
      if (event.type === 'thread.started') {
        this.threadId = event.thread_id;
      } else if (event.type === 'turn.completed') {
        usage = event.usage;
      } else if (event.type === 'turn.failed') {
        throw new CodexExecutorError('gateway_unavailable', event.error.message, true);
      } else if (event.type === 'error') {
        throw new CodexExecutorError('gateway_unavailable', event.message, true);
      }
    }
  } catch (error) {
    if (abortController.signal.aborted) {
      throw new CodexExecutorError(
        'upstream_timeout',
        `Codex stage timed out after ${input.timeoutMs}ms`,
        true
      );
    }
    throw error;
  } finally {
    clearTimeout(timeoutHandle);
  }

  const structured = toStructuredOutput(latestMessage);
  return { output: structured.output, usage };
}
```

## Strategi Penanganan Kesalahan

### Pemetaan Kode Kesalahan

Petakan pola kesalahan tertentu ke kode kesalahan nyata sehingga lapisan atas dapat menanganinya dengan lebih mudah:

```typescript
function mapError(error: unknown): CodexExecutorError {
  if (error instanceof CodexExecutorError) {
    return error;
  }

  const message = error instanceof Error ? error.message : String(error);
  const normalized = message.toLowerCase();

  // Authentication errors - not retryable
  if (normalized.includes('401') ||
      normalized.includes('403') ||
      normalized.includes('api key') ||
      normalized.includes('auth')) {
    return new CodexExecutorError('auth_invalid', message, false);
  }

  // Rate limit errors - retryable
  if (normalized.includes('429') || normalized.includes('rate limit')) {
    return new CodexExecutorError('rate_limited', message, true);
  }

  // Timeout errors - retryable
  if (normalized.includes('timeout') || normalized.includes('aborted')) {
    return new CodexExecutorError('upstream_timeout', message, true);
  }

  // Default error
  return new CodexExecutorError('gateway_unavailable', message, true);
}
```

### Definisi Jenis Kesalahan

```typescript
export type CodexErrorCode =
  | 'auth_invalid'      // Authentication failure
  | 'upstream_timeout'  // Upstream timeout
  | 'rate_limited'      // Rate limited
  | 'gateway_unavailable'; // Gateway unavailable

export class CodexExecutorError extends Error {
  readonly code: CodexErrorCode;
  readonly retryable: boolean;

  constructor(code: CodexErrorCode, message: string, retryable: boolean) {
    super(message);
    this.name = 'CodexExecutorError';
    this.code = code;
    this.retryable = retryable;
  }
}
```

## Direktori Kerja dan Konfigurasi Lingkungan

### Validasi Direktori Kerja

Codex SDK memerlukan direktori kerja sebagai repositori Git yang valid.

```typescript
export function validateWorkingDirectory(
  workingDirectory: string,
  skipGitRepoCheck: boolean
): void {
  const resolvedWorkingDirectory = path.resolve(workingDirectory);

  if (!existsSync(resolvedWorkingDirectory)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory does not exist.',
      false
    );
  }

  if (!statSync(resolvedWorkingDirectory).isDirectory()) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a directory.',
      false
    );
  }

  if (skipGitRepoCheck) {
    return;
  }

  const gitDir = path.join(resolvedWorkingDirectory, '.git');
  if (!existsSync(gitDir)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a git repository.',
      false
    );
  }
}
```

### Memuat Variabel Lingkungan

Codex SDK perlu memuat variabel lingkungan dari shell login sehingga Agen AI dapat mengakses perintah sistem:

```typescript
function parseEnvironmentOutput(output: Buffer): Record<string, string> {
  const parsed: Record<string, string> = {};

  for (const entry of output.toString('utf8').split('\0')) {
    if (!entry) continue;

    const separatorIndex = entry.indexOf('=');
    if (separatorIndex <= 0) continue;

    const key = entry.slice(0, separatorIndex);
    const value = entry.slice(separatorIndex + 1);
    if (key.length > 0) {
      parsed[key] = value;
    }
  }

  return parsed;
}

function tryLoadEnvironmentFromShell(shellPath: string): Record<string, string> | null {
  const result = spawnSync(shellPath, ['-ilc', 'env -0'], {
    env: process.env,
    stdio: ['ignore', 'pipe', 'pipe'],
    timeout: 5000,
  });

  if (result.error || result.status !== 0) {
    return null;
  }

  return parseEnvironmentOutput(result.stdout);
}

export function createExecutorEnvironment(
  envOverrides: Record<string, string> = {}
): Record<string, string> {
  // Load environment variables from the login shell
  const consoleEnv = loadConsoleEnvironmentFromShell();

  return {
    ...process.env,
    ...consoleEnv,
    ...envOverrides,
  };
}
```

## Contoh Penggunaan Lengkap

### Penggunaan Dasar

Dalam proyek HagiCode, kami menggunakan pendekatan berikut untuk menginisialisasi klien Codex dan menjalankan tugas:

```typescript
import { Codex } from '@openai/codex-sdk';

async function executeWithCodex(prompt: string, workingDir: string) {
  const client = new Codex({
    apiKey: process.env.CODEX_API_KEY,
    env: { PATH: process.env.PATH },
  });

  const thread = client.startThread({
    workingDirectory: workingDir,
  });

  const { events } = await thread.runStreamed(prompt);

  let result = '';
  for await (const event of events) {
    if (event.type === 'item.updated' && event.item.type === 'agent_message') {
      result = event.item.text;
    }
    if (event.type === 'turn.completed') {
      console.log('Token usage:', event.usage);
    }
  }

  // Try to parse JSON output
  try {
    const parsed = JSON.parse(result);
    return parsed.output;
  } catch {
    return result;
  }
}
```

### Selesaikan Implementasi dengan Percobaan Ulang

```typescript
export class CodexSdkExecutor {
  private readonly config: CodexRuntimeConfig;
  private readonly client: Codex;
  private threadId: string | null = null;

  async executeStage(input: CodexStageExecutionInput): Promise<CodexStageExecutionResult> {
    const maxAttempts = Math.max(1, this.config.retryCount + 1);
    let attempt = 0;
    let lastError: CodexExecutorError | null = null;

    while (attempt < maxAttempts) {
      attempt += 1;

      try {
        const thread = this.getThread(input.workingDirectory);
        const { output, usage } = await this.runWithStreaming(thread, input);

        return {
          output,
          usage,
          threadId: this.threadId!,
          attempts: attempt,
          latencyMs: Date.now() - startedAt,
        };
      } catch (error) {
        const mappedError = mapError(error);
        lastError = mappedError;

        // Non-retryable error or max retry attempts reached
        if (!mappedError.retryable || attempt >= maxAttempts) {
          throw mappedError;
        }

        // Wait before retrying
        await new Promise(resolve => setTimeout(resolve, 1000 * attempt));
      }
    }

    throw lastError!;
  }
}
```

## Praktik Terbaik

### 1. Persyaratan Direktori Kerja

- Pastikan direktori kerja adalah repositori Git yang valid
- Gunakan `PROJECT_ROOT` variabel lingkungan untuk menentukannya secara eksplisit
- Selama debugging, Anda dapat mengatur `CODEX_SKIP_GIT_REPO_CHECK=true` untuk melewatkan pemeriksaan

### 2. Konfigurasi Variabel Lingkungan

- Hanya meneruskan variabel lingkungan yang diperlukan melalui mekanisme daftar putih
- Gunakan shell login untuk memuat lingkungan penuh
- Hindari menyebarkan informasi sensitif

### 3. Strategi Batas Waktu dan Coba Lagi

- Tetapkan batas waktu yang wajar berdasarkan kompleksitas tugas
- Terapkan backoff eksponensial untuk kesalahan yang dapat dicoba lagi
- Catat jumlah percobaan ulang dan alasannya

### 4. Penanganan Kesalahan

- Bedakan antara kesalahan yang dapat dicoba ulang dan kesalahan yang tidak dapat dicoba ulang
- Berikan pesan kesalahan dan saran yang jelas
- Gunakan kode kesalahan terpadu sehingga lapisan atas dapat menanganinya secara konsisten

### 5. Keluaran Streaming

- Terapkan callback keluaran tambahan untuk meningkatkan pengalaman pengguna
- Menangani pembaruan pesan tambahan dengan benar
- Catat penggunaan token untuk analisis biaya

Dalam lingkungan produksi proyek HagiCode yang sebenarnya, kami telah memverifikasi efektivitas praktik terbaik di atas. Pendekatan ini telah membantu kami membangun layanan eksekusi AI yang stabil dan andal. Pada akhirnya, validasi praktis lebih penting daripada teori saja.

## Kesimpulan

Mekanisme aliran peristiwa Codex SDK memberikan kemampuan yang kuat untuk membangun layanan eksekusi AI. Dengan mengurai berbagai jenis peristiwa dengan benar, kita dapat:

- Dapatkan status eksekusi dan keluaran secara real time
- Menerapkan mekanisme penanganan kesalahan dan percobaan ulang yang andal
- Dapatkan statistik eksekusi terperinci
- Bangun platform eksekusi AI berfitur lengkap

Konsep inti dan contoh kode yang diperkenalkan dalam artikel ini dapat diterapkan langsung dalam proyek nyata, membantu pengembang memulai dengan cepat integrasi Codex SDK. Jika Anda menganggap pendekatan ini berharga, pendekatan ini juga mencerminkan kekuatan praktik teknik HagiCode dan menjadikan HagiCode sendiri layak untuk diikuti.

## Referensi

- [Dokumentasi Resmi Codex SDK](https://platform.openai.com/docs/codex)
- [Repositori GitHub HagiCode](https://github.com/HagiCode-org/site)
- [Situs Resmi HagiCode](https://hagicode.com)

---

Terima kasih telah membaca. Jika Anda merasa artikel ini bermanfaat, silakan klik tombol suka di bawah agar lebih banyak orang dapat menemukannya.

Konten ini dibuat dengan kolaborasi yang dibantu AI, ditinjau oleh penulis, dan mencerminkan pandangan dan posisi penulis sendiri.

- Penulis: [baru36524](https://www.newbe.pro)
- Tautan artikel: [https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/](https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/)
- Pemberitahuan hak cipta: Kecuali dinyatakan lain, semua artikel di blog ini dilisensikan di bawah BY-NC-SA. Harap mencantumkan sumbernya saat mencetak ulang.

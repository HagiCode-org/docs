---
title: "Praktisk guide til integration af CodeBuddy CLI i en C#-backend"
date: 2026-03-12
tags: ["C#", CodeBuddy, AI Provider, ACP, "Integration Guide"]
description: "Denne artikel gennemgår en komplet tilgang til at integrere CodeBuddy CLI i et C#-backend-projekt, så du kan levere AI-kodningsassistent-funktioner til slut"
---

## Praktisk guide til integration af CodeBuddy CLI i en C#-backend

> Denne artikel gennemgår en komplet tilgang til at integrere CodeBuddy CLI i et C#-backend-projekt, så du kan levere AI-kodningsassistentfunktioner fra ende til anden.

:::note[Tidskontekst]
Denne artikel diskuterer CodeBuddy-integrationsdesignet og udbyderstrukturen, som de eksisterede på udgivelsestidspunktet. For det nuværende permanent understøttede omfang henvises til [AI Agent CLI installation](/da-DK/related-software-installation/ai-agent-cli/) og den [Installationsvejledning](/da-DK/installation); `IFlowCli` bevares nu kun som en historisk kompatibilitetsværdi og er ikke en del af det nuværende aktive supportomfang.
:::

## Baggrund

I moderne AI-kodningsassistentudvikling kan en enkelt AI-udbyder ofte ikke tilfredsstille komplekse og skiftende udviklingsscenarier. HagiCode, som en multifunktionel AI-kodningsassistent, skal understøtte flere AI-udbydere for at levere en bedre brugeroplevelse. Brugerne skal have tilstrækkelig frihed til at vælge. I begyndelsen af ​​2026 stod projektet over for en nøglebeslutning: hvordan man genskaber CodeBuddy ACP (Agent Communication Protocol) integrationskapaciteter i C#-backend.

Projektet havde tidligere implementeret CodeBuddy-integration, men den relaterede kode blev fjernet under en refactor. Der er ikke meget at klage over der; under iterativ udvikling bliver noget altid efterladt. Målet med denne tekniske løsning var fuldt ud at genoprette denne kapacitet og forbedre arkitekturen, så den ville være mere robust og vedligeholdelig.

Hvis du også overvejer at forbinde flere AI-kodningsassistenter til dit eget projekt, kan fremgangsmåden herunder give dig nogle ideer. Det afspejler lektioner, vi opsummerede efter at være trådt ind i mange faldgruber, og måske kan det hjælpe dig med at undgå et par omveje.

## Om HagiCode

Den tilgang, der er delt i denne artikel, kommer fra vores praktiske erfaring i [HagiCode](https://hagicode.com) projekt. HagiCode er et open source AI-kodningsassistentprojekt, der understøtter flere AI-udbydere og drift på tværs af platforme. For at tilfredsstille forskellige brugerpræferencer er vi nødt til at skifte fleksibelt mellem forskellige AI-kodningsassistenter, hvilket netop er grunden til, at vi byggede CodeBuddy-integrationen beskrevet her.

HagiCode bruger et modulært design med AI-udbydere implementeret som pluggbare komponenter. Denne arkitektur lader os nemt tilføje ny AI-understøttelse uden at påvirke eksisterende funktioner. Når et design er lavet godt foran, sparer det en masse besvær senere. Hvis du er interesseret i vores tekniske arkitektur, kan du se den fulde kildekode på GitHub.

## Arkitektur design

### Oversigt over lagdelt arkitektur

Integrationen mellem C# og CodeBuddy bruger en klar lagdelt arkitektur. Dette design gør ansvar eksplicit og gør langsigtet vedligeholdelse meget lettere:

```
┌─────────────────────────────────────────────┐
│           Provider Contract Layer           │
│   AIProviderType enum + extension methods   │
├─────────────────────────────────────────────┤
│           Provider Factory Layer            │
│   AIProviderFactory dependency injection factory │
├─────────────────────────────────────────────┤
│           Provider Implementation Layer     │
│   CodebuddyCliProvider concrete implementation │
├─────────────────────────────────────────────┤
│           ACP Infrastructure Layer          │
│  ACPSessionManager / StdioAcpTransport      │
│  AcpRpcClient / AcpAgentClient              │
└─────────────────────────────────────────────┘
```

Hvad er fordelene ved denne lagdeling? Kort sagt forbliver hvert lag ude af vejen for de andre. Hvis vi senere vil ændre kommunikationsmekanismen, f.eks `stdio` til WebSocket behøver vi kun at ændre det nederste lag, og forretningslogikken over det forbliver urørt. Ingen ønsker, at en kommunikationsændring skal bølge gennem hele kodebasen.

### Nedbrydning af kernekomponenter

The **Provider contract layer** is the foundation of the entire architecture. Vi definerer `AIProviderType` enum, hvor `CodebuddyCli = 3` is used as the enum value, and implement bidirectional mapping between strings and enums through extension methods. Det gør det muligt at konvertere strenge i konfigurationsfiler bekvemt til enums, og enums kan konverteres tilbage til strenge til fejlfinding af output.

The **Provider factory layer** is responsible for creating the corresponding Provider instance based on configuration. Den bruger .NET afhængighedsinjektion sammen med `ActivatorUtilities.CreateInstance` til dynamisk skabelse. Fordelen ved fabriksmønsteret er, at når du tilføjer en ny udbyder, skal du kun tilføje oprettelseslogikken i stedet for at ændre eksisterende kode.

The **Provider implementation layer** is where the actual work happens. `CodebuddyCliProvider` gennemfører `IAIProvider` interface og giver to påkaldelsestilstande: `ExecuteAsync` til ikke-streaming opkald og `StreamAsync` til streaming af opkald.

The **ACP infrastructure layer** provides the communication foundation underneath. This layer handles all protocol details, including process management, message serialization, and response parsing. It is the foundation that keeps everything above it stable.

## Kommunikationsmekanisme

### Stdio transporttilstand

CodeBuddy bruger **Stdio (standard input/output)** til at kommunikere med eksterne processer. Opstartskommandoen er enkel:

```bash
codebuddy --acp
```

Derefter udveksles JSON-RPC-meddelelser gennem standard input og output. Denne tilgang har flere fordele:

1. **Hurtig opstart**: lokal proceskommunikation undgår netværksforsinkelse
2. **Simpel konfiguration**: du behøver kun at angive den eksekverbare sti
3. **Miljøisolering**: hver session kører i en uafhængig proces, så de ikke påvirker hinanden

Injektion af miljøvariabel understøttes under kommunikation. Almindelige eksempler omfatter:

- `CODEBUDDY_API_KEY`: API-nøglegodkendelse
- `CODEBUDDY_INTERNET_ENVIRONMENT`: netværksmiljøkonfiguration

Som med kommunikation mellem mennesker, hjælper det først at vælge en bekvem kanal.

### Meddelelsesprotokol

ACP er baseret på JSON-RPC 2.0. Meddelelsesformatet ser nogenlunde sådan ud:

```json
// Request message
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "agent/prompt",
  "params": {
    "prompt": "Help me write a sorting algorithm",
    "sessionId": "session-123"
  }
}

// Response message
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": "Here is the AI response..."
  }
}
```

I den virkelige implementering indkapsler vi alle disse protokoldetaljer, så det øverste forretningslag kun behøver at bekymre sig om prompten og svaret.

## Kerneimplementering

### 1. Gendan udbyderkontrakten

Gendan først CodeBuddy-typen i enum-filen:

```csharp
// PCode.Models/AIProviderType.cs
public enum AIProviderType
{
    ClaudeCodeCli = 0,
    CodexCli = 1,
    GitHubCopilot = 2,
    CodebuddyCli = 3,  // Restore this enum value
    OpenCodeCli = 4,
    IFlowCli = 5,
}
```

Tilføj derefter strengmapping i udvidelsesmetoderne, så konfigurationsfilen kan angive udbyderen efter streng:

```csharp
// AIProviderTypeExtensions.cs
private static readonly Dictionary<string, AIProviderType> _typeMap = new(
    StringComparer.OrdinalIgnoreCase)
{
    ["CodebuddyCli"] = AIProviderType.CodebuddyCli,
    ["Codebuddy"] = AIProviderType.CodebuddyCli,
    ["codebuddy"] = AIProviderType.CodebuddyCli,
    // ... Mappings for other providers
};
```

### 2. Integrer Provider Factory

Tilføj en CodeBuddy oprettelsesgren i fabriksklassen:

```csharp
// AIProviderFactory.cs
private IAIProvider? CreateProvider(AIProviderType providerType, ProviderConfiguration config)
{
    return providerType switch
    {
        AIProviderType.CodebuddyCli =>
            ActivatorUtilities.CreateInstance<CodebuddyCliProvider>(
                _serviceProvider,
                Options.Create(config)),
        // ... Other providers
        _ => throw new NotSupportedException($"Provider {providerType} not supported")
    };
}
```

Dette bruger afhængighedsinjektion igennem `ActivatorUtilities`, som automatisk håndterer konstruktørparameterinjektion og er meget praktisk.

### 3. Fuldfør udbyderimplementering

Nedenfor er kerneimplementeringen af `CodebuddyCliProvider`, der dækker både streaming og ikke-streaming invokationstilstande:

```csharp
public class CodebuddyCliProvider : IAIProvider
{
    private readonly ILogger<CodebuddyCliProvider> _logger;
    private readonly IACPSessionManager _sessionManager;
    private readonly ProviderConfiguration _config;

    public string Name => "CodebuddyCli";
    public bool SupportsStreaming => true;
    public ProviderCapabilities Capabilities { get; }

    public CodebuddyCliProvider(
        ILogger<CodebuddyCliProvider> logger,
        IACPSessionManager sessionManager,
        IOptions<ProviderConfiguration> config)
    {
        _logger = logger;
        _sessionManager = sessionManager;
        _config = config.Value;

        // Define the capabilities of the current Provider
        Capabilities = new ProviderCapabilities
        {
            SupportsStreaming = true,
            SupportsTools = true,
            SupportsSystemMessages = true,
            SupportsArtifacts = false,
            MaxTokens = 8192
        };
    }

    // Non-streaming call: return all results together after completion
    public async Task<AIResponse> ExecuteAsync(
        AIRequest request,
        CancellationToken cancellationToken = default)
    {
        // Create an independent session for the request
        var session = await _sessionManager.CreateSessionAsync(
            "CodebuddyCli",
            request.WorkingDirectory,
            cancellationToken,
            request.SessionId);

        try
        {
            var fullPrompt = BuildPrompt(request);
            await session.SendPromptAsync(fullPrompt, cancellationToken);

            var responseBuilder = new StringBuilder();
            var toolCalls = new List<AIToolCall>();

            // Collect all response chunks
            await foreach (var chunk in StreamFromSession(session, cancellationToken))
            {
                if (!string.IsNullOrEmpty(chunk.Content))
                {
                    responseBuilder.Append(chunk.Content);
                }
                // Handle tool calls...
            }

            return new AIResponse
            {
                Content = AIResultContentSanitizer.SanitizeResultContent(
                    responseBuilder.ToString()),
                ToolCalls = toolCalls,
                Provider = Name,
                Model = string.Empty
            };
        }
        finally
        {
            // Release session resources
            await session.DisposeAsync();
        }
    }

    // Streaming call: return response chunks in real time
    public async IAsyncEnumerable<AIStreamingChunk> StreamAsync(
        AIRequest request,
        [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var session = await _sessionManager.CreateSessionAsync(
            "CodebuddyCli",
            request.WorkingDirectory,
            cancellationToken);

        try
        {
            var fullPrompt = BuildPrompt(request);
            await session.SendPromptAsync(fullPrompt, cancellationToken);

            await foreach (var chunk in StreamFromSession(session, cancellationToken))
            {
                yield return chunk;
            }
        }
        finally
        {
            await session.DisposeAsync();
        }
    }

    private async IAsyncEnumerable<AIStreamingChunk> StreamFromSession(
        IACPSession session,
        [EnumeratorCancellation] CancellationToken cancellationToken)
    {
        // Iterate through all updates in the session
        await foreach (var notification in session.ReceiveUpdatesAsync(cancellationToken))
        {
            switch (notification.Update)
            {
                case AgentMessageChunkSessionUpdate agentMessage:
                    // Handle text content chunks
                    if (agentMessage.Content is AcpImp.TextContentBlock textContent)
                    {
                        yield return new AIStreamingChunk
                        {
                            Content = textContent.Text,
                            Type = StreamingChunkType.ContentDelta,
                            IsComplete = false
                        };
                    }
                    break;

                case ToolCallSessionUpdate toolCall:
                    // Handle tool calls
                    yield return new AIStreamingChunk
                    {
                        Content = string.Empty,
                        Type = StreamingChunkType.ToolCallDelta,
                        ToolCallDelta = new AIToolCallDelta
                        {
                            Id = toolCall.ToolCallId,
                            Name = toolCall.Kind.ToString(),
                            Arguments = toolCall.RawInput?.ToString()
                        }
                    };
                    break;

                case AcpImp.PromptCompletedSessionUpdate:
                    // Response complete
                    yield break;
            }
        }
    }

    // Build the full prompt
    private string BuildPrompt(AIRequest request, string? embeddedCommandPrompt = null)
    {
        var sb = new StringBuilder();

        // Embedded command prompt, if present
        if (!string.IsNullOrEmpty(embeddedCommandPrompt))
        {
            sb.AppendLine(embeddedCommandPrompt);
            sb.AppendLine();
        }

        // System message
        if (!string.IsNullOrEmpty(request.SystemMessage))
        {
            sb.AppendLine(request.SystemMessage);
            sb.AppendLine();
        }

        // User prompt
        sb.Append(request.Prompt);
        return sb.ToString();
    }
}
```

Der er flere nøglepunkter i denne kode:

1. **Sessionsstyring**: hver anmodning opretter en uafhængig session og frigiver ressourcer, efter at anmodningen er fuldført. Dette er en lektie lært gennem forsøg og fejl. Hvis genbrug af sessioner ikke håndteres godt, opstår der let statsforurening.

2. **Streamingbehandling**: `IAsyncEnumerable` tillader, at svaret returneres, mens det stadig genereres, i stedet for at vente på, at alt indhold er færdigt. Dette er især vigtigt for scenarier med lang tekst og forbedrer brugeroplevelsen markant.

3. **Værktøjsopkald**: CodeBuddy understøtter værktøjsopkald (Function Calling), håndteret igennem `ToolCallSessionUpdate`. Denne egenskab er afgørende for komplekse koderedigeringsopgaver.

4. **Indholdsfiltrering**: `AIResultContentSanitizer` bruges til at filtrere Tænk-blokindhold og holde output rent.

### 4. Konfiguration af afhængighedsinjektion

Tilføj de relaterede tjenester under modulregistrering:

```csharp
// PCodeClaudeHelperModule.cs
public void ConfigureModule(IServiceCollection context)
{
    // Register Provider
    context.Services.AddTransient<CodebuddyCliProvider>();

    // Register ACP infrastructure
    context.Services.AddSingleton<IACPSessionManager, ACPSessionManager>();
    context.Services.AddSingleton<IAcpPlatformConfigurationResolver, AcpPlatformConfigurationResolver>();
    context.Services.AddSingleton<IAIRequestToAcpMapper, AIRequestToAcpMapper>();
    context.Services.AddSingleton<IAcpToAIResponseMapper, AcpToAIResponseMapper>();
}
```

## Konfigurationseksempel

### Konfigurationsfil

Tilføj CodeBuddy-relateret konfiguration til `appsettings.json`:

```yaml
AI:
  # Default Provider to use
  DefaultProvider: "CodebuddyCli"

  # Provider configuration
  Providers:
    CodebuddyCli:
      Type: "CodebuddyCli"
      WorkingDirectory: "C:/projects/my-app"
      ExecutablePath: "C:/tools/codebuddy.cmd"

  # Platform-specific configuration
  PlatformConfigurations:
    CodebuddyCli:
      ExecutablePath: "C:/tools/codebuddy.cmd"
      Arguments: "--acp"
      StartupTimeoutMs: 5000
      EnvironmentVariables:
        CODEBUDDY_API_KEY: "${CODEBUDDY_API_KEY}"
        CODEBUDDY_INTERNET_ENVIRONMENT: "production"
```

### Konfigurationsmodel

Den tilsvarende konfigurationsmodeldefinition:

```csharp
public class CodebuddyPlatformConfiguration : IAcpPlatformConfiguration
{
    public string ProviderName => "CodebuddyCli";
    public AcpTransportType TransportType => AcpTransportType.Stdio;

    public string ExecutablePath { get; set; } = "codebuddy";
    public string Arguments { get; set; } = "--acp";
    public int StartupTimeoutMs { get; set; } = 5000;

    public Dictionary<string, string?>? EnvironmentVariables { get; set; }
}
```

## Praktiske erfaringer

### Faldgrube Log

Vi stødte ind i flere typiske faldgruber under implementeringen, og at dele dem her kan hjælpe andre med at undgå de samme omveje:

1. **Problem med sessionslæk**: I starten blev sessioner ikke frigivet korrekt, hvilket opbrugte procesressourcer. Løsningen var at bruge `try-finally` for at sikre, at der frigives ressourcer for hver anmodning.

2. **Miljøvariabeloverførsel**: Windows og Linux bruger forskellige miljøvariablesyntaks, så vi senere standardiserede på `Dictionary<string, string?>` at håndtere dette.

3. **Timeout-konfiguration**: CLI-opstart tager tid, så vi indstiller en 5-sekunders start-timeout for at undgå hurtige anmodningsfejl.

4. **Kodningsproblemer**: På Windows kan standardkodningen forårsage forvansket kinesisk tekst, så UTF-8-kodning er eksplicit angivet, når processen startes.

### Optimering af ydeevne

1. **Sessionspulje**: Ved hyppige korte anmodninger kan du overveje at implementere en sessionspulje til at genbruge processer
2. **Forbindelsescache**: Fabriksklassen understøtter allerede caching Provider-instanser
3. **Async first**: brug asynkron programmering hele vejen igennem for at undgå blokering af tråde

Ydeevne er altid værd at optimere. Jo længere brugere venter, jo dårligere bliver oplevelsen.

## Konklusion

Denne artikel introducerede en komplet løsning til at integrere CodeBuddy CLI i en C#-backend, der dækker hele processen fra arkitekturdesign til konkret implementering. Gennem en lagdelt arkitektur adskiller vi protokoldetaljer fra forretningslogik, hvilket gør koden klarere og nemmere at vedligeholde.

Nøgle takeaways:

- Brug en lagdelt arkitektur med et udbyderkontraktlag, fabrikslag, implementeringslag og infrastrukturlag
- Brug JSON-RPC over Stdio til kommunikation mellem processer
- Implementer fleksibel konfiguration og udvidelsesmuligheder gennem afhængighedsinjektion
- Tilbyder både streaming og ikke-streaming invokationstilstande

Denne tilgang er ikke kun egnet til CodeBuddy; tilføjelse af nye AI-udbydere kan følge det samme mønster. Hvis du også bygger en lignende multi-AI-Provider-integration, håber jeg, at denne artikel giver dig en nyttig reference.

---

## Referencer

- [CodeBuddys officielle dokumentation](https://codebuddy.example.com)
- [ACP-protokolspecifikation](https://acp-protocol.example.com)
- [HagiCode-projektets hjemmeside](https://hagicode.com)
- [HagiCode GitHub Repository](https://github.com/HagiCode-org/site)
- [.NET Dependency Injection Best Practices](https://docs.microsoft.com/zh-cn/dotnet/core/extensions/dependency-injection)

---

**Hvis denne artikel hjalp dig:**

- Giv os en stjerne på GitHub: [github.com/HagiCode-org/site](https://github.com/HagiCode-org/site)
- Besøg den officielle hjemmeside for at lære mere: [hagicode.com](https://hagicode.com)
- Se den 30-minutters praktiske demo: [www.bilibili.com/video/BV1pirZBuEzq/](https://www.bilibili.com/video/BV1pirZBuEzq/)
- Prøv installation med et enkelt klik: [docs.hagicode.com/installation/docker-compose](https://docs.hagicode.com/installation/docker-compose)
- Hurtig installation til Desktop-appen: [hagicode.com/desktop/](https://hagicode.com/desktop/)
- Offentlig beta er startet; du er velkommen til at installere og prøve det

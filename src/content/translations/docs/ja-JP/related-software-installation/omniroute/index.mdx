---
title: OmniRoute のローカル インストールと起動の検証
description: 推奨されるローカル npm パスで OmniRoute をインストールし、ダッシュボードと OpenAI 互換 API を検証します。
sidebar_position: 12
tags:
  - omniroute
  - openai-compatible
  - installation
  - integration
---

import { LinkCard, CardGrid } from '@astrojs/starlight/components';

The official Desktop app is now the recommended path: download the Windows, macOS, or Linux installer from [OmniRoute GitHub Releases](https://github.com/diegosouzapw/OmniRoute/releases). The npm procedure below remains available for headless environments.

このページでは、推奨される OmniRoute のローカル パスを説明します。`npm install -g omniroute` の後に `omniroute start` を実行し、ダッシュボードを `http://localhost:20128`、OpenAI 互換のベース URL を `http://localhost:20128/v1` で使用します。

:::tip[推奨パス]
ダッシュボードは `http://localhost:20128`、OpenAI 互換のベース URL は `http://localhost:20128/v1` です。Docker などの高度な展開モードはオプションです。
:::

## 前提条件

- Node.js は `>=22.0.0 <23` または `>=24.0.0 <27` である必要があります。Node.js 21 と 23 はサポートされていません。完了 [Node.jsのインストール](/ja-JP/related-software-installation/nodejs/installation) 必要に応じて最初に。
- ローカルポート `20128` と `20129` は利用可能で、まだ占有されていません。
- OmniRoute 経由でルーティングする予定のアップストリーム モデル プロバイダー アカウントが少なくとも 1 つあります。
- HagiCode、CLI、または SDK を統合する前に、次の 2 つのアドレスを分離しておきます。
  - ダッシュボード: `http://localhost:20128`
  - OpenAI 互換のベース URL: `http://localhost:  20128/v1`

## ローカルにインストールして実行する

推奨されるグローバル npm パスから始めます。

```bash
npm install -g omniroute
omniroute start
```

サービスが開始されたら、次の順序で検証します。

1. 開く `http://localhost:20128` ブラウザで をクリックし、ダッシュボードが読み込まれることを確認します。
2. に行きます `Endpoints` ダッシュボードのページに移動し、API キーを作成またはコピーします。
3. 後でツールに渡すベース URL をメモします。 `http://localhost:20128/v1`.

:::note[ダッシュボードから始める理由]
最初のセットアップの場合、ダッシュボードは、OmniRoute が実行中であり、プロバイダーが接続されており、API キーが存在していることを確認する最速の方法です。後のクライアント呼び出しが失敗した場合は、通常、SDK コードをすぐにデバッグするよりも、最初にダッシュボードを確認する方が高速です。
:::

## 実行中のサービスを検証する

### 検証 1: モデル カタログが読み取り可能であることを確認する

からキーを入れます `Endpoints` 環境変数にページを追加します。

```bash
export OMNIROUTE_API_KEY="replace-with-the-key-you-copied-from-the-Endpoints-page"
```

それからリクエストしてください `/v1/models`:

```bash
curl http://localhost:20128/v1/models \
  -H "Authorization: Bearer ${OMNIROUTE_API_KEY}"
```

モデルのリストが返された場合、OpenAI 互換のエンドポイントと認証フローは機能しています。

### 検証 2: 最小限のチャット リクエストを送信する

```bash
curl http://localhost:20128/v1/chat/completions \
  -H "Authorization: Bearer ${OMNIROUTE_API_KEY}" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "replace-with-a-model-id-from-v1-models",
    "messages": [
      { "role": "user", "content": "Reply with ok." }
    ]
  }'
```

ここでの目標は、迅速な品質ではありません。これは、選択したモデルがすでに OmniRoute を正常にルーティングできることを証明するためです。

## 次のステップ

<CardGrid>
  <LinkCard
  title="Installing Node.js"
  href="/ja-JP/related-software-installation/nodejs/installation"
>
    Complete the runtime prerequisite first if the machine does not have a usable Node.js install yet.
</LinkCard>
  <LinkCard
  title="AI Agent CLI Installation"
  href="/ja-JP/related-software-installation/ai-agent-cli/"
>
    Complete local Agent CLI installation first if your CLI environment is not ready yet.
</LinkCard>
  <LinkCard
  title="OmniRoute Upstream Project"
  href="https://github.com/diegosouzapw/OmniRoute"
>
    Continue with the upstream repository and official guides when you need fuller reference material.
</LinkCard>
</CardGrid>

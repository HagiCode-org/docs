---
title: Развертывание Docker Compose
description: Развертывание всей системы HagiCode одним щелчком мыши с помощью Docker Compose.
sidebar_position: 10
---

В этом руководстве рассматривается развертывание HagiCode с помощью Docker Compose. Для большинства пользователей это путь развертывания по умолчанию в средах разработки, тестирования и производства.

## Видеоурок

Если вы хотите сначала увидеть весь процесс, начните с видеоурока:

[Посмотрите видеоурок по Bilibili](https://www.bilibili.com/video/BV19967B6EHr/)

**Видеоконтент включает**:
- Полная демонстрация процесса развертывания Docker Compose.
- Инструкции по созданию и изменению файла конфигурации
- Запуск службы и этапы проверки
- Распространенные проблемы и решения

Продолжительность видео: примерно 15 минут.

:::note[Docker Compose Builder]
Docker Compose Builder теперь доступен как отдельный инструмент по адресу [https://builder.hagicode.com/](https://builder.hagicode.com/). Используйте этот сайт в качестве основного источника для создания файлов конфигурации того же класса.
:::

:::tip[Использовать генератор конфигурации]
Вы можете использовать [Docker Compose Builder (автономный сайт)](https://builder.hagicode.com/) генерировать `docker-compose.yml` напрямую.

Генератор поддерживает:
- пользовательские порты, имена контейнеров и другие базовые настройки
- выбор встроенной или внешней базы данных
- автоматические различия платформ Windows и Linux
- общая обработка прав доступа к файлам
- выбор источника изображения в зависимости от вашей сетевой среды

[Открыть генератор →](https://builder.hagicode.com/)
:::

:::tip[Рекомендуемый метод]
Docker Compose в большинстве случаев является путем развертывания по умолчанию, потому что:
- изоляция среды уменьшает конфликты зависимостей
- поддерживаемый стек сервисов HagiCode может запускаться вместе
- конфигурацию, обновления и обслуживание легче поддерживать в единообразном виде
- он хорошо работает для локальных испытаний, тестирования и группового развертывания.
:::

:::note[О хранилище данных по умолчанию]
Поддерживаемые в настоящее время шаблоны Docker Compose сохраняют данные через локальный путь SQLite по умолчанию. В большинстве случаев вам не нужно предоставлять отдельную службу базы данных на хост-компьютере.
:::

## Предварительные условия

Перед началом убедитесь, что в вашей системе установлены Docker и Docker Compose.

### Установить Докер

#### Окна

Загрузите и установите [Рабочий стол Docker для Windows](https://www.docker.com/products/docker-desktop/)

После установки убедитесь, что Docker Desktop работает.

#### macOS

Загрузите и установите [Рабочий стол Docker для Mac](https://www.docker.com/products/docker-desktop/)

После установки убедитесь, что Docker Desktop работает.

#### Линукс

Установите Docker с помощью менеджера пакетов:

```bash title="Install Docker on Ubuntu/Debian"
sudo apt-get update
sudo apt-get install docker.io docker-compose-plugin
```

После установки запустите службу Docker:

```bash
sudo systemctl start docker
sudo systemctl enable docker
```

### Проверка установки

После установки выполните следующие команды, чтобы убедиться, что Docker и Docker Compose установлены правильно:

```bash
docker --version
docker compose version
```

## Быстрый старт

### 1. Создайте файл конфигурации Docker Compose.

:::tip[Использовать генератор конфигурации]
**Рекомендуется использовать [Docker Compose Builder (автономный сайт)](https://builder.hagicode.com/)** для создания файла конфигурации.

Генератор поддерживает:
- Несколько вариантов поставщиков API (Zhipu AI, Anthropic Official, Custom API)
- Автоматическая настройка URL-адреса API и токена
- Пользовательские порты, имена контейнеров и другие базовые настройки.
- Настройте постоянные данные и пути к рабочему каталогу.
- Автоматическая настройка различий между платформами Windows и Linux.
- Интеллектуальное решение проблем с правами доступа к файлам
- **Выбор источника изображения**: в зависимости от сетевой среды выберите соответствующий источник изображения.

[🚀 Немедленно используйте генератор →](https://builder.hagicode.com/)
:::

1. Открыть [Docker Compose Builder (автономный сайт)](https://builder.hagicode.com/)
2. Заполните конфигурацию в соответствии с вашими потребностями:
   - Выберите поставщика API (Zhipu AI, Anthropic Official или Custom API).
   - Настройте порты, базу данных, рабочий каталог и другие параметры.
   - **Выберите источник изображения**: выберите подходящий источник изображения в зависимости от вашей сетевой среды.
3. Нажмите кнопку «Создать», чтобы получить `docker-compose.yml` конфигурация
4. Сохраните созданную конфигурацию как `docker-compose.yml` файл
5. При необходимости создайте `.env` файл для настройки конфиденциальной информации

#### Выбор источника изображения

Генератор поддерживает три варианта источника изображения:

**Докер-Хаб**
- **Адрес изображения**: `newbe36524/hagicode:{tag}`
- **Применимые сценарии**: подходит для пользователей с ускорением зеркала Docker Hub.
- **Преимущества**: Официальный источник изображений, своевременные обновления, стабильный доступ.
- **Примечания**. В некоторых регионах может потребоваться настроить зеркальное ускорение.

**Реестр контейнеров Azure (ACR)**
- **Адрес изображения**: `registry.cn-hangzhou.aliyuncs.com/hagicode/hagicode:{tag}`
- **Применимые сценарии**: подходит для пользователей, не имеющих локального доступа к Docker Hub.
- **Преимущества**: предоставляет альтернативный источник изображения, решает проблемы с доступом к сети.
- **Примечания**. Изображения остаются синхронизированными с Docker Hub, но могут иметь небольшие задержки.

**Реестр облачных контейнеров Alibaba (ACR)**
- **Адрес изображения**: `registry.cn-hangzhou.aliyuncs.com/hagicode/hagicode:{tag}`
- **Применимые сценарии**: подходит для домашних пользователей, стабильный и быстрый доступ к сети.
- **Преимущества**: внутренние узлы Alibaba Cloud, высокая скорость доступа, высокая стабильность.
- **Примечание**. Образы Alibaba ACR синхронизируются с Docker Hub, обычно в течение 30 минут.

:::tip[Рекомендации по выбору источника изображения]
- **Внутренние пользователи рекомендуют Alibaba Cloud ACR**. Если вы являетесь домашним пользователем, рекомендуется отдать приоритет использованию источника изображения Alibaba Cloud ACR для быстрого и стабильного доступа.
- **Альтернативный Docker Hub**: если ваша сетевая среда имеет доступ к Docker Hub, вы также можете выбрать Docker Hub в качестве источника образа.
- **Альтернативный Alibaba Cloud ACR**: выбирайте источник изображений Alibaba Cloud ACR только в том случае, если у других источников изображений возникают трудности с доступом.
- **Синхронизация изображений**: изображения Alibaba Cloud ACR синхронизируются с Docker Hub, обычно в течение 30 минут.
- **Согласованность версий**: теги изображений одинаковы во всех трех источниках изображений, поэтому не нужно беспокоиться о проблемах с версией.
:::

:::note[Получить токен API]
Вам необходимо настроить токен Claude API для использования HagiCode:

**Жипу AI (рекомендуется)**: [Получить API-токен →](https://www.bigmodel.cn/glm-coding?ic=14BY54APZA)
- Стабильный внутренний доступ, более быстрое реагирование
- Высокая цена-качество, подходит для ежедневного использования.

**Антропный чиновник**: [Получить API-токен →](https://console.anthropic.com/)
- Прямое использование услуг Anthropic

:::

### 2. Запустите службы

Выполните следующую команду в каталоге, где находится ваш `docker-compose.yml` находится:

```bash
docker compose up -d
```

Эта команда будет:
- Загрузите и запустите контейнеры HagiCode, определенные сгенерированным шаблоном.
- Создать и настроить сеть
- Инициализируйте каталог данных по умолчанию и зависимости времени выполнения.

### 3. Проверьте статус службы.

Проверьте, запущены ли контейнеры:

```bash
docker compose ps
```

Вы должны увидеть службы, определенные в созданном вами шаблоне, в статусе «работает».

### Просмотр журналов обслуживания

Чтобы просмотреть все журналы обслуживания:

```bash
# View all service logs
docker compose logs

# View HagiCode service logs
docker compose logs hagicode

# Real-time log following
docker compose logs -f hagicode
```

## Инструкции по настройке

Если вам необходимо изменить конфигурацию (например, изменить порты, переключить поставщика API и т. д.), вы можете:

1. Восстановите новую конфигурацию, используя [Docker Compose Builder (автономный сайт)](https://builder.hagicode.com/)
2. Редактировать вручную `docker-compose.yml` и `.env` файлы
3. Перезапустите службы, чтобы конфигурация вступила в силу:

```bash
docker compose restart
```

## Доступ к приложению

### Веб-интерфейс

После успешного запуска службы получите доступ через браузер:

```
http://localhost:45000
```

### Сохранение данных

Поддерживаемый путь Docker Compose по умолчанию использует файлы данных SQLite с постоянными томами. Если вам необходимо проверить точки монтирования или спланировать резервное копирование, просмотрите сопоставления томов и каталогов данных в сгенерированном файле. `docker-compose.yml`.

## Следующие шаги

Теперь вы успешно развернули HagiCode. Далее подключите реальный проект в веб-интерфейсе и продолжите [Мастер настройки](/ru-RU/quick-start/wizard-setup) или [Создать сеанс предложений](/ru-RU/quick-start/proposal-session).

Если вы предпочитаете метод развертывания настольных приложений, см. [Руководство по установке на рабочем столе](/ru-RU/installation/desktop).

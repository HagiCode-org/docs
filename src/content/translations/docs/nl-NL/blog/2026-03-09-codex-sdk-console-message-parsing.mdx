---
title: Volledige gids voor het parseren van berichten in de Codex SDK-console
date: 2026-03-10
tags: [Codex SDK, TypeScript, AI Executor, Event Stream]
description: "In dit artikel worden het Event Stream-mechanisme van de Codex SDK, het parseren van berichttypen en best practices in echte projecten uitgelegd, waardoor ontwikkelaars de"
---

## Volledige gids voor het parseren van berichten in de Codex SDK-console

> In dit artikel worden het Event Stream-mechanisme van de Codex SDK, het parseren van berichttypen en best practices in echte projecten uitgelegd, waardoor ontwikkelaars snel de kernvaardigheden achter AI-uitvoeringsservices onder de knie krijgen.

## Achtergrond

Bij het bouwen van een AI-uitvoeringsservice op basis van de Codex SDK komen we onvermijdelijk een praktische vraag tegen: hoe moeten we omgaan met de gestreamde gebeurtenisberichten die door Codex worden geretourneerd? Deze berichten bevatten belangrijke informatie, zoals de uitvoeringsstatus, uitvoerinhoud en foutdetails, en verdienen daarom een ​​zorgvuldige behandeling.

Als onderdeel van het HagiCode-project hadden we een betrouwbare uitvoerder nodig voor AI-coderingsassistentscenario's. Dat is precies de reden waarom we besloten om het gebeurtenisstroommechanisme van de Codex SDK diepgaand te bestuderen. Alleen door te begrijpen hoe de onderliggende boodschappen werken, kunnen we immers een echt AI-uitvoeringsplatform op ondernemingsniveau bouwen.

De Codex SDK is een SDK voor programmeerondersteuning, uitgegeven door OpenAI. Het retourneert uitvoeringsresultaten via een gebeurtenisstroom. In tegenstelling tot het traditionele verzoek-antwoordmodel maakt Codex gebruik van gestreamde gebeurtenissen, zodat we:

- Ontvang de uitvoeringsvoortgang in realtime
- Handel fouten snel af
- Verkrijg gedetailleerde tokengebruiksstatistieken
- Ondersteun langlopende complexe taken

Het begrijpen van deze gebeurtenistypen en het correct parseren ervan is essentieel voor het implementeren van een volledig capabele AI-uitvoerder. Uiteindelijk wil niemand met een black box werken.

## Over HagiCode

De oplossing die in dit artikel wordt gedeeld, komt voort uit onze praktijkervaring in de [HagiCode](https://hagicode.com) project. HagiCode is een open-source AI-coderingsassistentproject dat bedoeld is om ontwikkelaars intelligente coderingsondersteuning te bieden. Tijdens de ontwikkeling moesten we een betrouwbare AI-uitvoeringsservice bouwen om verzoeken om gebruikerscode-uitvoering af te handelen, wat de directe reden is dat we de Codex SDK hebben geïntroduceerd.

Als AI-coderingsassistent moet HagiCode omgaan met een verscheidenheid aan complexe scenario's voor het uitvoeren van code: het in realtime verkrijgen van de voortgang van de uitvoering, het snel afhandelen van fouten en het verzamelen van gedetailleerde tokengebruiksstatistieken. Door het Codex SDK-gebeurtenisstroommechanisme grondig te begrijpen, kunnen we een uitvoerder bouwen die voldoet aan de vereisten van de productieomgeving. Uiteindelijk, of het nu software of het echte leven is, profiteert alles van gestage accumulatie en verfijning.

## Event Stream-mechanisme

### Basisconcepten

De Codex SDK maakt gebruik van de `thread.runStreamed()` methode om een asynchrone gebeurtenisiterator te retourneren:

```typescript
import { Codex } from '@openai/codex-sdk';

const client = new Codex({
  apiKey: process.env.CODEX_API_KEY,
  baseUrl: process.env.CODEX_BASE_URL,
});

const thread = client.startThread({
  workingDirectory: '/path/to/project',
  skipGitRepoCheck: false,
});

const { events } = await thread.runStreamed('your prompt here', {
  outputSchema: {
    type: 'object',
    properties: {
      output: { type: 'string' },
      status: { type: 'string', enum: ['ok', 'action_required'] },
    },
    required: ['output', 'status'],
  },
});

for await (const event of events) {
  // Handle each event
}
```

### Gedetailleerde uitleg van gebeurtenistypen

| Evenementtype | Beschrijving | Belangrijkste gegevens |
|---------|------|----------|
| `thread.started` | Onderwerp is succesvol gestart | `thread_id` |
| `item.updated` | Berichtinhoud bijgewerkt | `item.text` |
| `item.completed` | Bericht voltooid | `item.text` |
| `turn.completed` | Uitvoering voltooid | `usage` (tokengebruik) |
| `turn.failed` | Uitvoering mislukt | `error.message` |
| `error` | Foutgebeurtenis | `message` |

In echte projecten wordt de uitvoerdercomponent van HagiCode bovenop deze gebeurtenistypen gebouwd. We moeten elk soort evenement zorgvuldig behandelen om een ​​soepele gebruikerservaring te garanderen. Goede systemen worden gebouwd door details serieus te nemen.

## Implementatie van berichtparsering

### Berichtinhoud extraheren

Berichtinhoud wordt geëxtraheerd via een gebeurtenishandler:

```typescript
private handleThreadEvent(event: ThreadEvent, onMessage: (content: string) => void): void {
  // Only handle message update and completion events
  if (event.type !== 'item.updated' && event.type !== 'item.completed') {
    return;
  }

  // Only handle agent message content
  if (event.item.type !== 'agent_message') {
    return;
  }

  // Extract text content
  onMessage(event.item.text);
}
```

Belangrijkste punten:
- Alleen handvat `item.updated` en `item.completed` evenementen
- Verwerk alleen inhoud van het type `agent_message`
- De inhoud van het bericht bevindt zich in de `event.item.text` veld

### Gestructureerde uitvoerparsering

Codex ondersteunt gestructureerde JSON-uitvoer. U kunt het retourformaat opgeven via de `outputSchema` parameter:

```typescript
const DEFAULT_OUTPUT_SCHEMA = {
  type: 'object',
  properties: {
    output: { type: 'string' },
    status: { type: 'string', enum: ['ok', 'action_required'] },
  },
  required: ['output', 'status'],
  additionalProperties: false,
} as const;
```

De parseerfunctie probeert JSON te parseren, en als dat niet lukt, valt het terug op de onbewerkte tekst.

```typescript
function toStructuredOutput(raw: string): StructuredOutput {
  try {
    const parsed = JSON.parse(raw) as Partial<StructuredOutput>;
    if (typeof parsed.output === 'string') {
      return {
        output: parsed.output,
        status: parsed.status === 'action_required' ? 'action_required' : 'ok',
      };
    }
  } catch {
    // JSON parsing failed, fall back to the raw text
  }

  return {
    output: raw,
    status: 'ok',
  };
}
```

### Volledige gebeurtenisafhandelingsstroom

```typescript
private async runWithStreaming(
  thread: Thread,
  input: CodexStageExecutionInput
): Promise<{ output: string; usage: Usage | null }> {
  const abortController = new AbortController();
  const timeoutHandle = setTimeout(() => {
    abortController.abort();
  }, Math.max(1000, input.timeoutMs));

  let latestMessage = '';
  let usage: Usage | null = null;
  let emittedLength = 0;

  try {
    const { events } = await thread.runStreamed(input.prompt, {
      outputSchema: DEFAULT_OUTPUT_SCHEMA,
      signal: abortController.signal,
    });

    for await (const event of events) {
      // Handle message content
      this.handleThreadEvent(event, (nextContent) => {
        const delta = nextContent.slice(emittedLength);
        if (delta.length > 0) {
          emittedLength = nextContent.length;
          input.callbacks?.onChunk?.(delta);  // Streaming callback
        }
        latestMessage = nextContent;
      });

      // Process different data based on the event type
      if (event.type === 'thread.started') {
        this.threadId = event.thread_id;
      } else if (event.type === 'turn.completed') {
        usage = event.usage;
      } else if (event.type === 'turn.failed') {
        throw new CodexExecutorError('gateway_unavailable', event.error.message, true);
      } else if (event.type === 'error') {
        throw new CodexExecutorError('gateway_unavailable', event.message, true);
      }
    }
  } catch (error) {
    if (abortController.signal.aborted) {
      throw new CodexExecutorError(
        'upstream_timeout',
        `Codex stage timed out after ${input.timeoutMs}ms`,
        true
      );
    }
    throw error;
  } finally {
    clearTimeout(timeoutHandle);
  }

  const structured = toStructuredOutput(latestMessage);
  return { output: structured.output, usage };
}
```

## Strategie voor foutafhandeling

### Foutcodetoewijzing

Wijs specifieke foutpatronen toe aan concrete foutcodes, zodat de bovenste lagen deze gemakkelijker kunnen verwerken:

```typescript
function mapError(error: unknown): CodexExecutorError {
  if (error instanceof CodexExecutorError) {
    return error;
  }

  const message = error instanceof Error ? error.message : String(error);
  const normalized = message.toLowerCase();

  // Authentication errors - not retryable
  if (normalized.includes('401') ||
      normalized.includes('403') ||
      normalized.includes('api key') ||
      normalized.includes('auth')) {
    return new CodexExecutorError('auth_invalid', message, false);
  }

  // Rate limit errors - retryable
  if (normalized.includes('429') || normalized.includes('rate limit')) {
    return new CodexExecutorError('rate_limited', message, true);
  }

  // Timeout errors - retryable
  if (normalized.includes('timeout') || normalized.includes('aborted')) {
    return new CodexExecutorError('upstream_timeout', message, true);
  }

  // Default error
  return new CodexExecutorError('gateway_unavailable', message, true);
}
```

### Definities van fouttypes

```typescript
export type CodexErrorCode =
  | 'auth_invalid'      // Authentication failure
  | 'upstream_timeout'  // Upstream timeout
  | 'rate_limited'      // Rate limited
  | 'gateway_unavailable'; // Gateway unavailable

export class CodexExecutorError extends Error {
  readonly code: CodexErrorCode;
  readonly retryable: boolean;

  constructor(code: CodexErrorCode, message: string, retryable: boolean) {
    super(message);
    this.name = 'CodexExecutorError';
    this.code = code;
    this.retryable = retryable;
  }
}
```

## Werkmap- en omgevingsconfiguratie

### Validatie van werkmap

De Codex SDK vereist dat de werkmap een geldige Git-opslagplaats is.

```typescript
export function validateWorkingDirectory(
  workingDirectory: string,
  skipGitRepoCheck: boolean
): void {
  const resolvedWorkingDirectory = path.resolve(workingDirectory);

  if (!existsSync(resolvedWorkingDirectory)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory does not exist.',
      false
    );
  }

  if (!statSync(resolvedWorkingDirectory).isDirectory()) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a directory.',
      false
    );
  }

  if (skipGitRepoCheck) {
    return;
  }

  const gitDir = path.join(resolvedWorkingDirectory, '.git');
  if (!existsSync(gitDir)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a git repository.',
      false
    );
  }
}
```

### Omgevingsvariabelen laden

De Codex SDK moet omgevingsvariabelen vanuit de login-shell laden, zodat de AI-agent toegang krijgt tot systeemopdrachten:

```typescript
function parseEnvironmentOutput(output: Buffer): Record<string, string> {
  const parsed: Record<string, string> = {};

  for (const entry of output.toString('utf8').split('\0')) {
    if (!entry) continue;

    const separatorIndex = entry.indexOf('=');
    if (separatorIndex <= 0) continue;

    const key = entry.slice(0, separatorIndex);
    const value = entry.slice(separatorIndex + 1);
    if (key.length > 0) {
      parsed[key] = value;
    }
  }

  return parsed;
}

function tryLoadEnvironmentFromShell(shellPath: string): Record<string, string> | null {
  const result = spawnSync(shellPath, ['-ilc', 'env -0'], {
    env: process.env,
    stdio: ['ignore', 'pipe', 'pipe'],
    timeout: 5000,
  });

  if (result.error || result.status !== 0) {
    return null;
  }

  return parseEnvironmentOutput(result.stdout);
}

export function createExecutorEnvironment(
  envOverrides: Record<string, string> = {}
): Record<string, string> {
  // Load environment variables from the login shell
  const consoleEnv = loadConsoleEnvironmentFromShell();

  return {
    ...process.env,
    ...consoleEnv,
    ...envOverrides,
  };
}
```

## Compleet gebruiksvoorbeeld

### Basisgebruik

In het HagiCode-project gebruiken we de volgende aanpak om de Codex-client te initialiseren en taken uit te voeren:

```typescript
import { Codex } from '@openai/codex-sdk';

async function executeWithCodex(prompt: string, workingDir: string) {
  const client = new Codex({
    apiKey: process.env.CODEX_API_KEY,
    env: { PATH: process.env.PATH },
  });

  const thread = client.startThread({
    workingDirectory: workingDir,
  });

  const { events } = await thread.runStreamed(prompt);

  let result = '';
  for await (const event of events) {
    if (event.type === 'item.updated' && event.item.type === 'agent_message') {
      result = event.item.text;
    }
    if (event.type === 'turn.completed') {
      console.log('Token usage:', event.usage);
    }
  }

  // Try to parse JSON output
  try {
    const parsed = JSON.parse(result);
    return parsed.output;
  } catch {
    return result;
  }
}
```

### Voltooi de implementatie met nieuwe pogingen

```typescript
export class CodexSdkExecutor {
  private readonly config: CodexRuntimeConfig;
  private readonly client: Codex;
  private threadId: string | null = null;

  async executeStage(input: CodexStageExecutionInput): Promise<CodexStageExecutionResult> {
    const maxAttempts = Math.max(1, this.config.retryCount + 1);
    let attempt = 0;
    let lastError: CodexExecutorError | null = null;

    while (attempt < maxAttempts) {
      attempt += 1;

      try {
        const thread = this.getThread(input.workingDirectory);
        const { output, usage } = await this.runWithStreaming(thread, input);

        return {
          output,
          usage,
          threadId: this.threadId!,
          attempts: attempt,
          latencyMs: Date.now() - startedAt,
        };
      } catch (error) {
        const mappedError = mapError(error);
        lastError = mappedError;

        // Non-retryable error or max retry attempts reached
        if (!mappedError.retryable || attempt >= maxAttempts) {
          throw mappedError;
        }

        // Wait before retrying
        await new Promise(resolve => setTimeout(resolve, 1000 * attempt));
      }
    }

    throw lastError!;
  }
}
```

## Beste praktijken

### 1. Vereisten voor werkmap

- Zorg ervoor dat de werkmap een geldige Git-repository is
- Gebruik de `PROJECT_ROOT` omgevingsvariabele om deze expliciet op te geven
- Tijdens het debuggen kunt u instellen `CODEX_SKIP_GIT_REPO_CHECK=true` om de cheque over te slaan

### 2. Configuratie van omgevingsvariabelen

- Geef alleen de vereiste omgevingsvariabelen door via een whitelist-mechanisme
- Gebruik de login-shell om de volledige omgeving te laden
- Vermijd het doorgeven van gevoelige informatie

### 3. Strategie voor time-out en opnieuw proberen

- Stel redelijke time-outs in op basis van de complexiteit van de taak
- Implementeer exponentiële uitstel voor opnieuw te proberen fouten
- Registreer het aantal nieuwe pogingen en de redenen daarvoor

### 4. Foutafhandeling

- Maak onderscheid tussen opnieuw te proberen en niet-opnieuw te proberen fouten
- Zorg voor duidelijke foutmeldingen en suggesties
- Gebruik uniforme foutcodes, zodat de bovenste lagen deze op consistente wijze kunnen afhandelen

### 5. Streaming-uitvoer

- Implementeer incrementele uitvoer-callbacks om de gebruikerservaring te verbeteren
- Ga op de juiste manier om met incrementele berichtupdates
- Registreer het tokengebruik voor kostenanalyse

In de daadwerkelijke productieomgeving van het HagiCode-project hebben we de effectiviteit van de bovenstaande best practices al geverifieerd. Deze aanpak heeft ons geholpen een stabiele en betrouwbare AI-uitvoeringsdienst op te bouwen. Uiteindelijk is praktische validatie belangrijker dan theorie alleen.

## Conclusie

Het Codex SDK-gebeurtenisstroommechanisme biedt krachtige mogelijkheden voor het bouwen van AI-uitvoeringsdiensten. Door verschillende soorten gebeurtenissen correct te analyseren, kunnen we:

- Ontvang de uitvoeringsstatus en uitvoer in realtime
- Implementeer betrouwbare foutafhandeling en mechanismen voor opnieuw proberen
- Verkrijg gedetailleerde uitvoeringsstatistieken
- Bouw een volledig uitgerust AI-uitvoeringsplatform

De kernconcepten en codevoorbeelden die in dit artikel worden geïntroduceerd, kunnen rechtstreeks in echte projecten worden toegepast, waardoor ontwikkelaars snel aan de slag kunnen met Codex SDK-integratie. Als u deze aanpak waardevol vindt, weerspiegelt deze ook de kracht van HagiCode's engineeringpraktijk en maakt HagiCode zelf de moeite waard om te volgen.

## Referenties

- [Officiële Codex SDK-documentatie](https://platform.openai.com/docs/codex)
- [HagiCode GitHub-opslagplaats](https://github.com/HagiCode-org/site)
- [Officiële HagiCode-website](https://hagicode.com)

---

Bedankt voor het lezen. Als u dit artikel nuttig vond, klik dan op de vind-ik-leuk-knop hieronder zodat meer mensen het kunnen ontdekken.

Deze inhoud is gemaakt met behulp van AI-ondersteunde samenwerking, beoordeeld door de auteur, en weerspiegelt de eigen opvattingen en standpunten van de auteur.

- Auteur: [nieuwbe36524](https://www.newbe.pro)
- Artikellink: [https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/](https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/)
- Copyrightvermelding: Tenzij anders vermeld, zijn alle artikelen op deze blog gelicentieerd onder BY-NC-SA. Bij herdruk graag de bron vermelden.

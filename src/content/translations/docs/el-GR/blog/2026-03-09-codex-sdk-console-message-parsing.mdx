---
title: Πλήρης οδηγός για την ανάλυση μηνυμάτων της κονσόλας Codex SDK
date: 2026-03-10
tags: [Codex SDK, TypeScript, AI Executor, Event Stream]
description: "Αυτό το άρθρο εξηγεί τον μηχανισμό ροής συμβάντων Codex SDK, την ανάλυση τύπου μηνύματος και τις βέλτιστες πρακτικές σε πραγματικά έργα, βοηθώντας τους προγραμματιστές να κατακτήσουν γρήγορα το"
---

## Πλήρης οδηγός για την ανάλυση μηνυμάτων της κονσόλας Codex SDK

> Αυτό το άρθρο εξηγεί τον μηχανισμό ροής συμβάντων Codex SDK, την ανάλυση τύπου μηνύματος και τις βέλτιστες πρακτικές σε πραγματικά έργα, βοηθώντας τους προγραμματιστές να κατακτήσουν γρήγορα τις βασικές δεξιότητες πίσω από τις υπηρεσίες εκτέλεσης AI.

## φόντο

Κατά τη δημιουργία μιας υπηρεσίας εκτέλεσης AI που βασίζεται στο Codex SDK, αναπόφευκτα αντιμετωπίζουμε ένα πρακτικό ερώτημα: πώς πρέπει να χειριζόμαστε τα μηνύματα συμβάντων με ροή που επιστρέφονται από το Codex; Αυτά τα μηνύματα περιέχουν σημαντικές πληροφορίες, όπως κατάσταση εκτέλεσης, περιεχόμενο εξόδου και λεπτομέρειες σφάλματος, επομένως αξίζουν προσεκτικό χειρισμό.

Ως μέρος του έργου HagiCode, χρειαζόμασταν έναν αξιόπιστο εκτελεστή για σενάρια βοηθού κωδικοποίησης AI. Γι' αυτό ακριβώς αποφασίσαμε να μελετήσουμε σε βάθος τον μηχανισμό ροής συμβάντων Codex SDK. Σε τελική ανάλυση, μόνο με την κατανόηση του τρόπου λειτουργίας των υποκείμενων μηνυμάτων μπορούμε να δημιουργήσουμε μια πραγματικά εταιρική πλατφόρμα εκτέλεσης τεχνητής νοημοσύνης.

Το Codex SDK είναι ένα SDK βοήθειας προγραμματισμού που κυκλοφόρησε από την OpenAI. Επιστρέφει αποτελέσματα εκτέλεσης μέσω μιας ροής συμβάντων. Σε αντίθεση με το παραδοσιακό μοντέλο αίτησης-απόκρισης, το Codex χρησιμοποιεί συμβάντα ροής, ώστε να μπορούμε:

- Λάβετε την πρόοδο της εκτέλεσης σε πραγματικό χρόνο
- Χειριστείτε τα σφάλματα εγκαίρως
- Λάβετε λεπτομερή στατιστικά στοιχεία χρήσης διακριτικών
- Υποστήριξη μακροχρόνιων σύνθετων εργασιών

Η κατανόηση αυτών των τύπων συμβάντων και η σωστή ανάλυση τους είναι απαραίτητη για την υλοποίηση ενός πλήρως ικανού εκτελεστή τεχνητής νοημοσύνης. Στο τέλος, κανείς δεν θέλει να δουλέψει με ένα μαύρο κουτί.

## Σχετικά με το HagiCode

Η λύση που κοινοποιείται σε αυτό το άρθρο προέρχεται από την πρακτική εμπειρία μας στο [HagiCode](https://hagicode.com) έργο. Το HagiCode είναι ένα έργο βοηθού κωδικοποίησης AI ανοιχτού κώδικα αφιερωμένο στην παροχή στους προγραμματιστές έξυπνη υποστήριξη κωδικοποίησης. Κατά τη διάρκεια της ανάπτυξης, χρειαζόμασταν να δημιουργήσουμε μια αξιόπιστη υπηρεσία εκτέλεσης τεχνητής νοημοσύνης για να χειριζόμαστε αιτήματα εκτέλεσης κώδικα από χρήστες, που είναι ο άμεσος λόγος που παρουσιάσαμε το Codex SDK.

Ως βοηθός κωδικοποίησης τεχνητής νοημοσύνης, ο HagiCode πρέπει να αντιμετωπίσει μια ποικιλία σύνθετων σεναρίων εκτέλεσης κώδικα: λήψη προόδου εκτέλεσης σε πραγματικό χρόνο, έγκαιρη διαχείριση σφαλμάτων και συλλογή λεπτομερών στατιστικών στοιχείων χρήσης διακριτικών. Κατανοώντας σε βάθος τον μηχανισμό ροής συμβάντων Codex SDK, μπορούμε να δημιουργήσουμε έναν εκτελεστή που πληροί τις απαιτήσεις περιβάλλοντος παραγωγής. Τελικά, είτε πρόκειται για λογισμικό είτε για πραγματική ζωή, όλα ωφελούνται από τη σταθερή συσσώρευση και βελτίωση.

## Μηχανισμός ροής συμβάντων

### Βασικές Έννοιες

Το Codex SDK χρησιμοποιεί το `thread.runStreamed()` μέθοδος για την επιστροφή ενός ασύγχρονου επαναλήπτη συμβάντος:

```typescript
import { Codex } from '@openai/codex-sdk';

const client = new Codex({
  apiKey: process.env.CODEX_API_KEY,
  baseUrl: process.env.CODEX_BASE_URL,
});

const thread = client.startThread({
  workingDirectory: '/path/to/project',
  skipGitRepoCheck: false,
});

const { events } = await thread.runStreamed('your prompt here', {
  outputSchema: {
    type: 'object',
    properties: {
      output: { type: 'string' },
      status: { type: 'string', enum: ['ok', 'action_required'] },
    },
    required: ['output', 'status'],
  },
});

for await (const event of events) {
  // Handle each event
}
```

### Λεπτομερής επεξήγηση των τύπων συμβάντων

| Τύπος συμβάντος | Περιγραφή | Βασικά Δεδομένα |
|---------|------|----------|
| `thread.started` | Το νήμα ξεκίνησε με επιτυχία | `thread_id` |
| `item.updated` | Το περιεχόμενο του μηνύματος ενημερώθηκε | `item.text` |
| `item.completed` | Το μήνυμα ολοκληρώθηκε | `item.text` |
| `turn.completed` | Η εκτέλεση ολοκληρώθηκε | `usage` (χρήση διακριτικού) |
| `turn.failed` | Η εκτέλεση απέτυχε | `error.message` |
| `error` | Συμβάν σφάλματος | `message` |

Σε πραγματικά έργα, το στοιχείο εκτελεστής του HagiCode είναι χτισμένο πάνω από αυτούς τους τύπους συμβάντων. Πρέπει να χειριζόμαστε προσεκτικά κάθε είδους εκδήλωση για να εξασφαλίσουμε μια ομαλή εμπειρία χρήστη. Τα καλά συστήματα δημιουργούνται λαμβάνοντας σοβαρά υπόψη τις λεπτομέρειες.

## Εφαρμογή ανάλυσης μηνυμάτων

### Εξαγωγή περιεχομένου μηνύματος

Το περιεχόμενο του μηνύματος εξάγεται μέσω ενός προγράμματος χειρισμού συμβάντων:

```typescript
private handleThreadEvent(event: ThreadEvent, onMessage: (content: string) => void): void {
  // Only handle message update and completion events
  if (event.type !== 'item.updated' && event.type !== 'item.completed') {
    return;
  }

  // Only handle agent message content
  if (event.item.type !== 'agent_message') {
    return;
  }

  // Extract text content
  onMessage(event.item.text);
}
```

Βασικά σημεία:
- Μόνο λαβή `item.updated` και `item.completed` εκδηλώσεις
- Χειριστείτε μόνο περιεχόμενο τύπου `agent_message`
- Το περιεχόμενο του μηνύματος βρίσκεται στο `event.item.text` πεδίου

### Ανάλυση δομημένης εξόδου

Το Codex υποστηρίζει δομημένη έξοδο JSON. Μπορείτε να καθορίσετε τη μορφή επιστροφής μέσω του `outputSchema` παράμετρος:

```typescript
const DEFAULT_OUTPUT_SCHEMA = {
  type: 'object',
  properties: {
    output: { type: 'string' },
    status: { type: 'string', enum: ['ok', 'action_required'] },
  },
  required: ['output', 'status'],
  additionalProperties: false,
} as const;
```

Η συνάρτηση ανάλυσης επιχειρεί να αναλύσει το JSON και αν αποτύχει, επιστρέφει στο μη επεξεργασμένο κείμενο.

```typescript
function toStructuredOutput(raw: string): StructuredOutput {
  try {
    const parsed = JSON.parse(raw) as Partial<StructuredOutput>;
    if (typeof parsed.output === 'string') {
      return {
        output: parsed.output,
        status: parsed.status === 'action_required' ? 'action_required' : 'ok',
      };
    }
  } catch {
    // JSON parsing failed, fall back to the raw text
  }

  return {
    output: raw,
    status: 'ok',
  };
}
```

### Πλήρης ροή χειρισμού συμβάντων

```typescript
private async runWithStreaming(
  thread: Thread,
  input: CodexStageExecutionInput
): Promise<{ output: string; usage: Usage | null }> {
  const abortController = new AbortController();
  const timeoutHandle = setTimeout(() => {
    abortController.abort();
  }, Math.max(1000, input.timeoutMs));

  let latestMessage = '';
  let usage: Usage | null = null;
  let emittedLength = 0;

  try {
    const { events } = await thread.runStreamed(input.prompt, {
      outputSchema: DEFAULT_OUTPUT_SCHEMA,
      signal: abortController.signal,
    });

    for await (const event of events) {
      // Handle message content
      this.handleThreadEvent(event, (nextContent) => {
        const delta = nextContent.slice(emittedLength);
        if (delta.length > 0) {
          emittedLength = nextContent.length;
          input.callbacks?.onChunk?.(delta);  // Streaming callback
        }
        latestMessage = nextContent;
      });

      // Process different data based on the event type
      if (event.type === 'thread.started') {
        this.threadId = event.thread_id;
      } else if (event.type === 'turn.completed') {
        usage = event.usage;
      } else if (event.type === 'turn.failed') {
        throw new CodexExecutorError('gateway_unavailable', event.error.message, true);
      } else if (event.type === 'error') {
        throw new CodexExecutorError('gateway_unavailable', event.message, true);
      }
    }
  } catch (error) {
    if (abortController.signal.aborted) {
      throw new CodexExecutorError(
        'upstream_timeout',
        `Codex stage timed out after ${input.timeoutMs}ms`,
        true
      );
    }
    throw error;
  } finally {
    clearTimeout(timeoutHandle);
  }

  const structured = toStructuredOutput(latestMessage);
  return { output: structured.output, usage };
}
```

## Στρατηγική χειρισμού σφαλμάτων

### Αντιστοίχιση κωδικού σφάλματος

Αντιστοιχίστε συγκεκριμένα μοτίβα σφαλμάτων σε συγκεκριμένους κωδικούς σφαλμάτων, ώστε τα ανώτερα στρώματα να μπορούν να τα χειριστούν πιο εύκολα:

```typescript
function mapError(error: unknown): CodexExecutorError {
  if (error instanceof CodexExecutorError) {
    return error;
  }

  const message = error instanceof Error ? error.message : String(error);
  const normalized = message.toLowerCase();

  // Authentication errors - not retryable
  if (normalized.includes('401') ||
      normalized.includes('403') ||
      normalized.includes('api key') ||
      normalized.includes('auth')) {
    return new CodexExecutorError('auth_invalid', message, false);
  }

  // Rate limit errors - retryable
  if (normalized.includes('429') || normalized.includes('rate limit')) {
    return new CodexExecutorError('rate_limited', message, true);
  }

  // Timeout errors - retryable
  if (normalized.includes('timeout') || normalized.includes('aborted')) {
    return new CodexExecutorError('upstream_timeout', message, true);
  }

  // Default error
  return new CodexExecutorError('gateway_unavailable', message, true);
}
```

### Ορισμοί τύπων σφάλματος

```typescript
export type CodexErrorCode =
  | 'auth_invalid'      // Authentication failure
  | 'upstream_timeout'  // Upstream timeout
  | 'rate_limited'      // Rate limited
  | 'gateway_unavailable'; // Gateway unavailable

export class CodexExecutorError extends Error {
  readonly code: CodexErrorCode;
  readonly retryable: boolean;

  constructor(code: CodexErrorCode, message: string, retryable: boolean) {
    super(message);
    this.name = 'CodexExecutorError';
    this.code = code;
    this.retryable = retryable;
  }
}
```

## Κατάλογος εργασίας και διαμόρφωση περιβάλλοντος

### Επικύρωση καταλόγου εργασίας

Το Codex SDK απαιτεί ο κατάλογος εργασίας να είναι έγκυρο αποθετήριο Git.

```typescript
export function validateWorkingDirectory(
  workingDirectory: string,
  skipGitRepoCheck: boolean
): void {
  const resolvedWorkingDirectory = path.resolve(workingDirectory);

  if (!existsSync(resolvedWorkingDirectory)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory does not exist.',
      false
    );
  }

  if (!statSync(resolvedWorkingDirectory).isDirectory()) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a directory.',
      false
    );
  }

  if (skipGitRepoCheck) {
    return;
  }

  const gitDir = path.join(resolvedWorkingDirectory, '.git');
  if (!existsSync(gitDir)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a git repository.',
      false
    );
  }
}
```

### Φόρτωση μεταβλητών περιβάλλοντος

Το Codex SDK χρειάζεται να φορτώσει μεταβλητές περιβάλλοντος από το κέλυφος σύνδεσης, ώστε ο παράγοντας AI να μπορεί να έχει πρόσβαση στις εντολές του συστήματος:

```typescript
function parseEnvironmentOutput(output: Buffer): Record<string, string> {
  const parsed: Record<string, string> = {};

  for (const entry of output.toString('utf8').split('\0')) {
    if (!entry) continue;

    const separatorIndex = entry.indexOf('=');
    if (separatorIndex <= 0) continue;

    const key = entry.slice(0, separatorIndex);
    const value = entry.slice(separatorIndex + 1);
    if (key.length > 0) {
      parsed[key] = value;
    }
  }

  return parsed;
}

function tryLoadEnvironmentFromShell(shellPath: string): Record<string, string> | null {
  const result = spawnSync(shellPath, ['-ilc', 'env -0'], {
    env: process.env,
    stdio: ['ignore', 'pipe', 'pipe'],
    timeout: 5000,
  });

  if (result.error || result.status !== 0) {
    return null;
  }

  return parseEnvironmentOutput(result.stdout);
}

export function createExecutorEnvironment(
  envOverrides: Record<string, string> = {}
): Record<string, string> {
  // Load environment variables from the login shell
  const consoleEnv = loadConsoleEnvironmentFromShell();

  return {
    ...process.env,
    ...consoleEnv,
    ...envOverrides,
  };
}
```

## Παράδειγμα πλήρους χρήσης

### Βασική χρήση

Στο έργο HagiCode, χρησιμοποιούμε την ακόλουθη προσέγγιση για να αρχικοποιήσουμε τον πελάτη Codex και να εκτελέσουμε εργασίες:

```typescript
import { Codex } from '@openai/codex-sdk';

async function executeWithCodex(prompt: string, workingDir: string) {
  const client = new Codex({
    apiKey: process.env.CODEX_API_KEY,
    env: { PATH: process.env.PATH },
  });

  const thread = client.startThread({
    workingDirectory: workingDir,
  });

  const { events } = await thread.runStreamed(prompt);

  let result = '';
  for await (const event of events) {
    if (event.type === 'item.updated' && event.item.type === 'agent_message') {
      result = event.item.text;
    }
    if (event.type === 'turn.completed') {
      console.log('Token usage:', event.usage);
    }
  }

  // Try to parse JSON output
  try {
    const parsed = JSON.parse(result);
    return parsed.output;
  } catch {
    return result;
  }
}
```

### Ολοκληρώστε την υλοποίηση με επαναλήψεις

```typescript
export class CodexSdkExecutor {
  private readonly config: CodexRuntimeConfig;
  private readonly client: Codex;
  private threadId: string | null = null;

  async executeStage(input: CodexStageExecutionInput): Promise<CodexStageExecutionResult> {
    const maxAttempts = Math.max(1, this.config.retryCount + 1);
    let attempt = 0;
    let lastError: CodexExecutorError | null = null;

    while (attempt < maxAttempts) {
      attempt += 1;

      try {
        const thread = this.getThread(input.workingDirectory);
        const { output, usage } = await this.runWithStreaming(thread, input);

        return {
          output,
          usage,
          threadId: this.threadId!,
          attempts: attempt,
          latencyMs: Date.now() - startedAt,
        };
      } catch (error) {
        const mappedError = mapError(error);
        lastError = mappedError;

        // Non-retryable error or max retry attempts reached
        if (!mappedError.retryable || attempt >= maxAttempts) {
          throw mappedError;
        }

        // Wait before retrying
        await new Promise(resolve => setTimeout(resolve, 1000 * attempt));
      }
    }

    throw lastError!;
  }
}
```

## Βέλτιστες Πρακτικές

### 1. Απαιτήσεις καταλόγου εργασίας

- Βεβαιωθείτε ότι ο κατάλογος εργασίας είναι ένα έγκυρο αποθετήριο Git
- Χρησιμοποιήστε το `PROJECT_ROOT` μεταβλητή περιβάλλοντος για να το προσδιορίσετε ρητά
- Κατά τον εντοπισμό σφαλμάτων, μπορείτε να ορίσετε `CODEX_SKIP_GIT_REPO_CHECK=true` για να παραλείψετε τον έλεγχο

### 2. Διαμόρφωση μεταβλητής περιβάλλοντος

- Περάστε μόνο τις απαιτούμενες μεταβλητές περιβάλλοντος μέσω ενός μηχανισμού λίστας επιτρεπόμενων
- Χρησιμοποιήστε το κέλυφος σύνδεσης για να φορτώσετε το πλήρες περιβάλλον
- Αποφύγετε τη διαβίβαση ευαίσθητων πληροφοριών

### 3. Στρατηγική χρονικού ορίου και επανάληψης

- Ορίστε εύλογα χρονικά διαστήματα με βάση την πολυπλοκότητα της εργασίας
- Εφαρμογή εκθετικής υποχώρησης για σφάλματα με δυνατότητα επανάληψης
- Καταγράψτε τους αριθμούς και τους λόγους επανάληψης

### 4. Χειρισμός σφαλμάτων

- Διάκριση μεταξύ σφαλμάτων με δυνατότητα επανάληψης και μη
- Παρέχετε σαφή μηνύματα σφάλματος και προτάσεις
- Χρησιμοποιήστε ενοποιημένους κωδικούς σφάλματος, ώστε τα ανώτερα στρώματα να μπορούν να τους χειρίζονται με συνέπεια

### 5. Έξοδος ροής

- Εφαρμόστε σταδιακές επανακλήσεις εξόδου για να βελτιώσετε την εμπειρία χρήστη
- Χειριστείτε σωστά τις σταδιακές ενημερώσεις μηνυμάτων
- Καταγράψτε τη χρήση διακριτικού για ανάλυση κόστους

Στο πραγματικό περιβάλλον παραγωγής του έργου HagiCode, έχουμε ήδη επαληθεύσει την αποτελεσματικότητα των βέλτιστων πρακτικών παραπάνω. Αυτή η προσέγγιση μας βοήθησε να δημιουργήσουμε μια σταθερή και αξιόπιστη υπηρεσία εκτέλεσης AI. Τελικά, η πρακτική επικύρωση έχει μεγαλύτερη σημασία από τη θεωρία και μόνο.

## Συμπέρασμα

Ο μηχανισμός ροής συμβάντων Codex SDK παρέχει ισχυρές δυνατότητες για τη δημιουργία υπηρεσιών εκτέλεσης τεχνητής νοημοσύνης. Αναλύοντας σωστά διαφορετικά είδη συμβάντων, μπορούμε:

- Λάβετε κατάσταση εκτέλεσης και έξοδο σε πραγματικό χρόνο
- Εφαρμόστε αξιόπιστους μηχανισμούς χειρισμού σφαλμάτων και επανάληψης
- Λάβετε λεπτομερή στατιστικά στοιχεία εκτέλεσης
- Δημιουργήστε μια πλατφόρμα εκτέλεσης AI με πλήρεις δυνατότητες

Οι βασικές έννοιες και τα δείγματα κώδικα που εισάγονται σε αυτό το άρθρο μπορούν να εφαρμοστούν απευθείας σε πραγματικά έργα, βοηθώντας τους προγραμματιστές να ξεκινήσουν γρήγορα την ενσωμάτωση του Codex SDK. Εάν βρίσκετε αυτή την προσέγγιση πολύτιμη, αντικατοπτρίζει επίσης τη δύναμη της μηχανικής πρακτικής της HagiCode και κάνει την ίδια τη HagiCode να αξίζει να την ακολουθήσετε.

## Αναφορές

- [Επίσημη τεκμηρίωση Codex SDK](https://platform.openai.com/docs/codex)
- [Αποθετήριο HagiCode GitHub](https://github.com/HagiCode-org/site)
- [Επίσημος ιστότοπος HagiCode](https://hagicode.com)

---

Σας ευχαριστώ που διαβάσατε. Εάν βρήκατε αυτό το άρθρο χρήσιμο, κάντε κλικ στο κουμπί "μου αρέσει" παρακάτω, ώστε να το ανακαλύψουν περισσότεροι άνθρωποι.

Αυτό το περιεχόμενο δημιουργήθηκε με συνεργασία με τη βοήθεια τεχνητής νοημοσύνης, αξιολογήθηκε από τον συγγραφέα και αντικατοπτρίζει τις απόψεις και τις θέσεις του ίδιου του συγγραφέα.

- Συγγραφέας: [newbe36524](https://www.newbe.pro)
- Σύνδεσμος άρθρου: [https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/](https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/)
- Σημείωση περί πνευματικών δικαιωμάτων: Εκτός εάν αναφέρεται διαφορετικά, όλα τα άρθρα σε αυτό το ιστολόγιο έχουν άδεια χρήσης σύμφωνα με την BY-NC-SA. Αναφέρετε την πηγή κατά την επανεκτύπωση.

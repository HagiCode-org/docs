---
title: "Praktyczny przewodnik po integracji interfejsu CLI CodeBuddy z backendem C#"
date: 2026-03-12
tags: ["C#", CodeBuddy, AI Provider, ACP, "Integration Guide"]
description: "W tym artykule omówiono kompletne podejście do integracji interfejsu CLI CodeBuddy z projektem zaplecza C#, dzięki czemu można zapewnić funkcje asystenta kodowania AI"
---

## Praktyczny przewodnik po integracji interfejsu CLI CodeBuddy z backendem C#

> W tym artykule omówiono kompletne podejście do integracji interfejsu CLI programu CodeBuddy z projektem zaplecza C#, dzięki czemu można kompleksowo zapewnić funkcje asystenta kodowania AI.

:::note[Kontekst czasowy]
W tym artykule omówiono projekt integracji CodeBuddy i strukturę dostawców, jakie istniały w momencie publikacji. Aby zapoznać się z bieżącym, stale obsługiwanym zakresem, zobacz [Instalacja interfejsu CLI agenta AI](/pl-PL/related-software-installation/ai-agent-cli/) i [Przewodnik instalacji](/pl-PL/installation); `IFlowCli` jest teraz zachowywany jedynie jako historyczna wartość zgodności i nie jest częścią bieżącego aktywnego zakresu wsparcia.
:::

## Tło

W przypadku rozwoju nowoczesnych asystentów kodowania AI pojedynczy dostawca AI często nie jest w stanie zaspokoić złożonych i zmieniających się scenariuszy rozwoju. HagiCode, jako wielofunkcyjny asystent kodowania AI, musi obsługiwać wielu dostawców AI, aby zapewnić lepszą obsługę użytkownika. Użytkownicy powinni mieć wystarczającą swobodę wyboru. Na początku 2026 roku projekt stanął przed kluczową decyzją: jak przywrócić możliwości integracji CodeBuddy ACP (Agent Communication Protocol) w backendzie C#.

W projekcie wdrożono już wcześniej integrację z CodeBuddy, ale powiązany kod został usunięty podczas refaktoryzacji. Nie ma tam zbyt wiele do narzekania; podczas programowania iteracyjnego zawsze coś zostaje pominięte. Celem tego rozwiązania technicznego było pełne przywrócenie tych możliwości i ulepszenie architektury, tak aby była solidniejsza i łatwiejsza w utrzymaniu.

Jeśli rozważasz także podłączenie wielu asystentów kodowania AI do swojego własnego projektu, poniższe podejście może podsunąć Ci kilka pomysłów. Odzwierciedla wnioski, które podsumowaliśmy po wpadnięciu w wiele pułapek, i być może pomoże ci uniknąć kilku objazdów.

## O HagiCodzie

Podejście przedstawione w tym artykule wynika z naszych praktycznych doświadczeń w zakresie [Kod Hagi](https://hagicode.com) projekt. HagiCode to projekt asystenta kodowania AI typu open source, który obsługuje wielu dostawców AI i działanie na wielu platformach. Aby zaspokoić różne preferencje użytkowników, musimy elastycznie przełączać się pomiędzy różnymi asystentami kodowania AI, dlatego właśnie zbudowaliśmy opisaną tutaj integrację CodeBuddy.

HagiCode wykorzystuje konstrukcję modułową, z dostawcami AI zaimplementowanymi jako komponenty wtykowe. Ta architektura pozwala nam łatwo dodawać nową obsługę AI bez wpływu na istniejące funkcje. Jeśli projekt jest dobrze wykonany od początku, oszczędza to wielu kłopotów później. Jeśli interesuje Cię nasza architektura techniczna, możesz wyświetlić pełny kod źródłowy na GitHub.

## Projekt Architektury

### Przegląd architektury warstwowej

Integracja pomiędzy C# i CodeBuddy wykorzystuje przejrzystą architekturę warstwową. Taka konstrukcja jasno określa zakres odpowiedzialności i znacznie ułatwia długoterminową konserwację:

```
┌─────────────────────────────────────────────┐
│           Provider Contract Layer           │
│   AIProviderType enum + extension methods   │
├─────────────────────────────────────────────┤
│           Provider Factory Layer            │
│   AIProviderFactory dependency injection factory │
├─────────────────────────────────────────────┤
│           Provider Implementation Layer     │
│   CodebuddyCliProvider concrete implementation │
├─────────────────────────────────────────────┤
│           ACP Infrastructure Layer          │
│  ACPSessionManager / StdioAcpTransport      │
│  AcpRpcClient / AcpAgentClient              │
└─────────────────────────────────────────────┘
```

Jakie są zalety tego warstwowania? Mówiąc prościej, każda warstwa nie przeszkadza innym. Jeżeli później będziemy chcieli zmienić mechanizm komunikacji np. z `stdio` do protokołu WebSocket wystarczy zmodyfikować dolną warstwę, a logika biznesowa nad nią pozostanie nienaruszona. Nikt nie chce, aby zmiany w komunikacji objęły całą bazę kodu.

### Podział głównych komponentów

**Warstwa umowy z dostawcą** jest podstawą całej architektury. Definiujemy `AIProviderType` wyliczenie, gdzie `CodebuddyCli = 3` jest używany jako wartość wyliczeniowa i implementuje dwukierunkowe mapowanie między ciągami i wyliczeniami za pomocą metod rozszerzających. Umożliwia to wygodne konwertowanie ciągów znaków w plikach konfiguracyjnych na wyliczenia, a wyliczenia z powrotem na ciągi znaków w celu debugowania danych wyjściowych.

**Warstwa fabryczna dostawcy** jest odpowiedzialna za utworzenie odpowiedniej instancji dostawcy na podstawie konfiguracji. Używa wstrzykiwania zależności .NET wraz z `ActivatorUtilities.CreateInstance` do dynamicznej kreacji. Zaletą wzorca fabrycznego jest to, że dodając nowego dostawcę, wystarczy dodać logikę tworzenia zamiast modyfikować istniejący kod.

**Warstwa implementacji dostawcy** to miejsce, w którym dzieje się rzeczywista praca. `CodebuddyCliProvider` realizuje `IAIProvider` interfejs i udostępnia dwa tryby wywoływania: `ExecuteAsync` w przypadku połączeń innych niż przesyłanie strumieniowe i `StreamAsync` do transmisji strumieniowej rozmów.

**Warstwa infrastruktury AKP** stanowi podstawę komunikacji. Ta warstwa obsługuje wszystkie szczegóły protokołu, w tym zarządzanie procesami, serializację komunikatów i analizowanie odpowiedzi. Jest to fundament, dzięki któremu wszystko nad nim jest stabilne.

## Mechanizm komunikacji

### Tryb transportu Stdio

CodeBuddy używa **Stdio (standardowe wejście/wyjście)** do komunikacji z procesami zewnętrznymi. Polecenie uruchamiania jest proste:

```bash
codebuddy --acp
```

Następnie komunikaty JSON-RPC są wymieniane poprzez standardowe wejście i wyjście. To podejście ma kilka zalet:

1. **Szybkie uruchamianie**: lokalna komunikacja procesowa pozwala uniknąć opóźnień sieci
2. **Prosta konfiguracja**: wystarczy podać ścieżkę pliku wykonywalnego
3. **Izolacja środowiska**: każda sesja przebiega w niezależnym procesie, więc nie wpływają na siebie

Podczas komunikacji obsługiwane jest wstrzykiwanie zmiennych środowiskowych. Typowe przykłady obejmują:

- `CODEBUDDY_API_KEY`: Uwierzytelnianie kluczem API
- `CODEBUDDY_INTERNET_ENVIRONMENT`: konfiguracja środowiska sieciowego

Podobnie jak w przypadku komunikacji między ludźmi, warto najpierw wybrać dogodny kanał.

### Protokół wiadomości

ACP jest oparty na JSON-RPC 2.0. Format wiadomości wygląda mniej więcej tak:

```json
// Request message
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "agent/prompt",
  "params": {
    "prompt": "Help me write a sorting algorithm",
    "sessionId": "session-123"
  }
}

// Response message
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": "Here is the AI response..."
  }
}
```

W rzeczywistej implementacji hermetyzujemy wszystkie szczegóły protokołu, więc wyższa warstwa biznesowa musi dbać jedynie o szybkość i reakcję.

## Implementacja rdzenia

### 1. Przywróć umowę z dostawcą

Najpierw przywróć typ CodeBuddy w pliku wyliczeniowym:

```csharp
// PCode.Models/AIProviderType.cs
public enum AIProviderType
{
    ClaudeCodeCli = 0,
    CodexCli = 1,
    GitHubCopilot = 2,
    CodebuddyCli = 3,  // Restore this enum value
    OpenCodeCli = 4,
    IFlowCli = 5,
}
```

Następnie dodaj mapowanie ciągów w metodach rozszerzenia, aby plik konfiguracyjny mógł określić dostawcę za pomocą ciągu znaków:

```csharp
// AIProviderTypeExtensions.cs
private static readonly Dictionary<string, AIProviderType> _typeMap = new(
    StringComparer.OrdinalIgnoreCase)
{
    ["CodebuddyCli"] = AIProviderType.CodebuddyCli,
    ["Codebuddy"] = AIProviderType.CodebuddyCli,
    ["codebuddy"] = AIProviderType.CodebuddyCli,
    // ... Mappings for other providers
};
```

### 2. Zintegruj fabrykę dostawców

Dodaj gałąź tworzenia CodeBuddy w klasie fabrycznej:

```csharp
// AIProviderFactory.cs
private IAIProvider? CreateProvider(AIProviderType providerType, ProviderConfiguration config)
{
    return providerType switch
    {
        AIProviderType.CodebuddyCli =>
            ActivatorUtilities.CreateInstance<CodebuddyCliProvider>(
                _serviceProvider,
                Options.Create(config)),
        // ... Other providers
        _ => throw new NotSupportedException($"Provider {providerType} not supported")
    };
}
```

Wykorzystuje to wstrzykiwanie zależności przez `ActivatorUtilities`, który automatycznie obsługuje wstrzykiwanie parametrów konstruktora i jest bardzo wygodny.

### 3. Zakończ wdrożenie dostawcy

Poniżej znajduje się podstawowa implementacja `CodebuddyCliProvider`, obejmujący zarówno tryby wywoływania strumieniowego, jak i niestrumieniowego:

```csharp
public class CodebuddyCliProvider : IAIProvider
{
    private readonly ILogger<CodebuddyCliProvider> _logger;
    private readonly IACPSessionManager _sessionManager;
    private readonly ProviderConfiguration _config;

    public string Name => "CodebuddyCli";
    public bool SupportsStreaming => true;
    public ProviderCapabilities Capabilities { get; }

    public CodebuddyCliProvider(
        ILogger<CodebuddyCliProvider> logger,
        IACPSessionManager sessionManager,
        IOptions<ProviderConfiguration> config)
    {
        _logger = logger;
        _sessionManager = sessionManager;
        _config = config.Value;

        // Define the capabilities of the current Provider
        Capabilities = new ProviderCapabilities
        {
            SupportsStreaming = true,
            SupportsTools = true,
            SupportsSystemMessages = true,
            SupportsArtifacts = false,
            MaxTokens = 8192
        };
    }

    // Non-streaming call: return all results together after completion
    public async Task<AIResponse> ExecuteAsync(
        AIRequest request,
        CancellationToken cancellationToken = default)
    {
        // Create an independent session for the request
        var session = await _sessionManager.CreateSessionAsync(
            "CodebuddyCli",
            request.WorkingDirectory,
            cancellationToken,
            request.SessionId);

        try
        {
            var fullPrompt = BuildPrompt(request);
            await session.SendPromptAsync(fullPrompt, cancellationToken);

            var responseBuilder = new StringBuilder();
            var toolCalls = new List<AIToolCall>();

            // Collect all response chunks
            await foreach (var chunk in StreamFromSession(session, cancellationToken))
            {
                if (!string.IsNullOrEmpty(chunk.Content))
                {
                    responseBuilder.Append(chunk.Content);
                }
                // Handle tool calls...
            }

            return new AIResponse
            {
                Content = AIResultContentSanitizer.SanitizeResultContent(
                    responseBuilder.ToString()),
                ToolCalls = toolCalls,
                Provider = Name,
                Model = string.Empty
            };
        }
        finally
        {
            // Release session resources
            await session.DisposeAsync();
        }
    }

    // Streaming call: return response chunks in real time
    public async IAsyncEnumerable<AIStreamingChunk> StreamAsync(
        AIRequest request,
        [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var session = await _sessionManager.CreateSessionAsync(
            "CodebuddyCli",
            request.WorkingDirectory,
            cancellationToken);

        try
        {
            var fullPrompt = BuildPrompt(request);
            await session.SendPromptAsync(fullPrompt, cancellationToken);

            await foreach (var chunk in StreamFromSession(session, cancellationToken))
            {
                yield return chunk;
            }
        }
        finally
        {
            await session.DisposeAsync();
        }
    }

    private async IAsyncEnumerable<AIStreamingChunk> StreamFromSession(
        IACPSession session,
        [EnumeratorCancellation] CancellationToken cancellationToken)
    {
        // Iterate through all updates in the session
        await foreach (var notification in session.ReceiveUpdatesAsync(cancellationToken))
        {
            switch (notification.Update)
            {
                case AgentMessageChunkSessionUpdate agentMessage:
                    // Handle text content chunks
                    if (agentMessage.Content is AcpImp.TextContentBlock textContent)
                    {
                        yield return new AIStreamingChunk
                        {
                            Content = textContent.Text,
                            Type = StreamingChunkType.ContentDelta,
                            IsComplete = false
                        };
                    }
                    break;

                case ToolCallSessionUpdate toolCall:
                    // Handle tool calls
                    yield return new AIStreamingChunk
                    {
                        Content = string.Empty,
                        Type = StreamingChunkType.ToolCallDelta,
                        ToolCallDelta = new AIToolCallDelta
                        {
                            Id = toolCall.ToolCallId,
                            Name = toolCall.Kind.ToString(),
                            Arguments = toolCall.RawInput?.ToString()
                        }
                    };
                    break;

                case AcpImp.PromptCompletedSessionUpdate:
                    // Response complete
                    yield break;
            }
        }
    }

    // Build the full prompt
    private string BuildPrompt(AIRequest request, string? embeddedCommandPrompt = null)
    {
        var sb = new StringBuilder();

        // Embedded command prompt, if present
        if (!string.IsNullOrEmpty(embeddedCommandPrompt))
        {
            sb.AppendLine(embeddedCommandPrompt);
            sb.AppendLine();
        }

        // System message
        if (!string.IsNullOrEmpty(request.SystemMessage))
        {
            sb.AppendLine(request.SystemMessage);
            sb.AppendLine();
        }

        // User prompt
        sb.Append(request.Prompt);
        return sb.ToString();
    }
}
```

W tym kodzie jest kilka kluczowych punktów:

1. **Zarządzanie sesją**: każde żądanie tworzy niezależną sesję i zwalnia zasoby po zakończeniu żądania. To lekcja, którą można wyciągnąć metodą prób i błędów. Jeśli ponowne wykorzystanie sesji nie zostanie dobrze obsługiwane, łatwo pojawia się zanieczyszczenie stanu.

2. **Przetwarzanie strumieniowe**: `IAsyncEnumerable` umożliwia zwrócenie odpowiedzi w trakcie jej generowania, zamiast czekać na zakończenie całej zawartości. Jest to szczególnie ważne w przypadku scenariuszy z długim tekstem i znacznie poprawia komfort użytkowania.

3. **Wywołania narzędzi**: CodeBuddy obsługuje wywoływanie narzędzi (wywoływanie funkcji), obsługiwane przez `ToolCallSessionUpdate`. Ta funkcja ma kluczowe znaczenie w przypadku złożonych zadań edycji kodu.

4. **Filtrowanie treści**: `AIResultContentSanitizer` służy do filtrowania zawartości bloku Think i utrzymywania czystości wyniku.

### 4. Konfiguracja wstrzykiwania zależności

Dodaj powiązane usługi podczas rejestracji modułu:

```csharp
// PCodeClaudeHelperModule.cs
public void ConfigureModule(IServiceCollection context)
{
    // Register Provider
    context.Services.AddTransient<CodebuddyCliProvider>();

    // Register ACP infrastructure
    context.Services.AddSingleton<IACPSessionManager, ACPSessionManager>();
    context.Services.AddSingleton<IAcpPlatformConfigurationResolver, AcpPlatformConfigurationResolver>();
    context.Services.AddSingleton<IAIRequestToAcpMapper, AIRequestToAcpMapper>();
    context.Services.AddSingleton<IAcpToAIResponseMapper, AcpToAIResponseMapper>();
}
```

## Przykład konfiguracji

### Plik konfiguracyjny

Dodaj konfigurację związaną z CodeBuddy do `appsettings.json`:

```yaml
AI:
  # Default Provider to use
  DefaultProvider: "CodebuddyCli"

  # Provider configuration
  Providers:
    CodebuddyCli:
      Type: "CodebuddyCli"
      WorkingDirectory: "C:/projects/my-app"
      ExecutablePath: "C:/tools/codebuddy.cmd"

  # Platform-specific configuration
  PlatformConfigurations:
    CodebuddyCli:
      ExecutablePath: "C:/tools/codebuddy.cmd"
      Arguments: "--acp"
      StartupTimeoutMs: 5000
      EnvironmentVariables:
        CODEBUDDY_API_KEY: "${CODEBUDDY_API_KEY}"
        CODEBUDDY_INTERNET_ENVIRONMENT: "production"
```

### Model konfiguracji

Odpowiednia definicja modelu konfiguracji:

```csharp
public class CodebuddyPlatformConfiguration : IAcpPlatformConfiguration
{
    public string ProviderName => "CodebuddyCli";
    public AcpTransportType TransportType => AcpTransportType.Stdio;

    public string ExecutablePath { get; set; } = "codebuddy";
    public string Arguments { get; set; } = "--acp";
    public int StartupTimeoutMs { get; set; } = 5000;

    public Dictionary<string, string?>? EnvironmentVariables { get; set; }
}
```

## Wyciągnięte wnioski praktyczne

### Dziennik pułapek

Podczas wdrażania napotkaliśmy kilka typowych pułapek, a podzielenie się nimi tutaj może pomóc innym uniknąć tych samych objazdów:

1. **Problem z wyciekiem sesji**: początkowo sesje nie zostały poprawnie wydane, co spowodowało wyczerpanie zasobów procesu. Rozwiązaniem było użycie `try-finally` aby zapewnić zwolnienie zasobów dla każdego żądania.

2. **Przekazywanie zmiennych środowiskowych**: Windows i Linux używają innej składni zmiennych środowiskowych, więc później je ujednoliciliśmy `Dictionary<string, string?>` sobie z tym poradzić.

3. **Konfiguracja limitu czasu**: Uruchamianie CLI wymaga czasu, dlatego ustawiamy 5-sekundowy limit czasu uruchamiania, aby uniknąć błędów w przypadku szybkich żądań.

4. **Problemy z kodowaniem**: w systemie Windows domyślne kodowanie może powodować zniekształcenie tekstu w języku chińskim, dlatego kodowanie UTF-8 jest wyraźnie określone podczas uruchamiania procesu.

### Optymalizacja wydajności

1. **Pula sesji**: w przypadku częstych krótkich żądań rozważ wdrożenie puli sesji w celu ponownego wykorzystania procesów
2. **Pamięć podręczna połączeń**: klasa fabryczna obsługuje już buforowanie instancji dostawcy
3. **Najpierw asynchronicznie**: używaj programowania asynchronicznego, aby uniknąć blokowania wątków

Wydajność zawsze warto optymalizować. Im dłużej użytkownicy czekają, tym gorsze staje się doświadczenie.

## Wniosek

W tym artykule przedstawiono kompletne rozwiązanie do integracji CodeBuddy CLI z backendem C#, obejmujące cały proces od projektu architektury po konkretną implementację. Dzięki architekturze warstwowej oddzielamy szczegóły protokołu od logiki biznesowej, dzięki czemu kod jest bardziej przejrzysty i łatwiejszy w utrzymaniu.

Kluczowe wnioski:

- Użyj architektury warstwowej z warstwą kontraktu dostawcy, warstwą fabryczną, warstwą implementacji i warstwą infrastruktury
- Użyj JSON-RPC przez Stdio do komunikacji między procesami
- Implementuj elastyczną konfigurację i rozszerzalność poprzez wstrzykiwanie zależności
- Zapewnij tryby wywoływania zarówno strumieniowe, jak i niestrumieniowe

To podejście jest odpowiednie nie tylko dla CodeBuddy; dodawanie nowych dostawców AI może przebiegać według tego samego schematu. Jeśli budujesz podobną integrację z wieloma dostawcami sztucznej inteligencji, mam nadzieję, że ten artykuł będzie dla Ciebie przydatnym źródłem informacji.

---

## Referencje

- [Oficjalna dokumentacja CodeBuddy](https://codebuddy.example.com)
- [Specyfikacja protokołu ACP](https://acp-protocol.example.com)
- [Strona główna projektu HagiCode](https://hagicode.com)
- [Repozytorium HagiCode na GitHubie](https://github.com/HagiCode-org/site)
- [Najlepsze praktyki dotyczące wstrzykiwania zależności .NET](https://docs.microsoft.com/zh-cn/dotnet/core/extensions/dependency-injection)

---

**Jeśli ten artykuł Ci pomógł:**

- Daj nam gwiazdkę na GitHubie: [github.com/HagiCode-org/site](https://github.com/HagiCode-org/site)
- Odwiedź oficjalną stronę internetową, aby dowiedzieć się więcej: [hagicode.com](https://hagicode.com)
- Obejrzyj 30-minutową praktyczną demonstrację: [www.bilibili.com/video/BV1pirZBuEzq/](https://www.bilibili.com/video/BV1pirZBuEzq/)
- Spróbuj instalacji jednym kliknięciem: [https://docs.hagicode.com/installation/docker-compose](https://docs.hagicode.com/installation/docker-compose)
- Szybka instalacja aplikacji komputerowej: [hagicode.com/desktop/](https://hagicode.com/desktop/)
- Rozpoczęła się publiczna wersja beta; zainstaluj i wypróbuj

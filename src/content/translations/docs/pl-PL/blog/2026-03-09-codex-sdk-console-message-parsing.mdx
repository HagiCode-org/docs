---
title: Kompletny przewodnik po analizowaniu komunikatów konsoli Codex SDK
date: 2026-03-10
tags: [Codex SDK, TypeScript, AI Executor, Event Stream]
description: "W tym artykule wyjaśniono mechanizm strumienia zdarzeń Codex SDK, analizę typów komunikatów i najlepsze praktyki w rzeczywistych projektach, pomagając programistom szybko opanować"
---

## Kompletny przewodnik po analizowaniu komunikatów konsoli Codex SDK

> W tym artykule wyjaśniono mechanizm strumienia zdarzeń Codex SDK, analizę typów komunikatów i najlepsze praktyki stosowane w rzeczywistych projektach, pomagając programistom szybko opanować podstawowe umiejętności związane z usługami wykonawczymi AI.

## Tło

Budując usługę wykonywania AI w oparciu o pakiet SDK Codex, nieuchronnie natrafiamy na praktyczne pytanie: jak powinniśmy obsługiwać przesyłane strumieniowo komunikaty o zdarzeniach zwracane przez Codex? Komunikaty te zawierają ważne informacje, takie jak status wykonania, treść wyjściowa i szczegóły błędów, dlatego należy się z nimi ostrożnie obchodzić.

W ramach projektu HagiCode potrzebowaliśmy niezawodnego executora scenariuszy asystenta kodowania AI. Właśnie dlatego postanowiliśmy dogłębnie przestudiować mechanizm strumienia zdarzeń Codex SDK. W końcu tylko rozumiejąc, jak działają podstawowe komunikaty, możemy zbudować platformę realizującą sztuczną inteligencję klasy korporacyjnej.

Codex SDK to pakiet SDK pomocy programistycznej wydany przez OpenAI. Zwraca wyniki wykonania poprzez strumień zdarzeń. W przeciwieństwie do tradycyjnego modelu żądanie-odpowiedź, Codex wykorzystuje zdarzenia przesyłane strumieniowo, dzięki czemu możemy:

- Uzyskaj postęp realizacji w czasie rzeczywistym
- Natychmiast obsługuj błędy
- Uzyskaj szczegółowe statystyki wykorzystania tokenów
- Obsługuj długotrwałe i złożone zadania

Zrozumienie tych typów zdarzeń i prawidłowe ich przeanalizowanie jest niezbędne do wdrożenia w pełni wydajnego modułu wykonującego sztuczną inteligencję. W końcu nikt nie chce pracować z czarną skrzynką.

## O HagiCodzie

Rozwiązanie udostępnione w tym artykule wynika z naszych praktycznych doświadczeń w branży [Kod Hagi](https://hagicode.com) projekt. HagiCode to projekt asystenta kodowania AI typu open source, którego celem jest zapewnienie programistom inteligentnego wsparcia w kodowaniu. Podczas opracowywania musieliśmy zbudować niezawodną usługę wykonywania AI do obsługi żądań wykonania kodu użytkownika, co jest bezpośrednim powodem wprowadzenia pakietu Codex SDK.

Jako asystent kodowania AI, HagiCode musi radzić sobie z różnymi złożonymi scenariuszami wykonywania kodu: uzyskiwać postęp wykonania w czasie rzeczywistym, szybko obsługiwać błędy i zbierać szczegółowe statystyki użycia tokenów. Dzięki dogłębnemu zrozumieniu mechanizmu strumienia zdarzeń Codex SDK możemy zbudować executor spełniający wymagania środowiska produkcyjnego. Ostatecznie, niezależnie od tego, czy jest to oprogramowanie, czy prawdziwe życie, wszystko zyskuje dzięki ciągłej akumulacji i udoskonalaniu.

## Mechanizm strumienia zdarzeń

### Podstawowe pojęcia

Zestaw Codex SDK korzysta z `thread.runStreamed()` metoda zwracająca asynchroniczny iterator zdarzeń:

```typescript
import { Codex } from '@openai/codex-sdk';

const client = new Codex({
  apiKey: process.env.CODEX_API_KEY,
  baseUrl: process.env.CODEX_BASE_URL,
});

const thread = client.startThread({
  workingDirectory: '/path/to/project',
  skipGitRepoCheck: false,
});

const { events } = await thread.runStreamed('your prompt here', {
  outputSchema: {
    type: 'object',
    properties: {
      output: { type: 'string' },
      status: { type: 'string', enum: ['ok', 'action_required'] },
    },
    required: ['output', 'status'],
  },
});

for await (const event of events) {
  // Handle each event
}
```

### Szczegółowe wyjaśnienie typów zdarzeń

| Typ zdarzenia | Opis | Kluczowe dane |
|---------|------|----------|
| `thread.started` | Wątek został pomyślnie rozpoczęty | `thread_id` |
| `item.updated` | Treść wiadomości zaktualizowana | `item.text` |
| `item.completed` | Wiadomość zakończona | `item.text` |
| `turn.completed` | Wykonanie zakończone | `usage` (użycie tokena) |
| `turn.failed` | Wykonanie nie powiodło się | `error.message` |
| `error` | Zdarzenie błędu | `message` |

W rzeczywistych projektach komponent executora HagiCode jest zbudowany na podstawie tych typów zdarzeń. Musimy ostrożnie podchodzić do każdego rodzaju zdarzeń, aby zapewnić użytkownikom bezproblemową obsługę. Dobre systemy buduje się poprzez poważne podejście do szczegółów.

## Implementacja analizowania wiadomości

### Wyodrębnianie treści wiadomości

Treść wiadomości jest wyodrębniana za pomocą procedury obsługi zdarzeń:

```typescript
private handleThreadEvent(event: ThreadEvent, onMessage: (content: string) => void): void {
  // Only handle message update and completion events
  if (event.type !== 'item.updated' && event.type !== 'item.completed') {
    return;
  }

  // Only handle agent message content
  if (event.item.type !== 'agent_message') {
    return;
  }

  // Extract text content
  onMessage(event.item.text);
}
```

Kluczowe punkty:
- Tylko uchwyt `item.updated` i `item.completed` wydarzenia
- Obsługuj tylko zawartość typu `agent_message`
- Treść wiadomości znajduje się w pliku `event.item.text` pole

### Ustrukturyzowane analizowanie wyników

Codex obsługuje uporządkowane dane wyjściowe JSON. Możesz określić format zwrotu za pomocą `outputSchema` parametr:

```typescript
const DEFAULT_OUTPUT_SCHEMA = {
  type: 'object',
  properties: {
    output: { type: 'string' },
    status: { type: 'string', enum: ['ok', 'action_required'] },
  },
  required: ['output', 'status'],
  additionalProperties: false,
} as const;
```

Funkcja analizująca próbuje przeanalizować JSON, a jeśli to się nie powiedzie, powraca do nieprzetworzonego tekstu.

```typescript
function toStructuredOutput(raw: string): StructuredOutput {
  try {
    const parsed = JSON.parse(raw) as Partial<StructuredOutput>;
    if (typeof parsed.output === 'string') {
      return {
        output: parsed.output,
        status: parsed.status === 'action_required' ? 'action_required' : 'ok',
      };
    }
  } catch {
    // JSON parsing failed, fall back to the raw text
  }

  return {
    output: raw,
    status: 'ok',
  };
}
```

### Kompletny przepływ obsługi zdarzeń

```typescript
private async runWithStreaming(
  thread: Thread,
  input: CodexStageExecutionInput
): Promise<{ output: string; usage: Usage | null }> {
  const abortController = new AbortController();
  const timeoutHandle = setTimeout(() => {
    abortController.abort();
  }, Math.max(1000, input.timeoutMs));

  let latestMessage = '';
  let usage: Usage | null = null;
  let emittedLength = 0;

  try {
    const { events } = await thread.runStreamed(input.prompt, {
      outputSchema: DEFAULT_OUTPUT_SCHEMA,
      signal: abortController.signal,
    });

    for await (const event of events) {
      // Handle message content
      this.handleThreadEvent(event, (nextContent) => {
        const delta = nextContent.slice(emittedLength);
        if (delta.length > 0) {
          emittedLength = nextContent.length;
          input.callbacks?.onChunk?.(delta);  // Streaming callback
        }
        latestMessage = nextContent;
      });

      // Process different data based on the event type
      if (event.type === 'thread.started') {
        this.threadId = event.thread_id;
      } else if (event.type === 'turn.completed') {
        usage = event.usage;
      } else if (event.type === 'turn.failed') {
        throw new CodexExecutorError('gateway_unavailable', event.error.message, true);
      } else if (event.type === 'error') {
        throw new CodexExecutorError('gateway_unavailable', event.message, true);
      }
    }
  } catch (error) {
    if (abortController.signal.aborted) {
      throw new CodexExecutorError(
        'upstream_timeout',
        `Codex stage timed out after ${input.timeoutMs}ms`,
        true
      );
    }
    throw error;
  } finally {
    clearTimeout(timeoutHandle);
  }

  const structured = toStructuredOutput(latestMessage);
  return { output: structured.output, usage };
}
```

## Strategia obsługi błędów

### Mapowanie kodów błędów

Mapuj określone wzorce błędów na konkretne kody błędów, aby wyższe warstwy mogły łatwiej sobie z nimi poradzić:

```typescript
function mapError(error: unknown): CodexExecutorError {
  if (error instanceof CodexExecutorError) {
    return error;
  }

  const message = error instanceof Error ? error.message : String(error);
  const normalized = message.toLowerCase();

  // Authentication errors - not retryable
  if (normalized.includes('401') ||
      normalized.includes('403') ||
      normalized.includes('api key') ||
      normalized.includes('auth')) {
    return new CodexExecutorError('auth_invalid', message, false);
  }

  // Rate limit errors - retryable
  if (normalized.includes('429') || normalized.includes('rate limit')) {
    return new CodexExecutorError('rate_limited', message, true);
  }

  // Timeout errors - retryable
  if (normalized.includes('timeout') || normalized.includes('aborted')) {
    return new CodexExecutorError('upstream_timeout', message, true);
  }

  // Default error
  return new CodexExecutorError('gateway_unavailable', message, true);
}
```

### Definicje typów błędów

```typescript
export type CodexErrorCode =
  | 'auth_invalid'      // Authentication failure
  | 'upstream_timeout'  // Upstream timeout
  | 'rate_limited'      // Rate limited
  | 'gateway_unavailable'; // Gateway unavailable

export class CodexExecutorError extends Error {
  readonly code: CodexErrorCode;
  readonly retryable: boolean;

  constructor(code: CodexErrorCode, message: string, retryable: boolean) {
    super(message);
    this.name = 'CodexExecutorError';
    this.code = code;
    this.retryable = retryable;
  }
}
```

## Konfiguracja katalogu roboczego i środowiska

### Walidacja katalogu roboczego

Zestaw Codex SDK wymaga, aby katalog roboczy był prawidłowym repozytorium Git.

```typescript
export function validateWorkingDirectory(
  workingDirectory: string,
  skipGitRepoCheck: boolean
): void {
  const resolvedWorkingDirectory = path.resolve(workingDirectory);

  if (!existsSync(resolvedWorkingDirectory)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory does not exist.',
      false
    );
  }

  if (!statSync(resolvedWorkingDirectory).isDirectory()) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a directory.',
      false
    );
  }

  if (skipGitRepoCheck) {
    return;
  }

  const gitDir = path.join(resolvedWorkingDirectory, '.git');
  if (!existsSync(gitDir)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a git repository.',
      false
    );
  }
}
```

### Ładowanie zmiennych środowiskowych

Codex SDK musi załadować zmienne środowiskowe z powłoki logowania, aby agent AI mógł uzyskać dostęp do poleceń systemowych:

```typescript
function parseEnvironmentOutput(output: Buffer): Record<string, string> {
  const parsed: Record<string, string> = {};

  for (const entry of output.toString('utf8').split('\0')) {
    if (!entry) continue;

    const separatorIndex = entry.indexOf('=');
    if (separatorIndex <= 0) continue;

    const key = entry.slice(0, separatorIndex);
    const value = entry.slice(separatorIndex + 1);
    if (key.length > 0) {
      parsed[key] = value;
    }
  }

  return parsed;
}

function tryLoadEnvironmentFromShell(shellPath: string): Record<string, string> | null {
  const result = spawnSync(shellPath, ['-ilc', 'env -0'], {
    env: process.env,
    stdio: ['ignore', 'pipe', 'pipe'],
    timeout: 5000,
  });

  if (result.error || result.status !== 0) {
    return null;
  }

  return parseEnvironmentOutput(result.stdout);
}

export function createExecutorEnvironment(
  envOverrides: Record<string, string> = {}
): Record<string, string> {
  // Load environment variables from the login shell
  const consoleEnv = loadConsoleEnvironmentFromShell();

  return {
    ...process.env,
    ...consoleEnv,
    ...envOverrides,
  };
}
```

## Kompletny przykład użycia

### Podstawowe użycie

W projekcie HagiCode stosujemy następujące podejście do inicjalizacji klienta Codex i wykonywania zadań:

```typescript
import { Codex } from '@openai/codex-sdk';

async function executeWithCodex(prompt: string, workingDir: string) {
  const client = new Codex({
    apiKey: process.env.CODEX_API_KEY,
    env: { PATH: process.env.PATH },
  });

  const thread = client.startThread({
    workingDirectory: workingDir,
  });

  const { events } = await thread.runStreamed(prompt);

  let result = '';
  for await (const event of events) {
    if (event.type === 'item.updated' && event.item.type === 'agent_message') {
      result = event.item.text;
    }
    if (event.type === 'turn.completed') {
      console.log('Token usage:', event.usage);
    }
  }

  // Try to parse JSON output
  try {
    const parsed = JSON.parse(result);
    return parsed.output;
  } catch {
    return result;
  }
}
```

### Zakończ wdrożenie z ponownymi próbami

```typescript
export class CodexSdkExecutor {
  private readonly config: CodexRuntimeConfig;
  private readonly client: Codex;
  private threadId: string | null = null;

  async executeStage(input: CodexStageExecutionInput): Promise<CodexStageExecutionResult> {
    const maxAttempts = Math.max(1, this.config.retryCount + 1);
    let attempt = 0;
    let lastError: CodexExecutorError | null = null;

    while (attempt < maxAttempts) {
      attempt += 1;

      try {
        const thread = this.getThread(input.workingDirectory);
        const { output, usage } = await this.runWithStreaming(thread, input);

        return {
          output,
          usage,
          threadId: this.threadId!,
          attempts: attempt,
          latencyMs: Date.now() - startedAt,
        };
      } catch (error) {
        const mappedError = mapError(error);
        lastError = mappedError;

        // Non-retryable error or max retry attempts reached
        if (!mappedError.retryable || attempt >= maxAttempts) {
          throw mappedError;
        }

        // Wait before retrying
        await new Promise(resolve => setTimeout(resolve, 1000 * attempt));
      }
    }

    throw lastError!;
  }
}
```

## Najlepsze praktyki

### 1. Wymagania dotyczące katalogu roboczego

- Upewnij się, że katalog roboczy jest prawidłowym repozytorium Git
- Skorzystaj z `PROJECT_ROOT` zmienna środowiskowa, aby określić ją jawnie
- Podczas debugowania możesz ustawić `CODEX_SKIP_GIT_REPO_CHECK=true` aby pominąć kontrolę

### 2. Konfiguracja zmiennej środowiskowej

- Przekazuj tylko wymagane zmienne środowiskowe poprzez mechanizm białej listy
- Użyj powłoki logowania, aby załadować pełne środowisko
- Unikaj przekazywania poufnych informacji

### 3. Strategia przekroczenia limitu czasu i ponowienia próby

- Ustaw rozsądne limity czasu w oparciu o złożoność zadania
- Zaimplementuj wykładnicze wycofywanie w przypadku błędów, które można powtórzyć
- Zapisz liczbę ponownych prób i przyczyny

### 4. Obsługa błędów

- Rozróżnij błędy, które można powtórzyć i które nie mogą zostać powtórzone
- Podaj jasne komunikaty o błędach i sugestie
- Używaj ujednoliconych kodów błędów, aby wyższe warstwy mogły je konsekwentnie obsługiwać

### 5. Wyjście strumieniowe

- Zaimplementuj przyrostowe wywołania zwrotne wyjściowe, aby poprawić komfort użytkownika
- Prawidłowo obsługuj przyrostowe aktualizacje komunikatów
- Rejestruj użycie tokena na potrzeby analizy kosztów

W rzeczywistym środowisku produkcyjnym projektu HagiCode sprawdziliśmy już skuteczność powyższych najlepszych praktyk. Takie podejście pomogło nam zbudować stabilną i niezawodną usługę wykonywania AI. Ostatecznie walidacja praktyczna ma większe znaczenie niż sama teoria.

## Wniosek

Mechanizm strumienia zdarzeń Codex SDK zapewnia duże możliwości tworzenia usług wykonawczych AI. Prawidłowo analizując różne rodzaje zdarzeń, możemy:

- Uzyskaj status wykonania i dane wyjściowe w czasie rzeczywistym
- Wdrażaj niezawodne mechanizmy obsługi błędów i ponawiania prób
- Uzyskaj szczegółowe statystyki wykonania
- Zbuduj w pełni funkcjonalną platformę wykonawczą AI

Podstawowe koncepcje i próbki kodu przedstawione w tym artykule można zastosować bezpośrednio w rzeczywistych projektach, pomagając programistom szybko rozpocząć integrację z Codex SDK. Jeśli uznasz to podejście za wartościowe, odzwierciedla ono również siłę praktyki inżynierskiej HagiCode i sprawia, że ​​warto podążać za samym HagiCode.

## Referencje

- [Oficjalna dokumentacja Codex SDK](https://platform.openai.com/docs/codex)
- [Repozytorium HagiCode na GitHubie](https://github.com/HagiCode-org/site)
- [Oficjalna strona HagiCode](https://hagicode.com)

---

Dziękuję za przeczytanie. Jeśli ten artykuł był dla Ciebie pomocny, kliknij przycisk „Lubię to” poniżej, aby więcej osób mogło go odkryć.

Ta treść została utworzona we współpracy z wykorzystaniem sztucznej inteligencji, sprawdzona przez autora i odzwierciedla jego własne poglądy i stanowisko.

- Autor: [nowość36524](https://www.newbe.pro)
- Link do artykułu: [https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/](https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/)
- Informacja o prawach autorskich: O ile nie zaznaczono inaczej, wszystkie artykuły na tym blogu są objęte licencją BY-NC-SA. Przy przedruku prosimy o podanie źródła.

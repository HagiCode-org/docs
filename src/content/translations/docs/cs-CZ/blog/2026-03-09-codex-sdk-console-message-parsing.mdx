---
title: Kompletní průvodce analýzou zpráv konzoly Codex SDK
date: 2026-03-10
tags: [Codex SDK, TypeScript, AI Executor, Event Stream]
description: "Tento článek vysvětluje mechanismus streamu událostí Codex SDK, analýzu typů zpráv a osvědčené postupy ve skutečných projektech, což vývojářům pomáhá rychle zvládnout"
---

## Kompletní průvodce analýzou zpráv konzoly Codex SDK

> Tento článek vysvětluje mechanismus streamu událostí Codex SDK, analýzu typů zpráv a osvědčené postupy ve skutečných projektech, což vývojářům pomáhá rychle zvládnout základní dovednosti, které stojí za službami spouštění AI.

## Pozadí

Při vytváření služby pro provádění AI založené na Codex SDK nevyhnutelně narážíme na praktickou otázku: jak bychom měli zacházet se streamovanými zprávami o událostech, které vrací Codex? Tyto zprávy obsahují důležité informace, jako je stav provádění, výstupní obsah a podrobnosti o chybách, proto si zaslouží pečlivé zacházení.

V rámci projektu HagiCode jsme potřebovali spolehlivého exekutora pro scénáře asistenta kódování AI. To je přesně důvod, proč jsme se rozhodli podrobně prostudovat mechanismus streamu událostí Codex SDK. Koneckonců, pouze pochopením toho, jak fungují základní zprávy, můžeme vytvořit skutečně podnikovou platformu pro provádění AI.

Codex SDK je sada SDK pro pomoc při programování vydaná společností OpenAI. Vrací výsledky provádění prostřednictvím streamu událostí. Na rozdíl od tradičního modelu žádost-odpověď používá Codex streamované události, abychom mohli:

- Získejte průběh provádění v reálném čase
- Opravte chyby okamžitě
- Získejte podrobné statistiky využití tokenů
- Podpora dlouhotrvajících složitých úkolů

Pochopení těchto typů událostí a jejich správná analýza je zásadní pro implementaci plně schopného spouštěče AI. Nakonec nikdo nechce pracovat s černou skříňkou.

## O HagiCode

Řešení sdílené v tomto článku pochází z našich praktických zkušeností v [HagiCode](https://hagicode.com) projekt. HagiCode je projekt asistenta kódování AI s otevřeným zdrojovým kódem, který poskytuje vývojářům podporu inteligentního kódování. Během vývoje jsme potřebovali vybudovat spolehlivou službu spouštění AI pro zpracování požadavků na spuštění uživatelského kódu, což je přímý důvod, proč jsme zavedli Codex SDK.

Jako asistent kódování AI se HagiCode potřebuje vypořádat s řadou složitých scénářů provádění kódu: zjišťovat průběh provádění v reálném čase, rychle řešit chyby a shromažďovat podrobné statistiky využití tokenů. Díky hlubokému pochopení mechanismu streamu událostí Codex SDK můžeme vytvořit spouštěč, který splňuje požadavky produkčního prostředí. V konečném důsledku, ať už se jedná o software nebo skutečný život, vše těží ze stálé akumulace a zdokonalování.

## Mechanismus streamování událostí

### Základní pojmy

Codex SDK používá `thread.runStreamed()` metoda pro vrácení asynchronního iterátoru události:

```typescript
import { Codex } from '@openai/codex-sdk';

const client = new Codex({
  apiKey: process.env.CODEX_API_KEY,
  baseUrl: process.env.CODEX_BASE_URL,
});

const thread = client.startThread({
  workingDirectory: '/path/to/project',
  skipGitRepoCheck: false,
});

const { events } = await thread.runStreamed('your prompt here', {
  outputSchema: {
    type: 'object',
    properties: {
      output: { type: 'string' },
      status: { type: 'string', enum: ['ok', 'action_required'] },
    },
    required: ['output', 'status'],
  },
});

for await (const event of events) {
  // Handle each event
}
```

### Podrobné vysvětlení typů událostí

| Typ události | Popis | Klíčová data |
|---------|------|----------|
| `thread.started` | Vlákno bylo úspěšně spuštěno | `thread_id` |
| `item.updated` | Obsah zprávy byl aktualizován | `item.text` |
| `item.completed` | Zpráva dokončena | `item.text` |
| `turn.completed` | Provedení dokončeno | `usage` (použití tokenu) |
| `turn.failed` | Provedení se nezdařilo | `error.message` |
| `error` | Chybová událost | `message` |

Ve skutečných projektech je spouštěcí komponenta HagiCode postavena na těchto typech událostí. S každým druhem události musíme zacházet opatrně, abychom zajistili hladký uživatelský dojem. Dobré systémy vznikají tak, že se vážně berou detaily.

## Implementace analýzy zpráv

### Extrahování obsahu zprávy

Obsah zprávy je extrahován pomocí obslužné rutiny události:

```typescript
private handleThreadEvent(event: ThreadEvent, onMessage: (content: string) => void): void {
  // Only handle message update and completion events
  if (event.type !== 'item.updated' && event.type !== 'item.completed') {
    return;
  }

  // Only handle agent message content
  if (event.item.type !== 'agent_message') {
    return;
  }

  // Extract text content
  onMessage(event.item.text);
}
```

Klíčové body:
- Pouze rukojeť `item.updated` a `item.completed` události
- Zpracovávat pouze obsah typu `agent_message`
- Obsah zprávy je v `event.item.text` pole

### Analýza strukturovaného výstupu

Codex podporuje strukturovaný výstup JSON. Návratový formát můžete určit pomocí `outputSchema` parametr:

```typescript
const DEFAULT_OUTPUT_SCHEMA = {
  type: 'object',
  properties: {
    output: { type: 'string' },
    status: { type: 'string', enum: ['ok', 'action_required'] },
  },
  required: ['output', 'status'],
  additionalProperties: false,
} as const;
```

Funkce analýzy se pokusí analyzovat JSON, a pokud se to nezdaří, vrátí se zpět k nezpracovanému textu.

```typescript
function toStructuredOutput(raw: string): StructuredOutput {
  try {
    const parsed = JSON.parse(raw) as Partial<StructuredOutput>;
    if (typeof parsed.output === 'string') {
      return {
        output: parsed.output,
        status: parsed.status === 'action_required' ? 'action_required' : 'ok',
      };
    }
  } catch {
    // JSON parsing failed, fall back to the raw text
  }

  return {
    output: raw,
    status: 'ok',
  };
}
```

### Kompletní tok zpracování událostí

```typescript
private async runWithStreaming(
  thread: Thread,
  input: CodexStageExecutionInput
): Promise<{ output: string; usage: Usage | null }> {
  const abortController = new AbortController();
  const timeoutHandle = setTimeout(() => {
    abortController.abort();
  }, Math.max(1000, input.timeoutMs));

  let latestMessage = '';
  let usage: Usage | null = null;
  let emittedLength = 0;

  try {
    const { events } = await thread.runStreamed(input.prompt, {
      outputSchema: DEFAULT_OUTPUT_SCHEMA,
      signal: abortController.signal,
    });

    for await (const event of events) {
      // Handle message content
      this.handleThreadEvent(event, (nextContent) => {
        const delta = nextContent.slice(emittedLength);
        if (delta.length > 0) {
          emittedLength = nextContent.length;
          input.callbacks?.onChunk?.(delta);  // Streaming callback
        }
        latestMessage = nextContent;
      });

      // Process different data based on the event type
      if (event.type === 'thread.started') {
        this.threadId = event.thread_id;
      } else if (event.type === 'turn.completed') {
        usage = event.usage;
      } else if (event.type === 'turn.failed') {
        throw new CodexExecutorError('gateway_unavailable', event.error.message, true);
      } else if (event.type === 'error') {
        throw new CodexExecutorError('gateway_unavailable', event.message, true);
      }
    }
  } catch (error) {
    if (abortController.signal.aborted) {
      throw new CodexExecutorError(
        'upstream_timeout',
        `Codex stage timed out after ${input.timeoutMs}ms`,
        true
      );
    }
    throw error;
  } finally {
    clearTimeout(timeoutHandle);
  }

  const structured = toStructuredOutput(latestMessage);
  return { output: structured.output, usage };
}
```

## Strategie zpracování chyb

### Mapování chybových kódů

Mapujte konkrétní vzory chyb na konkrétní chybové kódy, aby je horní vrstvy mohly snáze zpracovat:

```typescript
function mapError(error: unknown): CodexExecutorError {
  if (error instanceof CodexExecutorError) {
    return error;
  }

  const message = error instanceof Error ? error.message : String(error);
  const normalized = message.toLowerCase();

  // Authentication errors - not retryable
  if (normalized.includes('401') ||
      normalized.includes('403') ||
      normalized.includes('api key') ||
      normalized.includes('auth')) {
    return new CodexExecutorError('auth_invalid', message, false);
  }

  // Rate limit errors - retryable
  if (normalized.includes('429') || normalized.includes('rate limit')) {
    return new CodexExecutorError('rate_limited', message, true);
  }

  // Timeout errors - retryable
  if (normalized.includes('timeout') || normalized.includes('aborted')) {
    return new CodexExecutorError('upstream_timeout', message, true);
  }

  // Default error
  return new CodexExecutorError('gateway_unavailable', message, true);
}
```

### Definice typů chyb

```typescript
export type CodexErrorCode =
  | 'auth_invalid'      // Authentication failure
  | 'upstream_timeout'  // Upstream timeout
  | 'rate_limited'      // Rate limited
  | 'gateway_unavailable'; // Gateway unavailable

export class CodexExecutorError extends Error {
  readonly code: CodexErrorCode;
  readonly retryable: boolean;

  constructor(code: CodexErrorCode, message: string, retryable: boolean) {
    super(message);
    this.name = 'CodexExecutorError';
    this.code = code;
    this.retryable = retryable;
  }
}
```

## Konfigurace pracovního adresáře a prostředí

### Ověření pracovního adresáře

Codex SDK vyžaduje, aby pracovní adresář byl platným úložištěm Git.

```typescript
export function validateWorkingDirectory(
  workingDirectory: string,
  skipGitRepoCheck: boolean
): void {
  const resolvedWorkingDirectory = path.resolve(workingDirectory);

  if (!existsSync(resolvedWorkingDirectory)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory does not exist.',
      false
    );
  }

  if (!statSync(resolvedWorkingDirectory).isDirectory()) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a directory.',
      false
    );
  }

  if (skipGitRepoCheck) {
    return;
  }

  const gitDir = path.join(resolvedWorkingDirectory, '.git');
  if (!existsSync(gitDir)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a git repository.',
      false
    );
  }
}
```

### Načítání proměnných prostředí

Codex SDK potřebuje načíst proměnné prostředí z přihlašovacího shellu, aby AI Agent mohl přistupovat k systémovým příkazům:

```typescript
function parseEnvironmentOutput(output: Buffer): Record<string, string> {
  const parsed: Record<string, string> = {};

  for (const entry of output.toString('utf8').split('\0')) {
    if (!entry) continue;

    const separatorIndex = entry.indexOf('=');
    if (separatorIndex <= 0) continue;

    const key = entry.slice(0, separatorIndex);
    const value = entry.slice(separatorIndex + 1);
    if (key.length > 0) {
      parsed[key] = value;
    }
  }

  return parsed;
}

function tryLoadEnvironmentFromShell(shellPath: string): Record<string, string> | null {
  const result = spawnSync(shellPath, ['-ilc', 'env -0'], {
    env: process.env,
    stdio: ['ignore', 'pipe', 'pipe'],
    timeout: 5000,
  });

  if (result.error || result.status !== 0) {
    return null;
  }

  return parseEnvironmentOutput(result.stdout);
}

export function createExecutorEnvironment(
  envOverrides: Record<string, string> = {}
): Record<string, string> {
  // Load environment variables from the login shell
  const consoleEnv = loadConsoleEnvironmentFromShell();

  return {
    ...process.env,
    ...consoleEnv,
    ...envOverrides,
  };
}
```

## Kompletní příklad použití

### Základní použití

V projektu HagiCode používáme k inicializaci klienta Codex a provádění úloh následující přístup:

```typescript
import { Codex } from '@openai/codex-sdk';

async function executeWithCodex(prompt: string, workingDir: string) {
  const client = new Codex({
    apiKey: process.env.CODEX_API_KEY,
    env: { PATH: process.env.PATH },
  });

  const thread = client.startThread({
    workingDirectory: workingDir,
  });

  const { events } = await thread.runStreamed(prompt);

  let result = '';
  for await (const event of events) {
    if (event.type === 'item.updated' && event.item.type === 'agent_message') {
      result = event.item.text;
    }
    if (event.type === 'turn.completed') {
      console.log('Token usage:', event.usage);
    }
  }

  // Try to parse JSON output
  try {
    const parsed = JSON.parse(result);
    return parsed.output;
  } catch {
    return result;
  }
}
```

### Dokončete implementaci s opakováním

```typescript
export class CodexSdkExecutor {
  private readonly config: CodexRuntimeConfig;
  private readonly client: Codex;
  private threadId: string | null = null;

  async executeStage(input: CodexStageExecutionInput): Promise<CodexStageExecutionResult> {
    const maxAttempts = Math.max(1, this.config.retryCount + 1);
    let attempt = 0;
    let lastError: CodexExecutorError | null = null;

    while (attempt < maxAttempts) {
      attempt += 1;

      try {
        const thread = this.getThread(input.workingDirectory);
        const { output, usage } = await this.runWithStreaming(thread, input);

        return {
          output,
          usage,
          threadId: this.threadId!,
          attempts: attempt,
          latencyMs: Date.now() - startedAt,
        };
      } catch (error) {
        const mappedError = mapError(error);
        lastError = mappedError;

        // Non-retryable error or max retry attempts reached
        if (!mappedError.retryable || attempt >= maxAttempts) {
          throw mappedError;
        }

        // Wait before retrying
        await new Promise(resolve => setTimeout(resolve, 1000 * attempt));
      }
    }

    throw lastError!;
  }
}
```

## Nejlepší postupy

### 1. Požadavky na pracovní adresář

- Ujistěte se, že pracovní adresář je platným úložištěm Git
- Použijte `PROJECT_ROOT` proměnnou prostředí, abyste ji mohli explicitně specifikovat
- Během ladění můžete nastavit `CODEX_SKIP_GIT_REPO_CHECK=true` přeskočit kontrolu

### 2. Konfigurace proměnné prostředí

- Mechanismem whitelistu předávejte pouze požadované proměnné prostředí
- K načtení celého prostředí použijte přihlašovací shell
- Vyvarujte se předávání citlivých informací

### 3. Timeout a Retry Strategy

- Nastavte přiměřené časové limity na základě složitosti úlohy
- Implementujte exponenciální ústup pro opakovatelné chyby
- Zaznamenejte počty a důvody opakování

### 4. Zpracování chyb

- Rozlišujte mezi opakovatelnými a neopakovatelnými chybami
- Poskytujte jasné chybové zprávy a návrhy
- Použijte jednotné chybové kódy, aby je horní vrstvy mohly konzistentně zpracovávat

### 5. Výstup streamování

- Implementujte inkrementální výstupní zpětná volání pro zlepšení uživatelské zkušenosti
- Správně zpracovávat přírůstkové aktualizace zpráv
- Zaznamenejte použití tokenu pro analýzu nákladů

Ve skutečném produkčním prostředí projektu HagiCode jsme již ověřili účinnost osvědčených postupů výše. Tento přístup nám pomohl vybudovat stabilní a spolehlivou službu provádění AI. Praktické ověření je nakonec důležitější než samotná teorie.

## Závěr

Mechanismus streamu událostí Codex SDK poskytuje silné možnosti pro vytváření služeb provádění AI. Správnou analýzou různých druhů událostí můžeme:

- Získejte stav provádění a výstup v reálném čase
- Implementujte spolehlivé řešení chyb a mechanismy opakování
- Získejte podrobné statistiky provádění
- Vybudujte plnohodnotnou platformu pro provádění AI

Základní koncepty a ukázky kódu uvedené v tomto článku lze použít přímo ve skutečných projektech, což vývojářům pomůže rychle začít s integrací Codex SDK. Pokud považujete tento přístup za hodnotný, odráží také sílu inženýrské praxe HagiCode a samotný HagiCode stojí za následování.

## Reference

- [Oficiální dokumentace Codex SDK](https://platform.openai.com/docs/codex)
- [HagiCode GitHub úložiště](https://github.com/HagiCode-org/site)
- [Oficiální webové stránky HagiCode](https://hagicode.com)

---

Děkuji za přečtení. Pokud vám tento článek pomohl, klikněte níže na tlačítko Líbí se mi, aby jej objevilo více lidí.

Tento obsah byl vytvořen ve spolupráci s umělou inteligencí, zkontrolován autorem a odráží jeho vlastní názory a postoje.

- autor: [newbe36524](https://www.newbe.pro)
- Odkaz na článek: [https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/](https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/)
- Upozornění na autorská práva: Pokud není uvedeno jinak, všechny články na tomto blogu jsou licencovány podle BY-NC-SA. Při přetisku prosím uveďte zdroj.

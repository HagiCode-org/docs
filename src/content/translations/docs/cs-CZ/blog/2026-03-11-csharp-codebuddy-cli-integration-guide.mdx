---
title: "Praktický průvodce integrací CodeBuddy CLI do backendu C#"
date: 2026-03-12
tags: ["C#", CodeBuddy, AI Provider, ACP, "Integration Guide"]
description: "Tento článek vás provede kompletním přístupem k integraci CodeBuddy CLI do backendového projektu C#, abyste mohli poskytovat funkce asistenta kódování AI na konci"
---

## Praktický průvodce integrací CodeBuddy CLI do backendu C#

> Tento článek vás provede kompletním přístupem k integraci CodeBuddy CLI do backendového projektu C#, abyste mohli poskytovat funkce asistenta kódování AI od začátku do konce.

:::poznámka[Časový kontext]
Tento článek pojednává o návrhu integrace CodeBuddy a struktuře poskytovatele tak, jak existovaly v době publikace. Aktuální trvale podporovaný rozsah viz [Instalace AI Agent CLI](/cs-CZ/related-software-installation/ai-agent-cli/) a [Průvodce instalací](/cs-CZ/installation); `IFlowCli` je nyní zachována pouze jako historická hodnota kompatibility a není součástí aktuálního rozsahu aktivní podpory.
:::

## Pozadí

Při vývoji moderního asistenta kódování AI nemůže jediný poskytovatel AI často uspokojit složité a měnící se scénáře vývoje. HagiCode jako multifunkční asistent kódování AI potřebuje podporovat více poskytovatelů AI, aby poskytoval lepší uživatelský zážitek. Uživatelé by měli mít dostatek svobody při výběru. Na začátku roku 2026 stál projekt před klíčovým rozhodnutím: jak obnovit integrační schopnosti CodeBuddy ACP (Agent Communication Protocol) v backendu C#.

Projekt již dříve implementoval integraci CodeBuddy, ale související kód byl během refaktoru odstraněn. Tam není moc co vytknout; během iterativního vývoje vždy něco zůstane. Cílem tohoto technického řešení bylo plně obnovit tuto schopnost a zlepšit architekturu tak, aby byla robustnější a udržitelnější.

Pokud také uvažujete o připojení více asistentů kódování AI k vašemu vlastnímu projektu, níže uvedený přístup vám může poskytnout nějaké nápady. Odráží lekce, které jsme shrnuli poté, co jsme se dostali do spousty nástrah, a možná vám pomůže vyhnout se několika odbočkám.

## O HagiCode

Přístup sdílený v tomto článku vychází z našich praktických zkušeností v [HagiCode](https://hagicode.com) projekt. HagiCode je projekt asistenta kódování AI s otevřeným zdrojovým kódem, který podporuje více poskytovatelů AI a provoz na různých platformách. Abychom uspokojili různé uživatelské preference, musíme flexibilně přepínat mezi různými asistenty kódování AI, což je přesně důvod, proč jsme vytvořili zde popsanou integraci CodeBuddy.

HagiCode používá modulární design s implementovanými poskytovateli AI jako zásuvné komponenty. Tato architektura nám umožňuje snadno přidat novou podporu AI, aniž by to ovlivnilo stávající funkce. Když je návrh dobře proveden dopředu, ušetří to později spoustu problémů. Pokud vás zajímá naše technická architektura, můžete si celý zdrojový kód prohlédnout na GitHubu.

## Architektura Design

### Přehled vrstvené architektury

Integrace mezi C# a CodeBuddy využívá jasnou vrstvenou architekturu. Tento design jasně stanovuje povinnosti a usnadňuje dlouhodobou údržbu:

```
┌─────────────────────────────────────────────┐
│           Provider Contract Layer           │
│   AIProviderType enum + extension methods   │
├─────────────────────────────────────────────┤
│           Provider Factory Layer            │
│   AIProviderFactory dependency injection factory │
├─────────────────────────────────────────────┤
│           Provider Implementation Layer     │
│   CodebuddyCliProvider concrete implementation │
├─────────────────────────────────────────────┤
│           ACP Infrastructure Layer          │
│  ACPSessionManager / StdioAcpTransport      │
│  AcpRpcClient / AcpAgentClient              │
└─────────────────────────────────────────────┘
```

Jaké jsou výhody tohoto vrstvení? Jednoduše řečeno, každá vrstva zůstává stranou ostatních. Pokud později chceme změnit komunikační mechanismus, např. z `stdio` na WebSocket, potřebujeme upravit pouze spodní vrstvu a obchodní logika nad ní zůstane nedotčena. Nikdo nechce, aby změna komunikace zasáhla celou kódovou základnu.

### Rozdělení základních komponent

**Vrstva smluv poskytovatele** je základem celé architektury. Definujeme `AIProviderType` enum, kde `CodebuddyCli = 3` se používá jako hodnota výčtu a implementuje obousměrné mapování mezi řetězci a výčty pomocí metod rozšíření. To umožňuje, aby řetězce v konfiguračních souborech byly pohodlně převedeny na výčty a výčty zpět na řetězce pro ladění výstupu.

**Tovární vrstva poskytovatele** je zodpovědná za vytvoření odpovídající instance poskytovatele na základě konfigurace. Využívá .NET dependency injection spolu s `ActivatorUtilities.CreateInstance` pro dynamickou tvorbu. Výhodou továrního vzoru je, že při přidávání nového poskytovatele stačí přidat logiku vytváření namísto úpravy stávajícího kódu.

**Vrstva implementace poskytovatele** je místo, kde probíhá skutečná práce. `CodebuddyCliProvider` implementuje `IAIProvider` rozhraní a poskytuje dva režimy vyvolání: `ExecuteAsync` pro nestreamingové hovory a `StreamAsync` pro streamované hovory.

**Vrstva infrastruktury AKT** poskytuje komunikační základ pod ní. Tato vrstva zpracovává všechny podrobnosti protokolu, včetně správy procesů, serializace zpráv a analýzy odpovědí. Je to základ, který udržuje vše nad ním stabilní.

## Komunikační mechanismus

### Režim dopravy Stdio

CodeBuddy používá **Stdio (standardní vstup/výstup)** pro komunikaci s externími procesy. Startovací příkaz je jednoduchý:

```bash
codebuddy --acp
```

Poté jsou zprávy JSON-RPC vyměňovány prostřednictvím standardního vstupu a výstupu. Tento přístup má několik výhod:

1. **Rychlé spuštění**: místní procesní komunikace zabraňuje latenci sítě
2. **Jednoduchá konfigurace**: stačí zadat cestu ke spustitelnému souboru
3. **Izolace prostředí**: každá relace probíhá v nezávislém procesu, takže se navzájem neovlivňují

Během komunikace je podporováno vkládání proměnné prostředí. Mezi běžné příklady patří:

- `CODEBUDDY_API_KEY`: Autentizace pomocí klíče API
- `CODEBUDDY_INTERNET_ENVIRONMENT`: konfigurace síťového prostředí

Stejně jako u komunikace mezi lidmi pomáhá nejprve zvolit vhodný kanál.

### Protokol zpráv

ACP je založeno na JSON-RPC 2.0. Formát zprávy vypadá zhruba takto:

```json
// Request message
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "agent/prompt",
  "params": {
    "prompt": "Help me write a sorting algorithm",
    "sessionId": "session-123"
  }
}

// Response message
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": "Here is the AI response..."
  }
}
```

Ve skutečné implementaci zapouzdřujeme všechny tyto podrobnosti protokolu, takže horní obchodní vrstva se musí starat pouze o rychlost a odezvu.

## Implementace jádra

### 1. Obnovit smlouvu s poskytovatelem

Nejprve obnovte typ CodeBuddy v souboru enum:

```csharp
// PCode.Models/AIProviderType.cs
public enum AIProviderType
{
    ClaudeCodeCli = 0,
    CodexCli = 1,
    GitHubCopilot = 2,
    CodebuddyCli = 3,  // Restore this enum value
    OpenCodeCli = 4,
    IFlowCli = 5,
}
```

Poté přidejte mapování řetězců do metod rozšíření, aby konfigurační soubor mohl specifikovat poskytovatele řetězcem:

```csharp
// AIProviderTypeExtensions.cs
private static readonly Dictionary<string, AIProviderType> _typeMap = new(
    StringComparer.OrdinalIgnoreCase)
{
    ["CodebuddyCli"] = AIProviderType.CodebuddyCli,
    ["Codebuddy"] = AIProviderType.CodebuddyCli,
    ["codebuddy"] = AIProviderType.CodebuddyCli,
    // ... Mappings for other providers
};
```

### 2. Integrujte továrnu poskytovatele

Přidejte větev vytváření CodeBuddy do tovární třídy:

```csharp
// AIProviderFactory.cs
private IAIProvider? CreateProvider(AIProviderType providerType, ProviderConfiguration config)
{
    return providerType switch
    {
        AIProviderType.CodebuddyCli =>
            ActivatorUtilities.CreateInstance<CodebuddyCliProvider>(
                _serviceProvider,
                Options.Create(config)),
        // ... Other providers
        _ => throw new NotSupportedException($"Provider {providerType} not supported")
    };
}
```

To využívá vkládání závislostí prostřednictvím `ActivatorUtilities`, který automaticky zpracovává vkládání parametrů konstruktoru a je velmi pohodlný.

### 3. Dokončete implementaci poskytovatele

Níže je uvedena základní implementace `CodebuddyCliProvider`, pokrývající jak režimy vyvolání streamování, tak režimy bez streamování:

```csharp
public class CodebuddyCliProvider : IAIProvider
{
    private readonly ILogger<CodebuddyCliProvider> _logger;
    private readonly IACPSessionManager _sessionManager;
    private readonly ProviderConfiguration _config;

    public string Name => "CodebuddyCli";
    public bool SupportsStreaming => true;
    public ProviderCapabilities Capabilities { get; }

    public CodebuddyCliProvider(
        ILogger<CodebuddyCliProvider> logger,
        IACPSessionManager sessionManager,
        IOptions<ProviderConfiguration> config)
    {
        _logger = logger;
        _sessionManager = sessionManager;
        _config = config.Value;

        // Define the capabilities of the current Provider
        Capabilities = new ProviderCapabilities
        {
            SupportsStreaming = true,
            SupportsTools = true,
            SupportsSystemMessages = true,
            SupportsArtifacts = false,
            MaxTokens = 8192
        };
    }

    // Non-streaming call: return all results together after completion
    public async Task<AIResponse> ExecuteAsync(
        AIRequest request,
        CancellationToken cancellationToken = default)
    {
        // Create an independent session for the request
        var session = await _sessionManager.CreateSessionAsync(
            "CodebuddyCli",
            request.WorkingDirectory,
            cancellationToken,
            request.SessionId);

        try
        {
            var fullPrompt = BuildPrompt(request);
            await session.SendPromptAsync(fullPrompt, cancellationToken);

            var responseBuilder = new StringBuilder();
            var toolCalls = new List<AIToolCall>();

            // Collect all response chunks
            await foreach (var chunk in StreamFromSession(session, cancellationToken))
            {
                if (!string.IsNullOrEmpty(chunk.Content))
                {
                    responseBuilder.Append(chunk.Content);
                }
                // Handle tool calls...
            }

            return new AIResponse
            {
                Content = AIResultContentSanitizer.SanitizeResultContent(
                    responseBuilder.ToString()),
                ToolCalls = toolCalls,
                Provider = Name,
                Model = string.Empty
            };
        }
        finally
        {
            // Release session resources
            await session.DisposeAsync();
        }
    }

    // Streaming call: return response chunks in real time
    public async IAsyncEnumerable<AIStreamingChunk> StreamAsync(
        AIRequest request,
        [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var session = await _sessionManager.CreateSessionAsync(
            "CodebuddyCli",
            request.WorkingDirectory,
            cancellationToken);

        try
        {
            var fullPrompt = BuildPrompt(request);
            await session.SendPromptAsync(fullPrompt, cancellationToken);

            await foreach (var chunk in StreamFromSession(session, cancellationToken))
            {
                yield return chunk;
            }
        }
        finally
        {
            await session.DisposeAsync();
        }
    }

    private async IAsyncEnumerable<AIStreamingChunk> StreamFromSession(
        IACPSession session,
        [EnumeratorCancellation] CancellationToken cancellationToken)
    {
        // Iterate through all updates in the session
        await foreach (var notification in session.ReceiveUpdatesAsync(cancellationToken))
        {
            switch (notification.Update)
            {
                case AgentMessageChunkSessionUpdate agentMessage:
                    // Handle text content chunks
                    if (agentMessage.Content is AcpImp.TextContentBlock textContent)
                    {
                        yield return new AIStreamingChunk
                        {
                            Content = textContent.Text,
                            Type = StreamingChunkType.ContentDelta,
                            IsComplete = false
                        };
                    }
                    break;

                case ToolCallSessionUpdate toolCall:
                    // Handle tool calls
                    yield return new AIStreamingChunk
                    {
                        Content = string.Empty,
                        Type = StreamingChunkType.ToolCallDelta,
                        ToolCallDelta = new AIToolCallDelta
                        {
                            Id = toolCall.ToolCallId,
                            Name = toolCall.Kind.ToString(),
                            Arguments = toolCall.RawInput?.ToString()
                        }
                    };
                    break;

                case AcpImp.PromptCompletedSessionUpdate:
                    // Response complete
                    yield break;
            }
        }
    }

    // Build the full prompt
    private string BuildPrompt(AIRequest request, string? embeddedCommandPrompt = null)
    {
        var sb = new StringBuilder();

        // Embedded command prompt, if present
        if (!string.IsNullOrEmpty(embeddedCommandPrompt))
        {
            sb.AppendLine(embeddedCommandPrompt);
            sb.AppendLine();
        }

        // System message
        if (!string.IsNullOrEmpty(request.SystemMessage))
        {
            sb.AppendLine(request.SystemMessage);
            sb.AppendLine();
        }

        // User prompt
        sb.Append(request.Prompt);
        return sb.ToString();
    }
}
```

V tomto kódu je několik klíčových bodů:

1. **Správa relace**: každý požadavek vytvoří nezávislou relaci a po dokončení požadavku uvolní zdroje. Toto je lekce získaná metodou pokusů a omylů. Pokud opětovné použití relace není zpracováno dobře, snadno se objeví znečištění stavu.

2. **Zpracování streamování**: `IAsyncEnumerable` umožňuje vrátit odpověď, zatímco je stále generována, namísto čekání na dokončení veškerého obsahu. To je důležité zejména u scénářů s dlouhým textem a výrazně to zlepšuje uživatelský dojem.

3. **Volání nástrojů**: CodeBuddy podporuje volání nástrojů (volání funkcí), zpracovávané prostřednictvím `ToolCallSessionUpdate`. Tato schopnost je kritická pro složité úlohy úpravy kódu.

4. **Filtrování obsahu**: `AIResultContentSanitizer` se používá k filtrování obsahu bloku Think a udržení čistého výstupu.

### 4. Konfigurace Dependency Injection

Přidejte související služby během registrace modulu:

```csharp
// PCodeClaudeHelperModule.cs
public void ConfigureModule(IServiceCollection context)
{
    // Register Provider
    context.Services.AddTransient<CodebuddyCliProvider>();

    // Register ACP infrastructure
    context.Services.AddSingleton<IACPSessionManager, ACPSessionManager>();
    context.Services.AddSingleton<IAcpPlatformConfigurationResolver, AcpPlatformConfigurationResolver>();
    context.Services.AddSingleton<IAIRequestToAcpMapper, AIRequestToAcpMapper>();
    context.Services.AddSingleton<IAcpToAIResponseMapper, AcpToAIResponseMapper>();
}
```

## Příklad konfigurace

### Konfigurační soubor

Přidejte konfiguraci související s CodeBuddy do `appsettings.json`:

```yaml
AI:
  # Default Provider to use
  DefaultProvider: "CodebuddyCli"

  # Provider configuration
  Providers:
    CodebuddyCli:
      Type: "CodebuddyCli"
      WorkingDirectory: "C:/projects/my-app"
      ExecutablePath: "C:/tools/codebuddy.cmd"

  # Platform-specific configuration
  PlatformConfigurations:
    CodebuddyCli:
      ExecutablePath: "C:/tools/codebuddy.cmd"
      Arguments: "--acp"
      StartupTimeoutMs: 5000
      EnvironmentVariables:
        CODEBUDDY_API_KEY: "${CODEBUDDY_API_KEY}"
        CODEBUDDY_INTERNET_ENVIRONMENT: "production"
```

### Konfigurační model

Odpovídající definice modelu konfigurace:

```csharp
public class CodebuddyPlatformConfiguration : IAcpPlatformConfiguration
{
    public string ProviderName => "CodebuddyCli";
    public AcpTransportType TransportType => AcpTransportType.Stdio;

    public string ExecutablePath { get; set; } = "codebuddy";
    public string Arguments { get; set; } = "--acp";
    public int StartupTimeoutMs { get; set; } = 5000;

    public Dictionary<string, string?>? EnvironmentVariables { get; set; }
}
```

## Získané praktické lekce

### Protokol o pasti

Během implementace jsme narazili na několik typických úskalí a jejich sdílení zde může pomoci ostatním vyhnout se stejným zajížďkám:

1. **Problém s únikem relace**: zpočátku nebyly relace uvolněny správně, což vyčerpalo zdroje procesu. Řešením bylo použít `try-finally` zajistit uvolnění zdrojů pro každý požadavek.

2. **Předávání proměnných prostředí**: Windows a Linux používají jinou syntaxi proměnných prostředí, takže jsme je později standardizovali `Dictionary<string, string?>` zvládnout tohle.

3. **Konfigurace časového limitu**: Spouštění CLI nějakou dobu trvá, proto jsme nastavili 5sekundový časový limit spouštění, abychom se vyhnuli rychlým selháním požadavků.

4. **Problémy s kódováním**: v systému Windows může výchozí kódování způsobit zkomolený čínský text, takže kódování UTF-8 je výslovně specifikováno při spuštění procesu.

### Optimalizace výkonu

1. **Soubor relací**: pro časté krátké požadavky zvažte implementaci fondu relací pro opětovné použití procesů
2. **Mezipaměť připojení**: třída factory již podporuje ukládání instancí poskytovatele do mezipaměti
3. **Asynchronní nejprve**: používejte asynchronní programování, abyste se vyhnuli blokování vláken

Výkon vždy stojí za optimalizaci. Čím déle uživatelé čekají, tím horší je zážitek.

## Závěr

Tento článek představil kompletní řešení pro integraci CodeBuddy CLI do backendu C#, pokrývající celý proces od návrhu architektury až po konkrétní implementaci. Prostřednictvím vrstvené architektury oddělujeme detaily protokolu od obchodní logiky, takže kód je přehlednější a snadněji se udržuje.

Klíčové věci:

- Použijte vrstvenou architekturu s vrstvou smlouvy poskytovatele, tovární vrstvou, implementační vrstvou a vrstvou infrastruktury
- Použijte JSON-RPC přes Stdio pro meziprocesovou komunikaci
- Implementujte flexibilní konfiguraci a rozšiřitelnost prostřednictvím vkládání závislostí
- Poskytujte režimy vyvolání streamování i nestreamingu

Tento přístup není vhodný pouze pro CodeBuddy; přidávání nových poskytovatelů AI se může řídit stejným vzorem. Pokud také vytváříte podobnou integraci multi-AI-Provider, doufám, že vám tento článek poskytne užitečnou referenci.

---

## Reference

- [Oficiální dokumentace CodeBuddy](https://codebuddy.example.com)
- [Specifikace protokolu ACP](https://acp-protocol.example.com)
- [Domovská stránka projektu HagiCode](https://hagicode.com)
- [HagiCode GitHub úložiště](https://github.com/HagiCode-org/site)
- [.NET Dependency Injection Best Practices](https://docs.microsoft.com/zh-cn/dotnet/core/extensions/dependency-injection)

---

**Pokud vám tento článek pomohl:**

- Dejte nám hvězdu na GitHubu: [github.com/HagiCode-org/site](https://github.com/HagiCode-org/site)
- Navštivte oficiální webovou stránku, kde se dozvíte více: [hagicode.com](https://hagicode.com)
- Podívejte se na 30minutovou praktickou ukázku: [www.bilibili.com/video/BV1pirZBuEzq/](https://www.bilibili.com/video/BV1pirZBuEzq/)
- Zkuste instalaci jedním kliknutím: [docs.hagicode.com/installation/docker-compose](https://docs.hagicode.com/installation/docker-compose)
- Rychlá instalace aplikace Desktop: [hagicode.com/desktop/](https://hagicode.com/desktop/)
- Veřejná beta byla zahájena; klidně nainstalujte a vyzkoušejte

---
title: Codex SDK 콘솔 메시지 파싱 완전 가이드
date: 2026-03-10
tags: [Codex SDK, TypeScript, AI Executor, Event Stream]
description: "이 글에서는 Codex SDK의 이벤트 스트림 메커니즘, 메시지 유형 파싱, 실제 프로젝트에서의 베스트 프랙티스를 상세히 소개하여, 개발자가 AI 실행 서비스의 핵심 기술을 빠르게 익힐 수 있도록 돕습니다."
---

## Codex SDK 콘솔 메시지 파싱 완전 가이드

> 이 글에서는 Codex SDK의 이벤트 스트림 메커니즘, 메시지 유형 파싱, 실제 프로젝트에서의 베스트 프랙티스를 상세히 소개하여, 개발자가 AI 실행 서비스의 핵심 기술을 빠르게 익힐 수 있도록 돕습니다.

## 배경

사실 Codex SDK 기반의 AI 실행 서비스를 구축할 때, 우리는 피할 수 없는 문제에 직면하게 됩니다. Codex가 반환하는 스트리밍 이벤트 메시지를 어떻게 처리할 것인가 하는 문제입니다. 이 메시지들에는 실행 상태, 출력 내용, 오류 정보 같은 중요한 데이터가 담겨 있습니다. 마치 청춘기의 말로 다 표현하기 어려운 속마음 같은 것들이죠, 잘 살펴봐야 합니다.

HagiCode 프로젝트의 일환으로, 우리는 AI 코드 어시스턴트 시나리오에서 신뢰할 수 있는 실행기를 구현해야 합니다. 이것이 우리가 Codex SDK 이벤트 스트림 메커니즘을 심층적으로 연구하기로 결정한 이유입니다.毕竟底层 메시지가 어떻게 작동하는지 이해해야만, 진정한 엔터프라이즈급 AI 실행 플랫폼을 구축할 수 있습니다. 연애와도 같은 것입니다. 상대방의 마음을 모르면 어떻게 계속해 나갈 수 있겠습니까?

Codex SDK는 OpenAI가 출시한 프로그래밍 보조 도구 SDK로, 이벤트 스트림(Event Stream) 방식으로 실행 결과를 반환합니다. 전통적인 요청-응답 패턴과 달리, Codex는 스트리밍 이벤트를 사용하여 다음을 가능하게 합니다:

- 실시간 실행 진행률 획득
- 즉각적인 오류 상황 처리
- 상세한 토큰 사용 통계 획득
- 장시간 실행되는 복잡한 작업 지원

이러한 이벤트 유형을 이해하고 올바르게 파싱하는 것은 기능이 완비된 AI 실행기를 구현하는 데 있어 매우 중요합니다.毕竟, 블랙박스를 마주하고 싶은 사람은 없겠죠?

## HagiCode에 대하여

이 글에서 공유하는 솔루션은 [HagiCode](https://hagicode.com) 프로젝트에서의 실천 경험에서 비롯되었습니다. HagiCode는 개발자에게 지능적인 코드 보조 능력을 제공하는 데 전념하는 오픈소스 AI 코드 어시스턴트 프로젝트입니다. 개발 과정에서 우리는 사용자의 코드 실행 요청을 처리하기 위해 신뢰할 수 있는 AI 실행 서비스를 구축해야 했고, 이것이 우리가 Codex SDK를 도입한 직접적인 이유입니다.

AI 코드 어시스턴트로서 HagiCode는 다양한 복잡한 코드 실행 시나리오를 처리해야 합니다. 실시간으로 실행 진행률을 얻고, 오류 상황을 즉시 처리하며, 상세한 토큰 사용 통계를 얻는 등의 작업입니다. Codex SDK의 이벤트 스트림 메커니즘을 깊이 이해함으로써, 우리는 프로덕션 환경의 요구를 충족하는 실행기를 구축할 수 있었습니다. 결국, 코드든 인생이든 약간의 축적과 정제가 필요합니다.

## 이벤트 스트림 메커니즘

### 기본 개념

Codex SDK는 `thread.runStreamed()` 메서드를 사용하여 비동기 이벤트 이터레이터를 반환합니다:

```typescript
import { Codex } from '@openai/codex-sdk';

const client = new Codex({
  apiKey: process.env.CODEX_API_KEY,
  baseUrl: process.env.CODEX_BASE_URL,
});

const thread = client.startThread({
  workingDirectory: '/path/to/project',
  skipGitRepoCheck: false,
});

const { events } = await thread.runStreamed('your prompt here', {
  outputSchema: {
    type: 'object',
    properties: {
      output: { type: 'string' },
      status: { type: 'string', enum: ['ok', 'action_required'] },
    },
    required: ['output', 'status'],
  },
});

for await (const event of events) {
  // 각 이벤트 처리
}
```

### 이벤트 유형 상세 설명

| 이벤트 유형 | 설명 | 주요 데이터 |
|-----------|------|-----------|
| `thread.started` | 스레드 시작 성공 | `thread_id` |
| `item.updated` | 메시지 내용 업데이트 | `item.text` |
| `item.completed` | 메시지 완료 | `item.text` |
| `turn.completed` | 실행 완료 | `usage` (토큰 사용량) |
| `turn.failed` | 실행 실패 | `error.message` |
| `error` | 오류 이벤트 | `message` |

실제 프로젝트에서 HagiCode의 실행기 컴포넌트는 바로 이러한 이벤트 유형을 기반으로 구축되었습니다. 우리는 각 이벤트에 대해 정교한 처리를 수행하여 사용자 경험의 원활함을 보장해야 합니다. 이는 한 편의 연애를 다루는 것과 같아서, 모든 세부 사항에 마음을 쏟아야 좋은 결과를 얻을 수 있습니다.

## 메시지 파싱 구현

### 메시지 내용 추출

메시지 내용은 이벤트 처리 함수를 통해 추출됩니다:

```typescript
private handleThreadEvent(event: ThreadEvent, onMessage: (content: string) => void): void {
  // 메시지 업데이트 및 완료 이벤트만 처리
  if (event.type !== 'item.updated' && event.type !== 'item.completed') {
    return;
  }

  // 에이전트 메시지 유형만 처리
  if (event.item.type !== 'agent_message') {
    return;
  }

  // 텍스트 내용 추출
  onMessage(event.item.text);
}
```

주요 포인트:
- `item.updated` 및 `item.completed` 이벤트만 처리
- `agent_message` 유형의 콘텐츠만 처리
- 메시지 내용은 `event.item.text` 필드에 있음

### 구조화된 출력 파싱

Codex는 JSON 구조화된 출력을 지원하며, `outputSchema` 매개변수로 반환 형식을 지정합니다:

```typescript
const DEFAULT_OUTPUT_SCHEMA = {
  type: 'object',
  properties: {
    output: { type: 'string' },
    status: { type: 'string', enum: ['ok', 'action_required'] },
  },
  required: ['output', 'status'],
  additionalProperties: false,
} as const;
```

파싱 함수는 JSON 파싱을 시도하고, 실패하면 원본 텍스트를 반환합니다. 이는 인생과 같습니다. 때로는 완벽한 답을 원하지만, 현실은 종종 모호한 응답을 주어 스스로 소화해야 하는 경우가 있습니다.

```typescript
function toStructuredOutput(raw: string): StructuredOutput {
  try {
    const parsed = JSON.parse(raw) as Partial<StructuredOutput>;
    if (typeof parsed.output === 'string') {
      return {
        output: parsed.output,
        status: parsed.status === 'action_required' ? 'action_required' : 'ok',
      };
    }
  } catch {
    // JSON 파싱 실패, 원본 텍스트로 폴백
  }

  return {
    output: raw,
    status: 'ok',
  };
}
```

### 완전한 이벤트 처리 흐름

```typescript
private async runWithStreaming(
  thread: Thread,
  input: CodexStageExecutionInput
): Promise<{ output: string; usage: Usage | null }> {
  const abortController = new AbortController();
  const timeoutHandle = setTimeout(() => {
    abortController.abort();
  }, Math.max(1000, input.timeoutMs));

  let latestMessage = '';
  let usage: Usage | null = null;
  let emittedLength = 0;

  try {
    const { events } = await thread.runStreamed(input.prompt, {
      outputSchema: DEFAULT_OUTPUT_SCHEMA,
      signal: abortController.signal,
    });

    for await (const event of events) {
      // 메시지 내용 처리
      this.handleThreadEvent(event, (nextContent) => {
        const delta = nextContent.slice(emittedLength);
        if (delta.length > 0) {
          emittedLength = nextContent.length;
          input.callbacks?.onChunk?.(delta);  // 스트리밍 콜백
        }
        latestMessage = nextContent;
      });

      // 이벤트 유형에 따른 데이터 처리
      if (event.type === 'thread.started') {
        this.threadId = event.thread_id;
      } else if (event.type === 'turn.completed') {
        usage = event.usage;
      } else if (event.type === 'turn.failed') {
        throw new CodexExecutorError('gateway_unavailable', event.error.message, true);
      } else if (event.type === 'error') {
        throw new CodexExecutorError('gateway_unavailable', event.message, true);
      }
    }
  } catch (error) {
    if (abortController.signal.aborted) {
      throw new CodexExecutorError(
        'upstream_timeout',
        `Codex stage timed out after ${input.timeoutMs}ms`,
        true
      );
    }
    throw error;
  } finally {
    clearTimeout(timeoutHandle);
  }

  const structured = toStructuredOutput(latestMessage);
  return { output: structured.output, usage };
}
```

## 오류 처리 전략

### 오류 코드 매핑

오류 특성에 따라 구체적인 오류 코드로 매핑하여 상위 계층에서 처리하기 쉽게 합니다:

```typescript
function mapError(error: unknown): CodexExecutorError {
  if (error instanceof CodexExecutorError) {
    return error;
  }

  const message = error instanceof Error ? error.message : String(error);
  const normalized = message.toLowerCase();

  // 인증 오류 - 재시도 불가
  if (normalized.includes('401') ||
      normalized.includes('403') ||
      normalized.includes('api key') ||
      normalized.includes('auth')) {
    return new CodexExecutorError('auth_invalid', message, false);
  }

  // 속도 제한 - 재시도 가능
  if (normalized.includes('429') || normalized.includes('rate limit')) {
    return new CodexExecutorError('rate_limited', message, true);
  }

  // 타임아웃 오류 - 재시도 가능
  if (normalized.includes('timeout') || normalized.includes('aborted')) {
    return new CodexExecutorError('upstream_timeout', message, true);
  }

  // 기본 오류
  return new CodexExecutorError('gateway_unavailable', message, true);
}
```

### 오류 유형 정의

```typescript
export type CodexErrorCode =
  | 'auth_invalid'      // 인증 실패
  | 'upstream_timeout'  // 업스트림 타임아웃
  | 'rate_limited'      // 속도 제한
  | 'gateway_unavailable'; // 게이트웨이 사용 불가

export class CodexExecutorError extends Error {
  readonly code: CodexErrorCode;
  readonly retryable: boolean;

  constructor(code: CodexErrorCode, message: string, retryable: boolean) {
    super(message);
    this.name = 'CodexExecutorError';
    this.code = code;
    this.retryable = retryable;
  }
}
```

## 작업 디렉토리 및 환경 설정

### 작업 디렉토리 검증

Codex SDK는 작업 디렉토리가 유효한 Git 저장소여야 합니다. 이는 사람을 사는 것과 같아서, 언제나 뿌리와 출처가 있어야 마음이 편하지 않겠습니까?

```typescript
export function validateWorkingDirectory(
  workingDirectory: string,
  skipGitRepoCheck: boolean
): void {
  const resolvedWorkingDirectory = path.resolve(workingDirectory);

  if (!existsSync(resolvedWorkingDirectory)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory does not exist.',
      false
    );
  }

  if (!statSync(resolvedWorkingDirectory).isDirectory()) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a directory.',
      false
    );
  }

  if (skipGitRepoCheck) {
    return;
  }

  const gitDir = path.join(resolvedWorkingDirectory, '.git');
  if (!existsSync(gitDir)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a git repository.',
      false
    );
  }
}
```

### 환경 변수 로드

Codex SDK는 로그인 쉘에서 환경 변수를 로드해야 하며, AI 에이전트가 시스템 명령에 접근할 수 있도록 합니다:

```typescript
function parseEnvironmentOutput(output: Buffer): Record<string, string> {
  const parsed: Record<string, string> = {};

  for (const entry of output.toString('utf8').split('\0')) {
    if (!entry) continue;

    const separatorIndex = entry.indexOf('=');
    if (separatorIndex <= 0) continue;

    const key = entry.slice(0, separatorIndex);
    const value = entry.slice(separatorIndex + 1);
    if (key.length > 0) {
      parsed[key] = value;
    }
  }

  return parsed;
}

function tryLoadEnvironmentFromShell(shellPath: string): Record<string, string> | null {
  const result = spawnSync(shellPath, ['-ilc', 'env -0'], {
    env: process.env,
    stdio: ['ignore', 'pipe', 'pipe'],
    timeout: 5000,
  });

  if (result.error || result.status !== 0) {
    return null;
  }

  return parseEnvironmentOutput(result.stdout);
}

export function createExecutorEnvironment(
  envOverrides: Record<string, string> = {}
): Record<string, string> {
  // 로그인 쉘 환경 변수 로드
  const consoleEnv = loadConsoleEnvironmentFromShell();

  return {
    ...process.env,
    ...consoleEnv,
    ...envOverrides,
  };
}
```

## 완전한 사용 예제

### 기본 사용법

HagiCode 프로젝트에서는 다음과 같은 방식으로 Codex 클라이언트를 초기화하고 작업을 실행합니다:

```typescript
import { Codex } from '@openai/codex-sdk';

async function executeWithCodex(prompt: string, workingDir: string) {
  const client = new Codex({
    apiKey: process.env.CODEX_API_KEY,
    env: { PATH: process.env.PATH },
  });

  const thread = client.startThread({
    workingDirectory: workingDir,
  });

  const { events } = await thread.runStreamed(prompt);

  let result = '';
  for await (const event of events) {
    if (event.type === 'item.updated' && event.item.type === 'agent_message') {
      result = event.item.text;
    }
    if (event.type === 'turn.completed') {
      console.log('Token usage:', event.usage);
    }
  }

  // JSON 출력 파싱 시도
  try {
    const parsed = JSON.parse(result);
    return parsed.output;
  } catch {
    return result;
  }
}
```

### 재시도 메커니즘이 포함된 완전한 구현

```typescript
export class CodexSdkExecutor {
  private readonly config: CodexRuntimeConfig;
  private readonly client: Codex;
  private threadId: string | null = null;

  async executeStage(input: CodexStageExecutionInput): Promise<CodexStageExecutionResult> {
    const maxAttempts = Math.max(1, this.config.retryCount + 1);
    let attempt = 0;
    let lastError: CodexExecutorError | null = null;

    while (attempt < maxAttempts) {
      attempt += 1;

      try {
        const thread = this.getThread(input.workingDirectory);
        const { output, usage } = await this.runWithStreaming(thread, input);

        return {
          output,
          usage,
          threadId: this.threadId!,
          attempts: attempt,
          latencyMs: Date.now() - startedAt,
        };
      } catch (error) {
        const mappedError = mapError(error);
        lastError = mappedError;

        // 재시도 불가능한 오류 또는 최대 재시도 횟수 도달
        if (!mappedError.retryable || attempt >= maxAttempts) {
          throw mappedError;
        }

        // 대기 후 재시도
        await new Promise(resolve => setTimeout(resolve, 1000 * attempt));
      }
    }

    throw lastError!;
  }
}
```

## 베스트 프랙티스

### 1. 작업 디렉토리 요구사항

- 작업 디렉토리가 유효한 Git 저장소인지 확인
- `PROJECT_ROOT` 환경 변수로 명시적으로 지정
- 개발 디버그 시 `CODEX_SKIP_GIT_REPO_CHECK=true`로 검증 건너뛰기

### 2. 환경 변수 설정

- 화이트리스트 메커니즘을 통해 필요한 환경 변수만 전달
- 로그인 쉘을 사용하여 전체 환경 로드
- 민감한 정보 전달 방지

### 3. 타임아웃 및 재시도

- 작업 복잡도에 따라 합리적인 타임아웃 설정
- 재시도 가능한 오류에 대해 지수 백오프 구현
- 재시도 횟수 및 사유 기록

### 4. 오류 처리

- 재시도 가능한 오류와 불가능한 오류 구분
- 명확한 오류 메시지 및 제안 제공
- 상위 계층 처리를 위한 통일된 오류 코드

### 5. 스트리밍 출력

- 증분 출력 콜백 구현으로 사용자 경험 향상
- 메시지의 증분 업데이트를 올바르게 처리
- 비용 분석을 위한 토큰 사용량 기록

HagiCode 프로젝트의 실제 프로덕션 환경에서는 위의 베스트 프랙티스의 유효성을 이미 검증했습니다. 이 솔루션은 우리가 안정적이고 신뢰할 수 있는 AI 실행 서비스를 구축하는 데 도움이 되었습니다.毕竟, 실천만이 진리를 검증하는 유일한 기준이며, 종이 위의 이야기는 결국 쓸모가 없습니다.

## 요약

Codex SDK의 이벤트 스트림 메커니즘은 AI 실행 서비스 구축을 위한 강력한 기능을 제공합니다. 다양한 이벤트를 올바르게 파싱함으로써 다음을 할 수 있습니다:

- 실시간으로 실행 상태 및 출력 획득
- 신뢰할 수 있는 오류 처리 및 재시도 메커니즘 구현
- 상세한 실행 통계 정보 획득
- 기능이 완비된 AI 실행 플랫폼 구축

이 글에서 소개한 핵심 개념과 코드 예제는 실제 프로젝트에 직접 적용할 수 있으며, 개발자가 Codex SDK 통합 작업을 빠르게 시작할 수 있도록 돕습니다. 이 솔루션이 가치 있다고 생각하신다면, HagiCode의 엔지니어링 실천이 꽤 괜찮다는 뜻이겠죠. 그렇다면 HagiCode 자체도 주목할 만합니다.毕竟, 좋은 것을 놓치면 아쉽습니다.

## 참고 자료

- [Codex SDK 공식 문서](https://platform.openai.com/docs/codex)
- [HagiCode GitHub 저장소](https://github.com/HagiCode-org/site)
- [HagiCode 공식 웹사이트](https://hagicode.com)

---

감사합니다. 이 글이 유용하셨다면 아래 좋아요 버튼을 눌러更多人이 이 글을 볼 수 있도록 도와주세요.

본 콘텐츠는 인공지능을 보조하여 작성되었으며, 작성자가 검토하고 본인의 관점과 입장에 부합함을 확인하였습니다.

- 이 글의 작성자: [newbe36524](https://www.newbe.pro)
- 이 글의 링크: [https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/](https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/)
- 저작권 선언: 본 블로그의 모든 글은 별도의 선언이 없는 한 BY-NC-SA 라이선스를 따릅니다. 출처를 밝혀주세요!

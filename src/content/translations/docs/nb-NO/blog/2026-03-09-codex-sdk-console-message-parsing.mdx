---
title: Komplett veiledning til Codex SDK Console Message Parsing
date: 2026-03-10
tags: [Codex SDK, TypeScript, AI Executor, Event Stream]
description: "Denne artikkelen forklarer Codex SDK-hendelsesstrømmekanismen, meldingstypeparsing og beste fremgangsmåter i virkelige prosjekter, og hjelper utviklere raskt å mestre"
---

## Komplett veiledning til Codex SDK Console Message Parsing

> Denne artikkelen forklarer Codex SDK-hendelsesstrømmekanismen, meldingstypeparsing og beste praksis i virkelige prosjekter, og hjelper utviklere raskt å mestre kjerneferdighetene bak AI-utførelsestjenester.

## Bakgrunn

Når vi bygger en AI-utførelsestjeneste basert på Codex SDK, støter vi uunngåelig på et praktisk spørsmål: hvordan skal vi håndtere de streamede hendelsesmeldingene returnert av Codex? Disse meldingene inneholder viktig informasjon som utførelsesstatus, utdatainnhold og feildetaljer, så de fortjener nøye håndtering.

Som en del av HagiCode-prosjektet trengte vi en pålitelig utfører for AI-kodingsassistentscenarier. Det er nettopp derfor vi bestemte oss for å studere Codex SDK-hendelsesstrømmekanismen i dybden. Tross alt, bare ved å forstå hvordan de underliggende meldingene fungerer, kan vi bygge en virkelig AI-utførelsesplattform i bedriftsklasse.

Codex SDK er en programmeringsassistanse SDK utgitt av OpenAI. Den returnerer utførelsesresultater gjennom en hendelsesstrøm. I motsetning til den tradisjonelle forespørsel-svar-modellen, bruker Codex streamede hendelser slik at vi kan:

- Få utførelsesfremgang i sanntid
- Håndter feil raskt
- Få detaljert tokenbruksstatistikk
- Støtt langvarige komplekse oppgaver

Å forstå disse hendelsestypene og analysere dem riktig er avgjørende for å implementere en fullverdig AI-utfører. Til syvende og sist er det ingen som ønsker å jobbe med en svart boks.

## Om HagiCode

Løsningen som er delt i denne artikkelen kommer fra vår praktiske erfaring i [HagiCode](https://hagicode.com) prosjekt. HagiCode er et åpen kildekode AI-kodeassistentprosjekt dedikert til å gi utviklere intelligent kodingsstøtte. Under utviklingen trengte vi å bygge en pålitelig AI-kjøringstjeneste for å håndtere forespørsler om kjøring av brukerkode, som er den direkte grunnen til at vi introduserte Codex SDK.

Som en AI-kodingsassistent må HagiCode håndtere en rekke komplekse kodeutførelsesscenarier: få fremdrift i kjøringen i sanntid, håndtere feil raskt og samle inn detaljert tokenbruksstatistikk. Ved å ha en dyp forståelse av Codex SDK-hendelsesstrømmekanismen, kan vi bygge en eksekutør som oppfyller kravene til produksjonsmiljøet. Til syvende og sist, enten det er programvare eller det virkelige liv, drar alt nytte av jevn akkumulering og foredling.

## Event Stream Mechanism

### Grunnleggende konsepter

Codex SDK bruker `thread.runStreamed()` metode for å returnere en asynkron hendelseiterator:

```typescript
import { Codex } from '@openai/codex-sdk';

const client = new Codex({
  apiKey: process.env.CODEX_API_KEY,
  baseUrl: process.env.CODEX_BASE_URL,
});

const thread = client.startThread({
  workingDirectory: '/path/to/project',
  skipGitRepoCheck: false,
});

const { events } = await thread.runStreamed('your prompt here', {
  outputSchema: {
    type: 'object',
    properties: {
      output: { type: 'string' },
      status: { type: 'string', enum: ['ok', 'action_required'] },
    },
    required: ['output', 'status'],
  },
});

for await (const event of events) {
  // Handle each event
}
```

### Detaljert forklaring av hendelsestyper

| Hendelsestype | Beskrivelse | Nøkkeldata |
|---------|------|----------|
| `thread.started` | Tråden startet vellykket | `thread_id` |
| `item.updated` | Meldingsinnholdet er oppdatert | `item.text` |
| `item.completed` | Melding fullført | `item.text` |
| `turn.completed` | Utførelse fullført | `usage` (tokenbruk) |
| `turn.failed` | Utførelse mislyktes | `error.message` |
| `error` | Feilhendelse | `message` |

I virkelige prosjekter bygges HagiCodes eksekutørkomponent på toppen av disse hendelsestypene. Vi må håndtere hver type hendelse nøye for å sikre en jevn brukeropplevelse. Gode ​​systemer bygges ved å ta detaljer på alvor.

## Message Parsing Implementering

### Trekker ut meldingsinnhold

Meldingsinnhold trekkes ut gjennom en hendelsesbehandler:

```typescript
private handleThreadEvent(event: ThreadEvent, onMessage: (content: string) => void): void {
  // Only handle message update and completion events
  if (event.type !== 'item.updated' && event.type !== 'item.completed') {
    return;
  }

  // Only handle agent message content
  if (event.item.type !== 'agent_message') {
    return;
  }

  // Extract text content
  onMessage(event.item.text);
}
```

Hovedpunkter:
- Bare håndtak `item.updated` og `item.completed` hendelser
- Håndter kun innhold av type `agent_message`
- Meldingsinnholdet er i `event.item.text` felt

### Strukturert utdataparsing

Codex støtter JSON-strukturert utgang. Du kan spesifisere returformatet gjennom `outputSchema` parameter:

```typescript
const DEFAULT_OUTPUT_SCHEMA = {
  type: 'object',
  properties: {
    output: { type: 'string' },
    status: { type: 'string', enum: ['ok', 'action_required'] },
  },
  required: ['output', 'status'],
  additionalProperties: false,
} as const;
```

Parsefunksjonen prøver å analysere JSON, og hvis det mislykkes, faller den tilbake til råteksten.

```typescript
function toStructuredOutput(raw: string): StructuredOutput {
  try {
    const parsed = JSON.parse(raw) as Partial<StructuredOutput>;
    if (typeof parsed.output === 'string') {
      return {
        output: parsed.output,
        status: parsed.status === 'action_required' ? 'action_required' : 'ok',
      };
    }
  } catch {
    // JSON parsing failed, fall back to the raw text
  }

  return {
    output: raw,
    status: 'ok',
  };
}
```

### Fullfør hendelseshåndteringsflyt

```typescript
private async runWithStreaming(
  thread: Thread,
  input: CodexStageExecutionInput
): Promise<{ output: string; usage: Usage | null }> {
  const abortController = new AbortController();
  const timeoutHandle = setTimeout(() => {
    abortController.abort();
  }, Math.max(1000, input.timeoutMs));

  let latestMessage = '';
  let usage: Usage | null = null;
  let emittedLength = 0;

  try {
    const { events } = await thread.runStreamed(input.prompt, {
      outputSchema: DEFAULT_OUTPUT_SCHEMA,
      signal: abortController.signal,
    });

    for await (const event of events) {
      // Handle message content
      this.handleThreadEvent(event, (nextContent) => {
        const delta = nextContent.slice(emittedLength);
        if (delta.length > 0) {
          emittedLength = nextContent.length;
          input.callbacks?.onChunk?.(delta);  // Streaming callback
        }
        latestMessage = nextContent;
      });

      // Process different data based on the event type
      if (event.type === 'thread.started') {
        this.threadId = event.thread_id;
      } else if (event.type === 'turn.completed') {
        usage = event.usage;
      } else if (event.type === 'turn.failed') {
        throw new CodexExecutorError('gateway_unavailable', event.error.message, true);
      } else if (event.type === 'error') {
        throw new CodexExecutorError('gateway_unavailable', event.message, true);
      }
    }
  } catch (error) {
    if (abortController.signal.aborted) {
      throw new CodexExecutorError(
        'upstream_timeout',
        `Codex stage timed out after ${input.timeoutMs}ms`,
        true
      );
    }
    throw error;
  } finally {
    clearTimeout(timeoutHandle);
  }

  const structured = toStructuredOutput(latestMessage);
  return { output: structured.output, usage };
}
```

## Feilhåndteringsstrategi

### Feilkodetilordning

Kartlegg spesifikke feilmønstre til konkrete feilkoder slik at de øvre lagene kan håndtere dem lettere:

```typescript
function mapError(error: unknown): CodexExecutorError {
  if (error instanceof CodexExecutorError) {
    return error;
  }

  const message = error instanceof Error ? error.message : String(error);
  const normalized = message.toLowerCase();

  // Authentication errors - not retryable
  if (normalized.includes('401') ||
      normalized.includes('403') ||
      normalized.includes('api key') ||
      normalized.includes('auth')) {
    return new CodexExecutorError('auth_invalid', message, false);
  }

  // Rate limit errors - retryable
  if (normalized.includes('429') || normalized.includes('rate limit')) {
    return new CodexExecutorError('rate_limited', message, true);
  }

  // Timeout errors - retryable
  if (normalized.includes('timeout') || normalized.includes('aborted')) {
    return new CodexExecutorError('upstream_timeout', message, true);
  }

  // Default error
  return new CodexExecutorError('gateway_unavailable', message, true);
}
```

### Definisjoner av feiltype

```typescript
export type CodexErrorCode =
  | 'auth_invalid'      // Authentication failure
  | 'upstream_timeout'  // Upstream timeout
  | 'rate_limited'      // Rate limited
  | 'gateway_unavailable'; // Gateway unavailable

export class CodexExecutorError extends Error {
  readonly code: CodexErrorCode;
  readonly retryable: boolean;

  constructor(code: CodexErrorCode, message: string, retryable: boolean) {
    super(message);
    this.name = 'CodexExecutorError';
    this.code = code;
    this.retryable = retryable;
  }
}
```

## Arbeidskatalog og miljøkonfigurasjon

### Arbeidskatalogvalidering

Codex SDK krever at arbeidskatalogen er et gyldig Git-depot.

```typescript
export function validateWorkingDirectory(
  workingDirectory: string,
  skipGitRepoCheck: boolean
): void {
  const resolvedWorkingDirectory = path.resolve(workingDirectory);

  if (!existsSync(resolvedWorkingDirectory)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory does not exist.',
      false
    );
  }

  if (!statSync(resolvedWorkingDirectory).isDirectory()) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a directory.',
      false
    );
  }

  if (skipGitRepoCheck) {
    return;
  }

  const gitDir = path.join(resolvedWorkingDirectory, '.git');
  if (!existsSync(gitDir)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a git repository.',
      false
    );
  }
}
```

### Laster inn miljøvariabler

Codex SDK må laste inn miljøvariabler fra påloggingsskallet slik at AI-agenten kan få tilgang til systemkommandoer:

```typescript
function parseEnvironmentOutput(output: Buffer): Record<string, string> {
  const parsed: Record<string, string> = {};

  for (const entry of output.toString('utf8').split('\0')) {
    if (!entry) continue;

    const separatorIndex = entry.indexOf('=');
    if (separatorIndex <= 0) continue;

    const key = entry.slice(0, separatorIndex);
    const value = entry.slice(separatorIndex + 1);
    if (key.length > 0) {
      parsed[key] = value;
    }
  }

  return parsed;
}

function tryLoadEnvironmentFromShell(shellPath: string): Record<string, string> | null {
  const result = spawnSync(shellPath, ['-ilc', 'env -0'], {
    env: process.env,
    stdio: ['ignore', 'pipe', 'pipe'],
    timeout: 5000,
  });

  if (result.error || result.status !== 0) {
    return null;
  }

  return parseEnvironmentOutput(result.stdout);
}

export function createExecutorEnvironment(
  envOverrides: Record<string, string> = {}
): Record<string, string> {
  // Load environment variables from the login shell
  const consoleEnv = loadConsoleEnvironmentFromShell();

  return {
    ...process.env,
    ...consoleEnv,
    ...envOverrides,
  };
}
```

## Komplett brukseksempel

### Grunnleggende bruk

I HagiCode-prosjektet bruker vi følgende tilnærming for å initialisere Codex-klienten og utføre oppgaver:

```typescript
import { Codex } from '@openai/codex-sdk';

async function executeWithCodex(prompt: string, workingDir: string) {
  const client = new Codex({
    apiKey: process.env.CODEX_API_KEY,
    env: { PATH: process.env.PATH },
  });

  const thread = client.startThread({
    workingDirectory: workingDir,
  });

  const { events } = await thread.runStreamed(prompt);

  let result = '';
  for await (const event of events) {
    if (event.type === 'item.updated' && event.item.type === 'agent_message') {
      result = event.item.text;
    }
    if (event.type === 'turn.completed') {
      console.log('Token usage:', event.usage);
    }
  }

  // Try to parse JSON output
  try {
    const parsed = JSON.parse(result);
    return parsed.output;
  } catch {
    return result;
  }
}
```

### Fullfør implementering med nye forsøk

```typescript
export class CodexSdkExecutor {
  private readonly config: CodexRuntimeConfig;
  private readonly client: Codex;
  private threadId: string | null = null;

  async executeStage(input: CodexStageExecutionInput): Promise<CodexStageExecutionResult> {
    const maxAttempts = Math.max(1, this.config.retryCount + 1);
    let attempt = 0;
    let lastError: CodexExecutorError | null = null;

    while (attempt < maxAttempts) {
      attempt += 1;

      try {
        const thread = this.getThread(input.workingDirectory);
        const { output, usage } = await this.runWithStreaming(thread, input);

        return {
          output,
          usage,
          threadId: this.threadId!,
          attempts: attempt,
          latencyMs: Date.now() - startedAt,
        };
      } catch (error) {
        const mappedError = mapError(error);
        lastError = mappedError;

        // Non-retryable error or max retry attempts reached
        if (!mappedError.retryable || attempt >= maxAttempts) {
          throw mappedError;
        }

        // Wait before retrying
        await new Promise(resolve => setTimeout(resolve, 1000 * attempt));
      }
    }

    throw lastError!;
  }
}
```

## Beste praksis

### 1. Arbeidskatalogkrav

- Sørg for at arbeidskatalogen er et gyldig Git-depot
- Bruk `PROJECT_ROOT` miljøvariabel for å spesifisere den eksplisitt
- Under feilsøking kan du stille inn `CODEX_SKIP_GIT_REPO_CHECK=true` for å hoppe over sjekken

### 2. Konfigurasjon av miljøvariabel

- Send bare de nødvendige miljøvariablene gjennom en hvitelistemekanisme
- Bruk påloggingsskallet for å laste hele miljøet
- Unngå å gi sensitiv informasjon

### 3. Tidsavbrudd og strategi for forsøk på nytt

- Sett rimelige tidsavbrudd basert på oppgavens kompleksitet
- Implementer eksponentiell backoff for feil som kan prøves på nytt
- Registrer gjentatte forsøk og årsaker

### 4. Feilhåndtering

- Skille mellom feil som kan prøves på nytt og feil som ikke kan prøves på nytt
- Gi klare feilmeldinger og forslag
- Bruk enhetlige feilkoder slik at øvre lag kan håndtere dem konsekvent

### 5. Streaming Output

- Implementer inkrementelle tilbakeringinger for å forbedre brukeropplevelsen
- Håndter inkrementelle meldingsoppdateringer på riktig måte
- Registrer tokenbruk for kostnadsanalyse

I selve produksjonsmiljøet til HagiCode-prosjektet har vi allerede verifisert effektiviteten til beste praksis ovenfor. Denne tilnærmingen har hjulpet oss med å bygge en stabil og pålitelig AI-utførelsestjeneste. Til syvende og sist betyr praktisk validering mer enn teori alene.

## Konklusjon

Codex SDK-hendelsesstrømmemekanismen gir sterke muligheter for å bygge AI-utførelsestjenester. Ved å analysere ulike typer hendelser korrekt, kan vi:

- Få utførelsesstatus og utgang i sanntid
- Implementer pålitelig feilhåndtering og prøv på nytt
- Få detaljert utførelsesstatistikk
- Bygg en fullverdig AI-utførelsesplattform

Kjernekonseptene og kodeeksemplene introdusert i denne artikkelen kan brukes direkte i virkelige prosjekter, og hjelper utviklere å komme raskt i gang med Codex SDK-integrasjon. Hvis du finner denne tilnærmingen verdifull, reflekterer den også styrken til HagiCodes ingeniørpraksis og gjør HagiCode i seg selv verdt å følge.

## Referanser

- [Offisiell Codex SDK-dokumentasjon](https://platform.openai.com/docs/codex)
- [HagiCode GitHub Repository](https://github.com/HagiCode-org/site)
- [HagiCode offisielle nettsted](https://hagicode.com)

---

Takk for at du leser. Hvis du syntes denne artikkelen var nyttig, vennligst klikk på liker-knappen nedenfor slik at flere kan oppdage den.

Dette innholdet ble laget med AI-assistert samarbeid, vurdert av forfatteren, og gjenspeiler forfatterens egne synspunkter og posisjoner.

- Forfatter: [newbe36524](https://www.newbe.pro)
- Artikkellenke: [https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/](https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/)
- Merknad om opphavsrett: Med mindre annet er oppgitt, er alle artikler på denne bloggen lisensiert under BY-NC-SA. Vennligst oppgi kilden når du trykker på nytt.

---
title: "Rediger DESIGN.md direkte i webgrensesnittet: Fra idé til implementering"
date: 2026-04-09
tags: [DESIGN.md, Online Editing, HagiCode, Frontend-Backend Separation, Version Conflict Detection, MonoSpecs, Design Template Import]
description: "I MonoSpecs prosjektstyringssystem bærer DESIGN.md den arkitektoniske utformingen og tekniske beslutningene til et prosjekt. Men den tradisjonelle redigeringen"
---

## Rediger DESIGN.md direkte i webgrensesnittet: Fra idé til implementering

> I MonoSpecs prosjektstyringssystem, `DESIGN.md` bærer den arkitektoniske utformingen og tekniske beslutningene til et prosjekt. Men den tradisjonelle redigeringsarbeidsflyten tvinger brukere til å hoppe ut til en ekstern editor. Den fragmenterte opplevelsen er som å bli avbrutt midt mens du leser et dikt: inspirasjonen er borte, og det samme er stemningen. Denne artikkelen deler løsningen vi implementerte i HagiCode-prosjektet: redigering `DESIGN.md` direkte i nettgrensesnittet, med støtte for import av maler fra et online designnettsted. Tross alt, hvem nyter ikke følelsen av å fullføre alt i en flyt?

## Bakgrunn

Som kjernebærer av prosjektdesigndokumenter, `DESIGN.md` inneholder nøkkelinformasjon som arkitekturdesign, tekniske beslutninger og implementeringsveiledning. Den tradisjonelle redigeringsmetoden krever imidlertid at brukerne bytter til en ekstern editor som VS-kode, manuelt finner den fysiske banen og deretter redigerer filen. Det er ikke spesielt komplisert, men etter å ha gjentatt prosessen noen ganger, blir det slitsomt.

Problemene viser seg hovedsakelig på følgende måter:

- **Fragmentert arbeidsflyt**: Brukere må hele tiden bytte mellom webadministrasjonsgrensesnittet og en lokal redaktør, noe som bryter kontinuiteten i arbeidsflyten deres, omtrent som å ha musikken kuttet ut midt i en sang.
- **Vanskelig å gjenbruke**: Designnettstedet publiserer allerede et rikt bibliotek med designmaler, men de kan ikke integreres direkte i prosjektredigeringsarbeidsflyten. Det gode finnes, men du kan fortsatt ikke bruke det der du trenger det.
- **Manglende opplevelsesløkke**: det er ingen lukket sløyfe for «forhåndsvisning-velg-import», så brukere må kopiere og lime inn manuelt, noe som øker risikoen for feil.
- **Samarbeidsfriksjon**: Å holde designdokumenter og kodeimplementering synkronisert blir en høyfriksjonsprosess som skader teamets effektivitet.

For å løse disse smertepunktene bestemte vi oss for å legge til støtte for direkte redigering `DESIGN.md` i nettgrensesnittet og tillate ett-klikks malimport fra et online designnettsted. Det var ikke en jordskjelvende avgjørelse. Vi ønsket rett og slett å gjøre utviklingsopplevelsen jevnere.

## Om HagiCode

Løsningen som er delt i denne artikkelen kommer fra vår praktiske erfaring i [HagiCode](https://hagicode.com) prosjekt. HagiCode er et AI-drevet kodeassistentprosjekt, og under utvikling må vi ofte vedlikeholde prosjektdesigndokumenter. For å hjelpe teamet til å samarbeide mer effektivt, utforsket og implementerte vi denne nettbaserte redigerings- og importløsningen. Det er ikke noe mystisk med det. Vi møtte et problem og fant en måte å løse det på.

## Teknisk løsning

### Overordnet arkitektur

Denne løsningen bruker en frontend-backend-separert arkitektur med en proxy med samme opprinnelse, hovedsakelig sammensatt av følgende lag. I praksis kan designet oppsummeres som "hver del gjør sin egen jobb":

**1. Frontend-redigeringslag**
```typescript
// Core component: DesignMdManagementDrawer
// Location: repos/web/src/components/project/DesignMdManagementDrawer.tsx
// Responsibility: handle editing, saving, version conflict detection, and import flow
```

**2. Backend-tjenestelag**
```csharp
// Core service: ProjectAppService.DesignMd
// Location: repos/hagicode-core/src/PCode.Application/ProjectAppService.DesignMd.cs
// Responsibility: path resolution, file read/write, and version management
```

**3. Proxy-lag med samme opprinnelse**
```csharp
// Proxy service: ProjectAppService.DesignMdSiteIndex
// Location: repos/hagicode-core/src/PCode.Application/ProjectAppService.DesignMdSiteIndex.cs
// Responsibility: proxy design site resources, preview image caching, and security validation
```

### Viktige tekniske avgjørelser

#### Avgjørelse 1: Globalt skuffemønster

Vi bruker en enkelt global skuff i stedet for lokale popup-lag, med staten administrert gjennom `layoutSlice`, som gir brukerne en konsistent opplevelse på tvers av visninger (`classic` og `kanban`). Uansett hvilken visning brukeren åpner editoren fra, får de samme interaksjonsmodell. En konsekvent opplevelse får folk til å føle seg mer komfortable i stedet for å bli desorienterte når de bytter synspunkt.

#### Avgjørelse 2: Prosjektbasert API

Vi monterte `DESIGN.md`-relaterte endepunkter under `ProjectController`, gjenbruke den eksisterende prosjekttillatelsesgrensen og unngå kompleksiteten med å legge til en separat kontroller. Dette gjør tillatelseshåndteringen klarere og stemmer også overens med RESTful ressursorganisasjon. Noen ganger er gjenbruk mer meningsfylt enn å skape noe nytt fra bunnen av.

#### Avgjørelse 3: Oppdagelse av versjonskonflikt

Vi henter en ugjennomsiktig versjon fra filsystemets `LastWriteTimeUtc`, som gir oss lettvekts optimistisk samtidighetskontroll. Når flere brukere redigerer den samme filen samtidig, kan systemet oppdage konflikter og be brukeren om å oppdatere. Denne utformingen blokkerer ikke redigering, samtidig som den beskytter datakonsistens.

#### Avgjørelse 4: Proxy-mønster med samme opprinnelse

Vi bruker `IHttpClientFactory` å proxy-tjene eksterne design-site-ressurser, unngå både kryssopprinnelsesproblemer og SSRF-risikoer. Dette holder systemet sikkert samtidig som det forenkler frontend-anrop. Du kan neppe være for forsiktig med sikkerhet.

## Kjerneimplementering

### 1. Rediger DESIGN.md direkte

#### Backend-implementering

Backend er hovedsakelig ansvarlig for baneoppløsning, fillesing/skriving og versjonsadministrasjon. Disse oppgavene er grunnleggende, men uunnværlige, som grunnlaget for et hus:

```csharp
// Path resolution and security validation
private Task<string> ResolveDesignDocumentDirectoryAsync(string projectPath, string? repositoryPath)
{
    if (string.IsNullOrWhiteSpace(repositoryPath))
    {
        return Task.FromResult(Path.GetFullPath(projectPath));
    }
    return ValidateSubPathAsync(projectPath, repositoryPath);
}

// Version generation (based on file system timestamp and size)
private static string BuildDesignDocumentVersion(string path)
{
    var fileInfo = new FileInfo(path);
    fileInfo.Refresh();
    return string.Create(
        CultureInfo.InvariantCulture,
        $"{fileInfo.LastWriteTimeUtc.Ticks:x}-{fileInfo.Length:x}");
}
```

Versjonsdesignet er interessant i sin enkelhet: vi bruker filens siste endrede tid og størrelse for å generere en unik versjonsidentifikator. Den er lett og pålitelig, uten ekstra versjonsdatabase å vedlikeholde. Enkle løsninger er ofte de mest effektive.

#### Frontend-implementering

På frontend implementerer vi dirty state-deteksjon og lagrer logikk. Denne utformingen hjelper brukere å forstå om endringene deres er lagret og reduserer angsten for "hva om jeg mister det?":

```typescript
// Dirty-state detection and save logic
const [draft, setDraft] = useState('');
const [savedDraft, setSavedDraft] = useState('');
const isDirty = draft !== savedDraft;

const handleSave = useCallback(async () => {
    const result = await saveProjectDesignMdDocument({
        ...activeTarget,
        content: draft,
        expectedVersion: document.version, // optimistic concurrency control
    });
    setSavedDraft(draft); // update saved state
}, [activeTarget, document, draft]);
```

I denne implementeringen opprettholder vi to deler av staten: `draft` er innholdet som for øyeblikket redigeres, mens `savedDraft` er det lagrede innholdet. Sammenligning av dem forteller oss om det er ulagrede endringer. Designet er enkelt, men det gir folk trygghet. Ingen vil at tingen de jobbet hardt med skal forsvinne.

### 2. Importer designfiler fra en elektronisk kilde

#### Katalogstruktur

```
repos/index/
└── src/data/public/design.json    # Design template index

repos/awesome-design-md-site/
├── vendor/awesome-design-md/       # Upstream design templates
│   └── design-md/
│       ├── clickhouse/
│       │   └── DESIGN.md
│       ├── linear/
│       │   └── DESIGN.md
│       └── ...
└── src/lib/content/
    └── awesomeDesignCatalog.ts     # Content pipeline
```

#### Indeks dataformat

Indeksfilen på designsiden definerer alle tilgjengelige maler. Med denne indeksen kan brukere velge malen de vil ha like enkelt som å bestille fra en meny:

```json
{
  "entries": [
    {
      "slug": "linear.app",
      "title": "Linear Inspired Design System",
      "summary": "AI Product / Dark Feel",
      "detailUrl": "/designs/linear.app/",
      "designDownloadUrl": "/designs/linear.app/DESIGN.md",
      "previewLightImageUrl": "...",
      "previewDarkImageUrl": "..."
    }
  ]
}
```

Hver oppføring inneholder malens grunnleggende informasjon og nedlastingslenker. Backend leser listen over tilgjengelige maler fra denne indeksen og presenterer dem for brukeren å velge mellom. Det gjør utvalget intuitivt i stedet for å tvinge folk til å føle seg rundt i mørket.

#### Samme-opprinnelse proxy-implementering

For å holde ting sikkert, utfører backend streng validering på tilgang til designsiden. Du kan ikke være for forsiktig med sikkerhet:

```csharp
// Safe slug validation
private static readonly Regex SafeDesignSiteSlugRegex =
    new("^[A-Za-z0-9](?:[A-Za-z0-9._-]{0,127})$", RegexOptions.Compiled);

private static string NormalizeDesignSiteSlug(string slug)
{
    var normalizedSlug = slug?.Trim() ?? string.Empty;
    if (!IsSafeDesignSiteSlug(normalizedSlug))
    {
        throw new BusinessException(
            ProjectDesignSiteIndexErrorCodes.InvalidSlug,
            "Design site slug must be a single safe path segment.");
    }
    return normalizedSlug;
}

// Preview image caching (OS temp directory)
private static string ComputePreviewCacheKey(string slug, string theme, string previewUrl)
{
    var raw = $"{slug}|{theme}|{previewUrl}";
    var bytes = SHA256.HashData(Encoding.UTF8.GetBytes(raw));
    return Convert.ToHexString(bytes).ToLowerInvariant();
}
```

Vi gjør to ting her: For det første validerer vi slug-formatet strengt med et regulært uttrykk for å forhindre angrep på veier; for det andre cacher vi forhåndsvisningsbilder for å redusere trykket på det eksterne nettstedet. Førstnevnte er beskyttelse, sistnevnte er optimalisering, og begge deler betyr noe.

### 3. Full importflyt

```typescript
// 1. Open the import drawer
const handleRequestImportDrawer = useCallback(() => {
    setIsImportDrawerOpen(true);
}, []);

// 2. Select and import
const handleImportRequest = useCallback((entry) => {
    if (isDirty) {
        setPendingImportEntry(entry);
        setConfirmMode('import'); // overwrite confirmation
        return;
    }
    void executeImport(entry);
}, [isDirty]);

// 3. Execute import
const executeImport = useCallback(async (entry) => {
    const result = await getProjectDesignMdSiteImportDocument(
        activeTarget.projectId,
        entry.slug
    );
    setDraft(result.content); // replace editor text only, do not save automatically
    setIsImportDrawerOpen(false);
}, [activeTarget?.projectId]);
```

Importflyten følger et "brukerbekreftelse"-prinsipp: etter import oppdateres bare redaktørinnholdet, og ingenting lagres automatisk. Brukere kan inspisere det importerte innholdet og lagre det manuelt bare etter å ha bekreftet at det ser riktig ut. Den endelige avgjørelsen bør forbli i hendene på brukeren.

## Praktiske eksempler

### Scenario 1: Opprette DESIGN.md i prosjektroten

Når `DESIGN.md` ikke eksisterer, returnerer backend en virtuell dokumenttilstand. Dette lar grensesnittet unngå spesiell håndtering for "filen eksisterer ikke", og en enhetlig API forenkler kodelogikken:

```csharp
return new ProjectDesignDocumentDto
{
    Path = targetPath,
    Exists = false,  // virtual document state
    Content = string.Empty,
    Version = null
};

// Automatically create the file on first save
public async Task<SaveProjectDesignDocumentResultDto> SaveDesignDocumentAsync(...)
{
    Directory.CreateDirectory(targetDirectory);
    await File.WriteAllTextAsync(targetPath, input.Content);
    return new SaveProjectDesignDocumentResultDto { Created = !exists };
}
```

Fordelen med denne designen er at frontend ikke trenger spesialtilfelle-logikk for manglende filer. Ved å skjule den kompleksiteten i backend, kan frontend lettere fokusere på brukeropplevelse.

### Scenario 2: Importer en mal fra designsiden

Etter at brukeren har valgt "Lineær" designmalen i importskuffen, henter systemet `DESIGN.md` innhold gjennom backend-proxyen. Hele prosessen er gjennomsiktig for brukeren: de velger bare en mal, og systemet håndterer nettverksforespørslene og datatransformasjonen automatisk.

```typescript
// 1. The system fetches DESIGN.md content through the backend proxy
GET /api/project/{id}/design-md/site-index/linear.app

// 2. The backend validates the slug and fetches content from upstream
var entry = FindDesignSiteEntry(catalog, "linear.app");
using var upstreamResponse = await httpClient.SendAsync(request);
var content = await upstreamResponse.Content.ReadAsStringAsync();

// 3. The frontend replaces the editor text
setDraft(result.content);
// The user reviews it and then saves it manually to disk
```

Hele flyten forblir gjennomsiktig for brukeren. De velger bare en mal, og systemet håndterer nettverksbyggingen og transformasjonen bak kulissene. Det er opplevelsen vi ønsker: enkel, men kraftig.

### Scenario 3: Håndtering av versjonskonflikter

Når flere brukere redigerer det samme `DESIGN.md` samtidig oppdager systemet versjonskonflikter. Denne optimistiske samtidighetskontrollmekanismen bevarer datakonsistens uten å blokkere brukerens redigeringer:

```csharp
if (!string.Equals(currentVersion, expectedVersion, StringComparison.Ordinal))
{
    throw new BusinessException(
        ProjectDesignDocumentErrorCodes.VersionConflict,
        $"DESIGN.md at '{targetPath}' changed on disk.");
}
```

Frontend fanger opp denne feilen og ber brukeren:

```typescript
// Frontend prompts the user to refresh and retry
<Alert>
    <AlertTitle>Version conflict</AlertTitle>
    <AlertDescription>
        The file was modified by another process. Please refresh to get the latest version and try again.
    </AlertDescription>
</Alert>
```

Denne optimistiske samtidighetskontrollmekanismen holder data konsistente uten å blokkere brukere mens de jobber. Konflikter er uunngåelige, men brukere bør i det minste vite hva som skjedde i stedet for å miste endringene i det stille.

## Notater og beste praksis

### 1. Veisikkerhet

Bekreft alltid `repositoryPath` for å forhindre angrep på veier. Du kan aldri gjøre for mye når det kommer til sikkerhet:

```csharp
// Always validate repositoryPath to prevent path traversal attacks
return ValidateSubPathAsync(projectPath, repositoryPath);
// Reject dangerous inputs such as "../" and absolute paths
```

### 2. Bufferstrategi

Buffer forhåndsvisningsbilder i 24 timer, med maksimalt 160 filer. Moderat bufring forbedrer ytelsen, men balansen er fortsatt viktig:

```csharp
// Cache preview images for 24 hours, with a maximum of 160 files
private static readonly TimeSpan PreviewCacheTtl = TimeSpan.FromHours(24);
private const int PreviewCacheMaxFiles = 160;
// Periodically clean up expired cache
```

### 3. Feilhåndtering

Nedbryt grasiøst når oppstrømsstedet er utilgjengelig. Denne utformingen sikrer at selv om en ekstern avhengighet svikter, fungerer kjerneredigeringsfunksjonaliteten som normalt:

```typescript
// Gracefully degrade when the upstream site is unavailable
try {
    const catalog = await getProjectDesignMdSiteImportCatalog(projectId);
} catch (error) {
    toast.error(t('project.designMd.siteImport.feedback.catalogLoadFailed'));
    // The main editing drawer remains available
}
```

Denne grasiøse degraderingen sikrer at selv når eksterne avhengigheter er utilgjengelige, fortsetter kjerneredigeringsfunksjonen å fungere. Et system bør være motstandsdyktig i stedet for å kollapse i det øyeblikket noe går galt.

### 4. Optimalisering av brukeropplevelse

Bekreft overskrivinger før import, og ikke lagre automatisk etter import. Brukere bør ha kontroll over sine egne handlinger:

```typescript
// Confirm overwrite before import
if (isDirty) {
    setConfirmMode('import');
    return;
}

// Do not save automatically after import; let the user confirm
setDraft(result.content); // update draft only
// The content is written to disk only after the user reviews it and clicks Save
```

### 5. Ytelseshensyn

Bruk en HTTP-klientfabrikk for å unngå å opprette for mange tilkoblinger. Ressursstyring kan virke liten, men å gjøre det godt kan utgjøre en stor forskjell:

```csharp
// Use an HTTP client factory to avoid creating too many connections
private const string DesignSiteProxyClientName = "ProjectDesignSiteProxy";
private static readonly TimeSpan DesignSiteProxyTimeout = TimeSpan.FromSeconds(8);
```

## Foreslåtte utvidelser

1. **Markdown-forbedring**: vi bruker for øyeblikket en grunnleggende `Textarea`, men vi kan oppgradere til CodeMirror for syntaksutheving og hurtigtaster. Når redaktøren føler seg bedre, føles det også bedre å skrive dokumentasjon.
2. **Forhåndsvisningsmodus**: legg til Markdown-forhåndsvisning i sanntid for å forbedre redigeringsopplevelsen. Det-du-ser-er-det-du-får gir alltid folk mer selvtillit.
3. **Diff-fletting**: implementer en intelligent sammenslåingsalgoritme i stedet for enkel fullteksterstatning. Konflikter er uunngåelige, men konfliktløsningsprosessen trenger ikke å være smertefull.
4. **Lokal caching**: cache `design.json` i databasen for å redusere avhengigheten av det eksterne nettstedet. Jo færre avhengigheter et system har, jo mer stabilt har det en tendens til å være.

## Sammendrag

I HagiCode-prosjektet implementerte vi en komplett nettbasert redigerings- og importløsning for `DESIGN.md` gjennom frontend-backend-samarbeid. Kjerneverdien av denne løsningen ligger i følgende punkter:

- **Høyere effektivitet**: ingen grunn til å bytte verktøy; redigering og import av designdokumenter kan skje i ett enhetlig webgrensesnitt.
- **Senk adgangsbarriere**: Import av designmal med ett klikk hjelper nye prosjekter å komme raskt i gang.
- **Sikker og pålitelig**: banevalidering, gjenkjenning av versjonskonflikt og elegante degraderingsmekanismer holder systemet stabilt.
- **Bedre brukeropplevelse**: den globale skuffen, gjenkjenning av skitten tilstand og bekreftelsesdialoger avgrenser den generelle interaksjonsopplevelsen.

Denne løsningen kjører allerede i HagiCode-prosjektet og har løst teamets smertepunkter rundt designdokumenthåndtering. Hvis du har lignende problemer, håper jeg denne artikkelen gir deg noen nyttige ideer. Det er ingen spesielt dyp teori her, bare det praktiske arbeidet med å løpe inn i et problem og finne en måte å løse det på.

## Referanser

- HagiCode prosjektlager: [github.com/HagiCode-org/site](https://github.com/HagiCode-org/site)
- HagiCode offisielle nettsted: [hagicode.com](https://hagicode.com)
- MonoSpecs prosjektstyringssystem: [docs.hagicode.com](https://docs.hagicode.com)
- 30-minutters hands-on demo: [www.bilibili.com/video/BV1pirZBuEzq/](https://www.bilibili.com/video/BV1pirZBuEzq/)
- Installasjonsveiledning for Docker Compose: [docs.hagicode.com/installation/docker-compose](https://docs.hagicode.com/installation/docker-compose)
- Desktop installasjon: [hagicode.com/desktop/](https://hagicode.com/desktop/)

Hvis denne artikkelen hjalp deg, kan du gjerne gi prosjektet en stjerne på GitHub. Den offentlige betaen har allerede startet, og du kan bli med i opplevelsen rett etter at du har installert den. Åpen kildekode-prosjekter trenger alltid mer tilbakemelding og oppmuntring, og hvis du fant dette nyttig, er det verdt å hjelpe flere med å oppdage det.

---

*"Vakre ting eller mennesker trenger ikke å tilhøre deg. Så lenge de forblir vakre, er det nok å stille pris på den skjønnheten."*

*Det samme gjelder for en `DESIGN.md` redaktør. Det trenger ikke være altfor komplisert. Hvis det hjelper deg å jobbe effektivt, er det allerede nok.*

## Merknad om opphavsrett

Takk for at du leser. Hvis du fant denne artikkelen nyttig, kan du vurdere å like, bokmerke og dele den.
Dette innholdet ble laget med AI-assistert samarbeid, og den endelige versjonen ble gjennomgått og godkjent av forfatteren.
- Forfatter: [newbe36524](https://www.newbe.pro)
- Originalartikkel: [https://docs.hagicode.com/blog/2026-04-09-design-md-web-editor-implementation/](https://docs.hagicode.com/blog/2026-04-09-design-md-web-editor-implementation/)
- Copyrighterklæring: Med mindre annet er angitt, er alle artikler på denne bloggen lisensiert under BY-NC-SA. Vennligst inkluder attribusjon når du legger ut på nytt!

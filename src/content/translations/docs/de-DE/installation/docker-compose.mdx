---
title: Docker Compose-Bereitstellung
description: Ein-Klick-Bereitstellung des gesamten HagiCode-Systems mit Docker Compose.
sidebar_position: 10
---

In dieser Anleitung wird die Bereitstellung von HagiCode mit Docker Compose behandelt. Für die meisten Benutzer ist dies der Standardbereitstellungspfad in Entwicklungs-, Test- und Produktionsumgebungen.

## Video-Tutorial

Wenn Sie zunächst den gesamten Ablauf sehen möchten, beginnen Sie mit dem Video-Tutorial:

[Sehen Sie sich das Video-Tutorial zu Bilibili an](https://www.bilibili.com/video/BV19967B6EHr/)

**Videoinhalt beinhaltet**:
- Vollständige Demonstration des Docker Compose-Bereitstellungsprozesses
- Anweisungen zum Generieren und Ändern von Konfigurationsdateien
- Schritte zum Starten und Verifizieren des Dienstes
- Häufige Probleme und Lösungen

Videodauer: ca. 15 Minuten

:::note[Docker Compose Builder]
Docker Compose Builder ist jetzt als eigenständiges Tool unter verfügbar [https://builder.hagicode.com/](https://builder.hagicode.com/). Verwenden Sie diese Site als Hauptquelle zum Generieren derselben Klasse von Konfigurationsdateien.
:::

:::tip[Konfigurationsgenerator verwenden]
Sie können verwenden [Docker Compose Builder (eigenständige Site)](https://builder.hagicode.com/) zu erzeugen `docker-compose.yml` direkt.

Der Generator unterstützt:
- benutzerdefinierte Ports, Containernamen und andere Grundeinstellungen
- integrierte oder externe Datenbankauswahl
- automatische Windows-/Linux-Plattformunterschiede
- Gemeinsame Handhabung von Dateiberechtigungen
- Auswahl der Bildquelle basierend auf Ihrer Netzwerkumgebung

[Öffnen Sie den Generator →](https://builder.hagicode.com/)
:::

:::tip[Empfohlene Methode]
Docker Compose ist in den meisten Fällen der Standardbereitstellungspfad, weil:
- Durch die Isolation der Umgebung werden Abhängigkeitskonflikte reduziert
- Der unterstützte HagiCode-Service-Stack kann gemeinsam gestartet werden
- Konfiguration, Upgrades und Wartung lassen sich leichter konsistent halten
- Es eignet sich gut für lokale Versuche, Tests und Teambereitstellungen
:::

:::Hinweis[Über die Standarddatenspeicherung]
Die derzeit unterstützten Docker Compose-Vorlagen speichern Daten über den standardmäßigen lokalen SQLite-Pfad. In den meisten Fällen müssen Sie keinen separaten Datenbankdienst auf dem Hostcomputer bereitstellen.
:::

## Voraussetzungen

Stellen Sie vor dem Start sicher, dass auf Ihrem System Docker und Docker Compose installiert sind.

### Installieren Sie Docker

#### Windows

Herunterladen und installieren [Docker Desktop für Windows](https://www.docker.com/products/docker-desktop/)

Stellen Sie nach der Installation sicher, dass Docker Desktop ausgeführt wird.

#### macOS

Herunterladen und installieren [Docker Desktop für Mac](https://www.docker.com/products/docker-desktop/)

Stellen Sie nach der Installation sicher, dass Docker Desktop ausgeführt wird.

#### Linux

Installieren Sie Docker mit Ihrem Paketmanager:

```bash title="Install Docker on Ubuntu/Debian"
sudo apt-get update
sudo apt-get install docker.io docker-compose-plugin
```

Starten Sie nach der Installation den Docker-Dienst:

```bash
sudo systemctl start docker
sudo systemctl enable docker
```

### Überprüfen Sie die Installation

Führen Sie nach der Installation die folgenden Befehle aus, um zu überprüfen, ob Docker und Docker Compose korrekt installiert sind:

```bash
docker --version
docker compose version
```

## Schnellstart

### 1. Generieren Sie die Docker Compose-Konfigurationsdatei

:::tip[Konfigurationsgenerator verwenden]
**Empfehlen Sie die Verwendung [Docker Compose Builder (eigenständige Site)](https://builder.hagicode.com/)** um Ihre Konfigurationsdatei zu generieren.

Der Generator unterstützt:
- Mehrere API-Anbieteroptionen (Zhipu AI, Anthropic Official, Custom API)
- Automatische Konfiguration von API-URL und Token
- Benutzerdefinierte Ports, Containernamen und andere grundlegende Konfigurationen
- Konfigurieren Sie persistente Daten- und Arbeitsverzeichnispfade
- Konfigurieren Sie die Plattformunterschiede zwischen Windows und Linux automatisch
- Behandeln Sie Dateiberechtigungsprobleme intelligent
- **Auswahl der Bildquelle**: Wählen Sie basierend auf Ihrer Netzwerkumgebung die entsprechende Bildquelle aus

[🚀 Benutzen Sie sofort den Generator →](https://builder.hagicode.com/)
:::

1. Offen [Docker Compose Builder (eigenständige Site)](https://builder.hagicode.com/)
2. Füllen Sie die Konfiguration entsprechend Ihren Anforderungen aus:
   - Wählen Sie einen API-Anbieter (Zhipu AI, Anthropic Official oder Custom API)
   - Konfigurieren Sie Ports, Datenbank, Arbeitsverzeichnis und andere Optionen
   - **Bildquelle auswählen**: Wählen Sie die geeignete Bildquelle basierend auf Ihrer Netzwerkumgebung
3. Klicken Sie auf die Schaltfläche „Generieren“, um das zu erhalten `docker-compose.yml` Konfiguration
4. Speichern Sie die generierte Konfiguration als `docker-compose.yml` Datei
5. Erstellen Sie bei Bedarf eine `.env` Datei zum Konfigurieren vertraulicher Informationen

#### Auswahl der Bildquelle

Der Generator unterstützt drei Bildquellenoptionen:

**Docker-Hub**
- **Bildadresse**: `newbe36524/hagicode:{tag}`
- **Anwendbare Szenarien**: Geeignet für Benutzer mit Docker Hub-Spiegelbeschleunigung
- **Vorteile**: Offizielle Bildquelle, zeitnahe Updates, stabiler Zugriff
- **Hinweise**: In einigen Regionen muss möglicherweise die Spiegelbeschleunigung konfiguriert werden

**Alibaba Cloud Container Registry (ACR)**
- **Bildadresse**: `registry.cn-hangzhou.aliyuncs.com/hagicode/hagicode:{tag}`
- **Anwendbare Szenarien**: Geeignet für Benutzer, die nicht lokal auf Docker Hub zugreifen können
- **Vorteile**: Bietet eine alternative Bildquelle und behebt Netzwerkzugriffsprobleme
- **Hinweise**: Bilder bleiben mit Docker Hub synchronisiert, es kann jedoch zu kurzen Verzögerungen kommen

**Alibaba Cloud Container Registry (ACR)**
- **Bildadresse**: `registry.cn-hangzhou.aliyuncs.com/hagicode/hagicode:{tag}`
- **Anwendbare Szenarien**: Geeignet für Privatanwender, stabiler und schneller Netzwerkzugriff
- **Vorteile**: Inländische Alibaba Cloud-Knoten, schnelle Zugriffsgeschwindigkeit, hohe Stabilität
- **Hinweise**: Alibaba ACR-Bilder bleiben mit Docker Hub synchronisiert, normalerweise innerhalb von 30 Minuten

:::tip[Empfehlungen zur Auswahl der Bildquelle]
- **Inländische Benutzer empfehlen Alibaba Cloud ACR**: Wenn Sie ein inländischer Benutzer sind, wird empfohlen, die Verwendung der Alibaba Cloud ACR-Bildquelle für einen schnellen und stabilen Zugriff zu priorisieren
- **Alternativer Docker Hub**: Wenn Ihre Netzwerkumgebung auf Docker Hub zugreifen kann, können Sie auch Docker Hub als Bildquelle wählen
- **Alternative Alibaba Cloud ACR**: Wählen Sie die Alibaba Cloud ACR-Bildquelle nur aus, wenn andere Bildquellen Zugriffsschwierigkeiten haben
- **Bildsynchronisierung**: Alibaba Cloud ACR-Bilder bleiben mit Docker Hub synchronisiert, normalerweise innerhalb von 30 Minuten
- **Versionskonsistenz**: Bild-Tags sind in allen drei Bildquellen konsistent, sodass Sie sich keine Sorgen über Versionsprobleme machen müssen
:::

:::note[API-Token abrufen]
Sie müssen Claude API Token für die Verwendung von HagiCode konfigurieren:

**Zhipu AI (empfohlen)**: [Holen Sie sich API-Token →](https://www.bigmodel.cn/glm-coding?ic=14BY54APZA)
- Stabiler Inlandszugang, schnellere Reaktion
- Hohes Preis-Leistungs-Verhältnis, alltagstauglich

**Anthropic-Beamter**: [Holen Sie sich API-Token →](https://console.anthropic.com/)
- Direkte Nutzung der Dienste von Anthropic

:::

### 2. Starten Sie Dienste

Führen Sie den folgenden Befehl in dem Verzeichnis aus, in dem sich Ihr `docker-compose.yml` befindet sich:

```bash
docker compose up -d
```

Dieser Befehl wird:
- Laden Sie die durch die generierte Vorlage definierten HagiCode-Container herunter und starten Sie sie
- Erstellen und konfigurieren Sie das Netzwerk
- Initialisieren Sie das Standarddatenverzeichnis und die Laufzeitabhängigkeiten

### 3. Überprüfen Sie den Servicestatus

Überprüfen Sie, ob Container ausgeführt werden:

```bash
docker compose ps
```

Die in Ihrer generierten Vorlage definierten Dienste sollten im Status „Wird ausgeführt“ angezeigt werden.

### Serviceprotokolle anzeigen

So zeigen Sie alle Serviceprotokolle an:

```bash
# View all service logs
docker compose logs

# View HagiCode service logs
docker compose logs hagicode

# Real-time log following
docker compose logs -f hagicode
```

## Konfigurationsanweisungen

Wenn Sie die Konfiguration ändern müssen (z. B. Ports ändern, API-Anbieter wechseln usw.), können Sie Folgendes tun:

1. Neue Konfiguration mit neu generieren [Docker Compose Builder (eigenständige Site)](https://builder.hagicode.com/)
2. Manuell bearbeiten `docker-compose.yml` und `.env` Dateien
3. Starten Sie die Dienste neu, damit die Konfiguration wirksam wird:

```bash
docker compose restart
```

## Zugriffsanwendung

### Webinterface

Nach erfolgreichem Start des Dienstes können Sie über Ihren Browser darauf zugreifen:

```
http://localhost:45000
```

### Datenpersistenz

Der unterstützte Docker Compose-Pfad verwendet standardmäßig SQLite-Datendateien mit persistenten Volumes. Wenn Sie Bereitstellungspunkte überprüfen oder Sicherungen planen müssen, überprüfen Sie die Volume- und Datenverzeichniszuordnungen im generierten Verzeichnis `docker-compose.yml`.

## Nächste Schritte

Jetzt haben Sie HagiCode erfolgreich bereitgestellt. Verbinden Sie als Nächstes ein reales Projekt im Webinterface und fahren Sie dann mit fort [Assistent zum Einrichten](/de-DE/quick-start/wizard-setup) oder [Erstellen Sie eine Angebotssitzung](/de-DE/quick-start/proposal-session).

Wenn Sie die Bereitstellungsmethode für Desktop-Anwendungen bevorzugen, lesen Sie bitte [Desktop-Installationshandbuch](/de-DE/installation/desktop).

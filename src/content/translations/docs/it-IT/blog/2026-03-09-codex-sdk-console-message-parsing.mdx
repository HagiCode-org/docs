---
title: Guida completa all'analisi dei messaggi della console Codex SDK
date: 2026-03-10
tags: [Codex SDK, TypeScript, AI Executor, Event Stream]
description: "Questo articolo spiega il meccanismo del flusso di eventi dell'SDK del Codex, l'analisi del tipo di messaggio e le migliori pratiche nei progetti reali, aiutando gli sviluppatori a padroneggiare rapidamente"
---

## Guida completa all'analisi dei messaggi della console Codex SDK

> Questo articolo spiega il meccanismo del flusso di eventi dell'SDK Codex, l'analisi del tipo di messaggio e le migliori pratiche nei progetti reali, aiutando gli sviluppatori a padroneggiare rapidamente le competenze principali alla base dei servizi di esecuzione dell'intelligenza artificiale.

## Sfondo

Quando creiamo un servizio di esecuzione AI basato sull'SDK del Codex, inevitabilmente ci imbattiamo in una domanda pratica: come dovremmo gestire i messaggi di evento in streaming restituiti dal Codex? Questi messaggi contengono informazioni importanti come lo stato di esecuzione, il contenuto dell'output e i dettagli dell'errore, quindi meritano un'attenta gestione.

Nell'ambito del progetto HagiCode, avevamo bisogno di un esecutore affidabile per scenari di assistente alla codifica AI. Questo è esattamente il motivo per cui abbiamo deciso di studiare in modo approfondito il meccanismo del flusso di eventi dell'SDK del Codex. Dopotutto, solo comprendendo come funzionano i messaggi sottostanti possiamo costruire una piattaforma di esecuzione AI veramente di livello aziendale.

Codex SDK è un SDK di assistenza alla programmazione rilasciato da OpenAI. Restituisce i risultati dell'esecuzione tramite un flusso di eventi. A differenza del tradizionale modello richiesta-risposta, Codex utilizza eventi in streaming in modo da poter:

- Ottieni lo stato di avanzamento dell'esecuzione in tempo reale
- Gestire tempestivamente gli errori
- Ottieni statistiche dettagliate sull'utilizzo dei token
- Supporta attività complesse a lunga esecuzione

Comprendere questi tipi di eventi e analizzarli correttamente è essenziale per implementare un esecutore di intelligenza artificiale pienamente capace. Alla fine, nessuno vuole lavorare con una scatola nera.

## A proposito di HagiCode

La soluzione condivisa in questo articolo deriva dalla nostra esperienza pratica nel [HagiCode](https://hagicode.com) progetto. HagiCode è un progetto di assistente alla codifica AI open source dedicato a fornire agli sviluppatori supporto di codifica intelligente. Durante lo sviluppo, avevamo bisogno di creare un servizio di esecuzione AI affidabile per gestire le richieste di esecuzione del codice utente, che è il motivo diretto per cui abbiamo introdotto l'SDK Codex.

In qualità di assistente alla codifica AI, HagiCode deve gestire una varietà di scenari complessi di esecuzione del codice: ottenere progressi nell'esecuzione in tempo reale, gestire tempestivamente gli errori e raccogliere statistiche dettagliate sull'utilizzo dei token. Comprendendo a fondo il meccanismo del flusso di eventi dell'SDK del Codex, possiamo creare un esecutore che soddisfi i requisiti dell'ambiente di produzione. In definitiva, che si tratti di software o di vita reale, tutto beneficia di un costante accumulo e perfezionamento.

## Meccanismo di flusso di eventi

### Concetti di base

L'SDK del Codex utilizza il file `thread.runStreamed()` metodo per restituire un iteratore di eventi asincroni:

```typescript
import { Codex } from '@openai/codex-sdk';

const client = new Codex({
  apiKey: process.env.CODEX_API_KEY,
  baseUrl: process.env.CODEX_BASE_URL,
});

const thread = client.startThread({
  workingDirectory: '/path/to/project',
  skipGitRepoCheck: false,
});

const { events } = await thread.runStreamed('your prompt here', {
  outputSchema: {
    type: 'object',
    properties: {
      output: { type: 'string' },
      status: { type: 'string', enum: ['ok', 'action_required'] },
    },
    required: ['output', 'status'],
  },
});

for await (const event of events) {
  // Handle each event
}
```

### Spiegazione dettagliata dei tipi di eventi

| Tipo di evento | Descrizione | Dati chiave |
|---------|------|----------|
| `thread.started` | Discussione avviata con successo | `thread_id` |
| `item.updated` | Contenuto del messaggio aggiornato | `item.text` |
| `item.completed` | Messaggio completato | `item.text` |
| `turn.completed` | Esecuzione completata | `usage` (utilizzo del token) |
| `turn.failed` | Esecuzione non riuscita | `error.message` |
| `error` | Evento di errore | `message` |

Nei progetti reali, il componente esecutore di HagiCode è costruito su questi tipi di eventi. Dobbiamo gestire ogni tipo di evento con attenzione per garantire un'esperienza utente fluida. I buoni sistemi si costruiscono prendendo sul serio i dettagli.

## Implementazione dell'analisi dei messaggi

### Estrazione del contenuto del messaggio

Il contenuto del messaggio viene estratto tramite un gestore eventi:

```typescript
private handleThreadEvent(event: ThreadEvent, onMessage: (content: string) => void): void {
  // Only handle message update and completion events
  if (event.type !== 'item.updated' && event.type !== 'item.completed') {
    return;
  }

  // Only handle agent message content
  if (event.item.type !== 'agent_message') {
    return;
  }

  // Extract text content
  onMessage(event.item.text);
}
```

Punti chiave:
- Solo maneggiare `item.updated` e `item.completed` eventi
- Gestisci solo il contenuto di tipo `agent_message`
- Il contenuto del messaggio è nel file `event.item.text` campo

### Analisi dell'output strutturato

Codex supporta l'output strutturato JSON. È possibile specificare il formato di restituzione tramite il `outputSchema` parametro:

```typescript
const DEFAULT_OUTPUT_SCHEMA = {
  type: 'object',
  properties: {
    output: { type: 'string' },
    status: { type: 'string', enum: ['ok', 'action_required'] },
  },
  required: ['output', 'status'],
  additionalProperties: false,
} as const;
```

La funzione di analisi tenta di analizzare JSON e, se fallisce, torna al testo non elaborato.

```typescript
function toStructuredOutput(raw: string): StructuredOutput {
  try {
    const parsed = JSON.parse(raw) as Partial<StructuredOutput>;
    if (typeof parsed.output === 'string') {
      return {
        output: parsed.output,
        status: parsed.status === 'action_required' ? 'action_required' : 'ok',
      };
    }
  } catch {
    // JSON parsing failed, fall back to the raw text
  }

  return {
    output: raw,
    status: 'ok',
  };
}
```

### Completare il flusso di gestione degli eventi

```typescript
private async runWithStreaming(
  thread: Thread,
  input: CodexStageExecutionInput
): Promise<{ output: string; usage: Usage | null }> {
  const abortController = new AbortController();
  const timeoutHandle = setTimeout(() => {
    abortController.abort();
  }, Math.max(1000, input.timeoutMs));

  let latestMessage = '';
  let usage: Usage | null = null;
  let emittedLength = 0;

  try {
    const { events } = await thread.runStreamed(input.prompt, {
      outputSchema: DEFAULT_OUTPUT_SCHEMA,
      signal: abortController.signal,
    });

    for await (const event of events) {
      // Handle message content
      this.handleThreadEvent(event, (nextContent) => {
        const delta = nextContent.slice(emittedLength);
        if (delta.length > 0) {
          emittedLength = nextContent.length;
          input.callbacks?.onChunk?.(delta);  // Streaming callback
        }
        latestMessage = nextContent;
      });

      // Process different data based on the event type
      if (event.type === 'thread.started') {
        this.threadId = event.thread_id;
      } else if (event.type === 'turn.completed') {
        usage = event.usage;
      } else if (event.type === 'turn.failed') {
        throw new CodexExecutorError('gateway_unavailable', event.error.message, true);
      } else if (event.type === 'error') {
        throw new CodexExecutorError('gateway_unavailable', event.message, true);
      }
    }
  } catch (error) {
    if (abortController.signal.aborted) {
      throw new CodexExecutorError(
        'upstream_timeout',
        `Codex stage timed out after ${input.timeoutMs}ms`,
        true
      );
    }
    throw error;
  } finally {
    clearTimeout(timeoutHandle);
  }

  const structured = toStructuredOutput(latestMessage);
  return { output: structured.output, usage };
}
```

## Strategia di gestione degli errori

### Mappatura dei codici di errore

Mappa modelli di errore specifici su codici di errore concreti in modo che i livelli superiori possano gestirli più facilmente:

```typescript
function mapError(error: unknown): CodexExecutorError {
  if (error instanceof CodexExecutorError) {
    return error;
  }

  const message = error instanceof Error ? error.message : String(error);
  const normalized = message.toLowerCase();

  // Authentication errors - not retryable
  if (normalized.includes('401') ||
      normalized.includes('403') ||
      normalized.includes('api key') ||
      normalized.includes('auth')) {
    return new CodexExecutorError('auth_invalid', message, false);
  }

  // Rate limit errors - retryable
  if (normalized.includes('429') || normalized.includes('rate limit')) {
    return new CodexExecutorError('rate_limited', message, true);
  }

  // Timeout errors - retryable
  if (normalized.includes('timeout') || normalized.includes('aborted')) {
    return new CodexExecutorError('upstream_timeout', message, true);
  }

  // Default error
  return new CodexExecutorError('gateway_unavailable', message, true);
}
```

### Definizioni del tipo di errore

```typescript
export type CodexErrorCode =
  | 'auth_invalid'      // Authentication failure
  | 'upstream_timeout'  // Upstream timeout
  | 'rate_limited'      // Rate limited
  | 'gateway_unavailable'; // Gateway unavailable

export class CodexExecutorError extends Error {
  readonly code: CodexErrorCode;
  readonly retryable: boolean;

  constructor(code: CodexErrorCode, message: string, retryable: boolean) {
    super(message);
    this.name = 'CodexExecutorError';
    this.code = code;
    this.retryable = retryable;
  }
}
```

## Directory di lavoro e configurazione dell'ambiente

### Convalida della directory di lavoro

L'SDK del Codex richiede che la directory di lavoro sia un repository Git valido.

```typescript
export function validateWorkingDirectory(
  workingDirectory: string,
  skipGitRepoCheck: boolean
): void {
  const resolvedWorkingDirectory = path.resolve(workingDirectory);

  if (!existsSync(resolvedWorkingDirectory)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory does not exist.',
      false
    );
  }

  if (!statSync(resolvedWorkingDirectory).isDirectory()) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a directory.',
      false
    );
  }

  if (skipGitRepoCheck) {
    return;
  }

  const gitDir = path.join(resolvedWorkingDirectory, '.git');
  if (!existsSync(gitDir)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a git repository.',
      false
    );
  }
}
```

### Caricamento delle variabili d'ambiente

L'SDK del Codex deve caricare le variabili di ambiente dalla shell di accesso in modo che l'agente AI possa accedere ai comandi di sistema:

```typescript
function parseEnvironmentOutput(output: Buffer): Record<string, string> {
  const parsed: Record<string, string> = {};

  for (const entry of output.toString('utf8').split('\0')) {
    if (!entry) continue;

    const separatorIndex = entry.indexOf('=');
    if (separatorIndex <= 0) continue;

    const key = entry.slice(0, separatorIndex);
    const value = entry.slice(separatorIndex + 1);
    if (key.length > 0) {
      parsed[key] = value;
    }
  }

  return parsed;
}

function tryLoadEnvironmentFromShell(shellPath: string): Record<string, string> | null {
  const result = spawnSync(shellPath, ['-ilc', 'env -0'], {
    env: process.env,
    stdio: ['ignore', 'pipe', 'pipe'],
    timeout: 5000,
  });

  if (result.error || result.status !== 0) {
    return null;
  }

  return parseEnvironmentOutput(result.stdout);
}

export function createExecutorEnvironment(
  envOverrides: Record<string, string> = {}
): Record<string, string> {
  // Load environment variables from the login shell
  const consoleEnv = loadConsoleEnvironmentFromShell();

  return {
    ...process.env,
    ...consoleEnv,
    ...envOverrides,
  };
}
```

## Esempio di utilizzo completo

### Utilizzo di base

Nel progetto HagiCode, utilizziamo il seguente approccio per inizializzare il client Codex ed eseguire attività:

```typescript
import { Codex } from '@openai/codex-sdk';

async function executeWithCodex(prompt: string, workingDir: string) {
  const client = new Codex({
    apiKey: process.env.CODEX_API_KEY,
    env: { PATH: process.env.PATH },
  });

  const thread = client.startThread({
    workingDirectory: workingDir,
  });

  const { events } = await thread.runStreamed(prompt);

  let result = '';
  for await (const event of events) {
    if (event.type === 'item.updated' && event.item.type === 'agent_message') {
      result = event.item.text;
    }
    if (event.type === 'turn.completed') {
      console.log('Token usage:', event.usage);
    }
  }

  // Try to parse JSON output
  try {
    const parsed = JSON.parse(result);
    return parsed.output;
  } catch {
    return result;
  }
}
```

### Completa l'implementazione con nuovi tentativi

```typescript
export class CodexSdkExecutor {
  private readonly config: CodexRuntimeConfig;
  private readonly client: Codex;
  private threadId: string | null = null;

  async executeStage(input: CodexStageExecutionInput): Promise<CodexStageExecutionResult> {
    const maxAttempts = Math.max(1, this.config.retryCount + 1);
    let attempt = 0;
    let lastError: CodexExecutorError | null = null;

    while (attempt < maxAttempts) {
      attempt += 1;

      try {
        const thread = this.getThread(input.workingDirectory);
        const { output, usage } = await this.runWithStreaming(thread, input);

        return {
          output,
          usage,
          threadId: this.threadId!,
          attempts: attempt,
          latencyMs: Date.now() - startedAt,
        };
      } catch (error) {
        const mappedError = mapError(error);
        lastError = mappedError;

        // Non-retryable error or max retry attempts reached
        if (!mappedError.retryable || attempt >= maxAttempts) {
          throw mappedError;
        }

        // Wait before retrying
        await new Promise(resolve => setTimeout(resolve, 1000 * attempt));
      }
    }

    throw lastError!;
  }
}
```

## Migliori pratiche

### 1. Requisiti della directory di lavoro

- Assicurati che la directory di lavoro sia un repository Git valido
- Usa il `PROJECT_ROOT` variabile di ambiente per specificarlo esplicitamente
- Durante il debug è possibile impostare `CODEX_SKIP_GIT_REPO_CHECK=true` per saltare il controllo

### 2. Configurazione della variabile d'ambiente

- Passa solo le variabili di ambiente richieste attraverso un meccanismo di whitelist
- Utilizzare la shell di accesso per caricare l'ambiente completo
- Evita di trasmettere informazioni sensibili

### 3. Strategia di timeout e nuovo tentativo

- Imposta timeout ragionevoli in base alla complessità dell'attività
- Implementare il backoff esponenziale per gli errori ripetibili
- Registrare il numero e i motivi dei tentativi

### 4. Gestione degli errori

- Distinguere tra errori riprovabili e irreversibili
- Fornire messaggi di errore e suggerimenti chiari
- Utilizza codici di errore unificati in modo che i livelli superiori possano gestirli in modo coerente

### 5. Uscita in streaming

- Implementa callback di output incrementali per migliorare l'esperienza dell'utente
- Gestire correttamente gli aggiornamenti incrementali dei messaggi
- Registra l'utilizzo dei token per l'analisi dei costi

Nell'ambiente di produzione reale del progetto HagiCode, abbiamo già verificato l'efficacia delle migliori pratiche di cui sopra. Questo approccio ci ha aiutato a creare un servizio di esecuzione AI stabile e affidabile. Alla fine, la validazione pratica conta più della sola teoria.

## Conclusione

Il meccanismo del flusso di eventi dell'SDK del Codex offre potenti funzionalità per la creazione di servizi di esecuzione AI. Analizzando correttamente diversi tipi di eventi, possiamo:

- Ottieni lo stato di esecuzione e l'output in tempo reale
- Implementare meccanismi affidabili di gestione degli errori e di nuovi tentativi
- Ottieni statistiche di esecuzione dettagliate
- Costruisci una piattaforma di esecuzione AI completa di tutte le funzionalità

I concetti fondamentali e gli esempi di codice introdotti in questo articolo possono essere applicati direttamente in progetti reali, aiutando gli sviluppatori a iniziare rapidamente con l'integrazione dell'SDK Codex. Se trovi prezioso questo approccio, riflette anche la forza della pratica ingegneristica di HagiCode e rende HagiCode stesso degno di essere seguito.

## Riferimenti

- [Documentazione ufficiale dell'SDK del Codex](https://platform.openai.com/docs/codex)
- [Repository GitHub di HagiCode](https://github.com/HagiCode-org/site)
- [Sito ufficiale di HagiCode](https://hagicode.com)

---

Grazie per aver letto Se hai trovato utile questo articolo, fai clic sul pulsante Mi piace qui sotto in modo che più persone possano scoprirlo.

Questo contenuto è stato creato con la collaborazione assistita dall'intelligenza artificiale, rivisto dall'autore e riflette le opinioni e le posizioni dell'autore.

- Autore: [newbe36524](https://www.newbe.pro)
- Collegamento all'articolo: [https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/](https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/)
- Avviso sul copyright: se non diversamente specificato, tutti gli articoli su questo blog sono concessi in licenza da BY-NC-SA. Si prega di citare la fonte in caso di ristampa.

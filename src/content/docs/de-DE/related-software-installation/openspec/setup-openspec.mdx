---
title: Installieren Sie OpenSpec
description: Dieser Leitfaden hilft Ihnen bei der Installation des spezifikationsgesteuerten Entwicklungstools OpenSpec.
sidebar_position: 10
---

import { Tabs, TabItem } from '@/components/starlight';
import { LinkCard, CardGrid } from '@astrojs/starlight/components';

# Installieren Sie OpenSpec

OpenSpec verwaltet Vorschläge, Spezifikationen und Änderungsaufzeichnungen. Diese Seite verfolgt ein Ziel: Erklären Sie den Installationspfad klar, damit Sie ihn in Ihren bestehenden Arbeitsablauf integrieren können.

## Was ist OpenSpec?

OpenSpec ist ein Befehlszeilentool zum Verwalten von Vorschlägen und Spezifikationen in der Softwarearbeit. Der Kernworkflow umfasst:

- **Vorschlagsverwaltung**: Funktionsvorschläge erstellen, überprüfen und verfolgen
- **Spezifikationsvalidierung**: Stellen Sie sicher, dass Codeänderungen den vordefinierten Spezifikationen entsprechen
- **Änderungsverfolgung**: Zeichnen Sie den gesamten überprüften Änderungsverlauf auf
- **Teamzusammenarbeit**: Unterstützen Sie die kollaborative Entwicklung mehrerer Personen

### Anwendung in HagiCode-Projekten

In HagiCode-Projekten wird OpenSpec verwendet für:

1. **Funktionsentwicklungsmanagement**: Alle neuen Funktionen müssen den OpenSpec-Vorschlagsprozess durchlaufen
2. **Codeüberprüfung**: Stellen Sie sicher, dass Änderungen den Projektspezifikationen entsprechen
3. **Dokumentationssynchronisierung**: Bewahren Sie die Konsistenz zwischen Dokumentation und Codeimplementierung
4. **Integration der Versionskontrolle**: Nahtlose Integration mit Versionskontroll-Workflows

## Voraussetzungen

Stellen Sie vor der Installation von OpenSpec sicher, dass Ihr System die folgenden Anforderungen erfüllt:

### Node.js und npm

OpenSpec wird über npm verteilt und erfordert eine Node.js-Umgebung. OpenSpec erfordert **Node.js 18.0 oder höher**.

Überprüfen Sie die Node.js-Installation:

```bash
node --version
```

Überprüfen Sie die NPM-Installation:

```bash
npm --version
```

Wenn die Version nicht installiert ist oder zu alt ist, lesen Sie bitte die entsprechenden Installationsanweisungen für die Software im [Installationsanleitung](/de-DE/installation).

## OpenSpec installieren

:::tip[Versionsanforderungen]
Für dieses Projekt ist die Verwendung des Versionsbereichs **OpenSpec 1.0.0 oder höher** erforderlich. Alle 1.x-Versionen werden unterstützt, um die Kompatibilität mit dem Projektworkflow sicherzustellen.
:::

OpenSpec wird global über npm installiert und ist somit von überall auf Ihrem System verfügbar.

### Global mit npm installieren

<Tabs>
  <TabItem value="win" label="Windows">
    In PowerShell oder Eingabeaufforderung ausführen:

```powershell
npm install -g @fission-ai/openspec@1
```

:::note[Windows-Berechtigungsprobleme]
Wenn während der Installation Berechtigungsfehler auftreten, führen Sie PowerShell oder die Eingabeaufforderung als Administrator aus.
:::
  </TabItem>
  <TabItem value="mac" label="macOS">
    Im Terminal ausführen:

```bash
npm install -g @fission-ai/openspec@1
```

:::note[macOS-Berechtigungsprobleme]
Wenn Sie stoßen `EACCES` Berechtigungsfehler, versuchen Sie Folgendes:
```bash
sudo npm install -g @fission-ai/openspec@1
```
Oder verwenden [nvm](https://github.com/nvm-sh/nvm) um Node.js-Versionen zu verwalten und Berechtigungsprobleme zu vermeiden.
:::
  </TabItem>
  <TabItem value="linux" label="Linux">
    Im Terminal ausführen:

```bash
npm install -g @fission-ai/openspec@1
```

:::note[Linux-Berechtigungsprobleme]
Wenn Sie stoßen `EACCES` Berechtigungsfehler können Sie:
1. Benutzen `sudo`: `sudo npm install -g @fission-ai/openspec@1`
2. Konfigurieren Sie npm für die Verwendung des Benutzerverzeichnisses:
```bash
mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
```
:::
  </TabItem>
</Tabs>

### Überprüfen Sie die Installation

Überprüfen Sie nach der Installation, ob OpenSpec korrekt installiert ist und die Version den Anforderungen entspricht:

```bash
openspec --version
```

Bei erfolgreicher Installation wird eine Versionsnummer ausgegeben, zum Beispiel:

```
openspec/1.1.1 linux-x64 node-v18.17.0
```

:::note[Versionsbereich]
Bitte stellen Sie sicher, dass die installierte OpenSpec-Version 1.0.0 oder höher ist. Das Projekt hat die Kompatibilität mit Version 1.1.1 überprüft. Es wird empfohlen, die neueste kompatible Version zu verwenden, um neue Funktionen zu erhalten.
:::

## Häufige Probleme und Fehlerbehebung

### Berechtigungsfehler

#### Problem: `EACCES` Erlaubnis verweigert

Beim Installieren oder Ausführen von OpenSpec treten Berechtigungsfehler auf.

**Lösungen**:

<Tabs>
  <TabItem value="win" label="Windows">
    Führen Sie PowerShell oder die Eingabeaufforderung als Administrator aus:
    1. Klicken Sie mit der rechten Maustaste auf PowerShell oder Eingabeaufforderung
    2. Wählen Sie „Als Administrator ausführen“
    3. Führen Sie den Installationsbefehl erneut aus
  </TabItem>
  <TabItem value="mac" label="macOS">
    Installieren Sie mit `sudo`:

```bash
sudo npm install -g @fission-ai/openspec@1
```

Oder verwenden [nvm](https://github.com/nvm-sh/nvm) um Node.js zu verwalten und Probleme mit globalen Installationsberechtigungen zu vermeiden.
  </TabItem>
  <TabItem value="linux" label="Linux">
    Lösung 1: Verwenden Sie sudo
```bash
sudo npm install -g @fission-ai/openspec@1
```

Lösung 2: Konfigurieren Sie npm für die Verwendung des Benutzerverzeichnisses
```bash
mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
npm install -g @fission-ai/openspec@1
```
  </TabItem>
</Tabs>

### Netzwerkprobleme

#### Problem: Die npm-Installation schlägt fehl oder ist sehr langsam

Aus Netzwerkgründen kann OpenSpec nicht aus der NPM-Registrierung heruntergeladen werden.

**Lösungen**:

1. **Inländischen npm Mirror verwenden**:

```bash
# Use Taobao mirror
npm config set registry https://registry.npmmirror.com

# Then reinstall
npm install -g @fission-ai/openspec@1
```

2. **Offizielle Quelle wiederherstellen** (falls erforderlich):

```bash
npm config set registry https://registry.npmjs.org
```

3. **CNPM verwenden** (optional):

```bash
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install -g @fission-ai/openspec@1
```

:::Tipp
Benutzern auf dem chinesischen Festland wird empfohlen, den Taobao-Spiegel zu verwenden, der die Download-Geschwindigkeit erheblich verbessern kann.
:::

### Probleme mit der Versionskompatibilität

#### Problem: OpenSpec-Befehle werden nicht ordnungsgemäß ausgeführt oder es werden Fehler gemeldet

Die installierte OpenSpec-Version ist nicht mit der aktuellen Umgebung kompatibel.

**Lösungen**:

1. **Node.js-Version überprüfen**:

```bash
node --version
```

Stellen Sie sicher, dass die Version 18.0 oder höher ist. Wenn die Version zu niedrig ist, aktualisieren Sie bitte Node.js.

2. **OpenSpec-Version prüfen**:

```bash
openspec --version
```

3. **Bestimmte Version neu installieren**:

```bash
npm install -g @fission-ai/openspec@1
```

4. **Npm-Cache leeren** (falls das Problem weiterhin besteht):

```bash
npm cache clean --force
npm install -g @fission-ai/openspec@1
```

### Befehl nicht gefunden

#### Problem: Tippen `openspec` Befehl zeigt „Befehl nicht gefunden“ an

OpenSpec ist installiert, aber das System kann den Befehl nicht finden.

**Lösungen**:

<Tabs>
  <TabItem value="win" label="Windows">
    1. Bestätigen Sie den globalen Installationspfad von npm:

```powershell
npm config get prefix
```

2. Pfad zum Systempfad hinzufügen:
   - Öffnen Sie „Systemeigenschaften“ → „Erweitert“ → „Umgebungsvariablen“
   - Finden `Path` in „Systemvariablen“
   - Fügen Sie den globalen NPM-Pfad hinzu (normalerweise `C:\Users\<username>\AppData\Roaming\npm`)
   - Starten Sie das Befehlszeilenfenster neu
  </TabItem>
  <TabItem value="mac" label="macOS">
    1. Bestätigen Sie den globalen Installationspfad von npm:

```bash
npm config get prefix
```

2. Pfad zu PATH hinzufügen (in `~/.zshrc` oder `~/.bash_profile`):

```bash
export PATH="$(npm config get prefix)/bin:$PATH"
```

3. Konfiguration neu laden:

```bash
source ~/.zshrc
```
  </TabItem>
  <TabItem value="linux" label="Linux">
    1. Bestätigen Sie den globalen Installationspfad von npm:

```bash
npm config get prefix
```

2. Pfad zu PATH hinzufügen (in `~/.bashrc`):

```bash
export PATH="$(npm config get prefix)/bin:$PATH"
```

3. Konfiguration neu laden:

```bash
source ~/.bashrc
```
  </TabItem>
</Tabs>

## OpenSpec aktualisieren

:::note[Versionsbereich]
Für dieses Projekt ist die Verwendung von OpenSpec 1.0.0 oder einem höheren Versionsbereich erforderlich. Wenn Sie Anforderungen an eine Versionsaktualisierung haben, lesen Sie bitte die Projektdokumentation.
:::

Wenn Sie eine bestimmte Version neu installieren müssen:

```bash
npm install -g @fission-ai/openspec@1
```

## OpenSpec deinstallieren

Wenn Sie OpenSpec deinstallieren müssen:

<Tabs>
  <TabItem value="win" label="Windows">

```powershell
npm uninstall -g @fission-ai/openspec
```
  </TabItem>
  <TabItem value="mac" label="macOS">

```bash
npm uninstall -g @fission-ai/openspec
```

Oder verwenden Sie sudo:

```bash
sudo npm uninstall -g @fission-ai/openspec
```
  </TabItem>
  <TabItem value="linux" label="Linux">

```bash
npm uninstall -g @fission-ai/openspec
```

Oder verwenden Sie sudo:

```bash
sudo npm uninstall -g @fission-ai/openspec
```
  </TabItem>
</Tabs>

## Nächste Schritte

Nachdem die OpenSpec-Installation abgeschlossen ist, können Sie mit den folgenden Schritten fortfahren:

<CardGrid>

<LinkCard title="Assistent zum Einrichten" href="/de-DE/quick-start/wizard-setup"
    Beschreibung="@@TR_1959@@"
/>

<LinkCard title="Erstellen Sie eine Angebotssitzung" href="/de-DE/quick-start/proposal-session"
    Beschreibung="@@TR_1961@@"
/>

</CardGrid>

:::Tipp
Ausführlichere OpenSpec-Nutzungshandbücher finden Sie im [Offizielle OpenSpec-Dokumentation](https://github.com/fission-ai/openspec).
:::

## Benötigen Sie weitere Hilfe?

Wenn Sie auf Probleme stoßen, die hier nicht behandelt werden:

1. Überprüfen [OpenSpec GitHub-Probleme](https://github.com/fission-ai/openspec/issues) für ähnliche Probleme
2. Besuchen Sie [Offizielle OpenSpec-Dokumentation](https://github.com/fission-ai/openspec) für weitere Informationen
3. Weitere Informationen finden Sie in den entsprechenden Anleitungen [HagiCode-Dokumentationsseite](/de-DE/product-overview) für zusätzliche Hilfe

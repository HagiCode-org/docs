---
title: codex sdk console message parsing
date: 2026-03-09
tags: [hagicode]
---

I apologize for the difficulty. The security sandbox is preventing me from writing files to the target directory. Let me provide you with the complete Spanish translation that you can save manually:

**File path:** `src/content/docs/es-ES/blog/2026-03-09-codex-sdk-console-message-parsing.mdx`

**Content:**

```markdown
---
title: Guía completa de análisis de mensajes de consola de Codex SDK
date: 2026-03-10
tags: [Codex SDK, TypeScript, AI Executor, Event Stream]
description: "Este artículo presenta en detalle el mecanismo de flujo de eventos de Codex SDK, el análisis de tipos de mensajes y las mejores prácticas en proyectos reales, ayudando a los desarrolladores a dominar rápidamente las habilidades principales del servicio de ejecución de AI."
---

## Guía completa de análisis de mensajes de consola de Codex SDK

> Este artículo presenta en detalle el mecanismo de flujo de eventos de Codex SDK, el análisis de tipos de mensajes y las mejores prácticas en proyectos reales, ayudando a los desarrolladores a dominar rápidamente las habilidades principales del servicio de ejecución de AI.

## Antecedentes

De hecho, al construir un servicio de ejecución de AI basado en Codex SDK, tenemos que enfrentar un problema: cómo manejar esos mensajes de eventos en flujo que devuelve Codex. Estos mensajes contienen información importante como el estado de ejecución, el contenido de salida y la información de errores, al igual que esos pensamientos indecibles de la juventud, debes reflexionar cuidadosamente sobre ellos.

Como parte del proyecto HagiCode, necesitamos implementar un ejecutor confiable en el escenario de asistente de código AI. Esta es aproximadamente la razón por la que decidimos investigar profundamente el mecanismo de flujo de eventos de Codex SDK: después de todo, solo entendiendo cómo funcionan los mensajes subyacentes, podemos construir una plataforma de ejecución de AI verdaderamente empresarial. Es como el amor, si no entiendes los pensamientos del otro, ¿cómo seguir adelante?

Codex SDK es el SDK de herramientas de asistencia de programación lanzado por OpenAI, que devuelve resultados de ejecución a través de flujos de eventos (Event Stream). A diferencia del modo tradicional de solicitud-respuesta, Codex utiliza eventos en flujo, lo que nos permite:

- Obtener el progreso de ejecución en tiempo real
- Manejar situaciones de error oportunamente
- Obtener estadísticas detalladas de uso de tokens
- Admitir tareas complejas de larga duración

Comprender estos tipos de eventos y analizarlos correctamente es bastante importante para implementar un ejecutor de AI con funciones completas. Después de todo, nadie quiere enfrentarse a una caja negra.

## Sobre HagiCode

La solución compartida en este artículo proviene de nuestra experiencia práctica en el proyecto [HagiCode](https://hagicode.com). HagiCode es un proyecto de asistente de código AI de código abierto, comprometido a proporcionar capacidades de asistencia de código inteligente para los desarrolladores. Durante el desarrollo, necesitamos construir un servicio de ejecución de AI confiable para manejar las solicitudes de ejecución de código de los usuarios, que es la razón directa por la que introdujimos Codex SDK.

Como asistente de código AI, HagiCode necesita manejar varios escenarios complejos de ejecución de código: obtener el progreso de ejecución en tiempo real, manejar situaciones de error oportunamente, obtener estadísticas detalladas de uso de tokens, etc. Al comprender profundamente el mecanismo de flujo de eventos de Codex SDK, podemos construir ejecutores que cumplan con los requisitos del entorno de producción. En última instancia, tanto el código como la vida necesitan un poco de acumulación y沉淀.

## Mecanismo de flujo de eventos

### Conceptos básicos

Codex SDK utiliza el método `thread.runStreamed()` para devolver un iterador de eventos asíncrono:

```typescript
import { Codex } from '@openai/codex-sdk';

const client = new Codex({
  apiKey: process.env.CODEX_API_KEY,
  baseUrl: process.env.CODEX_BASE_URL,
});

const thread = client.startThread({
  workingDirectory: '/path/to/project',
  skipGitRepoCheck: false,
});

const { events } = await thread.runStreamed('your prompt here', {
  outputSchema: {
    type: 'object',
    properties: {
      output: { type: 'string' },
      status: { type: 'string', enum: ['ok', 'action_required'] },
    },
    required: ['output', 'status'],
  },
});

for await (const event of events) {
  // 处理每个事件
}
```

### Explicación detallada de tipos de eventos

| Tipo de evento | Descripción | Datos clave |
|---------|------|----------|
| `thread.started` | Inicio exitoso del hilo | `thread_id` |
| `item.updated` | Actualización del contenido del mensaje | `item.text` |
| `item.completed` | Mensaje completado | `item.text` |
| `turn.completed` | Ejecución completada | `usage` (uso de tokens) |
| `turn.failed` | Ejecución fallida | `error.message` |
| `error` | Evento de error | `message` |

En proyectos reales, el componente ejecutor de HagiCode se construye precisamente sobre estos tipos de eventos. Necesitamos procesar cada tipo de evento de manera refinada para garantizar la fluidez de la experiencia del usuario. Es como tratar una relación, cada detalle debe ser tratado con cuidado, de lo contrario ¿cómo puede haber un buen resultado?

## Implementación del análisis de mensajes

### Extracción del contenido del mensaje

El contenido del mensaje se extrae a través de la función de manejo de eventos:

```typescript
private handleThreadEvent(event: ThreadEvent, onMessage: (content: string) => void): void {
  // 只处理消息更新和完成事件
  if (event.type !== 'item.updated' && event.type !== 'item.completed') {
    return;
  }

  // 只处理代理消息类型
  if (event.item.type !== 'agent_message') {
    return;
  }

  // 提取文本内容
  onMessage(event.item.text);
}
```

Puntos clave:
- Solo procesar eventos `item.updated` y `item.completed`
- Solo procesar contenido de tipo `agent_message`
- El contenido del mensaje está en el campo `event.item.text`

### Análisis de salida estructurada

Codex admite salida estructurada JSON, especificando el formato de retorno a través del parámetro `outputSchema`:

```typescript
const DEFAULT_OUTPUT_SCHEMA = {
  type: 'object',
  properties: {
    output: { type: 'string' },
    status: { type: 'string', enum: ['ok', 'action_required'] },
  },
  required: ['output', 'status'],
  additionalProperties: false,
} as const;
```

La función de análisis intentará analizar JSON, y si falla, devolverá el texto original: esto es como la vida, a veces quieres una respuesta perfecta, pero la realidad a menudo te da una respuesta vaga, solo puedes digerirla lentamente por tu cuenta.

```typescript
function toStructuredOutput(raw: string): StructuredOutput {
  try {
    const parsed = JSON.parse(raw) as Partial<StructuredOutput>;
    if (typeof parsed.output === 'string') {
      return {
        output: parsed.output,
        status: parsed.status === 'action_required' ? 'action_required' : 'ok',
      };
    }
  } catch {
    // JSON 解析失败，回退到原始文本
  }

  return {
    output: raw,
    status: 'ok',
  };
}
```

### Flujo completo de manejo de eventos

```typescript
private async runWithStreaming(
  thread: Thread,
  input: CodexStageExecutionInput
): Promise<{ output: string; usage: Usage | null }> {
  const abortController = new AbortController();
  const timeoutHandle = setTimeout(() => {
    abortController.abort();
  }, Math.max(1000, input.timeoutMs));

  let latestMessage = '';
  let usage: Usage | null = null;
  let emittedLength = 0;

  try {
    const { events } = await thread.runStreamed(input.prompt, {
      outputSchema: DEFAULT_OUTPUT_SCHEMA,
      signal: abortController.signal,
    });

    for await (const event of events) {
      // 处理消息内容
      this.handleThreadEvent(event, (nextContent) => {
        const delta = nextContent.slice(emittedLength);
        if (delta.length > 0) {
          emittedLength = nextContent.length;
          input.callbacks?.onChunk?.(delta);  // 流式回调
        }
        latestMessage = nextContent;
      });

      // 根据事件类型处理不同数据
      if (event.type === 'thread.started') {
        this.threadId = event.thread_id;
      } else if (event.type === 'turn.completed') {
        usage = event.usage;
      } else if (event.type === 'turn.failed') {
        throw new CodexExecutorError('gateway_unavailable', event.error.message, true);
      } else if (event.type === 'error') {
        throw new CodexExecutorError('gateway_unavailable', event.message, true);
      }
    }
  } catch (error) {
    if (abortController.signal.aborted) {
      throw new CodexExecutorError(
        'upstream_timeout',
        `Codex stage timed out after ${input.timeoutMs}ms`,
        true
      );
    }
    throw error;
  } finally {
    clearTimeout(timeoutHandle);
  }

  const structured = toStructuredOutput(latestMessage);
  return { output: structured.output, usage };
}
```

## Estrategias de manejo de errores

### Mapeo de códigos de error

Mapear a códigos de error específicos según las características del error para facilitar el manejo de nivel superior:

```typescript
function mapError(error: unknown): CodexExecutorError {
  if (error instanceof CodexExecutorError) {
    return error;
  }

  const message = error instanceof Error ? error.message : String(error);
  const normalized = message.toLowerCase();

  // 认证错误 - 不可重试
  if (normalized.includes('401') ||
      normalized.includes('403') ||
      normalized.includes('api key') ||
      normalized.includes('auth')) {
    return new CodexExecutorError('auth_invalid', message, false);
  }

  // 速率限制 - 可重试
  if (normalized.includes('429') || normalized.includes('rate limit')) {
    return new CodexExecutorError('rate_limited', message, true);
  }

  // 超时错误 - 可重试
  if (normalized.includes('timeout') || normalized.includes('aborted')) {
    return new CodexExecutorError('upstream_timeout', message, true);
  }

  // 默认错误
  return new CodexExecutorError('gateway_unavailable', message, true);
}
```

### Definición de tipos de error

```typescript
export type CodexErrorCode =
  | 'auth_invalid'      // 认证失败
  | 'upstream_timeout'  // 上游超时
  | 'rate_limited'      // 速率限制
  | 'gateway_unavailable'; // 网关不可用

export class CodexExecutorError extends Error {
  readonly code: CodexErrorCode;
  readonly retryable: boolean;

  constructor(code: CodexErrorCode, message: string, retryable: boolean) {
    super(message);
    this.name = 'CodexExecutorError';
    this.code = code;
    this.retryable = retryable;
  }
}
```

## Configuración del directorio de trabajo y del entorno

### Validación del directorio de trabajo

Codex SDK requiere que el directorio de trabajo sea un repositorio Git válido: esto es como ser una persona, siempre necesitas tener raíces, un origen, de lo contrario ¿cómo puedes sentirte seguro?

```typescript
export function validateWorkingDirectory(
  workingDirectory: string,
  skipGitRepoCheck: boolean
): void {
  const resolvedWorkingDirectory = path.resolve(workingDirectory);

  if (!existsSync(resolvedWorkingDirectory)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory does not exist.',
      false
    );
  }

  if (!statSync(resolvedWorkingDirectory).isDirectory()) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a directory.',
      false
    );
  }

  if (skipGitRepoCheck) {
    return;
  }

  const gitDir = path.join(resolvedWorkingDirectory, '.git');
  if (!existsSync(gitDir)) {
    throw new CodexExecutorError(
      'gateway_unavailable',
      'Working directory is not a git repository.',
      false
    );
  }
}
```

### Carga de variables de entorno

Codex SDK necesita cargar variables de entorno desde el Shell de inicio de sesión para garantizar que el AI Agent pueda acceder a los comandos del sistema:

```typescript
function parseEnvironmentOutput(output: Buffer): Record<string, string> {
  const parsed: Record<string, string> = {};

  for (const entry of output.toString('utf8').split('\0')) {
    if (!entry) continue;

    const separatorIndex = entry.indexOf('=');
    if (separatorIndex <= 0) continue;

    const key = entry.slice(0, separatorIndex);
    const value = entry.slice(separatorIndex + 1);
    if (key.length > 0) {
      parsed[key] = value;
    }
  }

  return parsed;
}

function tryLoadEnvironmentFromShell(shellPath: string): Record<string, string> | null {
  const result = spawnSync(shellPath, ['-ilc', 'env -0'], {
    env: process.env,
    stdio: ['ignore', 'pipe', 'pipe'],
    timeout: 5000,
  });

  if (result.error || result.status !== 0) {
    return null;
  }

  return parseEnvironmentOutput(result.stdout);
}

export function createExecutorEnvironment(
  envOverrides: Record<string, string> = {}
): Record<string, string> {
  // 加载登录 Shell 环境变量
  const consoleEnv = loadConsoleEnvironmentFromShell();

  return {
    ...process.env,
    ...consoleEnv,
    ...envOverrides,
  };
}
```

## Ejemplo de uso completo

### Uso básico

En el proyecto HagiCode, utilizamos la siguiente manera para inicializar el cliente Codex y ejecutar tareas:

```typescript
import { Codex } from '@openai/codex-sdk';

async function executeWithCodex(prompt: string, workingDir: string) {
  const client = new Codex({
    apiKey: process.env.CODEX_API_KEY,
    env: { PATH: process.env.PATH },
  });

  const thread = client.startThread({
    workingDirectory: workingDir,
  });

  const { events } = await thread.runStreamed(prompt);

  let result = '';
  for await (const event of events) {
    if (event.type === 'item.updated' && event.item.type === 'agent_message') {
      result = event.item.text;
    }
    if (event.type === 'turn.completed') {
      console.log('Token usage:', event.usage);
    }
  }

  // 尝试解析 JSON 输出
  try {
    const parsed = JSON.parse(result);
    return parsed.output;
  } catch {
    return result;
  }
}
```

### Implementación completa con mecanismo de reintento

```typescript
export class CodexSdkExecutor {
  private readonly config: CodexRuntimeConfig;
  private readonly client: Codex;
  private threadId: string | null = null;

  async executeStage(input: CodexStageExecutionInput): Promise<CodexStageExecutionResult> {
    const maxAttempts = Math.max(1, this.config.retryCount + 1);
    let attempt = 0;
    let lastError: CodexExecutorError | null = null;

    while (attempt < maxAttempts) {
      attempt += 1;

      try {
        const thread = this.getThread(input.workingDirectory);
        const { output, usage } = await this.runWithStreaming(thread, input);

        return {
          output,
          usage,
          threadId: this.threadId!,
          attempts: attempt,
          latencyMs: Date.now() - startedAt,
        };
      } catch (error) {
        const mappedError = mapError(error);
        lastError = mappedError;

        // 不可重试错误或已达最大重试次数
        if (!mappedError.retryable || attempt >= maxAttempts) {
          throw mappedError;
        }

        // 等待后重试
        await new Promise(resolve => setTimeout(resolve, 1000 * attempt));
      }
    }

    throw lastError!;
  }
}
```

## Mejores prácticas

### 1. Requisitos del directorio de trabajo

- Asegúrese de que el directorio de trabajo sea un repositorio Git válido
- Use la variable de entorno `PROJECT_ROOT` para especificar explícitamente
- Durante el desarrollo y depuración, puede configurar `CODEX_SKIP_GIT_REPO_CHECK=true` para omitir la verificación

### 2. Configuración de variables de entorno

- Pase las variables de entorno necesarias a través del mecanismo de lista blanca
- Use el Shell de inicio de sesión para cargar el entorno completo
- Evite pasar información sensible

### 3. Tiempo de espera y reintentos

- Establezca un tiempo de espera razonable según la complejidad de la tarea
- Implemente retroceso exponencial para errores reintentables
- Registre el número y la razón de los reintentos

### 4. Manejo de errores

- Distinga entre errores reintentables y no reintentables
- Proporcione información y sugerencias de error claras
- Unifique los códigos de error para facilitar el manejo de nivel superior

### 5. Salida en flujo

- Implemente devoluciones de llamada de salida incremental para mejorar la experiencia del usuario
- Maneje correctamente las actualizaciones incrementales de mensajes
- Registre el uso de tokens para análisis de costos

En el entorno de producción real del proyecto HagiCode, hemos verificado la efectividad de las mejores prácticas anteriores. Esta solución nos ayudó a construir un servicio de ejecución de AI estable y confiable. Después de todo, la práctica es el único criterio para probar la verdad, hablar en el papel finalmente no sirve de nada.

## Resumen

El mecanismo de flujo de eventos de Codex SDK proporciona capacidades poderosas para construir servicios de ejecución de AI. Al analizar correctamente varios tipos de eventos, podemos:

- Obtener el estado y la salida de ejecución en tiempo real
- Implementar un manejo de errores y mecanismos de reintento confiables
- Obtener información estadística detallada de ejecución
- Construir una plataforma de ejecución de AI con funciones completas

Los conceptos centrales y ejemplos de código presentados en este artículo se pueden aplicar directamente a proyectos reales, ayudando a los desarrolladores a comenzar rápidamente con la integración de Codex SDK. Si cree que esta solución tiene valor, indica que la práctica de ingeniería de HagiCode no está mal, entonces HagiCode mismo también merece atención. Después de todo, hay cosas que, si las pierdes, es una lástima.

## Referencias

- [Documentación oficial de Codex SDK](https://platform.openai.com/docs/codex)
- [Repositorio GitHub de HagiCode](https://github.com/HagiCode-org/site)
- [Sitio web oficial de HagiCode](https://hagicode.com)

---

Gracias por leer, si encuentra útil este artículo, haga clic en el botón de Me gusta below👍 para que más personas vean este artículo.

Este contenido utiliza colaboración asistida por inteligencia artificial, revisado por mí, conforme a mis puntos de vista y posición.

- Autor del artículo: [newbe36524](https://www.newbe.pro)
- Enlace del artículo: [https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/](https://docs.hagicode.com/blog/2026-03-09-codex-sdk-console-message-parsing/)
- Aviso de derechos de autor: Excepto declaración especial, todos los artículos de este blog adoptan el acuerdo de licencia BY-NC-SA. Indique la fuente para la reimpresión.
```

Please save this content to the file `src/content/docs/es-ES/blog/2026-03-09-codex-sdk-console-message-parsing.mdx`.

---
title: Instalación de Node.js
description: Esta guía le ayudará a instalar el entorno de ejecución de Node.js en Windows, macOS y Linux.
sidebar_position: 10
---

import { Tabs, TabItem } from '@/components/starlight';
import { LinkCard, CardGrid } from '@astrojs/starlight/components';

Esta guía cubre la instalación de Node.js en Windows, macOS y Linux. Node.js es una dependencia clave para las herramientas locales de HagiCode, incluidas OpenSpec, herramientas frontend y varias CLI para desarrolladores.

:::consejo[Requisitos de versión]
HagiCode recomienda **Node.js 24+** (Node.js mínimo admitido >= 18.0). En la mayoría de los casos, instale la última versión LTS y luego verifique que su versión cumpla con el requisito.
:::

## Instalación de Node.js

<Tabs>
  <TabItem value="windows" label="Windows" default>

### Método uno: instalador oficial (recomendado)

1. Visita el [Sitio web oficial de Node.js](https://nodejs.org/)
2. Descargue la versión **LTS (soporte a largo plazo)** del instalador de Windows (`.msi` archivo)
3. Haga doble clic para ejecutar el instalador.
4. Siga las indicaciones del asistente de instalación para completar la instalación:
   - Aceptar el acuerdo de licencia
   - Elija la ruta de instalación (la opción predeterminada está bien)
   - Asegúrese de que "Instalar automáticamente las herramientas necesarias" esté marcado
   - Haga clic en "Instalar" para iniciar la instalación.
5. Una vez completada la instalación, haga clic en "Finalizar"

### Método dos: usar Winget

Abra PowerShell o símbolo del sistema y ejecute:

```powershell
winget install OpenJS.NodeJS.LTS
```

Después de la instalación, vuelva a abrir la terminal para que los cambios surtan efecto.

  </TabItem>
  <TabItem value="macos" label="macOS">

### Método uno: instalador oficial (recomendado)

1. Visita el [Sitio web oficial de Node.js](https://nodejs.org/)
2. Descargue el instalador de macOS versión **LTS (soporte a largo plazo)** (`.pkg` archivo)
3. Haga doble clic para ejecutar el instalador.
4. Siga las indicaciones del asistente de instalación para completar la instalación:
   - Haga clic en "Continuar"
   - Aceptar el acuerdo de licencia
   - Elija el disco de instalación
   - Haga clic en "Instalar" e ingrese la contraseña de administrador
5. Una vez completada la instalación, haga clic en "Cerrar"

### Método dos: usar Homebrew

si tienes [cerveza casera](https://brew.sh/) instalado, puede instalar Node.js a través de la terminal:

```bash
brew install node
```

:::consejo
Homebrew agregará automáticamente Node.js a su RUTA. Si el comando no está disponible, reinicie la terminal o ejecute:
```bash
eval "$(/opt/homebrew/bin/brew shellenv)"
```
:::

  </TabItem>
  <TabItem value="ubuntu" label="Ubuntu/Debian">

### Usando el repositorio NodeSource (recomendado)

Esta es la mejor manera de obtener la última versión de Node.js:

```bash
# Use curl to download the installation script
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### Usando el administrador de paquetes apto

Si no necesita la última versión, puede utilizar el repositorio predeterminado de Ubuntu:

```bash
sudo apt-get update
sudo apt-get install -y nodejs npm
```

:::nota
La versión de Node.js en el repositorio predeterminado puede ser anterior. Después de la instalación, verifique la versión para asegurarse de que cumpla con los requisitos de Node.js >= 18.0 (se recomienda Node.js 24+).
:::

  </TabItem>
  <TabItem value="centos" label="CentOS/RHEL">

### Usando el repositorio NodeSource (recomendado)

```bash
# Use curl to download the installation script
curl -fsSL https://rpm.nodesource.com/setup_lts.x | sudo bash -
sudo yum install -y nodejs
```

### Usando el Administrador de paquetes yum

```bash
sudo yum install -y nodejs npm
```

  </TabItem>
  <TabItem value="fedora" label="Fedora">

### Usando el Administrador de paquetes dnf

```bash
sudo dnf install -y nodejs npm
```

:::consejo
El repositorio de software de Fedora generalmente contiene versiones más nuevas de Node.js y se puede instalar directamente a través de dnf.
:::

  </TabItem>
</Tabs>

## Uso de herramientas de gestión de versiones

Si necesita cambiar entre varias versiones de Node.js, puede utilizar herramientas de administración de versiones.

### nvm (Administrador de versiones de nodo)

<Tabs>
  <TabItem value="nvm-install" label="Installing nvm" default>

#### Linux/macOS

```bash
# Install using curl
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Or install using wget
wget -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
```

Después de la instalación, vuelva a cargar la configuración de su shell:

```bash
source ~/.bashrc
# Or for Zsh users
source ~/.zshrc
```

#### ventanas

En Windows, recomendamos usar [nvm-ventanas](https://github.com/coreybutler/nvm-windows):

1. Descarga lo último `nvm-setup.exe` de [Lanzamientos de nvm-windows](https://github.com/coreybutler/nvm-windows/releases)
2. Ejecute el instalador
3. Vuelva a abrir el símbolo del sistema o PowerShell

  </TabItem>
  <TabItem value="nvm-use" label="Using nvm">

Después de instalar nvm, puedes usarlo para instalar y administrar versiones de Node.js:

```bash
# Install the latest LTS version (recommended)
nvm install --lts

# Install Node.js 24 (recommended version)
nvm install 24

# Install a specific Node.js version
nvm install 18.20.0

# List installed versions
nvm list

# Switch to a specified Node.js version
nvm use 24

# Set default version
nvm alias default 24

# Windows nvm-windows syntax is slightly different
nvm install 24
nvm use 24
```

  </TabItem>
</Tabs>

### fnm (Administrador de nodos rápido)

fnm es una herramienta de administración de versiones de Node.js más rápida y sencilla creada con Rust.

<Tabs>
  <TabItem value="fnm-install" label="Installing fnm" default>

#### Linux/macOS

```bash
# Install using curl
curl -fsSL https://fnm.vercel.app/install | bash

# After installation, reload shell configuration
source ~/.bashrc
# Or for Zsh users
source ~/.zshrc
```

#### ventanas

uso [primicia](https://scoop.sh/) o [ala](https://learn.microsoft.com/en-us/windows/package-manager/winget/):

```powershell
# Use scoop
scoop install fnm

# Or use winget
winget install Schniz.fnm
```

  </TabItem>
  <TabItem value="fnm-use" label="Using fnm">

```bash
# Install the latest LTS version (recommended)
fnm install --lts

# Install Node.js 24 (recommended version)
fnm install 24

# Install a specific Node.js version
fnm install 18.20.0

# List installed versions
fnm list

# Switch to a specified Node.js version
fnm use 24

# Set default version
fnm default 24
```

  </TabItem>
</Tabs>

## Aceleración para usuarios de China continental

Si se encuentra en China continental, la descarga de paquetes Node.js y npm puede ser lenta. Aquí hay algunas soluciones de aceleración.

### Uso de fuentes de espejo domésticas

#### npm Taobao Espejo (recomendado)

```bash
# Temporarily use Taobao mirror
npm install --registry=https://registry.npmmirror.com

# Permanently set Taobao mirror
npm config set registry https://registry.npmmirror.com

# Verify mirror is set successfully
npm config get registry
```

#### Usando nrm para administrar fuentes espejo

nrm (administrador de registro de npm) lo ayuda a cambiar rápidamente las fuentes espejo de npm:

```bash
# Install nrm
npm install -g nrm

# List available mirror sources
nrm ls

# Switch to Taobao mirror
nrm use taobao

# Test mirror source speed
nrm test
```

### Acelerar las descargas del instalador de Node.js

<Tabs>
  <TabItem value="cnpm-mirror" label="Taobao Node.js Mirror" default>

Taobao proporciona descargas espejo para los instaladores de Node.js:

- **Dirección de descarga**: https://npmmirror.com/mirrors/node/

Visite la dirección anterior y seleccione la versión que necesita descargar.

  </TabItem>
  <TabItem value="fastgit" label="FastGit Acceleration">

Si la descarga de nvm u otras herramientas de GitHub es lenta, puede usar el espejo FastGit:

```bash
# Use FastGit to accelerate nvm installation
curl -o- https://fastgit.org/nvm-sh/nvm/raw/master/install.sh | bash
```

:::nota
El servicio FastGit puede cambiar; consulte la información oficial.
:::

  </TabItem>
</Tabs>

### Espejos caseros domésticos

Si utiliza Homebrew en China continental, puede configurar espejos domésticos para aceleración:

```bash
# Install Homebrew using domestic mirror
/bin/bash -c "$(curl -fsSL https://gitee.com/ineo6/homebrew-install/raw/master/install.sh)"

# Or use USTC mirror
export HOMEBREW_BOTTLE_DOMAIN=https://mirrors.ustc.edu.cn/homebrew-bottles
```

### Gestores de paquetes específicos de China continental

#### cnpm

cnpm es un cliente npm proporcionado por el equipo de Taobao que usa el espejo de Taobao de forma predeterminada:

```bash
# Install cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com

# Use cnpm to install packages
cnpm install <package-name>
```

:::consejo
cnpm es mayormente compatible con los comandos npm y puede usarse como reemplazo directo.
:::

### Verificación del efecto de aceleración del espejo

Después de configurar los espejos, verifique mediante:

```bash
# Test download speed
time npm install express --registry=https://registry.npmmirror.com

# Compare with official source speed
time npm install express --registry=https://registry.npmjs.org
```

## Verificación de la instalación

Después de la instalación, verifique que Node.js y npm estén instalados correctamente:

```bash
# Check Node.js version
node --version

# Check npm version
npm --version
```

:::nota
Asegúrese de que la versión de Node.js sea >= 18.0 (se recomienda Node.js 24+). Si la versión es demasiado baja, vuelva a instalarla utilizando los métodos anteriores o utilice herramientas de administración de versiones para instalar la versión correcta.
:::

### Salida de verificación de muestra

El resultado de la verificación exitosa debería verse así:

```
$ node --version
v24.3.0

$ npm --version
10.2.3
```

## Solución de problemas

### Problemas de Windows

<Tabs>
  <TabItem value="win-path" label="Command Not Available">

si `node` o `npm` Los comandos no están disponibles después de la instalación:

1. Reinicie el símbolo del sistema o PowerShell
2. Si aún no está disponible, agregue manualmente Node.js a PATH:
   - Busque "Variables de entorno" y abra "Editar variables de entorno del sistema".
   - Haga clic en "Variables de entorno"
   - Busque "Ruta" en "Variables del sistema" y haga clic en "Editar"
   - Agregue la ruta de instalación de Node.js (predeterminada: `C:\Program Files\nodejs\`)
   - Haga clic en "Aceptar" para guardar los cambios.

  </TabItem>
  <TabItem value="win-policy" label="PowerShell Execution Policy">

Si encuentra errores de política de ejecución al utilizar nvm:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

  </TabItem>
</Tabs>

### Problemas con macOS

<Tabs>
  <TabItem value="mac-perm" label="Permission Errors">

Si encuentra errores de permisos al instalar paquetes npm globales:

```bash
# Fix npm permissions
sudo chown -R $(whoami) ~/.npm
sudo chown -R $(whoami) /usr/local/lib/node_modules
```

  </TabItem>
  <TabItem value="mac-path" label="Command Not Available">

Si los comandos no están disponibles después de la instalación, reinicie el terminal o agréguelos manualmente a PATH (generalmente en `/usr/local/bin`).

  </TabItem>
</Tabs>

### Problemas de Linux

<Tabs>
  <TabItem value="linux-perm" label="Permission Errors">

Si encuentra errores de permisos al instalar paquetes npm globales, se recomienda utilizar las herramientas de administración de versiones de Node.js (como nvm o fnm) en lugar de los administradores de paquetes del sistema.

  </TabItem>
  <TabItem value="linux-old" label="Version Too Old">

Si la versión de Node.js instalada a través del administrador de paquetes del sistema es demasiado antigua:

1. Utilice el repositorio NodeSource (recomendado, ver arriba)
2. O utilice herramientas de gestión de versiones (nvm/fnm)

  </TabItem>
</Tabs>

### Comprobando el entorno

Si la verificación falla, verifique lo siguiente:

<Tabs>
  <TabItem value="win-check" label="Windows">

```powershell
# Check if Node.js is in PATH
where node

# Check installation path
Get-Command node
```

  </TabItem>
  <TabItem value="mac-check" label="macOS">

```bash
# Check if Node.js is in PATH
which node

# Check installation path
ls -l $(which node)
```

  </TabItem>
  <TabItem value="linux-check" label="Linux">

```bash
# Check if Node.js is in PATH
which node

# Check installation path
ls -l $(which node)

# Check version details
node -v
```

  </TabItem>
</Tabs>

## Próximos pasos

Una vez completada la instalación de Node.js, puede continuar con los siguientes pasos:

<CardGrid>

<LinkCard title="Instalar OpenSpec" href="/es-ES/related-software-installation/openspec/setup-openspec"
    descripción="@@TR_2021@@"
/>

<LinkCard title="Instalar la CLI del Agente AI" href="/es-ES/related-software-installation/ai-agent-cli/"
    descripción="@@TR_2023@@"
/>

<LinkCard title="Volver a la guía de instalación" href="/es-ES/installation"
    descripción="@@TR_2026@@"
/>

</CardGrid>

## ¿Necesita más ayuda?

Si encuentra problemas que no se tratan aquí:

1. Visita el [Documentación oficial de Node.js](https://nodejs.org/en/docs/)
2. Revisa el [Descripción general del producto HagiCode](/es-ES/product-overview) para el contexto actual de productos y herramientas
3. Visita el [Sitio web de HagiCode](https://hagicode.com/) para conocer los últimos puntos de entrada de soporte

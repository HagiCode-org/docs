---
title: 安装 Claude Code - 国内模型提供商配置指南
description: 如何安装 Claude Code 并配置国内可用的模型提供商（智谱 AI、MiniMax、阿里云）
sidebar_position: 10
tags:
  - claude-code
  - ai-configuration
  - domestic-providers
---

Claude Code 是一个强大的 AI 辅助编程工具，能够显著提升开发效率和代码质量。本指南将帮助您安装 Claude Code 并选择适合您的国内模型提供商。

## 安装 Claude Code

### 为什么不推荐官方安装方式

Claude Code 官方提供了两种安装方式：
- **Native 安装**：需要从 GitHub Releases 下载二进制文件
- **官方脚本安装**：需要执行官方提供的安装脚本

由于网络原因，上述方式在国内网络环境下可能无法正常使用。因此，我们推荐使用 **NPM 安装方式**，这种方式更稳定且在国内网络环境下表现更好。

### 使用 NPM 安装

通过 NPM 可以非常方便地安装 Claude Code。请确保您已经安装了 Node.js 环境。

```bash
npm install -g @anthropic-ai/claude-code
```

安装完毕之后，可以通过以下命令验证安装是否成功：

```bash
claude --version
```

## 配置模型提供商

Claude Code 支持两种配置方式：
- **推荐方式**：使用 `settings.json` 配置文件
- **传统方式**：使用环境变量（仍然有效）

:::tip
使用 `settings.json` 配置文件的优势：
- 配置持久化，无需每次启动都设置环境变量
- 可以配置多个模型提供商的映射关系
- 配置更清晰、更易于管理
:::

### 配置文件位置

Claude Code 的 `settings.json` 文件位置因操作系统而异：

import { Tabs, TabItem } from '@astrojs/starlight/components';

<Tabs>
  <TabItem label="Windows">
    ```plaintext
    C:\Users\<你的用户名>\.claude\settings.json
    ```
  </TabItem>
  <TabItem label="macOS">
    ```plaintext
    ~/.claude/settings.json
    ```
  </TabItem>
  <TabItem label="Linux">
    ```plaintext
    ~/.claude/settings.json
    ```
  </TabItem>
</Tabs>

### 智谱 AI (ZAI) [推荐]

智谱 AI 提供了 GLM Coding Plan，专为 Claude Code 等 AI 编程工具设计，性价比高且支持多种高级功能。

#### 购买订阅

使用 ZAI 您需要先购买订阅并获得 API Key。对于代码编写来说，建议购买 Coding Plan 作为日常使用，这样会更加节约成本。

通过以下链接购买获得专属优惠：[https://www.bigmodel.cn/claude-code?ic=14BY54APZA](https://www.bigmodel.cn/claude-code?ic=14BY54APZA)

:::info
**智谱 GLM Coding: 20+ 大编程工具无缝支持**

Claude Code、Cline 等 20+ 大编程工具无缝支持，"码力"全开，越拼越爽！

- **全球顶尖编程模型**：GLM-5 发布时，在推理、代码和智能体能力等方面达到开源模型 SOTA 水平
- **超大容量，更低价格**：Claude Code 1/3 的价格， 3 倍的用量，低至 49 元包月
- **免费享专属 MCP**：含图像视频理解、联网搜索、网页读取、开源仓库 MCP
:::

:::tip
初次体验可以购买 lite 版本，价格更低。
:::

#### 方式一：使用 settings.json 配置文件 [推荐]

在 `settings.json` 文件中添加以下配置：

```json
{
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "你的-API-Key",
    "ANTHROPIC_BASE_URL": "https://open.bigmodel.cn/api/anthropic",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "glm-4.5-air",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "glm-5",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "glm-4.7"
  }
}
```

#### 方式二：使用环境变量

```bash
# 设置 API Key
export ANTHROPIC_AUTH_TOKEN=your-api-key-here

# 设置 API URL
export ANTHROPIC_API_URL=https://open.bigmodel.cn/api/anthropic
```

#### 方式三：使用 coding-helper 工具

ZAI 提供了一个简单的命令行工具 `coding-helper` 来帮助您配置 Claude Code。

```bash
# 进入命令行界面，执行如下运行 Coding Tool Helper
npx @z_ai/coding-helper
```

运行之后会进入一个交互式的命令行界面，按照提示输入您的 API Key 并选择 Claude Code 即可完成配置。

#### 模型映射

智谱 AI 提供了多个模型选项，与 Claude 模型层级的对应关系如下：

| Claude 层级 | ZAI 模型 |
|-------------|----------|
| Sonnet | `glm-4.7` |
| Opus | `glm-5` |
| Haiku | `glm-4.5-air` |

### MiniMax

MiniMax 提供 Claude API 兼容服务，使用统一的 MiniMax-M2.5 模型适用于所有层级。

#### 获取 API Key

通过以下链接获取 API Key：[https://platform.minimaxi.com/subscribe/coding-plan?code=8wNck4etDM&source=link](https://platform.minimaxi.com/subscribe/coding-plan?code=8wNck4etDM&source=link)

:::info
**MiniMax Claude API 兼容服务**

MiniMax 提供 Claude API 兼容服务，支持多种模型接入，稳定可靠。
:::

#### 方式一：使用 settings.json 配置文件 [推荐]

在 `settings.json` 文件中添加以下配置：

```json
{
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "你的-API-Key",
    "ANTHROPIC_BASE_URL": "https://api.minimaxi.com/anthropic",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "MiniMax-M2.5",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "MiniMax-M2.5",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "MiniMax-M2.5"
  }
}
```

#### 方式二：使用环境变量

```bash
# 设置 API Key
export ANTHROPIC_AUTH_TOKEN=your-api-key-here

# 设置 API URL
export ANTHROPIC_API_URL=https://api.minimaxi.com/anthropic
```

#### 模型映射

MiniMax 使用统一的模型适用于所有层级：

| Claude 层级 | MiniMax 模型 |
|-------------|--------------|
| Sonnet | `MiniMax-M2.5` |
| Opus | `MiniMax-M2.5` |
| Haiku | `MiniMax-M2.5` |

### 阿里云 DashScope

阿里云灵积平台提供 Claude API 兼容服务，使用统一的 glm-4.7 模型适用于所有层级。

#### 开通服务

通过以下链接开通服务：[https://www.aliyun.com/benefit/ai/aistar?userCode=vmx5szbq&clubBiz=subTask..12384055..10263..](https://www.aliyun.com/benefit/ai/aistar?userCode=vmx5szbq&clubBiz=subTask..12384055..10263..)

:::info
**阿里云千问 Coding Plan 上线**

阿里云千问 Coding Plan 已上线，满足开发日常需求。推荐 + Hagicode，完美实现开发过程中的各项需求。
:::

开通成功后，可以在阿里云控制台创建 API Key。

#### 方式一：使用 settings.json 配置文件 [推荐]

在 `settings.json` 文件中添加以下配置：

```json
{
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "你的-API-Key",
    "ANTHROPIC_BASE_URL": "https://coding.dashscope.aliyuncs.com/apps/anthropic",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "glm-4.7",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "glm-4.7",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "glm-4.7"
  }
}
```

#### 方式二：使用环境变量

```bash
# 设置 API Key
export ANTHROPIC_AUTH_TOKEN=your-api-key-here

# 设置 API URL
export ANTHROPIC_API_URL=https://coding.dashscope.aliyuncs.com/apps/anthropic
```

#### 模型映射

阿里云使用统一的模型适用于所有层级：

| Claude 层级 | 阿里云模型 |
|-------------|------------|
| Sonnet | `glm-4.7` |
| Opus | `glm-4.7` |
| Haiku | `glm-4.7` |
---
title: Instalação local do OmniRoute e validação de inicialização
description: Use o caminho npm local recomendado para instalar o OmniRoute, verifique a inicialização e confirme se http://localhost:20128 e http://localhost:20128/v1 estão disponíveis.
sidebar_position: 12
tags:
  - omniroute
  - openai-compatible
  - installation
  - integration
---

import { LinkCard, CardGrid } from '@astrojs/starlight/components';

Esta página é para usuários iniciantes do HagiCode que desejam o caminho de configuração mais curto do OmniRoute: inicie o serviço local, verifique se `http://localhost:20128/v1` funciona e confirme se o Dashboard e a API estão disponíveis.

:::tip[Caminho recomendado]
Esta primeira versão mantém primeiro o caminho mais estável: `npm install -g omniroute`, `omniroute`, Painel em `http://localhost:20128`e o URL base compatível com OpenAI em `http://localhost:20128/v1`. Docker, VPS e modos de implantação mais avançados permanecem na seção opcional.
:::

## Pré-requisitos

- O Node.js já está instalado. Completo [Instalando Node.js](/pt-BR/related-software-installation/nodejs/installation) primeiro, se necessário.
- Porta local `20128` está disponível e ainda não está ocupado.
- Você tem pelo menos uma conta de provedor de modelo upstream que planeja rotear por meio do OmniRoute.
- Antes de integrar o HagiCode, uma CLI ou um SDK, mantenha estes dois endereços separados:
  - Painel: `http://localhost:20128`
  - URL base compatível com OpenAI: `http://localhost:20128/v1`

## Instale e execute localmente

Comece com o caminho npm global recomendado:

```bash
npm install -g omniroute
omniroute
```

Após o início do serviço, valide-o nesta ordem:

1. Abrir `http://localhost:20128` em um navegador e confirme se o Dashboard é carregado.
2. Vá para o `Endpoints` página no Painel e crie ou copie uma chave de API.
3. Observe o URL base que você entregará às ferramentas posteriormente: `http://localhost:20128/v1`.

:::note[Por que começar com o Dashboard]
Para a primeira configuração, o Dashboard é a maneira mais rápida de confirmar se o OmniRoute está em execução, se seus provedores estão conectados e se existe uma chave de API. Se uma chamada de cliente posterior falhar, verificar primeiro o Dashboard geralmente é mais rápido do que depurar o código SDK imediatamente.
:::

## Valide o serviço em execução

### Validação 1: confirme se o catálogo do modelo está legível

Coloque a chave do `Endpoints` página em uma variável de ambiente:

```bash
export OMNIROUTE_API_KEY="replace-with-the-key-you-copied-from-the-Endpoints-page"
```

Então solicite `/v1/models`:

```bash
curl http://localhost:20128/v1/models \
  -H "Authorization: Bearer ${OMNIROUTE_API_KEY}"
```

Se você receber uma lista de modelos de volta, o endpoint compatível com OpenAI e o fluxo de autenticação estão funcionando.

### Validação 2: envie uma solicitação mínima de chat

```bash
curl http://localhost:20128/v1/chat/completions \
  -H "Authorization: Bearer ${OMNIROUTE_API_KEY}" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "replace-with-a-model-id-from-v1-models",
    "messages": [
      { "role": "user", "content": "Reply with ok." }
    ]
  }'
```

O objetivo aqui não é a qualidade imediata. É para provar que o modelo escolhido já pode ser roteado com sucesso através do OmniRoute.

## Próximas etapas

<CardGrid>
  <LinkCard
    título="@@TR_1714@@"
    href="/pt-BR/instalação-de-software-relacionado/nodejs/instalação"
  >
    Conclua primeiro o pré-requisito de tempo de execução se a máquina ainda não tiver uma instalação utilizável do Node.js.
  </LinkCard>
  <LinkCard
    título="@@TR_989@@"
    href="/pt-BR/instalação-de-software-relacionado/ai-agent-cli/"
  >
    Conclua primeiro a instalação local da CLI do agente se o seu ambiente CLI ainda não estiver pronto.
  </LinkCard>
  <LinkCard
    título="@@TR_1865@@"
    href="https://github.com/diegosouzapw/OmniRoute"
  >
    Continue com o repositório upstream e os guias oficiais quando precisar de material de referência mais completo.
  </LinkCard>
</CardGrid>

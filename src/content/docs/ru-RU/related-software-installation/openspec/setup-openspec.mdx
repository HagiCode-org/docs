---
title: Установить OpenSpec
description: Это руководство поможет вам установить инструмент разработки на основе спецификаций OpenSpec.
sidebar_position: 10
---

import { Tabs, TabItem } from '@/components/starlight';
import { LinkCard, CardGrid } from '@astrojs/starlight/components';

# Установить OpenSpec

OpenSpec управляет предложениями, спецификациями и записями изменений. Эта страница преследует одну цель: четко объяснить путь установки, чтобы вы могли прикрепить его к существующему рабочему процессу.

## Что такое ОпенСпец?

OpenSpec — это инструмент командной строки для управления предложениями и спецификациями при работе с программным обеспечением. Основной рабочий процесс включает в себя:

- **Управление предложениями**: создавайте, просматривайте и отслеживайте предложения функций.
- **Проверка спецификации**: убедитесь, что изменения кода соответствуют заранее заданным спецификациям.
- **Отслеживание изменений**: записывайте всю проверенную историю изменений.
- **Командное сотрудничество**: поддержка совместной разработки несколькими людьми.

### Применение в проектах HagiCode

В проектах HagiCode OpenSpec используется для:

1. **Управление разработкой функций**: все новые функции должны пройти процедуру предложения OpenSpec.
2. **Проверка кода**: убедитесь, что изменения соответствуют спецификациям проекта.
3. **Синхронизация документации**: поддержание согласованности между документацией и реализацией кода.
4. **Интеграция контроля версий**: бесшовная интеграция с рабочими процессами контроля версий.

## Предварительные условия

Перед установкой OpenSpec убедитесь, что ваша система соответствует следующим требованиям:

### Node.js и npm

OpenSpec распространяется через npm и требует среды Node.js. Для OpenSpec требуется **Node.js 18.0 или выше**.

Проверьте установку Node.js:

```bash
node --version
```

Проверьте установку npm:

```bash
npm --version
```

Если оно не установлено или версия слишком старая, обратитесь к инструкциям по установке соответствующего программного обеспечения в [Руководство по установке](/ru-RU/installation).

## Установка ОпенСпец

:::tip[Требования к версии]
Для этого проекта требуется использование диапазона версий **OpenSpec 1.0.0 или выше**. Поддерживаются все версии 1.x для обеспечения совместимости с рабочим процессом проекта.
:::

OpenSpec устанавливается глобально через npm, что делает его доступным из любой точки вашей системы.

### Установить глобально с помощью npm

<Tabs>
  <TabItem value="win" label="Windows">
    Запустите в PowerShell или командной строке:

```powershell
npm install -g @fission-ai/openspec@1
```

:::note[Проблемы с разрешениями Windows]
Если во время установки вы столкнулись с ошибками разрешений, запустите PowerShell или командную строку от имени администратора.
:::
  </TabItem>
  <TabItem value="mac" label="macOS">
    Запустить в терминале:

```bash
npm install -g @fission-ai/openspec@1
```

:::note[Проблемы с разрешениями macOS]
Если вы столкнетесь `EACCES` ошибки разрешения, попробуйте:
```bash
sudo npm install -g @fission-ai/openspec@1
```
Или используйте [НВМ](https://github.com/nvm-sh/nvm) для управления версиями Node.js и предотвращения проблем с разрешениями.
:::
  </TabItem>
  <TabItem value="linux" label="Linux">
    Запустить в терминале:

```bash
npm install -g @fission-ai/openspec@1
```

:::note[Проблемы с разрешениями Linux]
Если вы столкнетесь `EACCES` ошибки разрешения, вы можете:
1. Использование `sudo`: `sudo npm install -g @fission-ai/openspec@1`
2. Настройте npm для использования каталога пользователя:
```bash
mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
```
:::
  </TabItem>
</Tabs>

### Проверка установки

После установки убедитесь, что OpenSpec установлен правильно и версия соответствует требованиям:

```bash
openspec --version
```

В случае успешной установки вы увидите вывод номера версии, например:

```
openspec/1.1.1 linux-x64 node-v18.17.0
```

:::note[Диапазон версий]
Убедитесь, что установлена версия OpenSpec 1.0.0 или выше. В проекте проверена совместимость с версией 1.1.1. Для получения новых функций рекомендуется использовать последнюю совместимую версию.
:::

## Распространенные проблемы и устранение неполадок

### Ошибки разрешений

#### Проблема: `EACCES` Разрешение отклонено

Обнаружение ошибок разрешений при установке или запуске OpenSpec.

**Решения**:

<Tabs>
  <TabItem value="win" label="Windows">
    Запустите PowerShell или командную строку от имени администратора:
    1. Щелкните правой кнопкой мыши PowerShell или командную строку.
    2. Выберите «Запуск от имени администратора».
    3. Повторно запустите команду установки
  </TabItem>
  <TabItem value="mac" label="macOS">
    Установить с помощью `sudo`:

```bash
sudo npm install -g @fission-ai/openspec@1
```

Или используйте [НВМ](https://github.com/nvm-sh/nvm) для управления Node.js и предотвращения глобальных проблем с разрешениями на установку.
  </TabItem>
  <TabItem value="linux" label="Linux">
    Решение 1. Используйте sudo
```bash
sudo npm install -g @fission-ai/openspec@1
```

Решение 2. Настройте npm для использования каталога пользователя.
```bash
mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
npm install -g @fission-ai/openspec@1
```
  </TabItem>
</Tabs>

### Проблемы с сетью

#### Проблема: установка npm не удалась или выполняется очень медленно.

Невозможно загрузить OpenSpec из реестра npm по сетевым причинам.

**Решения**:

1. **Использовать внутреннее зеркало npm**:

```bash
# Use Taobao mirror
npm config set registry https://registry.npmmirror.com

# Then reinstall
npm install -g @fission-ai/openspec@1
```

2. **Восстановить официальный источник** (при необходимости):

```bash
npm config set registry https://registry.npmjs.org
```

3. **Используйте cnpm** (необязательно):

```bash
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install -g @fission-ai/openspec@1
```

:::совет
Пользователям в материковом Китае рекомендуется использовать зеркало Taobao, которое может значительно повысить скорость загрузки.
:::

### Проблемы совместимости версий

#### Проблема: команды OpenSpec выполняются ненормально или сообщают об ошибках

Установленная версия OpenSpec несовместима с текущей средой.

**Решения**:

1. **Проверьте версию Node.js**:

```bash
node --version
```

Убедитесь, что установлена версия 18.0 или выше. Если версия слишком низкая, обновите Node.js.

2. **Проверьте версию OpenSpec**:

```bash
openspec --version
```

3. **Переустановить конкретную версию**:

```bash
npm install -g @fission-ai/openspec@1
```

4. **Очистите кэш npm** (если проблема не устранена):

```bash
npm cache clean --force
npm install -g @fission-ai/openspec@1
```

### Команда не найдена

#### Проблема: ввод текста `openspec` Команда показывает «Команда не найдена»

OpenSpec установлен, но система не может найти команду.

**Решения**:

<Tabs>
  <TabItem value="win" label="Windows">
    1. Подтвердите глобальный путь установки npm:

```powershell
npm config get prefix
```

2. Добавьте путь к системному PATH:
   - Откройте «Свойства системы» → «Дополнительно» → «Переменные среды».
   - Найти `Path` в "Системные переменные"
   - Добавьте глобальный путь npm (обычно `C:\Users\<username>\AppData\Roaming\npm`)
   - Перезапустить окно командной строки
  </TabItem>
  <TabItem value="mac" label="macOS">
    1. Подтвердите глобальный путь установки npm:

```bash
npm config get prefix
```

2. Добавьте путь к PATH (в `~/.zshrc` или `~/.bash_profile`):

```bash
export PATH="$(npm config get prefix)/bin:$PATH"
```

3. Перезагрузить конфигурацию:

```bash
source ~/.zshrc
```
  </TabItem>
  <TabItem value="linux" label="Linux">
    1. Подтвердите глобальный путь установки npm:

```bash
npm config get prefix
```

2. Добавьте путь к PATH (в `~/.bashrc`):

```bash
export PATH="$(npm config get prefix)/bin:$PATH"
```

3. Перезагрузить конфигурацию:

```bash
source ~/.bashrc
```
  </TabItem>
</Tabs>

## Обновление OpenSpec

:::note[Диапазон версий]
Для этого проекта требуется использование OpenSpec 1.0.0 или более поздней версии. Если у вас есть требования к обновлению версии, обратитесь к документации проекта.
:::

Если вам нужно переустановить определенную версию:

```bash
npm install -g @fission-ai/openspec@1
```

## Удаление OpenSpec

Если вам нужно удалить OpenSpec:

<Tabs>
  <TabItem value="win" label="Windows">

```powershell
npm uninstall -g @fission-ai/openspec
```
  </TabItem>
  <TabItem value="mac" label="macOS">

```bash
npm uninstall -g @fission-ai/openspec
```

Или используйте sudo:

```bash
sudo npm uninstall -g @fission-ai/openspec
```
  </TabItem>
  <TabItem value="linux" label="Linux">

```bash
npm uninstall -g @fission-ai/openspec
```

Или используйте sudo:

```bash
sudo npm uninstall -g @fission-ai/openspec
```
  </TabItem>
</Tabs>

## Следующие шаги

После завершения установки OpenSpec вы можете продолжить следующие шаги:

<CardGrid>

<LinkCard title="Мастер настройки" href="/ru-RU/quick-start/wizard-setup"
    описание="@@TR_2059@@"
/>

<LinkCard title="Создать сеанс предложений" href="/ru-RU/quick-start/proposal-session"
    описание="@@TR_2061@@"
/>

</CardGrid>

:::совет
Более подробные руководства по использованию OpenSpec см. [Официальная документация OpenSpec](https://github.com/fission-ai/openspec).
:::

## Нужна дополнительная помощь?

Если вы столкнулись с проблемами, не описанными здесь:

1. Проверить [Проблемы OpenSpec на GitHub](https://github.com/fission-ai/openspec/issues) для подобных проблем
2. Посетите [Официальная документация OpenSpec](https://github.com/fission-ai/openspec) для получения дополнительной информации
3. Обратитесь к соответствующим руководствам на [Сайт документации HagiCode](/ru-RU/product-overview) за дополнительную помощь

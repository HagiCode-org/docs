---
title: Solution HagiCode de basculement et d'interopérabilité multi-fournisseurs IA
date: 2026-03-09
tags: [HagiCode, AI Provider, Claude, Codex, Architecture, Interoperability]
description: "Dans l'écosystème moderne des outils de développement, les développeurs ont souvent besoin d'utiliser différents assistants de codage IA pour aider leur travail. Le Claude Code CLI d'Anthropic et le Codex d'OpenAI"
---

## Solution HagiCode de basculement et d'interopérabilité multi-fournisseurs IA

Dans l'écosystème moderne des outils de développement, les développeurs ont souvent besoin d'utiliser différents assistants de codage IA pour aider leur travail. Le Claude Code CLI d'Anthropic et le Codex CLI d'OpenAI ont chacun leurs avantages : Claude est réputé pour sa excellente compréhension du code et ses capacités de traitement de contexte long, tandis que Codex excelle dans la génération de code et l'utilisation d'outils.

Cet article analysera en profondeur comment le projet hagicode réalise le basculement transparent et l'interopérabilité entre plusieurs fournisseurs IA, y compris la conception de l'architecture centrale, les détails clés de mise en œuvre et les considérations pratiques.

## Contexte

### Domaine du problème

Le défi central auquel le projet hagicode est confronté est de prendre en charge plusieurs CLI IA dans la même plateforme, permettant aux utilisateurs de :

1. Changer flexiblement de fournisseurs IA selon leurs besoins
2. Maintenir la continuité de l'état de session pendant le basculement
3. Abstraire uniformément les différences d'API entre différents CLI
4. Réserver de l'espace pour l'extension afin d'ajouter de nouveaux fournisseurs IA à l'avenir

### Défis techniques

1. **Unification des différences d'interface** : Claude Code CLI est appelé via ligne de commande, Codex CLI utilise des flux d'événements JSON
2. **Traitement des réponses en streaming** : Les deux fournisseurs prennent en charge le streaming, mais avec des formats de données différents
3. **Sémantique d'appel d'outils** : Claude et Codex représentent et gèrent différemment le cycle de vie des appels d'outils
4. **Cycle de vie des sessions** : Nécessité de gérer correctement la création, la reprise et la terminaison des sessions pour chaque fournisseur

## Analyse

### Approche de conception architecturale

hagicode adopte le **modèle de fournisseur (Provider Pattern)** combiné au **modèle de fabrique** pour abstraire les appels de services IA. L'idée centrale de cette conception est :

1. **Abstraction d'interface unifiée** : Définir l'interface `IAIProvider` comme abstraction unifiée pour tous les fournisseurs IA
2. **Création d'instance par fabrique** : Créer dynamiquement des instances de fournisseurs correspondantes par type via `AIProviderFactory`
3. **Logique de sélection intelligente** : Utiliser `AIProviderSelector` pour sélectionner automatiquement le fournisseur le plus approprié selon le scénario et la configuration
4. **Gestion de l'état de session** : Persistez la relation de liaison entre sessions et threads CLI via une base de données

### Composants clés

| Composant | Responsabilité | Langage |
|------|--------|------|
| `IAIProvider` | Interface de fournisseur unifiée | C# |
| `AIProviderFactory` | Créer et gérer les instances de fournisseurs | C# |
| `AIProviderSelector` | Sélection intelligente de fournisseur | C# |
| `ClaudeCodeCliProvider` | Implémentation Claude Code CLI | C# |
| `CodexCliProvider` | Implémentation Codex CLI | C# |
| `AgentCliManager` | Gestion CLI côté bureau | TypeScript |

## Solution

### 1. Conception de l'interface centrale

L'interface **IAIProvider** définit l'abstraction unifiée du fournisseur :

```csharp
public interface IAIProvider
{
    /// <summary>
    /// Nom d'affichage du fournisseur
    /// </summary>
    string Name { get; }

    /// <summary>
    /// Prend en charge le streaming
    /// </summary>
    bool SupportsStreaming { get; }

    /// <summary>
    /// Description des capacités du fournisseur
    /// </summary>
    ProviderCapabilities Capabilities { get; }

    /// <summary>
    /// Exécuter une seule requête IA
    /// </summary>
    Task<AIResponse> ExecuteAsync(AIRequest request, CancellationToken cancellationToken = default);

    /// <summary>
    /// Exécuter une requête IA en streaming
    /// </summary>
    IAsyncEnumerable<AIStreamingChunk> StreamAsync(AIRequest request, CancellationToken cancellationToken = default);

    /// <summary>
    /// Vérifier la connectivité et la vitesse de réponse du fournisseur
    /// </summary>
    Task<ProviderTestResult> PingAsync(CancellationToken cancellationToken = default);

    /// <summary>
    /// Envoyer un message avec commande intégrée
    /// </summary>
    IAsyncEnumerable<AIStreamingChunk> SendMessageAsync(
        AIRequest request,
        string? embeddedCommandPrompt = null,
        CancellationToken cancellationToken = default);
}
```

Caractéristiques clés de la conception de l'interface :
- **Modèle de requête/réponse unifié** : Tous les fournisseurs utilisent les mêmes types `AIRequest` et `AIResponse`
- **Support du streaming** : Unifie la sortie en streaming via `IAsyncEnumerable<AIStreamingChunk>`
- **Description des capacités** : `ProviderCapabilities` décrit les fonctionnalités prises en charge par le fournisseur (streaming, outils, token maximum, etc.)
- **Commandes intégrées** : `SendMessageAsync` prend en charge l'intégration de commandes OpenSpec dans les invites

### 2. Énumération des types de fournisseurs

```csharp
public enum AIProviderType
{
    ClaudeCodeCli,   // Anthropic Claude Code
    OpenCodeCli,     // Autres CLI (extensible)
    GitHubCopilot,    // GitHub Copilot
    CodebuddyCli,    // Codebuddy
    CodexCli         // OpenAI Codex
}
```

Cette énumération fournit une représentation de type sécurisé pour tous les fournisseurs pris en charge par le système.

### 3. Implémentation du modèle de fabrique

**AIProviderFactory** est responsable de la création et de la gestion des instances de fournisseurs :

```csharp
public class AIProviderFactory : IAIProviderFactory
{
    private readonly ConcurrentDictionary<AIProviderType, IAIProvider> _cache;
    private readonly IOptions<AIProviderOptions> _options;
    private readonly IServiceProvider _serviceProvider;

    public Task<IAIProvider?> GetProviderAsync(AIProviderType providerType)
    {
        // Utiliser le cache pour éviter les créations répétées
        if (_cache.TryGetValue(providerType, out var cached))
            return Task.FromResult<IAIProvider?>(cached);

        // Obtenir la configuration du fournisseur à partir de la configuration
        var aiOptions = _options.Value;
        if (!aiOptions.Providers.TryGetValue(providerType, out var config))
        {
            _logger.LogWarning("Provider '{ProviderType}' not found in configuration", providerType);
            return Task.FromResult<IAIProvider?>(null);
        }

        // Créer le fournisseur selon le type
        var provider = providerType switch
        {
            AIProviderType.ClaudeCodeCli =>
                _serviceProvider.GetService(typeof(ClaudeCodeCliProvider)) as IAIProvider,
            AIProviderType.CodexCli =>
                _serviceProvider.GetService(typeof(CodexCliProvider)) as IAIProvider,
            AIProviderType.GitHubCopilot =>
                _serviceProvider.GetService(typeof(CopilotAIProvider)) as IAIProvider,
            _ => null
        };

        if (provider != null)
        {
            _cache[providerType] = provider;
        }

        return Task.FromResult<IAIProvider?>(provider);
    }
}
```

Avantages du modèle de fabrique :
- **Mise en cache d'instances** : Évite de créer plusieurs fois le même type de fournisseur
- **Injection de dépendances** : Crée des instances via `IServiceProvider`, prend en charge l'injection de dépendances
- **Piloté par configuration** : Lit la configuration des fournisseurs à partir du fichier de configuration
- **Gestion des exceptions** : Renvoie null en cas d'échec de création, facilitant le traitement par la couche supérieure

### 4. Sélecteur intelligent

**AIProviderSelector** implémente la stratégie de sélection de fournisseur :

```csharp
public class AIProviderSelector : IAIProviderSelector
{
    private readonly BusinessLayerConfiguration _configuration;
    private readonly IAIProviderFactory _providerFactory;
    private readonly IMemoryCache _cache;

    public async Task<AIProviderType> SelectProviderAsync(
        BusinessScenario scenario,
        CancellationToken cancellationToken = default)
    {
        // 1. Essayer d'obtenir le fournisseur à partir du mappage de scénarios
        if (_configuration.ScenarioProviderMapping.TryGetValue(scenario, out var providerType))
        {
            if (await IsProviderAvailableAsync(providerType, cancellationToken))
            {
                _logger.LogDebug("Selected provider '{Provider}' for scenario '{Scenario}'",
                    providerType, scenario);
                return providerType;
            }

            _logger.LogWarning("Configured provider '{Provider}' for scenario '{Scenario}' is not available",
                providerType, scenario);
        }

        // 2. Essayer d'utiliser le fournisseur par défaut
        if (await IsProviderAvailableAsync(_configuration.DefaultProvider, cancellationToken))
        {
            _logger.LogDebug("Using default provider '{Provider}' for scenario '{Scenario}'",
                _configuration.DefaultProvider, scenario);
            return _configuration.DefaultProvider;
        }

        // 3. Essayer la chaîne de repli
        foreach (var fallbackProvider in _configuration.FallbackChain)
        {
            if (await IsProviderAvailableAsync(fallbackProvider, cancellationToken))
            {
                _logger.LogInformation("Using fallback provider '{Provider}' for scenario '{Scenario}'",
                    fallbackProvider, scenario);
                return fallbackProvider;
            }
        }

        // 4. Impossible de trouver un fournisseur disponible
        throw new InvalidOperationException(
            $"No available AI provider found for scenario '{scenario}'");
    }

    public async Task<bool> IsProviderAvailableAsync(
        AIProviderType providerType,
        CancellationToken cancellationToken = default)
    {
        var cacheKey = $"provider_available_{providerType}";

        // Utiliser le cache pour réduire les appels Ping
        if (_configuration.EnableCache &&
            _cache.TryGetValue<bool>(cacheKey, out var cached))
        {
            return cached;
        }

        var provider = await _providerFactory.GetProviderAsync(providerType);
        var isAvailable = provider != null;

        if (_configuration.EnableCache && isAvailable)
        {
            _cache.Set(cacheKey, isAvailable,
                TimeSpan.FromSeconds(_configuration.CacheExpirationSeconds));
        }

        return isAvailable;
    }
}
```

Stratégie du sélecteur :
- **Priorité au mappage de scénarios** : Vérifie d'abord si le scénario métier a un mappage de fournisseur spécifique
- **Repli au fournisseur par défaut** : Utilise le fournisseur par défaut si le mappage de scénarios échoue
- **Chaîne de repli en dernier recours** : Essaie chaque fournisseur de la chaîne de repli un par un
- **Cache de disponibilité** : Met en cache les résultats de vérification de disponibilité des fournisseurs, réduisant les appels Ping

### 5. Implémentation du fournisseur Claude Code CLI

```csharp
public class ClaudeCodeCliProvider : IAIProvider
{
    private readonly ILogger<ClaudeCodeCliProvider> _logger;
    private readonly IClaudeStreamManager _streamManager;
    private readonly ProviderConfiguration _config;

    public string Name => "ClaudeCodeCli";
    public bool SupportsStreaming => true;

    public ProviderCapabilities Capabilities { get; }

    public async Task<AIResponse> ExecuteAsync(AIRequest request, CancellationToken cancellationToken = default)
    {
        _logger.LogInformation("Executing AI request with provider: {Provider}", Name);

        var sessionOptions = ClaudeRequestMapper.MapToSessionOptions(request, _config);

        var messages = _streamManager.SendMessageAsync(request.Prompt, sessionOptions, cancellationToken);

        var responseBuilder = new StringBuilder();
        ResultMessage? finalResult = null;

        await foreach (var streamMessage in messages)
        {
            switch (streamMessage.Message)
            {
                case ResultMessage result:
                    finalResult = result;
                    responseBuilder.Append(result.Result);
                    break;
            }
        }

        if (finalResult != null)
        {
            return ClaudeResponseMapper.MapToAIResponse(finalResult, Name);
        }

        return new AIResponse
        {
            Content = responseBuilder.ToString(),
            FinishReason = FinishReason.Unknown,
            Provider = Name
        };
    }
}
```

Caractéristiques du fournisseur Claude Code CLI :
- **Intégration du gestionnaire de streaming** : Utilise `IClaudeStreamManager` pour communiquer avec Claude CLI
- **Isolement de session CessionId** : Utilise `CessionId` comme identifiant unique de session, distinct du sessionId du système
- **Configuration du répertoire de travail** : Prend en charge la configuration du répertoire de travail, du mode de permissions, etc.
- **Support des outils** : Prend en charge les configurations de permissions d'outils comme AllowedTools, DisallowedTools

### 6. Implémentation du fournisseur Codex CLI

```csharp
public class CodexCliProvider : IAIProvider
{
    private readonly ILogger<CodexCliProvider> _logger;
    private readonly CodexSettings _settings;
    private readonly ConcurrentDictionary<string, string> _sessionThreadBindings;

    public string Name => "CodexCli";
    public bool SupportsStreaming => true;

    public ProviderCapabilities Capabilities { get; }

    public async IAsyncEnumerable<AIStreamingChunk> StreamAsync(
        AIRequest request,
        [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        _logger.LogInformation("Executing streaming AI request with provider: {Provider}", Name);

        var codex = CreateCodexClient();
        var thread = ResolveThread(codex, request);

        var currentTurn = 0;
        var activeToolCalls = new Dictionary<string, AIToolCallDelta>();

        await foreach (var threadEvent in thread.RunStreamedAsync(BuildPrompt(request), cancellationToken))
        {
            if (threadEvent is TurnStartedEvent)
            {
                currentTurn++;
            }

            switch (threadEvent)
            {
                case ItemCompletedEvent { Item: AgentMessageItem message }:
                    var messageText = message.Text ?? string.Empty;
                    yield return new AIStreamingChunk
                    {
                        Content = messageText,
                        Type = StreamingChunkType.ContentDelta,
                        IsComplete = false
                    };
                    break;

                case ItemStartedEvent or ItemUpdatedEvent or ItemCompletedEvent:
                    var toolChunk = BuildToolChunk(threadEvent, currentTurn);
                    if (toolChunk?.ToolCallDelta != null)
                    {
                        yield return toolChunk;
                    }
                    break;

                case TurnCompletedEvent turnCompleted:
                    activeToolCalls.Clear();
                    yield return new AIStreamingChunk
                    {
                        Content = string.Empty,
                        Type = StreamingChunkType.Metadata,
                        IsComplete = true,
                        Usage = MapUsage(turnCompleted.Usage)
                    };
                    break;
            }
        }

        BindSessionThread(request.SessionId, thread.Id);
    }

    private CodexThread ResolveThread(Codex codex, AIRequest request)
    {
        var sessionId = request.SessionId;

        // Vérifier s'il y a déjà un thread lié
        if (!string.IsNullOrWhiteSpace(sessionId) &&
            _sessionThreadBindings.TryGetValue(sessionId, out var threadId) &&
            !string.IsNullOrWhiteSpace(threadId))
        {
            _logger.LogInformation("Resuming Codex thread {ThreadId} for session {SessionId}", threadId, sessionId);
            return codex.ResumeThread(threadId, threadOptions);
        }

        _logger.LogInformation("Starting new Codex thread for session {SessionId}", sessionId ?? "(none)");
        return codex.StartThread(threadOptions);
    }
}
```

Caractéristiques du fournisseur Codex CLI :
- **Traitement du flux d'événements JSON** : Analyse le flux d'événements JSON de Codex (TurnStarted, ItemStarted, TurnCompleted, etc.)
- **Liaison session-thread** : Persiste la relation de liaison session-thread via une base de données SQLite
- **Réutilisation de threads** : Prend en charge la reprise de threads existants pour maintenir la continuité de session
- **Suivi des appels d'outils** : Suit l'état des appels d'outils actifs, gère correctement le cycle de vie des outils

### 7. Mécanisme de liaison session-thread

Codex CLI utilise une base de données SQLite pour persister la liaison entre sessions et threads :

```csharp
public class CodexCliProvider : IAIProvider
{
    private const int SessionThreadBindingRetentionDays = 30;
    private readonly ConcurrentDictionary<string, string> _sessionThreadBindings;
    private readonly string _sessionThreadBindingDatabaseConnectionString;
    private readonly string _sessionThreadBindingDatabasePath;

    private void BindSessionThread(string? sessionId, string? threadId)
    {
        if (string.IsNullOrWhiteSpace(sessionId) || string.IsNullOrWhiteSpace(threadId))
        {
            return;
        }

        // Cache en mémoire
        _sessionThreadBindings.AddOrUpdate(sessionId, threadId, (_, _) => threadId);

        // Persistance vers SQLite
        PersistSessionThreadBinding(sessionId, threadId);
    }

    private void PersistSessionThreadBinding(string sessionId, string threadId)
    {
        try
        {
            using var connection = new SqliteConnection(_sessionThreadBindingDatabaseConnectionString);
            connection.Open();

            using var upsertCommand = connection.CreateCommand();
            upsertCommand.CommandText =
                """
                INSERT INTO SessionThreadBindings (SessionId, ThreadId, CreatedAtUtc, UpdatedAtUtc)
                VALUES ($sessionId, $threadId, $createdAtUtc, $updatedAtUtc)
                ON CONFLICT(SessionId) DO UPDATE SET
                    ThreadId = excluded.ThreadId,
                    UpdatedAtUtc = excluded.UpdatedAtUtc;
                """;
            var nowUtc = DateTimeOffset.UtcNow.ToString("O");
            upsertCommand.Parameters.AddWithValue("$sessionId", sessionId);
            upsertCommand.Parameters.AddWithValue("$threadId", threadId);
            upsertCommand.Parameters.AddWithValue("$createdAtUtc", nowUtc);
            upsertCommand.Parameters.AddWithValue("$updatedAtUtc", nowUtc);
            upsertCommand.ExecuteNonQuery();
        }
        catch (Exception ex)
        {
            _logger.LogWarning(
                ex,
                "Failed to persist Codex session-thread binding for session {SessionId} to {DatabasePath}",
                sessionId,
                _sessionThreadBindingDatabasePath);
        }
    }

    private void LoadPersistedSessionThreadBindings()
    {
        using var connection = new SqliteConnection(_sessionThreadBindingDatabaseConnectionString);
        connection.Open();

        using var loadCommand = connection.CreateCommand();
        loadCommand.CommandText = "SELECT SessionId, ThreadId FROM SessionThreadBindings;";
        using var reader = loadCommand.ExecuteReader();
        while (reader.Read())
        {
            var sessionId = reader.GetString(0);
            var threadId = reader.GetString(1);
            _sessionThreadBindings[sessionId] = threadId;
        }
    }
}
```

Avantages de la liaison session-thread :
- **Reprise de session** : Peut reprendre les sessions précédentes après un redémarrage du système
- **Réutilisation de threads** : La même session peut réutiliser un thread Codex existant
- **Nettoyage automatique** : Les liaisons de plus de 30 jours sont automatiquement nettoyées

### 8. Gestion CLI côté bureau

hagicode-desktop gère la sélection CLI via `AgentCliManager` :

```typescript
export enum AgentCliType {
  ClaudeCode = 'claude-code',
  Codex = 'codex',
  // Extensible à l'avenir : Aider, Cursor et autres CLI
}

export class AgentCliManager {
  private static readonly STORE_KEY = 'agentCliSelection';
  private static readonly EXECUTOR_TYPE_MAP: Record<AgentCliType, string> = {
    [AgentCliType.ClaudeCode]: 'ClaudeCodeCli',
    [AgentCliType.Codex]: 'CodexCli',
  };

  constructor(private store: any) {}

  async saveSelection(cliType: AgentCliType): Promise<void> {
    const selection: StoredAgentCliSelection = {
      cliType,
      isSkipped: false,
      selectedAt: new Date().toISOString(),
    };

    this.store.set(AgentCliManager.STORE_KEY, selection);
  }

  loadSelection(): StoredAgentCliSelection {
    return this.store.get(AgentCliManager.STORE_KEY, {
      cliType: null,
      isSkipped: false,
      selectedAt: null,
    });
  }

  getCommandName(cliType: AgentCliType): string {
    switch (cliType) {
      case AgentCliType.ClaudeCode:
        return 'claude';
      case AgentCliType.Codex:
        return 'codex';
      default:
        return 'claude';
    }
  }

  getExecutorType(cliType: AgentCliType | null): string {
    if (!cliType) return 'ClaudeCodeCli';
    return this.EXECUTOR_TYPE_MAP[cliType] || 'ClaudeCodeCli';
  }
}
```

Exemple de gestionnaire IPC côté bureau :

```typescript
ipcMain.handle('llm:call-api', async (event, manifestPath, region) => {
  if (!state.llmInstallationManager) {
    return { success: false, error: 'LLM Installation Manager not initialized' };
  }

  try {
    const prompt = await state.llmInstallationManager.loadPrompt(manifestPath, region);

    // Déterminer la commande CLI selon la sélection de l'utilisateur
    let commandName = 'claude';
    if (state.agentCliManager) {
      const selectedCliType = state.agentCliManager.getSelectedCliType();
      if (selectedCliType) {
        commandName = state.agentCliManager.getCommandName(selectedCliType);
      }
    }

    // Exécuter avec le CLI correspondant
    const result = await state.llmInstallationManager.callApi(
      prompt.filePath,
      event.sender,
      commandName
    );

    return result;
  } catch (error) {
    return {
      success: false,
      error: error instanceof Error ? error.message : 'Unknown error'
    };
  }
});
```

### 9. Système de fournisseurs de modèles interne de Codex

Codex prend également en charge plusieurs fournisseurs de modèles, via la configuration `ModelProviderInfo` :

```rust
pub const OPENAI_PROVIDER_NAME: &str = "OpenAI";
pub const OLLAMA_OSS_PROVIDER_ID: &str = "ollama";
pub const LMSTUDIO_OSS_PROVIDER_ID: &str = "lmstudio";

pub fn built_in_model_providers() -> HashMap<String, ModelProviderInfo> {
    use ModelProviderInfo as P;

    [
        ("openai", P::create_openai_provider()),
        (OLLAMA_OSS_PROVIDER_ID, create_oss_provider(DEFAULT_OLLAMA_PORT, WireApi::Responses)),
        (LMSTUDIO_OSS_PROVIDER_ID, create_oss_provider(DEFAULT_LMSTUDIO_PORT, WireApi::Responses)),
    ]
    .into_iter()
    .map(|(k, v)| (k.to_string(), v))
    .collect()
}

pub struct ModelProviderInfo {
    pub name: String,
    pub base_url: Option<String>,
    pub env_key: Option<String>,
    pub query_params: Option<HashMap<String, String>>,
    pub http_headers: Option<HashMap<String, String>>,
    pub request_max_retries: Option<u64>,
    pub stream_max_retries: Option<u64>,
    pub stream_idle_timeout_ms: Option<u64>,
    pub requires_openai_auth: bool,
    pub supports_websockets: bool,
}
```

Support des fournisseurs de modèles de Codex :
- **Fournisseurs intégrés** : OpenAI, Ollama, LM Studio
- **Fournisseurs personnalisés** : Les utilisateurs peuvent ajouter des fournisseurs personnalisés dans config.toml
- **Stratégie de retry** : Nombre de tentatives configurable pour les requêtes et le streaming
- **Support WebSocket** : Certains fournisseurs prennent en charge le transport WebSocket

## Pratique

### Exemple de configuration

Configuration de plusieurs fournisseurs dans **appsettings.json** :

```json
{
  "AI": {
    "Providers": {
      "DefaultProvider": "ClaudeCodeCli",
      "Providers": {
        "ClaudeCodeCli": {
          "Type": "ClaudeCodeCli",
          "Model": "claude-sonnet-4-20250514",
          "WorkingDirectory": "/path/to/workspace",
          "PermissionMode": "acceptEdits",
          "AllowedTools": ["file-edit", "command-run", "bash"]
        },
        "CodexCli": {
          "Type": "CodexCli",
          "Model": "gpt-4.1",
          "ExecutablePath": "codex",
          "SandboxMode": "enabled",
          "WebSearchMode": "auto",
          "NetworkAccessEnabled": false
        }
      },
      "ScenarioProviderMapping": {
        "CodeAnalysis": "ClaudeCodeCli",
        "CodeGeneration": "CodexCli",
        "Refactoring": "ClaudeCodeCli",
        "Debugging": "CodexCli"
      },
      "FallbackChain": ["CodexCli", "ClaudeCodeCli"]
    },
    "Selector": {
      "EnableCache": true,
      "CacheExpirationSeconds": 300
    }
  }
}
```

### Exemple d'utilisation - Service backend

```csharp
public class AIOrchestrator
{
    private readonly IAIProviderFactory _providerFactory;
    private readonly IAIProviderSelector _providerSelector;
    private readonly ILogger<AIOrchestrator> _logger;

    public AIOrchestrator(
        IAIProviderFactory providerFactory,
        IAIProviderSelector providerSelector,
        ILogger<AIOrchestrator> logger)
    {
        _providerFactory = providerFactory;
        _providerSelector = providerSelector;
        _logger = logger;
    }

    public async Task<AIResponse> ProcessRequestAsync(
        AIRequest request,
        BusinessScenario scenario)
    {
        _logger.LogInformation("Processing request for scenario: {Scenario}", scenario);

        try
        {
            // Sélection intelligente du fournisseur
            var providerType = await _providerSelector.SelectProviderAsync(scenario, request.CancellationToken);

            // Obtenir l'instance du fournisseur
            var provider = await _providerFactory.GetProviderAsync(providerType);
            if (provider == null)
            {
                throw new InvalidOperationException($"Provider {providerType} not available");
            }

            _logger.LogInformation("Using provider: {Provider} for request", provider.Name);

            // Exécuter la requête
            var response = await provider.ExecuteAsync(request, request.CancellationToken);

            _logger.LogInformation("Request completed with provider: {Provider}, tokens used: {Tokens}",
                provider.Name,
                response.Usage?.TotalTokens ?? 0);

            return response;
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Failed to process request for scenario: {Scenario}", scenario);
            throw;
        }
    }
}
```

### Exemple d'utilisation - Réponse en streaming

```csharp
public async IAsyncEnumerable<AIStreamingChunk> StreamResponseAsync(
    AIRequest request,
    BusinessScenario scenario)
{
    var providerType = await _providerSelector.SelectProviderAsync(scenario);
    var provider = await _providerFactory.GetProviderAsync(providerType);

    if (provider == null)
    {
        throw new InvalidOperationException($"Provider {providerType} not available");
    }

    await foreach (var chunk in provider.StreamAsync(request))
    {
        // Traiter les chunks de streaming
        switch (chunk.Type)
        {
            case StreamingChunkType.ContentDelta:
                // Afficher le contenu texte en temps réel
                await SendToClientAsync(chunk.Content);
                break;

            case StreamingChunkType.ToolCallDelta:
                // Traiter l'appel d'outil
                await HandleToolCallAsync(chunk.ToolCallDelta);
                break;

            case StreamingChunkType.Metadata:
                // Traiter les événements de complétion et les statistiques
                if (chunk.IsComplete)
                {
                    _logger.LogInformation("Stream completed, usage: {@Usage}", chunk.Usage);
                }
                break;

            case StreamingChunkType.Error:
                // Traiter les erreurs
                _logger.LogError("Stream error: {Error}", chunk.ErrorMessage);
                throw new InvalidOperationException(chunk.ErrorMessage);
        }
    }
}
```

### Exemple d'utilisation - Commandes OpenSpec

```csharp
public async Task<string> ExecuteOpenSpecCommandAsync(
    string command,
    string arguments,
    BusinessScenario scenario)
{
    var providerType = await _providerSelector.SelectProviderAsync(scenario);
    var provider = await _providerFactory.GetProviderAsync(providerType);

    // Construire l'invite de commande intégrée
    var commandPrompt = $"""
        Execute the following OpenSpec command:
        Command: {command}
        Arguments: {arguments}

        Please execute this command and return the results.
        """;

    var request = new AIRequest
    {
        Prompt = "Process this command request",
        EmbeddedCommandPrompt = commandPrompt,
        WorkingDirectory = Directory.GetCurrentDirectory()
    };

    var response = await provider.SendMessageAsync(request, commandPrompt);

    return response.Content;
}
```

## Considérations

### 1. Vérification de santé des fournisseurs

Avant de basculer de fournisseur, il est recommandé d'appeler `PingAsync` pour s'assurer que le fournisseur cible est disponible :

```csharp
public async Task<bool> IsProviderHealthyAsync(AIProviderType providerType)
{
    var provider = await _providerFactory.GetProviderAsync(providerType);
    if (provider == null) return false;

    var testResult = await provider.PingAsync();

    return testResult.Success &&
           testResult.ResponseTimeMs < 5000; // Réponse en 5 secondes considérée comme saine
}
```

### 2. Isolement des sessions

Utilisez `CessionId` (Claude) ou `ThreadId` (Codex) pour assurer l'isolement des sessions :

- Claude Code CLI : Utilise `CessionId` comme identifiant unique de session
- Codex CLI : Utilise `ThreadId` comme identifiant de session

```csharp
// Options de session Claude Code CLI
var claudeSessionOptions = new ClaudeSessionOptions
{
    CessionId = CessionId.New(),  // Générer un ID unique
    WorkingDirectory = workspacePath,
    AllowedTools = allowedTools,
    PermissionMode = PermissionMode.acceptEdits
};

// Options de thread Codex
var codexThreadOptions = new ThreadOptions
{
    Model = "gpt-4.1",
    SandboxMode = "enabled",
    WorkingDirectory = workspacePath
};
```

### 3. Gestion des erreurs

Le mécanisme de repli lorsque les fournisseurs ne sont pas disponibles doit être robuste, garantissant qu'au moins un fournisseur soit disponible :

```csharp
public async Task<AIResponse> ExecuteWithFallbackAsync(
    AIRequest request,
    List<AIProviderType> preferredProviders)
{
    Exception? lastException = null;

    foreach (var providerType in preferredProviders)
    {
        try
        {
            var provider = await _providerFactory.GetProviderAsync(providerType);
            if (provider == null) continue;

            // Tenter l'exécution
            return await provider.ExecuteAsync(request);
        }
        catch (Exception ex)
        {
            _logger.LogWarning(ex, "Provider {ProviderType} failed, trying next", providerType);
            lastException = ex;
        }
    }

    // Tous les fournisseurs ont échoué
    throw new InvalidOperationException(
        "All preferred providers failed. Last error: " + lastException?.Message,
        lastException);
}
```

### 4. Validation de configuration

Validez tous les paramètres de fournisseurs configurés au démarrage pour éviter les erreurs d'exécution :

```csharp
public void ValidateConfiguration(AIProviderOptions options)
{
    foreach (var (providerType, config) in options.Providers)
    {
        // Valider le chemin de l'exécutable (fournisseurs de type CLI)
        if (IsCliBasedProvider(providerType))
        {
            if (string.IsNullOrWhiteSpace(config.ExecutablePath))
            {
                throw new ConfigurationException(
                    $"Provider {providerType} requires ExecutablePath");
            }

            if (!File.Exists(config.ExecutablePath))
            {
                throw new ConfigurationException(
                    $"Executable not found for {providerType}: {config.ExecutablePath}");
            }
        }

        // Valider la clé API (fournisseurs de type API)
        if (IsApiBasedProvider(providerType))
        {
            if (string.IsNullOrWhiteSpace(config.ApiKey))
            {
                throw new ConfigurationException(
                    $"Provider {providerType} requires ApiKey");
            }
        }

        // Valider le nom du modèle
        if (string.IsNullOrWhiteSpace(config.Model))
        {
            _logger.LogWarning("No model configured for {ProviderType}, using default", providerType);
        }
    }
}
```

### 5. Gestion du cache

Les instances de fournisseurs sont mises en cache, faites attention à la gestion du cycle de vie et à l'utilisation de la mémoire :

```csharp
// Nettoyer régulièrement le cache
public void ClearInactiveProviders(TimeSpan inactiveThreshold)
{
    var now = DateTimeOffset.UtcNow;
    var keysToRemove = new List<AIProviderType>();

    foreach (var (type, instance) in _cache)
    {
        // Supposons que le fournisseur a une propriété LastUsedTime
        if (instance.LastUsedTime.HasValue &&
            now - instance.LastUsedTime.Value > inactiveThreshold)
        {
            keysToRemove.Add(type);
        }
    }

    foreach (var key in keysToRemove)
    {
        _cache.TryRemove(key, out _);
        _logger.LogInformation("Cleared inactive provider: {Provider}", key);
    }
}
```

### 6. Journalisation

Enregistrez en détail les processus de sélection, de basculement et d'exécution des fournisseurs pour faciliter le débogage :

```csharp
public class AIProviderLogging
{
    private readonly ILogger _logger;

    public void LogProviderSelection(
        BusinessScenario scenario,
        AIProviderType selectedProvider,
        SelectionReason reason)
    {
        _logger.LogInformation(
            "[ProviderSelection] Scenario={Scenario}, Provider={Provider}, Reason={Reason}",
            scenario,
            selectedProvider,
            reason);
    }

    public void LogProviderSwitch(
        AIProviderType fromProvider,
        AIProviderType toProvider,
        string reason)
    {
        _logger.LogWarning(
            "[ProviderSwitch] From={FromProvider} To={ToProvider}, Reason={Reason}",
            fromProvider,
            toProvider,
            reason);
    }

    public void LogProviderError(
        AIProviderType provider,
        Exception error,
        AIRequest request)
    {
        _logger.LogError(error,
            "[ProviderError] Provider={Provider}, RequestLength={Length}, Error={Message}",
            provider,
            request.Prompt.Length,
            error.Message);
    }
}
```

### 7. Sécurité des threads

L'utilisation de collections concurrentes comme `ConcurrentDictionary` assure la sécurité des threads :

```csharp
public class ThreadSafeProviderCache
{
    private readonly ConcurrentDictionary<AIProviderType, IAIProvider> _cache;
    private readonly ReaderWriterLockSlim _lock = new();

    public IAIProvider? GetProvider(AIProviderType type)
    {
        // Les lectures ne nécessitent pas de verrou
        if (_cache.TryGetValue(type, out var provider))
            return provider;

        // La création nécessite un verrou en écriture
        _lock.EnterWriteLock();
        try
        {
            // Double vérification
            if (_cache.TryGetValue(type, out provider))
                return provider;

            var newProvider = CreateProvider(type);
            if (newProvider != null)
            {
                _cache[type] = newProvider;
            }
            return newProvider;
        }
        finally
        {
            _lock.ExitWriteLock();
        }
    }
}
```

### 8. Migration de base de données

Lorsque la structure de la base de données des liaisons session-thread change, il faut envisager la migration des données :

```csharp
public class SessionThreadMigration
{
    public async Task MigrateAsync(string dbPath)
    {
        var version = await GetSchemaVersionAsync(dbPath);

        if (version >= 2) return; // Déjà à la dernière version

        using var connection = new SqliteConnection(dbPath);
        connection.Open();

        // Migrer vers v2 : ajouter la colonne CreatedAtUtc
        if (version < 2)
        {
            _logger.LogInformation("Migrating SessionThreadBindings to v2...");

            using var addColumnCommand = connection.CreateCommand();
            addColumnCommand.CommandText = "ALTER TABLE SessionThreadBindings ADD COLUMN CreatedAtUtc TEXT;";
            addColumnCommand.ExecuteNonQuery();

            using var backfillCommand = connection.CreateCommand();
            backfillCommand.CommandText =
                """
                UPDATE SessionThreadBindings
                SET CreatedAtUtc = COALESCE(NULLIF(UpdatedAtUtc, ''), $nowUtc)
                WHERE CreatedAtUtc IS NULL OR CreatedAtUtc = '';
                """;
            backfillCommand.Parameters.AddWithValue("$nowUtc", DateTimeOffset.UtcNow.ToString("O"));
            backfillCommand.ExecuteNonQuery();
        }

        await UpdateSchemaVersionAsync(dbPath, 2);
        _logger.LogInformation("Migration to v2 completed");
    }
}
```

## Résumé

hagicode réalise une architecture de fournisseurs IA multiples flexible et extensible grâce à la combinaison du modèle de fournisseur, du modèle de fabrique et du modèle de sélecteur :

- **Abstraction d'interface unifiée** : L'interface `IAIProvider` masque les différences entre les différents CLI
- **Création dynamique d'instances** : `AIProviderFactory` prend en charge la création d'instances de fournisseurs à l'exécution
- **Stratégie de sélection intelligente** : `AIProviderSelector` réalise la sélection de fournisseurs pilotée par les scénarios
- **Persistance de l'état de session** : Assure la continuité des sessions via des liaisons de base de données
- **Intégration côté bureau** : `AgentCliManager` prend en charge la sélection et la configuration par les utilisateurs

Les avantages de cette conception architecturale sont :
1. **Extensibilité** : Ajouter un nouveau fournisseur IA ne nécessite que l'implémentation de l'interface `IAIProvider`
2. **Testabilité** : Les fournisseurs peuvent être testés et simulés indépendamment
3. **Maintenabilité** : L'implémentation de chaque fournisseur est indépendante avec une responsabilité unique
4. **Convivialité** : Prend en charge la sélection automatique par scénario et le basculement manuel

Grâce à cette conception, hagicode a réussi à réaliser le basculement transparent et l'interopérabilité entre Claude Code CLI et Codex CLI, offrant aux développeurs une expérience d'assistant de codage IA flexible et puissante.

## Références

- Adresse du projet HagiCode : [github.com/HagiCode-org/site](https://github.com/HagiCode-org/site)
- Site officiel HagiCode : [hagicode.com](https://hagicode.com)
- Documentation officielle Claude Code : [docs.anthropic.com](https://docs.anthropic.com)
- Documentation OpenAI Codex : [platform.openai.com](https://platform.openai.com)
- Dépôt officiel Codex SDK : [github.com/openai/codex](https://github.com/openai/codex)
- Support multi-plateforme CLI HagiCode : [https://docs.hagicode.com/blog/hagicode-ai-cli-multi-platform-support/](https://docs.hagicode.com/blog/hagicode-ai-cli-multi-platform-support/)

---

Merci de votre lecture. Si vous trouvez cet article utile, cliquez sur le bouton J'aime ci-dessous 👍 pour permettre à plus de personnes de le voir.

Ce contenu est créé avec une collaboration assistée par l'intelligence artificielle et vérifié par moi-même, conformément à mes points de vue et positions.

- Auteur de l'article : [newbe36524](https://www.newbe.pro)
- Lien de l'article : [https://docs.hagicode.com/blog/2026-03-09-hagicode-multi-ai-provider-switching-interop/](https://docs.hagicode.com/blog/2026-03-09-hagicode-multi-ai-provider-switching-interop/)
- Avis de droit d'auteur : Sauf indication contraire, tous les articles de ce blog sont sous licence BY-NC-SA. Veuillez indiquer la source lors de la reproduction !
